# Founder decision record

All Founder decisions required for the initial structure are resolved. The initial admin identity remains administrative. The initial teacher is a distinct new user with username `ctomala.docente`; it must not reuse or modify the admin account. The temporary password is supplied only at authorized execution and is absent from this repository.

The only initial period is `2026-2027-CII`, from `2026-10-01` through `2027-02-25`, initially in the schema-supported `draft` status. The existing five subjects remain active; `ALG` keeps its stable code and requires a display-name normalization from `Álgebra` to `Álgebra Lineal` during an authorized future transaction.

The Founder approved Course/Parallel normalization: CALC-DIF=`PMF-MA-4`/A1,A2; EST=`PMF-MA-3`/3A1,3A2; ALG=`PMF-MA-5`/5A1; ALG-ELEM=`PMF-MA-2`/2A1; CALC-INT=`PMF-MA-5C`/5C1,5C2. The supplied CALC-INT source strings are normalized typographically to `PMF-MA-5C-5C1` and `PMF-MA-5C-5C2` without changing institutional meaning.

The first population is structure only: one new teacher user/entity, one period, five courses, five assignments, eight parallels, eight cohorts, and zero enrollments. No new students, historical-user enrollments, or historical reconciliation are authorized.

`FOUNDER_DECISIONS_REMAINING=0`.
