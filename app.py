import os, re, json, hashlib, time, threading, random, unicodedata

from datetime import datetime, date, timedelta

from io import BytesIO, StringIO



import pandas as pd

import plotly.express as px

import streamlit as st

import psycopg2

from psycopg2.extras import RealDictCursor

from dotenv import load_dotenv

from google import genai



from cohort_management import (

    OFFICIAL_COHORT_CODE,

    build_enrollment_template_excel,

    generate_temporary_password,

    prepare_enrollment_dataframe,

)



try:

    from research_analytics import build_research_word_report

    RESEARCH_ANALYTICS_IMPORT_ERROR = ""

except Exception as research_analytics_error:

    build_research_word_report = None

    RESEARCH_ANALYTICS_IMPORT_ERROR = str(research_analytics_error)



try:

    from research_engine import build_scientific_package

    RESEARCH_ENGINE_IMPORT_ERROR = ""

except Exception as research_engine_error:

    build_scientific_package = None

    RESEARCH_ENGINE_IMPORT_ERROR = str(research_engine_error)



# Exportaciones opcionales: instalar python-docx y reportlab para Word/PDF.

try:

    from docx import Document

except Exception:

    Document = None

try:

    from reportlab.lib.pagesizes import A4

    from reportlab.pdfgen import canvas

except Exception:

    A4 = None

    canvas = None



# GPS automático desde el navegador.

# Instalar si se desea captura automática real: pip install streamlit-js-eval

try:

    from streamlit_js_eval import get_geolocation

except Exception:

    get_geolocation = None



# =========================================================

# CONFIGURACIÓN GENERAL

# =========================================================

# Carga el archivo .env desde la misma carpeta donde está app.py

BASE_DIR = os.path.dirname(os.path.abspath(__file__))

ENV_PATH = os.path.join(BASE_DIR, ".env")

load_dotenv(dotenv_path=ENV_PATH)



APP_NAME = "BunsekiChat"


BUNSEKI_FRONTEND_LOGO_DATA_URI = 'data:image/png;base64,'
# Supabase/PostgreSQL: usar DATABASE_URL en Streamlit Secrets o .env

TEST_DATABASE_URL = os.getenv("BUNSEKI_TEST_DATABASE_URL", "").strip()

if TEST_DATABASE_URL:

    DATABASE_URL = TEST_DATABASE_URL

else:

    try:

        DATABASE_URL = st.secrets["DATABASE_URL"]

    except Exception:

        DATABASE_URL = os.getenv("DATABASE_URL", "").strip()



DB_LOCK = threading.RLock()

DATABASE_SCHEMA_VERSION = "2026-06-27-cohort-enrollment-v2"

SESSION_TIMEOUT_MINUTES = int(os.getenv("SESSION_TIMEOUT_MINUTES", "20"))

# Lee la API desde .env. Para pruebas locales, puedes pegar una clave temporal en GEMINI_API_KEY_FALLBACK.

# IMPORTANTE: no subas claves reales a internet ni a GitHub.

GEMINI_API_KEY_FALLBACK = ""



# Compatible con local (.env) y Streamlit Cloud (Secrets)

try:

    GEMINI_API_KEY = st.secrets["GEMINI_API_KEY"]

except Exception:

    GEMINI_API_KEY = os.getenv("GEMINI_API_KEY", "").strip() or GEMINI_API_KEY_FALLBACK.strip()

ADMIN_USER = os.getenv("ADMIN_USER", "admin")

ADMIN_PASSWORD = os.getenv("ADMIN_PASSWORD", "admin123")



TOPICS = {'Matemáticas básicas': ['Álgebra elemental', 'Trigonometría', 'Geometría analítica'],
 'Álgebra lineal': ['Vectores',
                    'Matrices',
                    'Determinantes',
                    'Espacios vectoriales',
                    'Autovalores y autovectores'],
 'Cálculo diferencial': ['Funciones',
                         'Límites',
                         'Continuidad',
                         'Derivadas',
                         'Aplicaciones de la derivada'],
 'Cálculo integral': ['Antiderivadas',
                      'Integral definida',
                      'Técnicas de integración',
                      'Aplicaciones de la integral',
                      'Series básicas'],
 'Estadística descriptiva': ['Tipos de variables',
                             'Tablas y gráficos',
                             'Medidas de tendencia central',
                             'Medidas de dispersión',
                             'Posición y forma'],
 'Estadística inferencial': ['Probabilidad',
                             'Distribuciones muestrales',
                             'Estimación e intervalos de confianza',
                             'Pruebas de hipótesis',
                             'Correlación y regresión'],
 'Estadística': ['Estadística descriptiva',
                 'Probabilidad',
                 'Distribuciones',
                 'Inferencia',
                 'Regresión']}

LEVELS = ["Inicial", "Básico", "Intermedio", "Avanzado"]





def env_list(name, default):

    raw = os.getenv(name, "").strip()

    if not raw:

        return default

    values = [x.strip() for x in raw.split(",") if x.strip()]

    return values or default





def env_int(name, default):

    try:

        return int(os.getenv(name, str(default)))

    except (TypeError, ValueError):

        return default





ACADEMIC_SUBJECTS = list(dict.fromkeys(['Matemáticas básicas',
 'Álgebra lineal',
 'Cálculo diferencial',
 'Cálculo integral',
 'Estadística descriptiva',
 'Estadística inferencial',
 'Estadística',
 'Cálculo I'] + list(env_list("BUNSEKI_SUBJECTS", [

    "Cálculo diferencial",

    "Cálculo integral",

    "Álgebra lineal",

    "Estadística",

]))))

ACADEMIC_COURSE_LEVELS = env_list("BUNSEKI_COURSE_LEVELS", ["4", "1", "2", "3", "5", "6", "7", "8"])

ACADEMIC_PARALLELS = env_list("BUNSEKI_PARALLELS", ["4-A1", "4-A2", "4-B1", "4-B2"])

ACADEMIC_SHIFTS = env_list("BUNSEKI_SHIFTS", ["Matutino", "Vespertino", "Nocturno"])

RESEARCH_TITLE = os.getenv(

    "BUNSEKI_RESEARCH_TITLE",

    "Efectos de un Tutor Inteligente basado en Inteligencia Artificial Generativa sobre el Rendimiento Académico en Cálculo Diferencial en la Universidad de Guayaquil",

)

RESEARCH_QUESTION_COUNT = env_int("BUNSEKI_RESEARCH_QUESTION_COUNT", 20)

RESEARCH_FOCUS_TOPIC = os.getenv("BUNSEKI_RESEARCH_FOCUS_TOPIC", "Aplicaciones de la derivada").strip() or "Aplicaciones de la derivada"

RESEARCH_TEST_TYPES = {

    "adaptive": "Evaluación adaptativa",

    "pretest": "Pretest",

    "posttest": "Posttest",

}

RESEARCH_GROUPS = ["Sin asignar", "Experimental", "Control"]

DATA_STATUSES = ["piloto", "oficial"]

INFORMED_CONSENT_VERSION = "BUNSEKI-CALCULOI-2026-v1"

INFORMED_CONSENT_TEXT = """

### Consentimiento informado para participar en la investigación



**Título:** Efectos de un Tutor Inteligente basado en Inteligencia Artificial Generativa sobre el Rendimiento Académico en Cálculo Diferencial en la Universidad de Guayaquil.



Su participación es voluntaria. La investigación analizará resultados de pretest y postest, actividad académica dentro de BunsekiChat, tiempo de uso, ejercicios y percepción de la herramienta. Los datos se tratarán con fines académicos y se exportarán mediante un identificador controlado por el equipo investigador.



Participar o no participar no debe producir sanciones académicas. Puede solicitar aclaraciones o retirar su autorización antes del cierre de la base oficial. La aceptación confirma que leyó esta información y autoriza el uso de sus datos dentro de la cohorte indicada.

"""

RESEARCH_DIMENSION_BLUEPRINT = [

    {"dimension": "Comprensión conceptual", "weight": 0.20, "indicator": "Interpreta la derivada como tasa de cambio", "competence": "Comprende conceptos fundamentales"},

    {"dimension": "Procedimientos", "weight": 0.25, "indicator": "Aplica reglas y criterios de derivación en contextos", "competence": "Ejecuta procedimientos matemáticos"},

    {"dimension": "Aplicaciones", "weight": 0.35, "indicator": "Modela situaciones con derivadas", "competence": "Transfiere el cálculo a problemas reales"},

    {"dimension": "Resolución de problemas", "weight": 0.20, "indicator": "Resuelve problemas multietapa e interpreta resultados", "competence": "Argumenta y valida soluciones"},

]

RESEARCH_SURVEY_ITEMS = [

    ("Usabilidad del sistema", "El sistema fue fácil de navegar durante la actividad."),

    ("Usabilidad del sistema", "Encontré rápidamente las opciones de pretest, clase y posttest."),

    ("Facilidad de uso", "Pude completar las actividades sin ayuda externa."),

    ("Facilidad de uso", "Las instrucciones de la plataforma fueron claras."),

    ("Claridad de las explicaciones", "Las explicaciones de Bunseki Chat fueron comprensibles."),

    ("Claridad de las explicaciones", "Los ejemplos me ayudaron a entender aplicaciones de la derivada."),

    ("Calidad de la retroalimentación", "La retroalimentación me ayudó a identificar mis errores."),

    ("Calidad de la retroalimentación", "Las correcciones fueron útiles para mejorar."),

    ("Motivación para aprender", "El uso del tutor aumentó mi interés por el tema."),

    ("Motivación para aprender", "Me sentí motivado a continuar practicando."),

    ("Autoeficacia percibida", "Después de usar el tutor me siento más capaz de resolver ejercicios."),

    ("Autoeficacia percibida", "Puedo explicar mejor los pasos para resolver problemas aplicados."),

    ("Utilidad del tutor", "Bunseki Chat fue útil para estudiar aplicaciones de la derivada."),

    ("Utilidad del tutor", "Recomendaría usar Bunseki Chat como apoyo en Cálculo Diferencial."),

    ("Satisfacción general", "Estoy satisfecho con la experiencia de aprendizaje."),

]



# Banco local de respaldo para que la investigación funcione aun si el servicio generativo no responde.

DIFFERENTIAL_CALCULUS_QUESTION_BANK = [

    {"question": "Si f(x)=3x^2-5x+2, ¿cuál es f'(x)?", "options": ["6x-5", "3x-5", "6x+2", "x^3-5x"], "correct_answer": "6x-5", "explanation": "Se aplica la regla de la potencia término a término.", "bloom_level": "Aplicar", "topic": "Derivadas"},

    {"question": "El límite de (x^2-9)/(x-3) cuando x tiende a 3 es:", "options": ["6", "3", "0", "No existe"], "correct_answer": "6", "explanation": "Se factoriza x^2-9=(x-3)(x+3) y queda x+3.", "bloom_level": "Aplicar", "topic": "Límites"},

    {"question": "La derivada de sen(x) es:", "options": ["cos(x)", "-sen(x)", "-cos(x)", "tan(x)"], "correct_answer": "cos(x)", "explanation": "Es una derivada trigonométrica fundamental.", "bloom_level": "Recordar", "topic": "Derivadas trigonométricas"},

    {"question": "Para que una función sea continua en x=a debe cumplirse que:", "options": ["f(a) existe, el límite existe y ambos son iguales", "Solo exista f(a)", "Solo exista el límite lateral derecho", "La derivada sea cero"], "correct_answer": "f(a) existe, el límite existe y ambos son iguales", "explanation": "La continuidad exige existencia de valor, existencia de límite y coincidencia.", "bloom_level": "Comprender", "topic": "Continuidad"},

    {"question": "Si f'(a)>0, entonces cerca de a la función tiende a:", "options": ["Crecer", "Decrecer", "Ser constante siempre", "No ser continua"], "correct_answer": "Crecer", "explanation": "Una derivada positiva indica tasa de cambio positiva.", "bloom_level": "Comprender", "topic": "Aplicaciones de la derivada"},

    {"question": "La derivada de ln(x), para x>0, es:", "options": ["1/x", "x", "ln(x)", "e^x"], "correct_answer": "1/x", "explanation": "Es la regla básica de la función logaritmo natural.", "bloom_level": "Recordar", "topic": "Derivadas logarítmicas"},

    {"question": "¿Cuál es la derivada de (x^2+1)^5?", "options": ["10x(x^2+1)^4", "5(x^2+1)^4", "2x(x^2+1)^5", "10(x^2+1)^4"], "correct_answer": "10x(x^2+1)^4", "explanation": "Se usa regla de la cadena: 5(x^2+1)^4 por 2x.", "bloom_level": "Aplicar", "topic": "Regla de la cadena"},

    {"question": "La pendiente de la recta tangente a una curva en un punto se obtiene con:", "options": ["La derivada en ese punto", "El área bajo la curva", "La ordenada al origen", "El promedio de los valores de y"], "correct_answer": "La derivada en ese punto", "explanation": "La derivada representa la tasa instantánea y la pendiente tangente.", "bloom_level": "Comprender", "topic": "Interpretación de la derivada"},

    {"question": "El límite de sen(x)/x cuando x tiende a 0 es:", "options": ["1", "0", "Infinito", "No existe"], "correct_answer": "1", "explanation": "Es un límite trigonométrico notable.", "bloom_level": "Recordar", "topic": "Límites trigonométricos"},

    {"question": "Si f''(x)>0 en un intervalo, la gráfica es:", "options": ["Cóncava hacia arriba", "Cóncava hacia abajo", "Siempre decreciente", "Discontinua"], "correct_answer": "Cóncava hacia arriba", "explanation": "La segunda derivada positiva indica concavidad hacia arriba.", "bloom_level": "Comprender", "topic": "Concavidad"},

    {"question": "Un punto crítico puede aparecer cuando:", "options": ["f'(x)=0 o f'(x) no existe", "f(x)=0 siempre", "x es negativo", "La función es lineal"], "correct_answer": "f'(x)=0 o f'(x) no existe", "explanation": "Los candidatos a extremos locales se buscan donde la derivada se anula o no existe.", "bloom_level": "Comprender", "topic": "Puntos críticos"},

    {"question": "La derivada de e^(2x) es:", "options": ["2e^(2x)", "e^(2x)", "2x e^(2x)", "e^2"], "correct_answer": "2e^(2x)", "explanation": "Se aplica regla de la cadena a la función exponencial.", "bloom_level": "Aplicar", "topic": "Derivadas exponenciales"},

    {"question": "La regla del producto para derivar u(x)v(x) es:", "options": ["u'v+uv'", "u'v'", "u/v", "u'-v'"], "correct_answer": "u'v+uv'", "explanation": "La derivada del producto combina la derivada de cada factor.", "bloom_level": "Recordar", "topic": "Regla del producto"},

    {"question": "Para hallar máximos y mínimos absolutos en un intervalo cerrado se revisan:", "options": ["Puntos críticos y extremos del intervalo", "Solo puntos donde f(x)=0", "Solo valores negativos", "Solo el punto medio"], "correct_answer": "Puntos críticos y extremos del intervalo", "explanation": "El teorema del valor extremo exige comparar candidatos internos y extremos.", "bloom_level": "Aplicar", "topic": "Optimización"},

    {"question": "La linealización de f en x=a tiene la forma:", "options": ["L(x)=f(a)+f'(a)(x-a)", "L(x)=f'(a)+a(x-a)", "L(x)=f(a)(x-a)", "L(x)=f''(a)"], "correct_answer": "L(x)=f(a)+f'(a)(x-a)", "explanation": "La aproximación lineal usa el valor y la pendiente en a.", "bloom_level": "Aplicar", "topic": "Aproximación lineal"},

    {"question": "En la ecuación x^2+y^2=25, la derivada implícita dy/dx es:", "options": ["-x/y", "x/y", "-y/x", "2x+2y"], "correct_answer": "-x/y", "explanation": "Derivando: 2x+2y y'=0, por tanto y'=-x/y.", "bloom_level": "Aplicar", "topic": "Derivación implícita"},

    {"question": "La función f(x)=|x| no es derivable en x=0 porque:", "options": ["Las derivadas laterales son diferentes", "No existe f(0)", "No es continua en 0", "Su dominio es vacío"], "correct_answer": "Las derivadas laterales son diferentes", "explanation": "En x=0 la pendiente lateral izquierda y derecha no coinciden.", "bloom_level": "Analizar", "topic": "Derivabilidad"},

    {"question": "El teorema del valor medio requiere que f sea:", "options": ["Continua en [a,b] y derivable en (a,b)", "Solo derivable en a", "Discontinua en [a,b]", "Constante en todo su dominio"], "correct_answer": "Continua en [a,b] y derivable en (a,b)", "explanation": "Son las condiciones necesarias para aplicar el teorema.", "bloom_level": "Comprender", "topic": "Teorema del valor medio"},

    {"question": "La derivada de tan(x) es:", "options": ["sec^2(x)", "cos^2(x)", "-csc^2(x)", "sen(x)"], "correct_answer": "sec^2(x)", "explanation": "Es una identidad básica de derivación trigonométrica.", "bloom_level": "Recordar", "topic": "Derivadas trigonométricas"},

    {"question": "La derivada de una constante c es:", "options": ["0", "c", "1", "x"], "correct_answer": "0", "explanation": "Una constante no cambia respecto de x.", "bloom_level": "Recordar", "topic": "Reglas básicas de derivación"},

    {"question": "La regla de la potencia establece que la derivada de x^n es:", "options": ["nx^(n-1)", "x^(n+1)", "n/x", "x^n"], "correct_answer": "nx^(n-1)", "explanation": "El exponente baja como coeficiente y se reduce en una unidad.", "bloom_level": "Recordar", "topic": "Regla de la potencia"},

    {"question": "Si f'(x)=2x, entonces f crece en:", "options": ["(0, infinito)", "(-infinito, 0)", "Todo R", "Ningún intervalo"], "correct_answer": "(0, infinito)", "explanation": "La función crece donde la derivada es positiva.", "bloom_level": "Analizar", "topic": "Monotonía"},

    {"question": "Un límite bilateral existe cuando:", "options": ["Los límites laterales izquierdo y derecho son iguales", "Solo existe el límite izquierdo", "La función no está definida", "La derivada es cero"], "correct_answer": "Los límites laterales izquierdo y derecho son iguales", "explanation": "La igualdad de límites laterales garantiza el límite bilateral.", "bloom_level": "Comprender", "topic": "Límites laterales"},

    {"question": "La derivada de cos(3x) es:", "options": ["-3sen(3x)", "-sen(3x)", "3cos(3x)", "sen(3x)"], "correct_answer": "-3sen(3x)", "explanation": "Se deriva cos(u) como -sen(u) y se multiplica por u'=3.", "bloom_level": "Aplicar", "topic": "Regla de la cadena"},

    {"question": "La regla de L'Hôpital puede aplicarse inicialmente ante formas:", "options": ["0/0 o infinito/infinito", "Solo 1/0", "Solo 0+0", "Cualquier suma"], "correct_answer": "0/0 o infinito/infinito", "explanation": "La regla se usa bajo condiciones de indeterminación de cocientes.", "bloom_level": "Comprender", "topic": "Límites indeterminados"},

    {"question": "En problemas de razones relacionadas, el primer paso recomendable es:", "options": ["Definir variables y una ecuación que las relacione", "Derivar sin leer el enunciado", "Elegir una respuesta al azar", "Eliminar todas las unidades"], "correct_answer": "Definir variables y una ecuación que las relacione", "explanation": "La modelación correcta permite derivar respecto del tiempo.", "bloom_level": "Aplicar", "topic": "Razones relacionadas"},

    {"question": "Si f''(x)<0 en un intervalo, la gráfica es:", "options": ["Cóncava hacia abajo", "Cóncava hacia arriba", "Siempre creciente", "Lineal horizontal"], "correct_answer": "Cóncava hacia abajo", "explanation": "La segunda derivada negativa indica concavidad hacia abajo.", "bloom_level": "Comprender", "topic": "Concavidad"},

    {"question": "Un máximo local suele identificarse cuando f'(x) cambia de:", "options": ["Positiva a negativa", "Negativa a positiva", "Cero a cero siempre", "No definida a positiva siempre"], "correct_answer": "Positiva a negativa", "explanation": "El cambio de crecimiento a decrecimiento indica máximo local.", "bloom_level": "Analizar", "topic": "Criterio de la primera derivada"},

    {"question": "Si f(x)=x^3, entonces f'(2) es:", "options": ["12", "6", "8", "3"], "correct_answer": "12", "explanation": "f'(x)=3x^2, por tanto f'(2)=3(4)=12.", "bloom_level": "Aplicar", "topic": "Derivadas"},

]



DERIVATIVE_APPLICATIONS_QUESTION_BANK = [

    {"question": "Una empresa modela su costo con C(x)=0.02x^2+5x+120. ¿Qué representa C'(x)?", "options": ["El costo marginal de producir una unidad adicional", "El costo fijo total", "La utilidad máxima", "El precio promedio de venta"], "correct_answer": "El costo marginal de producir una unidad adicional", "explanation": "La derivada del costo mide la variación instantánea del costo respecto a la producción.", "bloom_level": "Comprender", "topic": "Aplicaciones de la derivada"},

    {"question": "Si la posición de un móvil es s(t)=t^3-6t^2+9t, ¿qué se obtiene con s'(t)?", "options": ["La velocidad instantánea", "La aceleración promedio", "La distancia total siempre positiva", "El tiempo máximo"], "correct_answer": "La velocidad instantánea", "explanation": "La derivada de la posición respecto del tiempo es la velocidad instantánea.", "bloom_level": "Comprender", "topic": "Aplicaciones de la derivada"},

    {"question": "Para maximizar el área de un rectángulo con perímetro fijo, el procedimiento correcto inicia con:", "options": ["Expresar el área en función de una sola variable", "Derivar el perímetro dos veces", "Igualar el área a cero", "Eliminar la restricción"], "correct_answer": "Expresar el área en función de una sola variable", "explanation": "Los problemas de optimización requieren formular la función objetivo usando la restricción.", "bloom_level": "Aplicar", "topic": "Optimización"},

    {"question": "Si f'(x)>0 en un intervalo, entonces f en ese intervalo:", "options": ["Es creciente", "Es decreciente", "Tiene concavidad negativa necesariamente", "No es continua"], "correct_answer": "Es creciente", "explanation": "La derivada positiva indica aumento de la función.", "bloom_level": "Comprender", "topic": "Crecimiento y decrecimiento"},

    {"question": "Si f'(x)<0 en un intervalo, entonces f en ese intervalo:", "options": ["Es decreciente", "Es creciente", "Tiene un máximo absoluto siempre", "No tiene pendiente"], "correct_answer": "Es decreciente", "explanation": "La derivada negativa indica disminución de la función.", "bloom_level": "Comprender", "topic": "Crecimiento y decrecimiento"},

    {"question": "Un punto crítico de f se busca donde:", "options": ["f'(x)=0 o f'(x) no existe", "f(x)=1 siempre", "f''(x)>0 siempre", "x no pertenece al dominio"], "correct_answer": "f'(x)=0 o f'(x) no existe", "explanation": "Los puntos críticos son candidatos para extremos locales.", "bloom_level": "Recordar", "topic": "Puntos críticos"},

    {"question": "Si f'(x) cambia de negativa a positiva en x=a, entonces en x=a hay:", "options": ["Un mínimo local", "Un máximo local", "Una asíntota vertical", "Una discontinuidad removible"], "correct_answer": "Un mínimo local", "explanation": "La función pasa de decrecer a crecer, por eso aparece un mínimo local.", "bloom_level": "Analizar", "topic": "Criterio de la primera derivada"},

    {"question": "Si f'(x) cambia de positiva a negativa en x=a, entonces en x=a hay:", "options": ["Un máximo local", "Un mínimo local", "Una recta horizontal sin extremo", "Una raíz doble siempre"], "correct_answer": "Un máximo local", "explanation": "La función pasa de crecer a decrecer, indicando máximo local.", "bloom_level": "Analizar", "topic": "Criterio de la primera derivada"},

    {"question": "Si f''(x)>0 en un intervalo, la gráfica de f es:", "options": ["Cóncava hacia arriba", "Cóncava hacia abajo", "Siempre decreciente", "Siempre constante"], "correct_answer": "Cóncava hacia arriba", "explanation": "La segunda derivada positiva indica concavidad hacia arriba.", "bloom_level": "Comprender", "topic": "Concavidad"},

    {"question": "Si f''(x)<0 en un intervalo, la gráfica de f es:", "options": ["Cóncava hacia abajo", "Cóncava hacia arriba", "Siempre creciente", "Siempre lineal"], "correct_answer": "Cóncava hacia abajo", "explanation": "La segunda derivada negativa indica concavidad hacia abajo.", "bloom_level": "Comprender", "topic": "Concavidad"},

    {"question": "Un punto de inflexión puede ocurrir donde:", "options": ["Cambia la concavidad", "La función siempre vale cero", "La derivada primera siempre es positiva", "La función no tiene dominio"], "correct_answer": "Cambia la concavidad", "explanation": "El punto de inflexión se asocia al cambio de concavidad.", "bloom_level": "Comprender", "topic": "Concavidad"},

    {"question": "La pendiente de la recta tangente a y=f(x) en x=a se calcula con:", "options": ["f'(a)", "f(a)", "f''(a)+a", "a/f(a) siempre"], "correct_answer": "f'(a)", "explanation": "La derivada en el punto es la pendiente de la tangente.", "bloom_level": "Aplicar", "topic": "Recta tangente"},

    {"question": "La ecuación de la recta tangente a f en x=a usa:", "options": ["y-f(a)=f'(a)(x-a)", "y=f'(x)+a", "y=a/x", "y=f(a)x^2"], "correct_answer": "y-f(a)=f'(a)(x-a)", "explanation": "Se usa la forma punto-pendiente con pendiente f'(a).", "bloom_level": "Aplicar", "topic": "Recta tangente"},

    {"question": "En razones relacionadas, si el radio de un círculo cambia con el tiempo, para hallar dA/dt se usa:", "options": ["Derivación implícita respecto al tiempo", "Solo sustitución numérica sin derivar", "La regla de L'Hôpital", "La fórmula cuadrática"], "correct_answer": "Derivación implícita respecto al tiempo", "explanation": "Las razones relacionadas derivan variables dependientes del tiempo.", "bloom_level": "Aplicar", "topic": "Razones relacionadas"},

    {"question": "Si A=πr^2 y dr/dt=3, entonces dA/dt es:", "options": ["2πr(3)", "πr^2(3)", "2π(3)", "r/3"], "correct_answer": "2πr(3)", "explanation": "Al derivar A=πr^2 respecto al tiempo se obtiene dA/dt=2πr dr/dt.", "bloom_level": "Aplicar", "topic": "Razones relacionadas"},

    {"question": "Para resolver un problema de optimización, después de construir la función objetivo se debe:", "options": ["Derivar e igualar a cero para hallar candidatos", "Borrar la restricción", "Elegir el mayor número del enunciado", "Integrar siempre"], "correct_answer": "Derivar e igualar a cero para hallar candidatos", "explanation": "Los extremos se buscan en puntos críticos y extremos del dominio.", "bloom_level": "Aplicar", "topic": "Optimización"},

    {"question": "Si el ingreso es R(x)=50x-0.1x^2, el ingreso marginal es:", "options": ["R'(x)=50-0.2x", "R'(x)=50x", "R'(x)=0.1x", "R'(x)=50+x^2"], "correct_answer": "R'(x)=50-0.2x", "explanation": "Se deriva término a término para obtener el cambio instantáneo del ingreso.", "bloom_level": "Aplicar", "topic": "Marginalidad"},

    {"question": "Si U(x)=R(x)-C(x), para maximizar utilidad se analiza principalmente:", "options": ["U'(x)=0 y el comportamiento alrededor", "R(x)=0 únicamente", "C(x)>0 únicamente", "El valor de x más pequeño siempre"], "correct_answer": "U'(x)=0 y el comportamiento alrededor", "explanation": "Los máximos de utilidad se buscan con puntos críticos y criterios de extremo.", "bloom_level": "Analizar", "topic": "Optimización"},

    {"question": "La aproximación lineal L(x)=f(a)+f'(a)(x-a) sirve para:", "options": ["Estimar valores cercanos a a", "Calcular áreas exactas", "Eliminar el dominio", "Hallar raíces complejas siempre"], "correct_answer": "Estimar valores cercanos a a", "explanation": "La linealización usa la tangente para aproximar la función cerca de un punto.", "bloom_level": "Comprender", "topic": "Aproximación lineal"},

    {"question": "Si una función modela temperatura T(t), ¿qué interpreta T'(5)?", "options": ["La tasa instantánea de cambio de temperatura en t=5", "La temperatura total acumulada siempre", "El promedio de todas las temperaturas", "El valor mínimo de t"], "correct_answer": "La tasa instantánea de cambio de temperatura en t=5", "explanation": "La derivada mide la rapidez de cambio de la magnitud modelada.", "bloom_level": "Comprender", "topic": "Tasa de cambio"},

    {"question": "En el contexto de demanda p(x), la derivada p'(x) representa:", "options": ["Cómo cambia el precio respecto a la cantidad", "La cantidad total vendida", "El costo fijo", "La utilidad máxima siempre"], "correct_answer": "Cómo cambia el precio respecto a la cantidad", "explanation": "La derivada interpreta sensibilidad o tasa de cambio de una variable respecto a otra.", "bloom_level": "Comprender", "topic": "Tasa de cambio"},

    {"question": "Si f'(a)=0 y f''(a)>0, el criterio de la segunda derivada indica:", "options": ["Mínimo local", "Máximo local", "No hay extremo posible", "Asíntota horizontal"], "correct_answer": "Mínimo local", "explanation": "Segunda derivada positiva en un punto crítico indica concavidad hacia arriba y mínimo local.", "bloom_level": "Analizar", "topic": "Criterio de la segunda derivada"},

    {"question": "Si f'(a)=0 y f''(a)<0, el criterio de la segunda derivada indica:", "options": ["Máximo local", "Mínimo local", "Punto no derivable", "Función lineal"], "correct_answer": "Máximo local", "explanation": "Segunda derivada negativa en un punto crítico indica concavidad hacia abajo y máximo local.", "bloom_level": "Analizar", "topic": "Criterio de la segunda derivada"},

    {"question": "En un problema aplicado, la unidad de f'(x) se interpreta como:", "options": ["Unidades de salida por unidad de entrada", "Siempre metros cuadrados", "Siempre segundos", "No tiene unidades"], "correct_answer": "Unidades de salida por unidad de entrada", "explanation": "La derivada conserva significado dimensional como razón de cambio.", "bloom_level": "Comprender", "topic": "Interpretación de la derivada"},

]



st.set_page_config(page_title=APP_NAME, page_icon="∑", layout="wide", initial_sidebar_state="expanded")



# Control interno: evita crear varias veces el componente GPS en un mismo rerun de Streamlit.

st.session_state['_gps_widget_rendered_this_run'] = False



CSS = """

<style>

:root{

    --magenta:#d4147f;

    --magenta-2:#ff2f93;

    --magenta-dark:#6f0f49;

    --magenta-soft:#ffe4f2;

    --bg:#eef0f3;

    --card:#ffffff;

    --line:#d9dde3;

    --text:#1f2937;

    --muted:#6b7280;

    --shadow:0 22px 55px rgba(31,41,55,.13);

}



footer, #MainMenu {visibility:hidden; height:0 !important;}

header[data-testid="stHeader"]{

    visibility:visible !important;

    height:56px !important;

    min-height:56px !important;

    background:transparent !important;

    overflow:visible !important;

    pointer-events:auto !important;

    z-index:2147483647 !important;

}

div[data-testid="stToolbar"], div[data-testid="stDecoration"], div[data-testid="stStatusWidget"]{display:none !important;}

html, body, [class*="css"]{font-family:"Segoe UI", Tahoma, Geneva, Verdana, sans-serif;}

.stApp{background:linear-gradient(135deg,#eef0f3 0%,#e5e7eb 55%,#f6dce9 100%); color:var(--text);}

.block-container{max-width:1180px; padding-top:0 !important; padding-bottom:1.1rem !important;}

section.main > div{padding-top:0 !important;}

.element-container:empty{display:none !important;}

/* Corrección de scroll y espacio superior del login */

section.main > div.block-container{padding-top:0!important;}

section.main div[data-testid="stVerticalBlock"]{gap:0.35rem;}

.login-premium-page + div{display:none!important;}





/* ===================== LOGIN PREMIUM ===================== */

.login-premium-page{min-height:auto; display:block; padding:34px 0 14px; margin:0;}

.ad-panel{

    min-height:300px;

    border:1px dashed #cbd1da;

    border-radius:26px;

    background:rgba(255,255,255,.52);

    display:flex;

    align-items:center;

    justify-content:center;

    text-align:center;

    padding:18px;

    color:#8a94a3;

    box-shadow:inset 0 1px 0 rgba(255,255,255,.8);

}

.ad-panel strong{display:block;color:#5f6875;margin-bottom:6px;font-size:.9rem;}

.ad-panel span{font-size:.78rem;line-height:1.45;}

.login-card-premium{

    width:100%;

    max-width:305px;

    margin:26px auto 0;

    background:rgba(255,255,255,.94);

    border:1px solid rgba(217,221,227,.95);

    border-radius:28px;

    padding:30px 18px 14px;

    box-shadow:var(--shadow);

    text-align:center;

    position:relative;

}

.logo-orb{

    width:58px;height:58px;border-radius:50%;

    background:linear-gradient(135deg,var(--magenta),var(--magenta-2));

    color:white;

    display:flex;align-items:center;justify-content:center;

    font-weight:900;font-size:1.28rem;

    margin:-58px auto 8px;

    border:4px solid rgba(255,255,255,.9);

    box-shadow:0 14px 32px rgba(212,20,127,.32);

}

.login-title{font-size:1.28rem;font-weight:900;color:var(--magenta-dark);line-height:1;margin:0;}

.login-subtitle{font-size:.78rem;color:var(--muted);margin:6px 0 10px;}

.login-note{font-size:.72rem;color:var(--muted);text-align:center;margin-top:8px;}

.powered{font-size:.73rem;color:#6b7280;text-align:center;margin-top:8px;}

.powered strong{color:var(--magenta-dark);}



/* Tabs del login */

.login-card-premium [data-testid="stTabs"]{margin-top:0 !important;}

.login-card-premium [data-testid="stTabs"] button{padding:4px 8px !important;}

.login-card-premium [data-testid="stTabs"] button p{font-size:.80rem !important;font-weight:800 !important;}

.login-card-premium hr{margin:.35rem 0 .55rem !important;}

.login-card-premium div[data-testid="stVerticalBlock"]{gap:.12rem !important;}

.login-card-premium .stTextInput{margin-bottom:.05rem !important;}

.login-card-premium .stButton{margin-top:.18rem !important;}

.login-card-premium .stAlert{font-size:.74rem;padding:.35rem .55rem;}



/* Inputs compactos globales */

.stTextInput label,.stTextArea label{font-size:.76rem;color:var(--text)!important;font-weight:800;margin-bottom:0!important;}

.stTextInput input,.stTextArea textarea{

    min-height:30px!important;

    height:30px!important;

    border-radius:999px!important;

    border:1px solid #d4d8df!important;

    background:#f3f4f6!important;

    color:var(--text)!important;

    font-size:.78rem!important;

    padding:.18rem .72rem!important;

}

.stTextInput input:focus,.stTextArea textarea:focus{border-color:var(--magenta)!important;box-shadow:0 0 0 3px rgba(212,20,127,.11)!important;}

.stButton>button{

    background:linear-gradient(90deg,var(--magenta),var(--magenta-2));

    color:white;border:0;border-radius:999px;

    min-height:32px;height:32px;

    font-size:.76rem;font-weight:900;

    box-shadow:0 12px 24px rgba(212,20,127,.24);

}

.stButton>button:hover{color:white;border:0;filter:brightness(.98);}



/* Marca y resto app */

.bunseki-brand{display:flex;align-items:center;gap:12px;}

.bunseki-logo{width:48px;height:48px;border-radius:16px;background:linear-gradient(135deg,var(--magenta),var(--magenta-2));display:flex;align-items:center;justify-content:center;color:white;font-weight:900;font-size:1.25rem;box-shadow:0 12px 28px rgba(212,20,127,.25);border:1px solid rgba(255,255,255,.45);}

.bunseki-title{font-size:1.45rem;font-weight:900;color:var(--magenta-dark);line-height:1;margin:0;}

.bunseki-subtitle{font-size:.82rem;color:var(--muted);margin-top:5px;}

.card{background:var(--card);border:1px solid var(--line);border-radius:18px;padding:18px;box-shadow:0 10px 26px rgba(31,41,55,.06);margin-bottom:14px;}

.hero{background:linear-gradient(135deg,#ffffff 0%,#fff1f8 100%);border:1px solid #f3c6df;border-radius:22px;padding:20px 22px;box-shadow:0 12px 28px rgba(194,24,122,.08);margin-bottom:16px;}

.badge{display:inline-block;background:#fce7f3;color:var(--magenta-dark);padding:6px 12px;border-radius:999px;font-weight:800;border:1px solid #f5b8d5;font-size:.82rem;}

.small{color:var(--muted);font-size:.86rem;}

.user{background:#fff7fb;border-left:5px solid var(--magenta);border-radius:14px;padding:12px;margin:10px 0;}

.bot{background:#ffffff;border-left:5px solid #9ca3af;border-radius:14px;padding:12px;margin:10px 0;border:1px solid var(--line);}

.metric{background:#fff;border-radius:16px;padding:14px;border:1px solid var(--line);text-align:center;box-shadow:0 8px 20px rgba(31,41,55,.04);}

.metric b{font-size:1.25rem;color:var(--magenta-dark);}

.metric span{color:var(--muted);font-size:.84rem;}

section[data-testid="stSidebar"]{background:#ffffff;border-right:1px solid var(--line);}

section[data-testid="stSidebar"] h1,section[data-testid="stSidebar"] h2,section[data-testid="stSidebar"] h3{color:var(--magenta-dark)!important;}

[data-testid="stDataFrame"]{border-radius:14px;overflow:hidden;}



/* Responsive celular */

@media(max-width:980px){

    .login-premium-page{min-height:auto;display:block;padding:24px 0 12px;}

    .ad-panel{display:none;}

    .login-card-premium{max-width:305px;margin:22px auto 0;padding:30px 17px 14px;}

    .block-container{padding-left:.85rem!important;padding-right:.85rem!important;}

}

@media(max-width:430px){

    .login-card-premium{max-width:92vw;border-radius:24px;}

    .login-title{font-size:1.24rem;}

    .login-subtitle{font-size:.74rem;}

    .logo-orb{width:54px;height:54px;font-size:1.18rem;margin-top:-60px;}

    .stTextInput input{height:29px!important;min-height:29px!important;font-size:.76rem!important;}

    .stButton>button{height:31px;min-height:31px;}

}



/* ===================== AULA POST-LOGIN PREMIUM ===================== */

.hero{position:relative;overflow:hidden;background:linear-gradient(135deg,#ffffff 0%,#fff7fb 55%,#fde7f3 100%)!important;border:1px solid #f4b8d5!important;box-shadow:0 18px 42px rgba(111,15,73,.10)!important;}

.hero::after{content:"";position:absolute;right:-55px;top:-55px;width:180px;height:180px;background:radial-gradient(circle,rgba(212,20,127,.16),rgba(212,20,127,0) 70%);}

.hero .bunseki-brand{position:relative;z-index:2;}

section[data-testid="stSidebar"]{background:linear-gradient(180deg,#ffffff 0%,#fff7fb 58%,#f3f4f6 100%)!important;border-right:1px solid #f1b8d4!important;box-shadow:12px 0 30px rgba(111,15,73,.06)!important;}

section[data-testid="stSidebar"] [data-testid="stVerticalBlock"]{gap:.7rem!important;}

section[data-testid="stSidebar"] .stSelectbox{background:#ffffff;border:1px solid #f1d1e3;border-radius:15px;padding:8px 10px;box-shadow:0 8px 18px rgba(31,41,55,.04);}

section[data-testid="stSidebar"] .stButton>button{width:100%;}

.metric{border:1px solid #f0c8dc!important;background:linear-gradient(180deg,#ffffff 0%,#fffafd 100%)!important;}

.card{border:1px solid #e8d5e0!important;}

.quiz-card-premium{background:linear-gradient(180deg,#ffffff 0%,#fff7fb 100%);border:1px solid #f0c8dc;border-radius:18px;padding:16px;box-shadow:0 10px 26px rgba(31,41,55,.06);margin-bottom:14px;}

.quiz-card-premium h3{margin-top:0;color:var(--magenta-dark);}

.quiz-topic-chip{display:inline-block;background:#fce7f3;border:1px solid #f5b8d5;color:var(--magenta-dark);font-weight:800;border-radius:999px;padding:4px 10px;font-size:.78rem;margin-bottom:8px;}





/* ===================== SIDEBAR PROFESIONAL TIPO APP ===================== */

section[data-testid="stSidebar"]{

    display:block !important;

    min-width:285px !important;

    background:linear-gradient(180deg,#ffffff 0%,#fff7fb 54%,#f3f4f6 100%)!important;

    border-right:1px solid #f1b8d4!important;

    box-shadow:12px 0 30px rgba(111,15,73,.08)!important;

}

section[data-testid="stSidebar"] [data-testid="stSidebarContent"]{padding:1.05rem .9rem!important;}

.sidebar-brand-pro{background:linear-gradient(135deg,#d4147f,#ff2f93);color:#fff;border-radius:20px;padding:14px 14px;margin-bottom:14px;box-shadow:0 16px 32px rgba(212,20,127,.23)}

.sidebar-brand-pro .sb-title{font-weight:950;font-size:1.15rem;line-height:1}.sidebar-brand-pro .sb-sub{font-size:.76rem;opacity:.92;margin-top:5px}

.sidebar-chip-pro{display:inline-block;background:#fff;color:#6f0f49;border:1px solid #f5b8d5;border-radius:999px;padding:4px 10px;font-size:.74rem;font-weight:900;margin-top:10px}

.sidebar-section-title{font-size:.74rem;text-transform:uppercase;letter-spacing:.08em;color:#8a476e;font-weight:900;margin:12px 0 6px}

section[data-testid="stSidebar"] .stSelectbox{background:#ffffff;border:1px solid #f1d1e3;border-radius:16px;padding:8px 10px;box-shadow:0 8px 18px rgba(31,41,55,.04);}

section[data-testid="stSidebar"] .stButton>button{width:100%;height:36px;min-height:36px;border-radius:999px;background:linear-gradient(90deg,#d4147f,#ff2f93);}

.teacher-card{background:linear-gradient(180deg,#ffffff 0%,#fffafd 100%);border:1px solid #f0c8dc;border-radius:18px;padding:16px;margin:12px 0;box-shadow:0 10px 26px rgba(31,41,55,.06)}

.teacher-card h3{margin:.1rem 0 .35rem;color:#6f0f49}.teacher-chip{display:inline-block;background:#fce7f3;border:1px solid #f5b8d5;color:#6f0f49;font-weight:800;border-radius:999px;padding:4px 10px;font-size:.76rem;margin:4px 6px 2px 0}

.export-grid-note{font-size:.82rem;color:#6b7280;margin-top:4px}

@media(max-width:760px){section[data-testid="stSidebar"]{min-width:250px!important}.block-container{padding-left:.7rem!important;padding-right:.7rem!important}}





.student-export-card{background:linear-gradient(180deg,#ffffff 0%,#fff7fb 100%);border:1px solid #f0c8dc;border-radius:18px;padding:15px;box-shadow:0 10px 26px rgba(31,41,55,.06);margin:12px 0 14px;}

.student-export-card h3{margin:.1rem 0 .35rem;color:#6f0f49}.student-export-card p{margin:.25rem 0 .65rem;color:#6b7280;font-size:.84rem}

section[data-testid="stSidebar"]{display:block!important;visibility:visible!important;}



/* ===================== SIDEBAR SAAS PRO NOTION / CHATGPT ===================== */

[data-testid="collapsedControl"]{

    display:flex !important;

    visibility:visible !important;

    position:fixed !important;

    top:14px !important;

    left:14px !important;

    z-index:999999 !important;

    background:#ffffff !important;

    border:1px solid #f1b8d4 !important;

    border-radius:12px !important;

    box-shadow:0 10px 24px rgba(111,15,73,.16) !important;

}

button[kind="header"]{display:flex !important; visibility:visible !important;}

section[data-testid="stSidebar"]{

    display:block !important;

    visibility:visible !important;

    min-width:292px !important;

    max-width:292px !important;

    transition:all .28s ease-in-out !important;

    background:linear-gradient(180deg,#ffffff 0%,#fff7fb 52%,#f5f6f8 100%)!important;

    border-right:1px solid #f1b8d4!important;

    box-shadow:14px 0 36px rgba(111,15,73,.08)!important;

}

section[data-testid="stSidebar"] [data-testid="stSidebarContent"]{

    padding:1rem .85rem 1.25rem !important;

}

.saas-sidebar-brand{

    background:linear-gradient(135deg,#d4147f 0%,#ff2f93 100%);

    color:#fff;border-radius:22px;padding:16px 15px;margin:4px 0 14px;

    box-shadow:0 18px 36px rgba(212,20,127,.26);

}

.saas-sidebar-brand .logo-row{display:flex;align-items:center;gap:10px;}

.saas-sidebar-brand .logo-mini{width:40px;height:40px;border-radius:13px;background:rgba(255,255,255,.18);display:flex;align-items:center;justify-content:center;font-weight:950;border:1px solid rgba(255,255,255,.35)}

.saas-sidebar-brand .title{font-weight:950;font-size:1.05rem;line-height:1}

.saas-sidebar-brand .subtitle{font-size:.74rem;opacity:.92;margin-top:4px}

.saas-role-chip{display:inline-block;background:#fff;color:#6f0f49;border:1px solid #f5b8d5;border-radius:999px;padding:4px 10px;font-size:.72rem;font-weight:900;margin-top:12px}

.saas-section-title{font-size:.70rem;text-transform:uppercase;letter-spacing:.10em;color:#8a476e;font-weight:950;margin:13px 0 7px}

.saas-help-card{background:#fff;border:1px solid #f1d1e3;border-radius:16px;padding:11px 12px;margin:10px 0;color:#6b7280;font-size:.78rem;box-shadow:0 8px 18px rgba(31,41,55,.04)}

section[data-testid="stSidebar"] div[role="radiogroup"] label{

    background:#fff !important;

    border:1px solid #f1d1e3 !important;

    border-radius:14px !important;

    padding:7px 10px !important;

    margin:3px 0 !important;

    box-shadow:0 7px 15px rgba(31,41,55,.035);

}

section[data-testid="stSidebar"] div[role="radiogroup"] label:hover{

    border-color:#d4147f !important;

    background:#fff7fb !important;

}

section[data-testid="stSidebar"] .stSelectbox,

section[data-testid="stSidebar"] .stNumberInput{

    background:#fff;border:1px solid #f1d1e3;border-radius:16px;padding:8px 10px;margin-bottom:8px;box-shadow:0 8px 18px rgba(31,41,55,.04);

}

section[data-testid="stSidebar"] .stButton>button{

    width:100%;height:36px;min-height:36px;border-radius:999px;background:linear-gradient(90deg,#d4147f,#ff2f93);

}

@media(max-width:760px){

    section[data-testid="stSidebar"]{min-width:265px!important;max-width:265px!important;}

    [data-testid="collapsedControl"]{top:8px!important;left:8px!important;}

    .block-container{padding-left:.7rem!important;padding-right:.7rem!important;}

}



</style>

"""

st.markdown(CSS, unsafe_allow_html=True)

st.markdown("""

<style>



/* =========================================

   FIX QUIZ (AZUL MARINO BAJO)

========================================= */

.quiz-card-premium{

    background:linear-gradient(180deg,#1e293b 0%, #0f172a 100%) !important;

    border:1px solid #334155 !important;

    color:#e5e7eb !important;

}



.quiz-card-premium h3,

.quiz-card-premium p,

.quiz-card-premium span{

    color:#e5e7eb !important;

}



.quiz-topic-chip{

    background:#334155 !important;

    border:1px solid #475569 !important;

    color:#e2e8f0 !important;

}



/* preguntas del quiz */

div[data-testid="stRadio"] label{

    color:#e5e7eb !important;

}



/* opciones */

div[role="radiogroup"] label{

    background:#0f172a !important;

    border:1px solid #334155 !important;

    color:#e5e7eb !important;

}



div[role="radiogroup"] label:hover{

    background:#1e293b !important;

    border-color:#ff2f93 !important;

}



div[role="radiogroup"] input:checked + div{

    color:#ff2f93 !important;

}





/* =========================================

   FIX CONFIGURACIÓN ACADÉMICA (PLOMO OSCURO)

========================================= */

section[data-testid="stSidebar"] .stSelectbox{

    background:#ffe4f2 !important; /* plomo oscuro */

    border:1px solid #374151 !important;

    border-radius:14px !important;

}



/* texto dentro del select */

section[data-testid="stSidebar"] .stSelectbox div,

section[data-testid="stSidebar"] .stSelectbox span{

    color:#ffe4f2 !important;

}



/* dropdown abierto */

div[data-baseweb="select"]{

    background:#1f2937 !important;

    color:#ffe4f2 !important;

}



/* inputs dentro del sidebar */

section[data-testid="stSidebar"] input{

    background:#1f2937 !important;

    color:#e5e7eb !important;

    border:1px solid #374151 !important;

}





/* =========================================

   EXTRA: mejora contraste general sidebar

========================================= */

section[data-testid="stSidebar"]{

    background:linear-gradient(180deg,#1f2937 0%, #111827 100%) !important;

}



section[data-testid="stSidebar"] *{

    color:#ffe4f2 !important;

}



</style>

""", unsafe_allow_html=True)



# =========================================================

# NIVEL PRO TOTAL: FIX FINAL DE SIDEBAR, CONTRASTE Y BOTONES

# =========================================================

st.markdown("""

<style>

/* ---------- Base visual limpia ---------- */

html, body, .stApp, [data-testid="stAppViewContainer"]{

    color:#111827 !important;

}

.block-container{

    padding-top:.75rem !important;

}



/* ---------- Botón nativo de abrir/cerrar sidebar MUY visible ---------- */

[data-testid="collapsedControl"]{

    display:flex !important;

    visibility:visible !important;

    opacity:1 !important;

    position:fixed !important;

    top:14px !important;

    left:14px !important;

    z-index:2147483647 !important;

    width:46px !important;

    height:46px !important;

    min-width:46px !important;

    min-height:46px !important;

    align-items:center !important;

    justify-content:center !important;

    background:linear-gradient(135deg,#d4147f,#ff2f93) !important;

    border:2px solid #ffffff !important;

    border-radius:16px !important;

    box-shadow:0 14px 30px rgba(212,20,127,.45) !important;

}

[data-testid="collapsedControl"] svg,

[data-testid="collapsedControl"] svg path{

    fill:#ffffff !important;

    stroke:#ffffff !important;

    color:#ffffff !important;

}

[data-testid="collapsedControl"]::after{

    content:"Menú";

    position:absolute;

    left:54px;

    top:8px;

    background:#ffffff;

    color:#6f0f49;

    border:1px solid #f1b8d4;

    border-radius:999px;

    padding:5px 10px;

    font-size:.74rem;

    font-weight:900;

    white-space:nowrap;

    box-shadow:0 8px 18px rgba(31,41,55,.12);

}

button[kind="header"]{

    display:flex !important;

    visibility:visible !important;

    opacity:1 !important;

}



/* ---------- Sidebar tipo Notion / ChatGPT: contraste fuerte ---------- */

section[data-testid="stSidebar"]{

    display:block !important;

    visibility:visible !important;

    background:linear-gradient(180deg,#ffffff 0%,#fff7fb 56%,#f4f5f7 100%) !important;

    border-right:1px solid #f1b8d4 !important;

    box-shadow:14px 0 34px rgba(111,15,73,.10) !important;

    min-width:292px !important;

    max-width:292px !important;

    transition:all .28s ease-in-out !important;

}

section[data-testid="stSidebar"] [data-testid="stSidebarContent"]{

    padding:1rem .85rem 1.25rem !important;

}

section[data-testid="stSidebar"] *,

section[data-testid="stSidebar"] p,

section[data-testid="stSidebar"] span,

section[data-testid="stSidebar"] label,

section[data-testid="stSidebar"] div{

    color:#1f2937 !important;

}

section[data-testid="stSidebar"] .saas-sidebar-brand,

section[data-testid="stSidebar"] .saas-sidebar-brand *,

section[data-testid="stSidebar"] .sidebar-brand-pro,

section[data-testid="stSidebar"] .sidebar-brand-pro *{

    color:#ffffff !important;

}

section[data-testid="stSidebar"] .saas-role-chip,

section[data-testid="stSidebar"] .sidebar-chip-pro{

    color:#6f0f49 !important;

    background:#ffffff !important;

}

.saas-section-title,

.sidebar-section-title{

    color:#6f0f49 !important;

    font-weight:950 !important;

}

.saas-help-card,

.saas-help-card *,

.sidebar-help-card,

.sidebar-help-card *{

    color:#374151 !important;

}



/* ---------- Menú radio: legible y con selección magenta ---------- */

section[data-testid="stSidebar"] div[role="radiogroup"] label{

    background:#ffffff !important;

    border:1px solid #f1d1e3 !important;

    border-radius:14px !important;

    padding:8px 10px !important;

    margin:4px 0 !important;

    box-shadow:0 7px 15px rgba(31,41,55,.04) !important;

}

section[data-testid="stSidebar"] div[role="radiogroup"] label *{

    color:#111827 !important;

    font-weight:800 !important;

}

section[data-testid="stSidebar"] div[role="radiogroup"] label:hover{

    background:#fff0f7 !important;

    border-color:#d4147f !important;

}

section[data-testid="stSidebar"] div[role="radiogroup"] input:checked + div,

section[data-testid="stSidebar"] div[role="radiogroup"] [aria-checked="true"]{

    color:#d4147f !important;

}



/* ---------- Selectbox y campos en sidebar ---------- */

section[data-testid="stSidebar"] .stSelectbox,

section[data-testid="stSidebar"] .stNumberInput{

    background:#ffffff !important;

    border:1px solid #f1d1e3 !important;

    border-radius:16px !important;

    padding:8px 10px !important;

    margin-bottom:8px !important;

    box-shadow:0 8px 18px rgba(31,41,55,.04) !important;

}

section[data-testid="stSidebar"] input,

section[data-testid="stSidebar"] textarea,

section[data-testid="stSidebar"] [data-baseweb="select"] *,

section[data-testid="stSidebar"] [data-baseweb="input"] *{

    color:#111827 !important;

    -webkit-text-fill-color:#111827 !important;

}



/* ---------- Botones normales y de descarga PRO ---------- */

.stButton > button,

.stDownloadButton > button,

div[data-testid="stDownloadButton"] button{

    background:linear-gradient(90deg,#d4147f,#ff2f93) !important;

    color:#ffffff !important;

    -webkit-text-fill-color:#ffffff !important;

    border:0 !important;

    border-radius:999px !important;

    min-height:38px !important;

    height:38px !important;

    font-weight:900 !important;

    box-shadow:0 12px 24px rgba(212,20,127,.22) !important;

}

.stButton > button *,

.stDownloadButton > button *,

div[data-testid="stDownloadButton"] button *{

    color:#ffffff !important;

    -webkit-text-fill-color:#ffffff !important;

}

.stButton > button:hover,

.stDownloadButton > button:hover,

div[data-testid="stDownloadButton"] button:hover{

    filter:brightness(.96) !important;

    transform:translateY(-1px) !important;

    border:0 !important;

}

.stDownloadButton > button:focus,

div[data-testid="stDownloadButton"] button:focus{

    outline:3px solid rgba(212,20,127,.18) !important;

}



/* ---------- Botones del área de descargas en tarjetas ---------- */

.student-export-card + div .stDownloadButton button,

.teacher-card + div .stDownloadButton button{

    background:#ffffff !important;

    color:#6f0f49 !important;

    -webkit-text-fill-color:#6f0f49 !important;

    border:1px solid #f1b8d4 !important;

    box-shadow:0 8px 18px rgba(111,15,73,.08) !important;

}

.student-export-card + div .stDownloadButton button *,

.teacher-card + div .stDownloadButton button *{

    color:#6f0f49 !important;

    -webkit-text-fill-color:#6f0f49 !important;

}

.student-export-card + div .stDownloadButton button:hover,

.teacher-card + div .stDownloadButton button:hover{

    background:linear-gradient(90deg,#d4147f,#ff2f93) !important;

    color:#ffffff !important;

    -webkit-text-fill-color:#ffffff !important;

}

.student-export-card + div .stDownloadButton button:hover *,

.teacher-card + div .stDownloadButton button:hover *{

    color:#ffffff !important;

    -webkit-text-fill-color:#ffffff !important;

}



/* ---------- Área de contenido: tarjetas premium ---------- */

.hero, .card, .metric, .student-export-card, .quiz-card-premium, .teacher-card{

    backdrop-filter:blur(8px) !important;

}

.metric b{color:#6f0f49 !important;}

.metric span,.small{color:#4b5563 !important;}



/* ---------- Responsive móvil ---------- */

@media(max-width:760px){

    section[data-testid="stSidebar"]{

        min-width:270px !important;

        max-width:270px !important;

    }

    [data-testid="collapsedControl"]{

        top:8px !important;

        left:8px !important;

        width:42px !important;

        height:42px !important;

    }

    [data-testid="collapsedControl"]::after{

        display:none !important;

    }

    .block-container{

        padding-left:.75rem !important;

        padding-right:.75rem !important;

    }

}

</style>

""", unsafe_allow_html=True)







st.markdown("""

<style>

/* ===================== EVALUACIÓN INTELIGENTE ADAPTATIVA ===================== */

.eval-hero{

    background:linear-gradient(135deg,#ffffff 0%,#fff1f8 60%,#fde7f3 100%);

    border:1px solid #f4b8d5;

    border-radius:24px;

    padding:20px 22px;

    box-shadow:0 16px 38px rgba(111,15,73,.10);

    margin:14px 0;

}

.eval-hero h2{margin:.1rem 0 .35rem;color:#6f0f49;font-size:1.55rem;}

.eval-hero p{margin:.25rem 0;color:#4b5563;font-size:.92rem;}

.eval-card{

    background:#ffffff;

    border:1px solid #f1c8dc;

    border-radius:20px;

    padding:16px;

    margin:12px 0;

    box-shadow:0 10px 26px rgba(31,41,55,.06);

}

.eval-card h3{margin:.1rem 0 .45rem;color:#6f0f49;}

.eval-chip{

    display:inline-block;

    background:#fce7f3;

    border:1px solid #f5b8d5;

    color:#6f0f49;

    border-radius:999px;

    padding:5px 11px;

    font-weight:850;

    font-size:.76rem;

    margin:3px 5px 3px 0;

}

.eval-question{

    background:linear-gradient(180deg,#ffffff 0%,#fff7fb 100%);

    border:1px solid #f0c8dc;

    border-radius:18px;

    padding:14px;

    margin:12px 0;

}

.eval-result-pass{

    background:#ecfdf5;border:1px solid #86efac;color:#14532d;

    border-radius:18px;padding:16px;margin:14px 0;font-weight:800;

}

.eval-result-fail{

    background:#fff7ed;border:1px solid #fdba74;color:#7c2d12;

    border-radius:18px;padding:16px;margin:14px 0;font-weight:800;

}

.plan-upload-box{

    background:linear-gradient(135deg,#ffffff 0%,#fff7fb 100%);

    border:1px dashed #d4147f;

    border-radius:20px;

    padding:16px;

    margin:12px 0;

}

</style>

""", unsafe_allow_html=True)



st.markdown("""

<style>

/* Lectura clara de radios superiores y opciones de respuesta */

div[data-testid="stRadio"] div[role="radiogroup"] label{

    background:#ffffff !important;

    border:1px solid #f1b8d4 !important;

    border-radius:14px !important;

    padding:8px 12px !important;

    box-shadow:0 8px 18px rgba(31,41,55,.05) !important;

}

div[data-testid="stRadio"] div[role="radiogroup"] label *,

div[data-testid="stRadio"] div[role="radiogroup"] label p,

div[data-testid="stRadio"] div[role="radiogroup"] label span{

    color:#111827 !important;

    font-weight:850 !important;

}

div[data-testid="stRadio"] div[role="radiogroup"] label:hover{

    background:#fff7fb !important;

    border-color:#d4147f !important;

}

div[data-testid="stRadio"] div[role="radiogroup"] input:checked + div,

div[data-testid="stRadio"] div[role="radiogroup"] [aria-checked="true"]{

    background:#fce7f3 !important;

    color:#9d0b5c !important;

}

div[data-testid="stRadio"] div[role="radiogroup"] [aria-checked="true"] *,

div[data-testid="stRadio"] div[role="radiogroup"] input:checked + div *{

    color:#9d0b5c !important;

}

</style>

""", unsafe_allow_html=True)



# =========================================================

# PRACTICE ACCESSIBILITY / HIGH-CONTRAST ANSWERS

# Keeps question stems readable independently of browser/theme.

# =========================================================

st.markdown("""

<style>

div[data-testid="stRadio"] > label,

div[data-testid="stRadio"] > label *,

div[data-testid="stRadio"] > label p,

div[data-testid="stRadio"] > label span{

    color:#2A1B4D !important;

    font-weight:800 !important;

    opacity:1 !important;

}

div[data-testid="stRadio"] div[role="radiogroup"] label{

    background:#ffffff !important;

    border:1px solid #e8b8cf !important;

}

div[data-testid="stRadio"] div[role="radiogroup"] label *,

div[data-testid="stRadio"] div[role="radiogroup"] label p,

div[data-testid="stRadio"] div[role="radiogroup"] label span{

    color:#1f2937 !important;

    opacity:1 !important;

}

div[data-testid="stRadio"] div[role="radiogroup"] [aria-checked="true"],

div[data-testid="stRadio"] div[role="radiogroup"] input:checked + div{

    background:#fff0f7 !important;

    border-color:#E0177D !important;

}

div[data-testid="stRadio"] div[role="radiogroup"] [aria-checked="true"] *,

div[data-testid="stRadio"] div[role="radiogroup"] input:checked + div *{

    color:#7A0D4C !important;

}

</style>

""", unsafe_allow_html=True)



# =========================================================

# SIDEBAR STABILITY V2

# Native collapse/reopen controls + permanent main navigation fallback.

# This block intentionally stays after all legacy CSS.

# =========================================================

st.markdown("""

<style id="bunseki-sidebar-stability-v2">



header[data-testid="stHeader"]{

    visibility:visible !important;

    overflow:visible !important;

    height:56px !important;

    min-height:56px !important;

    pointer-events:auto !important;

    background:transparent !important;

    z-index:2147483647 !important;

}

header[data-testid="stHeader"] > div{

    visibility:visible !important;

    overflow:visible !important;

    pointer-events:auto !important;

}



section[data-testid="stSidebar"] [data-testid="stSidebarContent"]{

    overflow-y:auto !important;

    overflow-x:hidden !important;

    height:100dvh !important;

    max-height:100dvh !important;

    padding-bottom:calc(env(safe-area-inset-bottom, 0px) + 1.5rem) !important;

}



[data-testid="collapsedControl"],

[data-testid="stSidebarCollapsedControl"],

[data-testid="stExpandSidebarButton"],

button[data-testid="stSidebarCollapseButton"],

header[data-testid="stHeader"] button[aria-label*="sidebar" i],

header[data-testid="stHeader"] button[title*="sidebar" i],

header[data-testid="stHeader"] button[aria-label*="menu" i],

header[data-testid="stHeader"] button[kind="header"]{

    display:flex !important;

    visibility:visible !important;

    opacity:1 !important;

    pointer-events:auto !important;

    z-index:2147483647 !important;

}



[data-testid="collapsedControl"],

[data-testid="stSidebarCollapsedControl"],

[data-testid="stExpandSidebarButton"],

header[data-testid="stHeader"] button[aria-label*="open sidebar" i],

header[data-testid="stHeader"] button[aria-label*="expand sidebar" i]{

    position:fixed !important;

    top:10px !important;

    left:10px !important;

    width:48px !important;

    height:48px !important;

    min-width:48px !important;

    min-height:48px !important;

    align-items:center !important;

    justify-content:center !important;

    background:#d4147f !important;

    color:#ffffff !important;

    border:2px solid #ffffff !important;

    border-radius:12px !important;

    box-shadow:0 10px 28px rgba(111,15,73,.42) !important;

}



[data-testid="collapsedControl"] svg,

[data-testid="stSidebarCollapsedControl"] svg,

[data-testid="stExpandSidebarButton"] svg,

header[data-testid="stHeader"] button[aria-label*="sidebar" i] svg{

    color:#ffffff !important;

    fill:#ffffff !important;

    stroke:#ffffff !important;

}



section[data-testid="stSidebar"] .stButton > button{

    white-space:normal !important;

    min-height:42px !important;

    height:auto !important;

    line-height:1.2 !important;

}



section[data-testid="stSidebar"] div[role="radiogroup"]{

    overflow:visible !important;

    max-height:none !important;

}



@media (max-width:1024px){

    section[data-testid="stSidebar"]{

        width:min(88vw,320px) !important;

        min-width:min(88vw,320px) !important;

        max-width:min(88vw,320px) !important;

        height:100dvh !important;

        z-index:2147483646 !important;

    }



    .block-container{

        padding-top:4.25rem !important;

        padding-left:.75rem !important;

        padding-right:.75rem !important;

    }

}

</style>

""", unsafe_allow_html=True)



def bunseki_logo_html(title="BunsekiChat", subtitle="Tutor matemático que personaliza el apoyo y genera evidencia útil de aprendizaje"):
    title = str(title or "BunsekiChat")
    subtitle = str(subtitle or "")
    return (
        "<div class='bunseki-brand bunseki-brand-compact'>"
        f"<img class='bunseki-brand-logo' src='{BUNSEKI_FRONTEND_LOGO_DATA_URI}' alt='BunsekiChat'>"
        "<div class='bunseki-brand-copy'>"
        f"<div class='bunseki-title'>{title}</div>"
        f"<div class='bunseki-subtitle'>{subtitle}</div>"
        "</div>"
        "</div>"
    )

def inject_bunseki_frontend_skin():
    if st.session_state.get("_bunseki_frontend_skin_applied"):
        return
    st.session_state["_bunseki_frontend_skin_applied"] = True
    st.markdown(
        f"""
        <style>
        .block-container {{
            padding-top: 0.65rem !important;
            padding-bottom: 0.8rem !important;
        }}

        .login-premium-page {{
            width: 100%;
            max-width: 980px;
            margin: 0 auto;
        }}

        .ad-panel {{
            display: none !important;
        }}

        .login-card-premium {{
            width: min(560px, 94vw) !important;
            max-width: 560px !important;
            margin: 0.35rem auto 0.85rem !important;
            padding: 1.1rem 1rem 0.9rem !important;
            border-radius: 24px !important;
            box-shadow: 0 10px 28px rgba(42,27,77,.08) !important;
        }}

        .logo-orb {{
            width: min(360px, 82vw) !important;
            height: 120px !important;
            margin: 0 auto 0.25rem !important;
            background: url('{BUNSEKI_FRONTEND_LOGO_DATA_URI}') center center / contain no-repeat !important;
            color: transparent !important;
            border: 0 !important;
            border-radius: 0 !important;
            box-shadow: none !important;
            text-indent: -9999px !important;
            overflow: hidden !important;
        }}

        .login-title {{
            display: none !important;
        }}

        .login-subtitle {{
            margin: 0.2rem auto 0.8rem !important;
            max-width: 29rem !important;
            color: #5e5874 !important;
            font-size: 0.98rem !important;
            line-height: 1.5 !important;
        }}

        .login-card-premium [data-baseweb="tab-list"] {{
            margin-top: 0.25rem !important;
            margin-bottom: 0.55rem !important;
        }}

        .login-card-premium .stTextInput,
        .login-card-premium .stButton {{
            margin-top: 0.15rem !important;
        }}

        .hero {{
            padding: 0.95rem 1rem !important;
            margin-bottom: 0.75rem !important;
            border-radius: 22px !important;
        }}

        .bunseki-brand-compact {{
            display: flex !important;
            align-items: center !important;
            gap: 0.9rem !important;
            flex-wrap: wrap !important;
        }}

        .bunseki-brand-logo {{
            width: min(320px, 100%) !important;
            max-width: 320px !important;
            height: auto !important;
            flex: 0 1 auto !important;
            display: block !important;
        }}

        .bunseki-brand-copy {{
            flex: 1 1 220px !important;
            min-width: 220px !important;
        }}

        .bunseki-title {{
            color: #1f3566 !important;
            font-size: clamp(1.55rem, 2vw, 2.25rem) !important;
            line-height: 1.06 !important;
            margin: 0 0 0.18rem !important;
            font-weight: 800 !important;
        }}

        .bunseki-subtitle {{
            color: #6a6183 !important;
            line-height: 1.45 !important;
            font-size: 0.98rem !important;
            margin: 0 !important;
        }}

        .stSelectbox label, .stRadio label, .stTextInput label {{
            color: #2A1B4D !important;
        }}

        @media (max-width: 900px) {{
            .login-card-premium {{
                width: min(94vw, 560px) !important;
                padding: 1rem 0.95rem 0.85rem !important;
            }}

            .logo-orb {{
                width: min(320px, 84vw) !important;
                height: 104px !important;
            }}

            .bunseki-brand-logo {{
                width: min(280px, 100%) !important;
            }}
        }}

        @media (max-width: 640px) {{
            .block-container {{
                padding-top: 0.35rem !important;
                padding-bottom: 0.55rem !important;
            }}

            .login-card-premium {{
                width: 95vw !important;
                max-width: 95vw !important;
                border-radius: 20px !important;
                padding: 0.9rem 0.85rem 0.8rem !important;
            }}

            .logo-orb {{
                width: min(290px, 80vw) !important;
                height: 92px !important;
            }}

            .login-subtitle {{
                font-size: 0.92rem !important;
            }}

            .bunseki-brand-compact {{
                gap: 0.65rem !important;
            }}

            .bunseki-brand-logo {{
                width: min(250px, 100%) !important;
            }}

            .bunseki-title {{
                font-size: 1.35rem !important;
            }}

            .bunseki-subtitle {{
                font-size: 0.92rem !important;
            }}
        }}
        </style>
        """,
        unsafe_allow_html=True,
    )








# ---------------- DB PostgreSQL / Supabase ----------------



from bunseki.assessment_attempts import (

    AttemptError as ResumableAttemptError,

    get_or_create_attempt as resumable_get_or_create_attempt,

    load_attempt_progress as resumable_load_attempt_progress,

    resume_attempt as resumable_resume_attempt,

    save_attempt_answer as resumable_save_attempt_answer,

    submit_attempt as resumable_submit_attempt,

)



def _pg_connect():

    """Conexión PostgreSQL compatible con Supabase."""

    if not DATABASE_URL:

        st.error("Falta DATABASE_URL. Configúralo en Streamlit Cloud → App → Settings → Secrets.")

        st.stop()

    kwargs = {"cursor_factory": RealDictCursor}

    if "sslmode=" not in DATABASE_URL.lower():

        kwargs["sslmode"] = "require"

    c = psycopg2.connect(DATABASE_URL, **kwargs)

    c.autocommit = True

    return c





def conn():

    """Mantiene el nombre conn() para no cambiar la lógica visual de la app."""

    last_error = None

    for attempt in range(5):

        try:

            return _pg_connect()

        except Exception as e:

            last_error = e

            time.sleep(0.4 * (attempt + 1))

    raise last_error





def fetchone(sql, params=None):

    c = conn()

    try:

        with c.cursor() as cur:

            cur.execute(sql, params or ())

            row = cur.fetchone()

            return dict(row) if row else None

    finally:

        c.close()





def fetchall(sql, params=None):

    c = conn()

    try:

        with c.cursor() as cur:

            cur.execute(sql, params or ())

            rows = cur.fetchall()

            return [dict(r) for r in rows]

    finally:

        c.close()





def execute(sql, params=None, returning=False):

    c = conn()

    try:

        with c.cursor() as cur:

            cur.execute(sql, params or ())

            if returning:

                row = cur.fetchone()

                return dict(row) if row else None

            return None

    finally:

        c.close()





from bunseki.security.passwords import (

    hash_password,

    safe_password,

    verify_password,

)





def init_db():

    """Crea el esquema PostgreSQL equivalente al antiguo SQLite."""

    with DB_LOCK:

        c = conn()

        try:

            with c.cursor() as cur:

                cur.execute("""

                CREATE TABLE IF NOT EXISTS users(

                    id SERIAL PRIMARY KEY,

                    username TEXT UNIQUE NOT NULL,

                    password_hash TEXT NOT NULL,

                    role TEXT DEFAULT 'student',

                    active BOOLEAN DEFAULT TRUE,

                    created_at TEXT NOT NULL,

                    last_login TEXT,

                    password_temporal BOOLEAN NOT NULL DEFAULT FALSE,

                    primer_ingreso BOOLEAN NOT NULL DEFAULT FALSE

                );

                CREATE TABLE IF NOT EXISTS profiles(

                    user_id INTEGER PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE,

                    first_names TEXT,

                    last_names TEXT,

                    birth_date TEXT,

                    course TEXT,

                    subject TEXT,

                    course_level TEXT,

                    parallel TEXT,

                    shift TEXT,

                    cohort TEXT,

                    full_name_normalized TEXT,

                    research_group TEXT DEFAULT 'Sin asignar',

                    cedula TEXT,

                    correo TEXT,

                    estado_dato TEXT NOT NULL DEFAULT 'piloto',

                    usar_en_investigacion BOOLEAN NOT NULL DEFAULT FALSE,

                    consentimiento_informado BOOLEAN NOT NULL DEFAULT FALSE,

                    fecha_consentimiento TEXT,

                    pretest_estado TEXT NOT NULL DEFAULT 'pendiente',

                    posttest_estado TEXT NOT NULL DEFAULT 'pendiente',

                    teacher TEXT,

                    phone TEXT,

                    province TEXT,

                    city TEXT,

                    canton TEXT,

                    address TEXT,

                    gps_lat DOUBLE PRECISION,

                    gps_lon DOUBLE PRECISION,

                    gps_accuracy TEXT,

                    level TEXT DEFAULT 'Inicial',

                    profile_completed INTEGER DEFAULT 0,

                    last_gps_lat DOUBLE PRECISION,

                    last_gps_lon DOUBLE PRECISION,

                    last_gps_accuracy DOUBLE PRECISION,

                    last_gps_at TEXT

                );

                CREATE TABLE IF NOT EXISTS interactions(

                    id SERIAL PRIMARY KEY,

                    user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,

                    role TEXT NOT NULL,

                    topic TEXT,

                    subtopic TEXT,

                    level TEXT,

                    message TEXT NOT NULL,

                    clean_message TEXT,

                    model TEXT,

                    tokens_est INTEGER,

                    latency_ms INTEGER,

                    created_at TEXT NOT NULL,

                    ip_info TEXT,

                    gps_lat DOUBLE PRECISION,

                    gps_lon DOUBLE PRECISION,

                    gps_accuracy DOUBLE PRECISION,

                    gps_source TEXT

                );

                CREATE TABLE IF NOT EXISTS location_events(

                    id SERIAL PRIMARY KEY,

                    user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,

                    event_type TEXT NOT NULL,

                    page TEXT,

                    topic TEXT,

                    subtopic TEXT,

                    gps_lat DOUBLE PRECISION,

                    gps_lon DOUBLE PRECISION,

                    gps_accuracy DOUBLE PRECISION,

                    gps_source TEXT,

                    created_at TEXT NOT NULL

                );

                CREATE TABLE IF NOT EXISTS quizzes(

                    id SERIAL PRIMARY KEY,

                    user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,

                    topic TEXT,

                    level_from TEXT,

                    level_to TEXT,

                    score DOUBLE PRECISION,

                    passed INTEGER,

                    answers_json TEXT,

                    created_at TEXT NOT NULL

                );

                CREATE TABLE IF NOT EXISTS analytic_plans(

                    id SERIAL PRIMARY KEY,

                    title TEXT NOT NULL,

                    course TEXT,

                    subject TEXT,

                    course_level TEXT,

                    parallel TEXT,

                    shift TEXT,

                    teacher_id INTEGER REFERENCES users(id) ON DELETE SET NULL,

                    filename TEXT,

                    raw_text TEXT,

                    summary TEXT,

                    created_at TEXT NOT NULL

                );

                CREATE TABLE IF NOT EXISTS plan_topics(

                    id SERIAL PRIMARY KEY,

                    plan_id INTEGER REFERENCES analytic_plans(id) ON DELETE CASCADE,

                    unit_name TEXT,

                    topic TEXT,

                    subtopic TEXT,

                    learning_outcome TEXT,

                    bloom_level TEXT,

                    keywords TEXT

                );

                CREATE TABLE IF NOT EXISTS adaptive_quizzes(

                    id SERIAL PRIMARY KEY,

                    user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,

                    plan_id INTEGER REFERENCES analytic_plans(id) ON DELETE SET NULL,

                    title TEXT,

                    quiz_type TEXT DEFAULT 'adaptive',

                    source_topic TEXT,

                    difficulty TEXT,

                    subject TEXT,

                    course_level TEXT,

                    parallel TEXT,

                    shift TEXT,

                    cohort TEXT,

                    research_title TEXT,

                    question_count INTEGER,

                    random_seed TEXT,

                    version_code TEXT,

                    dimension_scores_json TEXT,

                    diagnosis_json TEXT,

                    cognitive_profile TEXT,

                    learning_plan_json TEXT,

                    total_time_seconds DOUBLE PRECISION,

                    score DOUBLE PRECISION,

                    passed BOOLEAN DEFAULT FALSE,

                    attempt_no INTEGER DEFAULT 1,

                    parent_quiz_id INTEGER,

                    status TEXT DEFAULT 'generated',

                    recommendation TEXT,

                    created_at TEXT NOT NULL,

                    completed_at TEXT

                );

                CREATE TABLE IF NOT EXISTS adaptive_questions(

                    id SERIAL PRIMARY KEY,

                    quiz_id INTEGER NOT NULL REFERENCES adaptive_quizzes(id) ON DELETE CASCADE,

                    question TEXT NOT NULL,

                    options_json TEXT,

                    correct_answer TEXT,

                    item_code TEXT,

                    position INTEGER,

                    topic TEXT,

                    subtopic TEXT,

                    dimension TEXT,

                    indicator TEXT,

                    competence TEXT,

                    difficulty_level TEXT,

                    user_answer TEXT,

                    response_time_seconds DOUBLE PRECISION,

                    attempts INTEGER DEFAULT 1,

                    explanation TEXT,

                    bloom_level TEXT,

                    conceptual_error TEXT,

                    estimated_confidence DOUBLE PRECISION,

                    is_correct BOOLEAN

                );

                CREATE TABLE IF NOT EXISTS research_exercise_attempts(

                    id SERIAL PRIMARY KEY,

                    user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,

                    quiz_id INTEGER REFERENCES adaptive_quizzes(id) ON DELETE SET NULL,

                    topic TEXT,

                    subtopic TEXT,

                    exercise_text TEXT NOT NULL,

                    options_json TEXT,

                    correct_answer TEXT,

                    user_answer TEXT,

                    difficulty_level TEXT,

                    is_correct BOOLEAN,

                    feedback TEXT,

                    created_at TEXT NOT NULL

                );

                CREATE TABLE IF NOT EXISTS research_survey_responses(

                    id SERIAL PRIMARY KEY,

                    user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,

                    quiz_id INTEGER REFERENCES adaptive_quizzes(id) ON DELETE SET NULL,

                    item_no INTEGER,

                    dimension TEXT,

                    item_text TEXT,

                    score INTEGER,

                    created_at TEXT NOT NULL

                );

                CREATE TABLE IF NOT EXISTS research_learning_events(

                    id SERIAL PRIMARY KEY,

                    user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,

                    quiz_id INTEGER REFERENCES adaptive_quizzes(id) ON DELETE SET NULL,

                    event_type TEXT NOT NULL,

                    duration_seconds DOUBLE PRECISION,

                    metadata_json TEXT,

                    created_at TEXT NOT NULL

                );

                CREATE TABLE IF NOT EXISTS settings(

                    key TEXT PRIMARY KEY,

                    value TEXT

                );

                CREATE TABLE IF NOT EXISTS research_cohorts(

                    code TEXT PRIMARY KEY,

                    name TEXT NOT NULL,

                    research_title TEXT NOT NULL,

                    subject TEXT,

                    course_level TEXT,

                    academic_year INTEGER,

                    active BOOLEAN NOT NULL DEFAULT TRUE,

                    created_at TEXT NOT NULL

                );

                CREATE TABLE IF NOT EXISTS bulk_enrollment_batches(

                    id SERIAL PRIMARY KEY,

                    cohort_code TEXT REFERENCES research_cohorts(code) ON DELETE RESTRICT,

                    source_filename TEXT,

                    imported_by INTEGER REFERENCES users(id) ON DELETE SET NULL,

                    total_rows INTEGER NOT NULL DEFAULT 0,

                    created_rows INTEGER NOT NULL DEFAULT 0,

                    rejected_rows INTEGER NOT NULL DEFAULT 0,

                    created_at TEXT NOT NULL

                );

                CREATE TABLE IF NOT EXISTS bulk_enrollment_rows(

                    id SERIAL PRIMARY KEY,

                    batch_id INTEGER NOT NULL REFERENCES bulk_enrollment_batches(id) ON DELETE CASCADE,

                    source_row INTEGER,

                    user_id INTEGER REFERENCES users(id) ON DELETE SET NULL,

                    cedula TEXT,

                    correo TEXT,

                    status TEXT NOT NULL,

                    detail TEXT,

                    created_at TEXT NOT NULL

                );

                CREATE INDEX IF NOT EXISTS idx_interactions_user_id ON interactions(user_id);

                CREATE INDEX IF NOT EXISTS idx_interactions_created_at ON interactions(created_at);

                CREATE INDEX IF NOT EXISTS idx_location_events_user_id ON location_events(user_id);

                CREATE INDEX IF NOT EXISTS idx_quizzes_user_id ON quizzes(user_id);

                CREATE INDEX IF NOT EXISTS idx_plan_topics_plan_id ON plan_topics(plan_id);

                CREATE INDEX IF NOT EXISTS idx_adaptive_quizzes_user_id ON adaptive_quizzes(user_id);

                CREATE INDEX IF NOT EXISTS idx_adaptive_questions_quiz_id ON adaptive_questions(quiz_id);

                """)

                cur.execute("""

                ALTER TABLE IF EXISTS profiles ADD COLUMN IF NOT EXISTS subject TEXT;

                ALTER TABLE IF EXISTS profiles ADD COLUMN IF NOT EXISTS course_level TEXT;

                ALTER TABLE IF EXISTS profiles ADD COLUMN IF NOT EXISTS parallel TEXT;

                ALTER TABLE IF EXISTS profiles ADD COLUMN IF NOT EXISTS shift TEXT;

                ALTER TABLE IF EXISTS profiles ADD COLUMN IF NOT EXISTS cohort TEXT;

                ALTER TABLE IF EXISTS profiles ADD COLUMN IF NOT EXISTS full_name_normalized TEXT;

                ALTER TABLE IF EXISTS profiles ADD COLUMN IF NOT EXISTS research_group TEXT DEFAULT 'Sin asignar';

                ALTER TABLE IF EXISTS profiles ADD COLUMN IF NOT EXISTS cedula TEXT;

                ALTER TABLE IF EXISTS profiles ADD COLUMN IF NOT EXISTS correo TEXT;

                ALTER TABLE IF EXISTS profiles ADD COLUMN IF NOT EXISTS estado_dato TEXT NOT NULL DEFAULT 'piloto';

                ALTER TABLE IF EXISTS profiles ADD COLUMN IF NOT EXISTS usar_en_investigacion BOOLEAN NOT NULL DEFAULT FALSE;

                ALTER TABLE IF EXISTS profiles ADD COLUMN IF NOT EXISTS consentimiento_informado BOOLEAN NOT NULL DEFAULT FALSE;

                ALTER TABLE IF EXISTS profiles ADD COLUMN IF NOT EXISTS fecha_consentimiento TEXT;

                ALTER TABLE IF EXISTS profiles ADD COLUMN IF NOT EXISTS pretest_estado TEXT NOT NULL DEFAULT 'pendiente';

                ALTER TABLE IF EXISTS profiles ADD COLUMN IF NOT EXISTS posttest_estado TEXT NOT NULL DEFAULT 'pendiente';

                ALTER TABLE IF EXISTS users ADD COLUMN IF NOT EXISTS password_temporal BOOLEAN NOT NULL DEFAULT FALSE;

                ALTER TABLE IF EXISTS users ADD COLUMN IF NOT EXISTS primer_ingreso BOOLEAN NOT NULL DEFAULT FALSE;

                ALTER TABLE IF EXISTS analytic_plans ADD COLUMN IF NOT EXISTS subject TEXT;

                ALTER TABLE IF EXISTS analytic_plans ADD COLUMN IF NOT EXISTS course_level TEXT;

                ALTER TABLE IF EXISTS analytic_plans ADD COLUMN IF NOT EXISTS parallel TEXT;

                ALTER TABLE IF EXISTS analytic_plans ADD COLUMN IF NOT EXISTS shift TEXT;

                ALTER TABLE IF EXISTS adaptive_quizzes ADD COLUMN IF NOT EXISTS quiz_type TEXT DEFAULT 'adaptive';

                ALTER TABLE IF EXISTS adaptive_quizzes ADD COLUMN IF NOT EXISTS subject TEXT;

                ALTER TABLE IF EXISTS adaptive_quizzes ADD COLUMN IF NOT EXISTS course_level TEXT;

                ALTER TABLE IF EXISTS adaptive_quizzes ADD COLUMN IF NOT EXISTS parallel TEXT;

                ALTER TABLE IF EXISTS adaptive_quizzes ADD COLUMN IF NOT EXISTS shift TEXT;

                ALTER TABLE IF EXISTS adaptive_quizzes ADD COLUMN IF NOT EXISTS cohort TEXT;

                ALTER TABLE IF EXISTS adaptive_quizzes ADD COLUMN IF NOT EXISTS research_title TEXT;

                ALTER TABLE IF EXISTS adaptive_quizzes ADD COLUMN IF NOT EXISTS question_count INTEGER;

                ALTER TABLE IF EXISTS adaptive_quizzes ADD COLUMN IF NOT EXISTS random_seed TEXT;

                ALTER TABLE IF EXISTS adaptive_quizzes ADD COLUMN IF NOT EXISTS version_code TEXT;

                ALTER TABLE IF EXISTS adaptive_quizzes ADD COLUMN IF NOT EXISTS dimension_scores_json TEXT;

                ALTER TABLE IF EXISTS adaptive_quizzes ADD COLUMN IF NOT EXISTS diagnosis_json TEXT;

                ALTER TABLE IF EXISTS adaptive_quizzes ADD COLUMN IF NOT EXISTS cognitive_profile TEXT;

                ALTER TABLE IF EXISTS adaptive_quizzes ADD COLUMN IF NOT EXISTS learning_plan_json TEXT;

                ALTER TABLE IF EXISTS adaptive_quizzes ADD COLUMN IF NOT EXISTS total_time_seconds DOUBLE PRECISION;

                ALTER TABLE IF EXISTS adaptive_questions ADD COLUMN IF NOT EXISTS position INTEGER;

                ALTER TABLE IF EXISTS adaptive_questions ADD COLUMN IF NOT EXISTS item_code TEXT;

                ALTER TABLE IF EXISTS adaptive_questions ADD COLUMN IF NOT EXISTS topic TEXT;

                ALTER TABLE IF EXISTS adaptive_questions ADD COLUMN IF NOT EXISTS subtopic TEXT;

                ALTER TABLE IF EXISTS adaptive_questions ADD COLUMN IF NOT EXISTS dimension TEXT;

                ALTER TABLE IF EXISTS adaptive_questions ADD COLUMN IF NOT EXISTS indicator TEXT;

                ALTER TABLE IF EXISTS adaptive_questions ADD COLUMN IF NOT EXISTS competence TEXT;

                ALTER TABLE IF EXISTS adaptive_questions ADD COLUMN IF NOT EXISTS difficulty_level TEXT;

                ALTER TABLE IF EXISTS adaptive_questions ADD COLUMN IF NOT EXISTS response_time_seconds DOUBLE PRECISION;

                ALTER TABLE IF EXISTS adaptive_questions ADD COLUMN IF NOT EXISTS attempts INTEGER DEFAULT 1;

                ALTER TABLE IF EXISTS adaptive_questions ADD COLUMN IF NOT EXISTS conceptual_error TEXT;

                ALTER TABLE IF EXISTS adaptive_questions ADD COLUMN IF NOT EXISTS estimated_confidence DOUBLE PRECISION;

                CREATE INDEX IF NOT EXISTS idx_profiles_cohort ON profiles(cohort);

                CREATE UNIQUE INDEX IF NOT EXISTS uq_profiles_cedula ON profiles(cedula) WHERE cedula IS NOT NULL AND BTRIM(cedula)<>'';

                CREATE UNIQUE INDEX IF NOT EXISTS uq_profiles_correo_lower ON profiles(LOWER(correo)) WHERE correo IS NOT NULL AND BTRIM(correo)<>'';

                CREATE INDEX IF NOT EXISTS idx_profiles_research_scope ON profiles(estado_dato, usar_en_investigacion, consentimiento_informado, cohort, research_group);

                CREATE INDEX IF NOT EXISTS idx_bulk_enrollment_rows_batch ON bulk_enrollment_rows(batch_id, status);

                CREATE INDEX IF NOT EXISTS idx_adaptive_quizzes_research ON adaptive_quizzes(quiz_type, subject, course_level, parallel, shift);

                CREATE INDEX IF NOT EXISTS idx_research_exercise_attempts_user_id ON research_exercise_attempts(user_id);

                CREATE INDEX IF NOT EXISTS idx_research_survey_user_id ON research_survey_responses(user_id);

                CREATE INDEX IF NOT EXISTS idx_research_learning_events_user_id ON research_learning_events(user_id);

                """)

                cur.execute("""

                INSERT INTO research_cohorts(code,name,research_title,subject,course_level,academic_year,active,created_at)

                VALUES(%s,%s,%s,%s,%s,%s,TRUE,%s)

                ON CONFLICT (code) DO UPDATE SET

                    name=EXCLUDED.name,

                    research_title=EXCLUDED.research_title,

                    subject=EXCLUDED.subject,

                    course_level=EXCLUDED.course_level,

                    academic_year=EXCLUDED.academic_year,

                    active=TRUE

                """, (

                    OFFICIAL_COHORT_CODE,

                    "Cohorte oficial Cálculo I 2026",

                    RESEARCH_TITLE,

                    "Cálculo I",

                    "1",

                    2026,

                    now(),

                ))

                cur.execute("""

                UPDATE profiles

                SET estado_dato='piloto', usar_en_investigacion=FALSE

                WHERE estado_dato IS NULL OR BTRIM(estado_dato)='';

                UPDATE profiles p SET pretest_estado='completado'

                WHERE EXISTS (

                    SELECT 1 FROM adaptive_quizzes aq

                    WHERE aq.user_id=p.user_id AND aq.quiz_type='pretest' AND aq.status='completed'

                );

                UPDATE profiles p SET posttest_estado='completado'

                WHERE EXISTS (

                    SELECT 1 FROM adaptive_quizzes aq

                    WHERE aq.user_id=p.user_id AND aq.quiz_type='posttest' AND aq.status='completed'

                );

                CREATE OR REPLACE VIEW research_student_registry AS

                SELECT

                    u.id AS user_id,

                    p.cedula,

                    p.first_names AS nombres,

                    p.last_names AS apellidos,

                    p.correo,

                    p.subject AS materia,

                    p.course_level AS curso,

                    p.parallel AS paralelo,

                    p.shift AS jornada,

                    p.research_group AS grupo_investigacion,

                    p.cohort AS cohorte,

                    p.estado_dato,

                    p.usar_en_investigacion,

                    p.consentimiento_informado,

                    p.fecha_consentimiento,

                    p.pretest_estado,

                    p.posttest_estado,

                    u.password_temporal,

                    u.primer_ingreso,

                    u.active,

                    u.created_at

                FROM users u

                JOIN profiles p ON p.user_id=u.id

                WHERE LOWER(COALESCE(u.role, 'student'))='student';

                """)

                cur.execute("INSERT INTO settings(key,value) VALUES('session_timeout_minutes', %s) ON CONFLICT (key) DO NOTHING", (str(SESSION_TIMEOUT_MINUTES),))

                cur.execute("SELECT id FROM users WHERE username=%s", (ADMIN_USER,))

                if not cur.fetchone():

                    cur.execute("INSERT INTO users(username,password_hash,role,active,created_at) VALUES(%s,%s,%s,%s,%s)", (ADMIN_USER, hash_password(ADMIN_PASSWORD), 'admin', True, now()))

        finally:

            c.close()





@st.cache_resource

def setup_database_once(schema_version):

    # schema_version forma parte de la clave de caché y obliga a ejecutar

    # migraciones nuevas después de un despliegue en caliente de Streamlit.

    init_db()

    return True





def ensure_cohort_schema_compatibility():

    schema = fetchone("""

        SELECT

            EXISTS(

                SELECT 1 FROM information_schema.columns

                WHERE table_schema='public' AND table_name='profiles' AND column_name='estado_dato'

            ) AS has_profile_scope,

            EXISTS(

                SELECT 1 FROM information_schema.columns

                WHERE table_schema='public' AND table_name='profiles' AND column_name='consentimiento_informado'

            ) AS has_consent,

            EXISTS(

                SELECT 1 FROM information_schema.columns

                WHERE table_schema='public' AND table_name='users' AND column_name='password_temporal'

            ) AS has_temporary_password,

            EXISTS(

                SELECT 1 FROM information_schema.columns

                WHERE table_schema='public' AND table_name='users' AND column_name='primer_ingreso'

            ) AS has_first_login

    """) or {}

    if not all(schema.get(key) for key in ["has_profile_scope", "has_consent", "has_temporary_password", "has_first_login"]):

        init_db()





def now(): return datetime.now().strftime("%Y-%m-%d %H:%M:%S")





def normalize_spaces(value):

    return re.sub(r"\s+", " ", str(value or "").strip())





def strip_accents(value):

    normalized = unicodedata.normalize("NFKD", str(value or ""))

    return "".join(ch for ch in normalized if not unicodedata.combining(ch))





def normalize_person_name(value):

    value = normalize_spaces(value)

    value = re.sub(r"[^A-Za-zÁÉÍÓÚÜÑáéíóúüñ' -]", "", value)

    small_words = {"de", "del", "la", "las", "los", "y", "da", "das", "do", "dos"}

    parts = []

    for token in value.lower().split():

        if token in small_words:

            parts.append(token)

        else:

            parts.append("-".join(piece.capitalize() for piece in token.split("-") if piece))

    return normalize_spaces(" ".join(parts))





def normalize_academic_text(value):

    return normalize_spaces(value).title()





def normalize_course_level(value):

    return normalize_spaces(value).upper()





def normalize_parallel(value):

    return normalize_spaces(value).upper().replace(" ", "")





def normalize_shift(value):

    return normalize_spaces(value).title()





def unique_values(values, normalizer=normalize_academic_text):

    seen, clean = set(), []

    for value in values:

        item = normalizer(value)

        key = strip_accents(item).lower()

        if item and key not in seen:

            clean.append(item)

            seen.add(key)

    return clean





def parse_course_label(course):

    course = normalize_spaces(course)

    if not course:

        return {}

    parts = course.split()

    shift = parts[-1].title() if parts else ""

    code = " ".join(parts[:-1]) if len(parts) > 1 else course

    if "-" in code:

        level, parallel = code.split("-", 1)

        return {

            "course_level": normalize_course_level(level),

            "parallel": normalize_parallel(parallel),

            "shift": normalize_shift(shift),

        }

    return {"course_level": normalize_course_level(code), "parallel": "", "shift": normalize_shift(shift)}





def build_course_label(course_level, parallel, shift):

    code = "-".join([x for x in [normalize_course_level(course_level), normalize_parallel(parallel)] if x])

    shift = normalize_shift(shift)

    return normalize_spaces(f"{code} {shift}") if shift else code





def academic_context_from_profile(prof):

    parsed = parse_course_label(prof.get("course") if prof else "")

    subject = normalize_academic_text((prof or {}).get("subject") or ACADEMIC_SUBJECTS[0])

    course_level = normalize_course_level((prof or {}).get("course_level") or parsed.get("course_level") or ACADEMIC_COURSE_LEVELS[0])

    parallel = normalize_parallel((prof or {}).get("parallel") or parsed.get("parallel") or ACADEMIC_PARALLELS[0])

    shift = normalize_shift((prof or {}).get("shift") or parsed.get("shift") or ACADEMIC_SHIFTS[0])

    course_label = build_course_label(course_level, parallel, shift)

    cohort = normalize_spaces((prof or {}).get("cohort")) or course_label

    return {

        "subject": subject,

        "course_level": course_level,

        "parallel": parallel,

        "shift": shift,

        "cohort": cohort,

        "course": course_label,

    }





def academic_selectbox(label, options, current, key, normalizer=normalize_academic_text, placeholder="Escribe el valor"):

    cleaned_options = unique_values(options, normalizer)

    current_clean = normalizer(current)

    choices = cleaned_options + ["Otro..."]

    index = cleaned_options.index(current_clean) if current_clean in cleaned_options else len(cleaned_options)

    choice = st.selectbox(label, choices, index=index, key=key)

    if choice == "Otro...":

        return normalizer(st.text_input(f"{label} personalizada", value=current_clean, key=f"{key}_other", placeholder=placeholder))

    return choice





def birth_date_value(raw):

    try:

        return datetime.strptime(str(raw), "%Y-%m-%d").date()

    except Exception:

        return date(2005, 1, 1)





def get_timeout():

    r = fetchone("SELECT value FROM settings WHERE key=%s", ('session_timeout_minutes',))

    return int(r['value']) if r else SESSION_TIMEOUT_MINUTES





def set_timeout(minutes:int):

    with DB_LOCK:

        execute("INSERT INTO settings(key,value) VALUES('session_timeout_minutes', %s) ON CONFLICT (key) DO UPDATE SET value=EXCLUDED.value", (str(minutes),))





def user_by_username(username):

    return fetchone("SELECT * FROM users WHERE username=%s", (username.strip().lower(),))





def create_user(username, password):

    with DB_LOCK:

        row = execute("INSERT INTO users(username,password_hash,role,active,created_at) VALUES(%s,%s,%s,%s,%s) RETURNING id", (username.strip().lower(), hash_password(password), 'student', True, now()), returning=True)

        uid = row['id']

        execute("INSERT INTO profiles(user_id) VALUES(%s) ON CONFLICT (user_id) DO NOTHING", (uid,))

        return uid





def authenticate(username, password):

    u = user_by_username(username)

    if not u or not u.get('active') or not verify_password(password, u['password_hash']):

        return None

    with DB_LOCK:

        execute("UPDATE users SET last_login=%s WHERE id=%s", (now(), u['id']))

    return u





def get_profile(uid):

    return fetchone("SELECT * FROM profiles WHERE user_id=%s", (uid,)) or {}





def get_user(uid):

    return fetchone("SELECT * FROM users WHERE id=%s", (uid,)) or {}





def update_profile(uid, data):

    if not data:

        return

    cols = ','.join([f"{k}=%s" for k in data])

    vals = list(data.values()) + [uid]

    with DB_LOCK:

        execute(f"UPDATE profiles SET {cols} WHERE user_id=%s", vals)





def update_user(uid, data):

    if not data:

        return

    allowed = {"password_hash", "password_temporal", "primer_ingreso", "active", "last_login"}

    clean = {key: value for key, value in data.items() if key in allowed}

    if not clean:

        return

    cols = ",".join([f"{key}=%s" for key in clean])

    vals = list(clean.values()) + [uid]

    with DB_LOCK:

        execute(f"UPDATE users SET {cols} WHERE id=%s", vals)





def is_official_profile(prof):

    return str((prof or {}).get("estado_dato") or "piloto").strip().lower() == "oficial"





def official_profile_ready(user, prof):

    if not is_official_profile(prof):

        return True

    return bool(

        prof.get("consentimiento_informado")

        and prof.get("profile_completed")

        and not user.get("password_temporal")

        and not user.get("primer_ingreso")

    )





def set_research_assessment_state(uid, quiz_type, status):

    field = {"pretest": "pretest_estado", "posttest": "posttest_estado"}.get(str(quiz_type or "").lower())

    if field:

        update_profile(uid, {field: status})





def invalidate_research_posttests_for_repeat(user_ids, reason, admin_user=None):

    clean_ids = sorted({int(uid) for uid in user_ids if pd.notna(uid)})

    if not clean_ids:

        return {"students": 0, "attempts": 0}

    reason = normalize_spaces(reason) or "Incidencia metodologica reportada por el docente."

    admin_label = (admin_user or {}).get("username") or (admin_user or {}).get("id") or "admin"

    stamp = now()

    note = (

        f"Intento anulado para repeticion metodologica el {stamp}. "

        f"Responsable: {admin_label}. Motivo: {reason}"

    )

    placeholders = ",".join(["%s"] * len(clean_ids))

    rows = fetchall(f"""

        UPDATE adaptive_quizzes

        SET status='invalidado_metodologia',

            recommendation=TRIM(COALESCE(recommendation,'') || E'\n' || %s)

        WHERE user_id IN ({placeholders})

          AND COALESCE(quiz_type,'adaptive')='posttest'

          AND LOWER(COALESCE(status,'')) NOT IN ('invalidado_metodologia','superseded')

        RETURNING id, user_id

    """, [note, *clean_ids])

    execute(f"""

        UPDATE profiles

        SET posttest_estado='pendiente'

        WHERE user_id IN ({placeholders})

    """, clean_ids)

    return {"students": len(clean_ids), "attempts": len(rows)}





def read_enrollment_upload(uploaded_file):

    content = uploaded_file.getvalue()

    filename = str(uploaded_file.name or "").lower()

    if filename.endswith(".xlsx"):

        return pd.read_excel(BytesIO(content), sheet_name=0, dtype=str).fillna("")

    try:

        text = content.decode("utf-8-sig")

    except UnicodeDecodeError:

        text = content.decode("latin-1")

    return pd.read_csv(StringIO(text), dtype=str, sep=None, engine="python").fillna("")





def validate_enrollment_against_database(normalized_df):

    checked = normalized_df.copy()

    if checked.empty:

        return checked, pd.DataFrame(columns=["fila", "campo", "error"])

    existing = fetchall("""

        SELECT u.username, p.cedula, p.correo

        FROM users u

        LEFT JOIN profiles p ON p.user_id=u.id

    """)

    existing_cedulas = {str(row.get("cedula") or "").strip() for row in existing if str(row.get("cedula") or "").strip()}

    existing_emails = {str(row.get("correo") or "").strip().lower() for row in existing if str(row.get("correo") or "").strip()}

    existing_usernames = {str(row.get("username") or "").strip().lower() for row in existing}

    issues = []

    for index, row in checked.iterrows():

        row_issues = []

        cedula = str(row.get("cedula") or "").strip()

        correo = str(row.get("correo") or "").strip().lower()

        if cedula in existing_cedulas:

            row_issues.append(("cedula", "La cédula ya está registrada en BunsekiChat."))

        if correo in existing_emails:

            row_issues.append(("correo", "El correo ya está registrado en BunsekiChat."))

        if cedula.lower() in existing_usernames:

            row_issues.append(("cedula", "La cédula coincide con un nombre de usuario existente."))

        for field, message in row_issues:

            issues.append({"fila": int(row.get("fila_origen") or 0), "campo": field, "error": message})

            checked.at[index, "errores"] = " | ".join(filter(None, [str(checked.at[index, "errores"] or ""), message]))

    checked["valido"] = checked["errores"].astype(str).eq("")

    return checked, pd.DataFrame(issues, columns=["fila", "campo", "error"])





def import_bulk_enrollment(normalized_df, admin_id, source_filename):

    checked, database_issues = validate_enrollment_against_database(normalized_df)

    credentials = []

    rejected = []

    created = 0

    c = conn()

    c.autocommit = False

    try:

        with c.cursor() as cur:

            cur.execute("""

                INSERT INTO bulk_enrollment_batches(

                    cohort_code, source_filename, imported_by, total_rows, created_rows, rejected_rows, created_at

                ) VALUES(%s,%s,%s,%s,0,0,%s) RETURNING id

            """, (OFFICIAL_COHORT_CODE, source_filename, admin_id, len(checked), now()))

            batch_id = int(cur.fetchone()["id"])

            for index, row in checked.iterrows():

                source_row = int(row.get("fila_origen") or index + 2)

                if not bool(row.get("valido")):

                    detail = str(row.get("errores") or "Fila no válida.")

                    rejected.append({"fila": source_row, "cedula": row.get("cedula"), "correo": row.get("correo"), "error": detail})

                    cur.execute("""

                        INSERT INTO bulk_enrollment_rows(batch_id,source_row,cedula,correo,status,detail,created_at)

                        VALUES(%s,%s,%s,%s,'rechazado',%s,%s)

                    """, (batch_id, source_row, row.get("cedula"), row.get("correo"), detail, now()))

                    continue



                cur.execute(f"SAVEPOINT enrollment_row_{index}")

                try:

                    temporary_password = generate_temporary_password()

                    username = str(row["cedula"]).strip()

                    cur.execute("""

                        INSERT INTO users(

                            username,password_hash,role,active,created_at,password_temporal,primer_ingreso

                        ) VALUES(%s,%s,'student',TRUE,%s,TRUE,TRUE) RETURNING id

                    """, (username, hash_password(temporary_password), now()))

                    user_id = int(cur.fetchone()["id"])

                    course_label = build_course_label(row.get("curso"), row.get("paralelo"), row.get("jornada"))

                    full_name = normalize_spaces(f"{row.get('nombres')} {row.get('apellidos')}")

                    cur.execute("""

                        INSERT INTO profiles(

                            user_id,cedula,first_names,last_names,correo,subject,course_level,parallel,shift,

                            course,cohort,full_name_normalized,research_group,estado_dato,usar_en_investigacion,

                            consentimiento_informado,pretest_estado,posttest_estado,profile_completed,level

                        ) VALUES(

                            %s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,'oficial',FALSE,FALSE,

                            'pendiente','pendiente',0,'Inicial'

                        )

                    """, (

                        user_id, row.get("cedula"), row.get("nombres"), row.get("apellidos"), row.get("correo"),

                        row.get("materia"), row.get("curso"), row.get("paralelo"), row.get("jornada"),

                        course_label, row.get("cohorte") or OFFICIAL_COHORT_CODE, full_name,

                        row.get("grupo_investigacion"),

                    ))

                    detail = "Creado; pendiente primer ingreso, cambio de contraseña y consentimiento."

                    cur.execute("""

                        INSERT INTO bulk_enrollment_rows(batch_id,source_row,user_id,cedula,correo,status,detail,created_at)

                        VALUES(%s,%s,%s,%s,%s,'creado',%s,%s)

                    """, (batch_id, source_row, user_id, row.get("cedula"), row.get("correo"), detail, now()))

                    cur.execute(f"RELEASE SAVEPOINT enrollment_row_{index}")

                    created += 1

                    credentials.append({

                        "cedula": row.get("cedula"),

                        "usuario": username,

                        "contrasena_temporal": temporary_password,

                        "nombres": row.get("nombres"),

                        "apellidos": row.get("apellidos"),

                        "correo": row.get("correo"),

                        "cohorte": row.get("cohorte") or OFFICIAL_COHORT_CODE,

                        "grupo_investigacion": row.get("grupo_investigacion"),

                    })

                except Exception as row_error:

                    cur.execute(f"ROLLBACK TO SAVEPOINT enrollment_row_{index}")

                    cur.execute(f"RELEASE SAVEPOINT enrollment_row_{index}")

                    detail = f"No creado: {str(row_error)[:350]}"

                    rejected.append({"fila": source_row, "cedula": row.get("cedula"), "correo": row.get("correo"), "error": detail})

                    cur.execute("""

                        INSERT INTO bulk_enrollment_rows(batch_id,source_row,cedula,correo,status,detail,created_at)

                        VALUES(%s,%s,%s,%s,'rechazado',%s,%s)

                    """, (batch_id, source_row, row.get("cedula"), row.get("correo"), detail, now()))

            cur.execute("""

                UPDATE bulk_enrollment_batches SET created_rows=%s,rejected_rows=%s WHERE id=%s

            """, (created, len(checked) - created, batch_id))

        c.commit()

    except Exception:

        c.rollback()

        raise

    finally:

        c.close()

    return {

        "batch_id": batch_id,

        "created": created,

        "rejected": len(checked) - created,

        "credentials": pd.DataFrame(credentials),

        "errors": pd.DataFrame(rejected),

        "database_issues": database_issues,

    }





def get_recent_enrollment_batches(limit=20):

    return fetchall("""

        SELECT beb.*, u.username AS imported_by_username

        FROM bulk_enrollment_batches beb

        LEFT JOIN users u ON u.id=beb.imported_by

        ORDER BY beb.id DESC LIMIT %s

    """, (int(limit),))





def normalize_gps_payload(raw):

    if not raw or not isinstance(raw, dict):

        return None

    coords = raw.get('coords') if isinstance(raw.get('coords'), dict) else raw

    lat = coords.get('latitude', coords.get('lat'))

    lon = coords.get('longitude', coords.get('lon', coords.get('lng')))

    acc = coords.get('accuracy')

    try:

        lat = float(lat); lon = float(lon); acc = float(acc) if acc is not None else None

    except (TypeError, ValueError):

        return None

    if not (-90 <= lat <= 90 and -180 <= lon <= 180):

        return None

    return {'lat': lat, 'lon': lon, 'accuracy': acc, 'source': 'browser_geolocation'}





def capture_browser_gps(uid=None, page='', topic='', subtopic='', event_type='gps_refresh', show_status=False):

    if 'current_gps' not in st.session_state:

        st.session_state.current_gps = None

    if get_geolocation is None:

        if show_status:

            st.info('GPS automático no activo. Instala: pip install streamlit-js-eval')

        return st.session_state.current_gps

    gps = None

    if not st.session_state.get('_gps_widget_rendered_this_run', False):

        st.session_state['_gps_widget_rendered_this_run'] = True

        try:

            try:

                raw = get_geolocation(key='bunsekichat_browser_gps_unique')

            except TypeError:

                raw = get_geolocation()

            gps = normalize_gps_payload(raw)

        except Exception:

            gps = None

    else:

        gps = st.session_state.get('current_gps')

    if gps:

        st.session_state.current_gps = gps

        if uid:

            session_key = f'location_open_logged_{uid}'

            if event_type != 'app_open' or not st.session_state.get(session_key):

                log_location_event(uid, event_type, page=page, topic=topic, subtopic=subtopic, gps=gps)

                if event_type == 'app_open':

                    st.session_state[session_key] = True

            update_profile_last_gps(uid, gps)

        if show_status:

            st.caption(f"📍 GPS activo: {gps['lat']:.6f}, {gps['lon']:.6f}")

    elif show_status and st.session_state.current_gps is None:

        st.caption('📍 Esperando permiso de ubicación del navegador...')

    return st.session_state.current_gps





def update_profile_last_gps(uid, gps):

    if not gps: return

    with DB_LOCK:

        execute("""UPDATE profiles SET last_gps_lat=%s, last_gps_lon=%s, last_gps_accuracy=%s, last_gps_at=%s WHERE user_id=%s""", (gps.get('lat'), gps.get('lon'), gps.get('accuracy'), now(), uid))





def log_location_event(uid, event_type, page='', topic='', subtopic='', gps=None):

    if not gps: return

    with DB_LOCK:

        execute("""INSERT INTO location_events(user_id,event_type,page,topic,subtopic,gps_lat,gps_lon,gps_accuracy,gps_source,created_at) VALUES(%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)""", (uid, event_type, page, topic, subtopic, gps.get('lat'), gps.get('lon'), gps.get('accuracy'), gps.get('source'), now()))





def log_interaction(uid, role, message, topic='', subtopic='', level='', model='', latency=0, gps=None):

    clean = normalize_math(message)

    gps = gps or st.session_state.get('current_gps') or {}

    with DB_LOCK:

        execute("""INSERT INTO interactions(user_id,role,topic,subtopic,level,message,clean_message,model,tokens_est,latency_ms,created_at,gps_lat,gps_lon,gps_accuracy,gps_source) VALUES(%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)""", (uid, role, topic, subtopic, level, message, clean, model, max(1, len(message)//4), latency, now(), gps.get('lat') if gps else None, gps.get('lon') if gps else None, gps.get('accuracy') if gps else None, gps.get('source') if gps else None))





def log_research_learning_event(uid, event_type, quiz_id=None, duration_seconds=None, metadata=None):

    execute("""

        INSERT INTO research_learning_events(user_id, quiz_id, event_type, duration_seconds, metadata_json, created_at)

        VALUES(%s,%s,%s,%s,%s,%s)

    """, (uid, quiz_id, event_type, duration_seconds, json.dumps(metadata or {}, ensure_ascii=False), now()))





def research_learning_event_exists(uid, event_type, quiz_id=None):

    if quiz_id is None:

        row = fetchone(

            "SELECT 1 AS found FROM research_learning_events WHERE user_id=%s AND event_type=%s LIMIT 1",

            (uid, event_type),

        )

    else:

        row = fetchone(

            "SELECT 1 AS found FROM research_learning_events WHERE user_id=%s AND event_type=%s AND quiz_id=%s LIMIT 1",

            (uid, event_type, quiz_id),

        )

    return bool(row)





def interactions(uid=None):

    if uid:

        return fetchall("SELECT * FROM interactions WHERE user_id=%s ORDER BY id", (uid,))

    return fetchall("SELECT i.*,u.username FROM interactions i JOIN users u ON u.id=i.user_id ORDER BY i.id DESC")





def get_location_events(uid=None):

    if uid:

        return fetchall("SELECT * FROM location_events WHERE user_id=%s ORDER BY id DESC", (uid,))

    return fetchall("SELECT le.*,u.username FROM location_events le JOIN users u ON u.id=le.user_id ORDER BY le.id DESC")



# ---------------- Servicio generativo ----------------

def normalize_math(text:str)->str:

    if not text: return ""

    text = text.replace('\\(','$').replace('\\)','$').replace('\\[','$$').replace('\\]','$$')

    text = re.sub(r'```+\s*', '', text)

    text = text.replace('âˆ’','-').replace('×','\\times').replace('÷','\\div')

    # evita caracteres invisibles frecuentes

    return ''.join(ch for ch in text if ch.isprintable() or ch in '\n\t')



@st.cache_resource

def ai_client():

    if not GEMINI_API_KEY: return None

    return genai.Client(api_key=GEMINI_API_KEY)



def build_prompt(question, topic, subtopic, level, profile):

    return f"""

Eres BunsekiChat, tutor personalizado de matemáticas universitarias para estudiantes de bajos recursos.

Área: {topic}. Subtema: {subtopic}. Nivel: {level}.

Estudiante: {profile.get('first_names','')} {profile.get('last_names','')}. Materia: {profile.get('subject','')}. Curso: {profile.get('course','')}.



Reglas de respuesta:

1) Español claro, humano y docente.

2) Explica paso a paso, sin saltos algebraicos importantes.

3) Usa LaTeX limpio para fórmulas: $x^2$, $$\\int x dx$$. No uses caracteres raros ni escapes innecesarios.

4) Termina con una pregunta breve de verificación.

5) Si la consulta no es matemática, redirige amablemente al tema académico.



Pregunta del estudiante: {question}

"""



def ask_ai(question, topic, subtopic, level, profile):

    """Consulta el servicio generativo con fallback automático entre modelos Gemini.

    Prioriza Gemini 2.5 Flash y, si hay saturación/cuota/error temporal,

    intenta modelos alternativos antes de fallar.

    """

    client = ai_client()

    if not client:

        return (

            f"Configura GEMINI_API_KEY en el archivo .env ubicado en: {ENV_PATH}. "

            "Debe llamarse exactamente .env y contener GEMINI_API_KEY=tu_clave.",

            "sin_modelo",

            0,

        )



    prompt = build_prompt(question, topic, subtopic, level, profile)

    modelos = [

        "gemini-2.5-flash",

        "gemini-2.5-flash-lite",

        "gemini-2.0-flash-lite",

        "gemini-2.0-flash",

    ]



    errores = []

    start_total = datetime.now()



    for modelo in modelos:

        start = datetime.now()

        try:

            resp = client.models.generate_content(

                model=modelo,

                contents=prompt,

            )

            ms = int((datetime.now() - start).total_seconds() * 1000)

            texto = normalize_math(resp.text or "No pude generar respuesta.")

            return texto, modelo, ms

        except Exception as e:

            err = str(e)

            errores.append(f"{modelo}: {err[:220]}")



            # Si es cuota/saturación/error temporal, probar siguiente modelo.

            err_lower = err.lower()

            if any(x in err_lower for x in [

                "429", "resource_exhausted", "quota", "rate", "503", "unavailable", "timeout"

            ]):

                time.sleep(1.5)

                continue



            # Para errores de API key o permisos, no tiene sentido probar otros modelos.

            if any(x in err_lower for x in ["api key", "permission", "unauthorized", "401", "403"]):

                break



            # Para otros errores, igual intenta el siguiente por robustez.

            time.sleep(0.8)

            continue



    total_ms = int((datetime.now() - start_total).total_seconds() * 1000)

    detalle = " | ".join(errores[-3:])

    return (

        "No se pudo consultar el servicio generativo con los modelos disponibles. "

        "Puede ser cuota agotada, saturación temporal o API key sin permisos. "

        f"Detalle resumido: {detalle}",

        "error_modelos_gemini",

        total_ms,

    )



# ---------------- UI helpers ----------------

def session_guard():

    if 'user' not in st.session_state: return

    timeout = get_timeout()

    last = st.session_state.get('last_activity', datetime.now())

    if datetime.now() - last > timedelta(minutes=timeout):

        st.session_state.clear(); st.warning("Sesión cerrada automáticamente por inactividad."); st.stop()

    st.session_state.last_activity = datetime.now()



def render_message(role, msg):

    cls='user' if role=='user' else 'bot'

    title='👨‍🎓 Estudiante' if role=='user' else 'BΣ BunsekiChat'

    st.markdown(f"<div class='{cls}'><b>{title}</b></div>", unsafe_allow_html=True)

    st.markdown(normalize_math(msg))



# ---------------- Quizzes ----------------

QUIZ_BANK = {

    "Álgebra lineal": [("Si A es 2x2, ¿qué indica det(A)=0?", ["A es invertible", "A no es invertible", "A es identidad"], 1), ("Producto punto de vectores ortogonales", ["1", "0", "-1"], 1)],

    "Estadística": [("La media es", ["El valor más frecuente", "El promedio", "La raíz cuadrada de la varianza"], 1), ("La desviación estándar mide", ["Dispersión", "Centro", "Asimetría siempre"], 0)],

    "Cálculo diferencial": [("Derivada de x^3", ["3x^2", "x^2", "3x"], 0), ("Si f'(x)>0, la función", ["crece", "decrece", "es constante"], 0)],

    "Cálculo integral": [("Integral de 2x dx", ["x^2+C", "2+C", "ln x"], 0), ("La integral definida representa", ["Solo pendiente", "Área neta acumulada", "Solo máximo"], 1)]

}





# Banco ampliado para pruebas adaptativas de 10 preguntas.

# La selección prioriza el historial real del estudiante: temas y subtemas más consultados.

ADAPTIVE_QUIZ_BANK = {

    "Álgebra lineal": [

        ("¿Qué representa un vector en R² o R³?", ["Una magnitud con dirección y sentido", "Solo un número", "Una ecuación cuadrática"], 0),

        ("Si dos vectores son ortogonales, su producto punto es:", ["1", "0", "-1"], 1),

        ("Una matriz cuadrada A es invertible si:", ["det(A) ≠ 0", "det(A)=0", "todas sus entradas son positivas"], 0),

        ("El determinante igual a cero indica que la matriz:", ["No es invertible", "Es identidad", "Tiene inversa única"], 0),

        ("Un sistema lineal homogéneo siempre tiene:", ["Al menos la solución trivial", "Ninguna solución", "Solo soluciones negativas"], 0),

        ("Un autovector de A cumple:", ["A v = λ v", "A+v=λ", "det(v)=0"], 0),

        ("El rango de una matriz mide:", ["La cantidad de filas/columnas linealmente independientes", "El número de decimales", "La suma de entradas"], 0),

        ("Dos vectores son linealmente dependientes si:", ["Uno puede escribirse como múltiplo del otro", "Siempre son perpendiculares", "Su producto punto es 1"], 0),

        ("La matriz identidad funciona como:", ["Elemento neutro de la multiplicación matricial", "Elemento nulo", "Matriz sin diagonal"], 0),

        ("El espacio columna está formado por:", ["Combinaciones lineales de las columnas", "Solo la diagonal", "Los determinantes parciales"], 0),

    ],

    "Estadística": [

        ("La media aritmética representa:", ["El promedio", "El dato más repetido", "La diferencia máxima"], 0),

        ("La mediana divide los datos ordenados en:", ["Dos partes iguales", "Tres partes", "Diez partes"], 0),

        ("La desviación estándar mide:", ["Dispersión de los datos", "Tamaño de muestra únicamente", "Moda"], 0),

        ("La probabilidad siempre está entre:", ["0 y 1", "-1 y 1", "1 y 1000"], 0),

        ("Una distribución normal se caracteriza por:", ["Forma de campana", "Solo valores enteros", "No tener media"], 0),

        ("La varianza es:", ["El promedio de desviaciones cuadráticas", "La raíz de la media", "La moda elevada"], 0),

        ("En regresión lineal simple se modela:", ["Relación entre X e Y", "Solo frecuencias", "Únicamente porcentajes"], 0),

        ("Un intervalo de confianza expresa:", ["Rango plausible para un parámetro", "Un dato exacto garantizado", "La moda"], 0),

        ("La hipótesis nula usualmente representa:", ["Ausencia de efecto o diferencia", "La conclusión obligatoria", "El promedio de la muestra"], 0),

        ("Una muestra debe procurar ser:", ["Representativa", "Siempre pequeña", "Sin variabilidad"], 0),

    ],

    "Cálculo diferencial": [

        ("La derivada representa principalmente:", ["Tasa de cambio instantánea", "Área acumulada", "Promedio de datos"], 0),

        ("Si f'(x)>0 en un intervalo, la función:", ["Crece", "Decrece", "Es discontinua siempre"], 0),

        ("La derivada de x³ es:", ["3x²", "x²", "3x"], 0),

        ("Un punto crítico ocurre cuando:", ["f'(x)=0 o no existe", "f(x)=1 siempre", "x es negativo"], 0),

        ("La segunda derivada ayuda a estudiar:", ["Concavidad", "Dominio únicamente", "Intercepto y"], 0),

        ("La regla de la cadena se usa para derivar:", ["Funciones compuestas", "Solo constantes", "Matrices"], 0),

        ("La derivada de sen(x) es:", ["cos(x)", "-cos(x)", "tan(x)"], 0),

        ("La derivada de una constante es:", ["0", "1", "La misma constante"], 0),

        ("Si f'(x)<0 en un intervalo, la función:", ["Decrece", "Crece", "Tiene máximo absoluto siempre"], 0),

        ("Una asíntota vertical suele asociarse a:", ["Valores donde la función crece sin límite", "Un punto medio", "Una raíz simple siempre"], 0),

    ],

    "Cálculo integral": [

        ("La integral indefinida representa:", ["Familia de antiderivadas", "Solo pendiente", "Solo máximo"], 0),

        ("La integral definida puede interpretarse como:", ["Área neta acumulada", "Derivada segunda", "Moda"], 0),

        ("La integral de 2x dx es:", ["x² + C", "2 + C", "ln(x) + C"], 0),

        ("La constante C aparece en:", ["Integrales indefinidas", "Solo límites", "Matrices"], 0),

        ("El teorema fundamental del cálculo relaciona:", ["Derivadas e integrales", "Matrices y vectores", "Media y moda"], 0),

        ("La sustitución se usa cuando:", ["Hay una composición y su derivada", "Solo hay números", "No hay variable"], 0),

        ("La integral de cos(x) dx es:", ["sen(x)+C", "-sen(x)+C", "tan(x)+C"], 0),

        ("La integral de 1/x dx es:", ["ln|x|+C", "x²+C", "e^x+C"], 0),

        ("En integración por partes se usa:", ["∫u dv = uv - ∫v du", "a²+b²=c²", "y=mx+b"], 0),

        ("Una integral impropia puede aparecer por:", ["Intervalo infinito o discontinuidad", "Solo por enteros", "Porque no tiene variable"], 0),

    ],

}



def adaptive_quiz_for_user(uid, current_topic):

    """Devuelve 10 preguntas vinculadas al historial del estudiante."""

    rows = interactions(uid)

    user_rows = [r for r in rows if r.get("role") == "user"]

    topic_counts = {}

    for r in user_rows:

        t = r.get("topic") or current_topic

        topic_counts[t] = topic_counts.get(t, 0) + 1

    ranked_topics = sorted(topic_counts, key=topic_counts.get, reverse=True)

    if current_topic not in ranked_topics:

        ranked_topics.append(current_topic)

    selected = []

    used = set()

    for t in ranked_topics:

        for q in ADAPTIVE_QUIZ_BANK.get(t, []):

            if q[0] not in used:

                selected.append((t, q))

                used.add(q[0])

            if len(selected) >= 10:

                return selected

    for t, bank in ADAPTIVE_QUIZ_BANK.items():

        for q in bank:

            if q[0] not in used:

                selected.append((t, q))

                used.add(q[0])

            if len(selected) >= 10:

                return selected

    return selected[:10]



def save_quiz(uid, topic, old, new, score, passed, answers):

    with DB_LOCK:

        execute("INSERT INTO quizzes(user_id,topic,level_from,level_to,score,passed,answers_json,created_at) VALUES(%s,%s,%s,%s,%s,%s,%s,%s)",

                (uid, topic, old, new, score, int(passed), json.dumps(answers, ensure_ascii=False), now()))





# ---------------- Control de lenguaje y sugerencias ----------------

BAD_WORDS = [

    "puta", "puto", "mierda", "carajo", "verga", "pendejo", "pendeja",

    "idiota", "imbecil", "imbécil", "maldito", "maldita", "cojudo", "cojuda"

]



def contains_bad_language(text: str) -> bool:

    text = (text or "").lower()

    return any(re.search(rf"\b{re.escape(w)}\b", text) for w in BAD_WORDS)



def prompt_suggestions(topic: str, subtopic: str, level: str):

    return [

        f"Explícame {subtopic} desde cero con un ejemplo universitario.",

        f"Dame 3 ejercicios resueltos de {subtopic} en nivel {level}.",

        f"Corrige mi procedimiento paso a paso sobre {subtopic}.",

        f"Crea una prueba corta de {topic} y califícame.",

        f"Muéstrame un error común al estudiar {subtopic} y cómo evitarlo.",

    ]



# ---------------- Exportación docente ----------------

def _rows_to_df(rows, columns=None):

    """Convierte listas de diccionarios en DataFrame sin depender de pandas.read_sql_query.

    Esto evita exportaciones vacías o filas extrañas cuando el driver PostgreSQL cambia el cursor.

    """

    df = pd.DataFrame(rows or [])

    if df.empty and columns:

        return pd.DataFrame(columns=columns)

    return df





def _clean_report_df(df: pd.DataFrame) -> pd.DataFrame:

    """Limpia filas inválidas y asegura columnas esperadas para exportar."""

    expected = ["id", "created_at", "role", "topic", "subtopic", "level", "model",

                "tokens_est", "latency_ms", "gps_lat", "gps_lon", "gps_accuracy",

                "gps_source", "message"]

    if df is None or df.empty:

        return pd.DataFrame(columns=expected)



    df = df.copy()

    for col in expected:

        if col not in df.columns:

            df[col] = ""



    # Corrige casos donde alguna exportación anterior registró filas como encabezados.

    mask_bad = pd.Series(False, index=df.index)

    for col in ["created_at", "role", "topic", "subtopic", "level", "message"]:

        mask_bad = mask_bad | df[col].astype(str).str.strip().eq(col)



    df = df[~mask_bad].copy()

    df = df[expected]



    # Orden y limpieza visual

    df["created_at"] = df["created_at"].fillna("").astype(str)

    df["message"] = df["message"].fillna("").astype(str)

    return df





def get_teacher_tables():

    ensure_cohort_schema_compatibility()

    users_rows = fetchall("""

        SELECT u.id,u.username,u.role,u.active,u.created_at,u.last_login,

               p.first_names,p.last_names,p.course,p.subject,p.course_level,p.parallel,p.shift,p.cohort,p.full_name_normalized,

               p.teacher,p.province,p.city,p.level,p.cedula,p.correo,p.research_group,p.estado_dato,

               p.usar_en_investigacion,p.consentimiento_informado,p.fecha_consentimiento,p.pretest_estado,p.posttest_estado,

               u.password_temporal,u.primer_ingreso

        FROM users u LEFT JOIN profiles p ON p.user_id=u.id

        ORDER BY u.id DESC

    """)

    logs_rows = fetchall("""

        SELECT i.*,u.username,p.first_names,p.last_names,p.course,p.subject,p.course_level,p.parallel,p.shift,p.cohort,

               p.teacher,p.level AS profile_level

        FROM interactions i

        JOIN users u ON u.id=i.user_id

        LEFT JOIN profiles p ON p.user_id=i.user_id

        ORDER BY i.id DESC

    """)

    quizzes_rows = fetchall("""

        SELECT q.*,u.username,p.first_names,p.last_names,p.course,p.subject,p.course_level,p.parallel,p.shift,p.cohort,

               p.teacher,p.level AS profile_level

        FROM quizzes q

        JOIN users u ON u.id=q.user_id

        LEFT JOIN profiles p ON p.user_id=q.user_id

        ORDER BY q.id DESC

    """)

    users = _rows_to_df(users_rows, ["id","username","role","active","created_at","last_login","first_names","last_names","course","subject","course_level","parallel","shift","cohort","full_name_normalized","teacher","province","city","level","cedula","correo","research_group","estado_dato","usar_en_investigacion","consentimiento_informado","fecha_consentimiento","pretest_estado","posttest_estado","password_temporal","primer_ingreso"])

    logs = _rows_to_df(logs_rows, ["id","user_id","role","topic","subtopic","level","message","clean_message","model","tokens_est","latency_ms","created_at","username","first_names","last_names","course","subject","course_level","parallel","shift","cohort","teacher","profile_level","gps_lat","gps_lon","gps_accuracy","gps_source"])

    quizzes = _rows_to_df(quizzes_rows, ["id","user_id","topic","level_from","level_to","score","passed","answers_json","created_at","username","first_names","last_names","course","subject","course_level","parallel","shift","cohort","teacher","profile_level"])

    return users, logs, quizzes





def build_student_summary(uid: int):

    profile = fetchone("""

        SELECT u.id,u.username,u.role,u.active,u.created_at,u.last_login,

               p.first_names,p.last_names,p.course,p.subject,p.course_level,p.parallel,p.shift,p.cohort,p.full_name_normalized,

               p.teacher,p.phone,p.province,p.city,p.canton,p.address,p.level,p.cedula,p.correo,p.research_group,

               p.estado_dato,p.usar_en_investigacion,p.consentimiento_informado,p.pretest_estado,p.posttest_estado

        FROM users u LEFT JOIN profiles p ON p.user_id=u.id WHERE u.id=%s

    """, (uid,)) or {"id": uid, "username": f"usuario_{uid}"}



    rows = fetchall("""

        SELECT id,created_at,role,topic,subtopic,level,model,tokens_est,latency_ms,

               gps_lat,gps_lon,gps_accuracy,gps_source,

               COALESCE(NULLIF(clean_message,''), message) AS message

        FROM interactions

        WHERE user_id=%s

        ORDER BY id ASC

    """, (uid,))

    logs = _clean_report_df(pd.DataFrame(rows or []))

    return profile, logs





def export_student_docx(uid: int) -> bytes:

    if Document is None:

        raise RuntimeError("Instala python-docx: pip install python-docx")

    profile, df = build_student_summary(uid)

    doc = Document()

    doc.add_heading("Reporte individual BunsekiChat", level=1)

    nombre = f"{profile.get('first_names') or ''} {profile.get('last_names') or ''}".strip()

    doc.add_paragraph(f"Estudiante: {nombre or profile.get('username','')}")

    doc.add_paragraph(f"Usuario: {profile.get('username','')}")

    doc.add_paragraph(f"Materia: {profile.get('subject') or 'Sin materia'}")

    doc.add_paragraph(f"Curso: {profile.get('course') or 'Sin curso'}")

    doc.add_paragraph(f"Cohorte: {profile.get('cohort') or profile.get('course') or 'Sin cohorte'}")

    doc.add_paragraph(f"Docente: {profile.get('teacher') or 'Sin docente'}")

    doc.add_paragraph(f"Nivel actual: {profile.get('level') or 'Sin nivel'}")

    doc.add_paragraph(f"Fecha de exportación: {now()}")

    doc.add_heading("Consultas e interacciones", level=2)

    if df.empty:

        doc.add_paragraph("No existen consultas registradas para este estudiante.")

    else:

        table = doc.add_table(rows=1, cols=6)

        table.style = "Table Grid"

        hdr = table.rows[0].cells

        for i, h in enumerate(["Fecha", "Rol", "Tema", "Subtema", "Nivel", "Mensaje"]):

            hdr[i].text = h

        for _, r in df.iterrows():

            cells = table.add_row().cells

            cells[0].text = str(r.get("created_at", ""))

            cells[1].text = str(r.get("role", ""))

            cells[2].text = str(r.get("topic", ""))

            cells[3].text = str(r.get("subtopic", ""))

            cells[4].text = str(r.get("level", ""))

            msg = str(r.get("message", ""))

            cells[5].text = msg[:1200]

    bio = BytesIO()

    doc.save(bio)

    bio.seek(0)

    return bio.getvalue()





def export_student_pdf(uid: int) -> bytes:

    if canvas is None or A4 is None:

        raise RuntimeError("Instala reportlab: pip install reportlab")

    profile, df = build_student_summary(uid)

    bio = BytesIO()

    c = canvas.Canvas(bio, pagesize=A4)

    width, height = A4

    y = height - 50

    def line(text, size=10, bold=False):

        nonlocal y

        if y < 55:

            c.showPage(); y = height - 50

        c.setFont("Helvetica-Bold" if bold else "Helvetica", size)

        safe = str(text).replace("\n", " ")

        c.drawString(45, y, safe[:115])

        y -= size + 7

    line("Reporte individual BunsekiChat", 16, True)

    nombre = f"{profile.get('first_names') or ''} {profile.get('last_names') or ''}".strip()

    line(f"Estudiante: {nombre or profile.get('username','')}", 10)

    line(f"Usuario: {profile.get('username','')} | Materia: {profile.get('subject') or 'Sin materia'} | Curso: {profile.get('course') or 'Sin curso'}", 10)

    line(f"Cohorte: {profile.get('cohort') or profile.get('course') or 'Sin cohorte'} | Nivel: {profile.get('level') or 'Sin nivel'}", 10)

    line(f"Docente: {profile.get('teacher') or 'Sin docente'} | Exportado: {now()}", 10)

    line("Consultas e interacciones", 13, True)

    if df.empty:

        line("No existen consultas registradas para este estudiante.")

    else:

        for _, r in df.iterrows():

            line(f"[{r.get('created_at','')}] {r.get('role','')} · {r.get('topic','')} / {r.get('subtopic','')}", 9, True)

            msg = str(r.get("message", ""))

            for chunk in [msg[i:i+100] for i in range(0, min(len(msg), 700), 100)]:

                line("  " + chunk, 8)

            y -= 4

    c.save()

    bio.seek(0)

    return bio.getvalue()





# ---------------- Planes analíticos y evaluación adaptativa ----------------

def _strip_code_fences(raw: str) -> str:

    raw = (raw or "").strip()

    raw = re.sub(r"^```(?:json)?\s*", "", raw, flags=re.I)

    raw = re.sub(r"\s*```$", "", raw)

    return raw.strip()





def _safe_json_loads(raw, default):

    try:

        return json.loads(_strip_code_fences(raw))

    except Exception:

        return default





def read_uploaded_plan_text(uploaded_file) -> str:

    """Extrae texto de TXT, DOCX o PDF sin romper la app si falta una librería."""

    if uploaded_file is None:

        return ""

    name = uploaded_file.name.lower()

    data = uploaded_file.read()

    uploaded_file.seek(0)

    if name.endswith(".txt") or name.endswith(".md"):

        return data.decode("utf-8", errors="ignore")

    if name.endswith(".docx"):

        if Document is None:

            return ""

        try:

            doc = Document(BytesIO(data))

            return "\n".join([p.text for p in doc.paragraphs if p.text.strip()])

        except Exception:

            return ""

    if name.endswith(".pdf"):

        try:

            from pypdf import PdfReader

            reader = PdfReader(BytesIO(data))

            return "\n".join([(page.extract_text() or "") for page in reader.pages])

        except Exception:

            try:

                from PyPDF2 import PdfReader

                reader = PdfReader(BytesIO(data))

                return "\n".join([(page.extract_text() or "") for page in reader.pages])

            except Exception:

                return ""

    return data.decode("utf-8", errors="ignore")





# BUNSEKI_R2_SOURCE_TRACEABILITY_V3
def _normalize_source_trace_text(value):
    return re.sub(r"\s+", " ", str(value or "")).strip()


def _locate_plan_source_evidence(raw_text, item):
    lines = str(raw_text or "").splitlines()
    if not lines:
        return "", ""

    normalized_lines = [_normalize_source_trace_text(line) for line in lines]
    candidate = _normalize_source_trace_text((item or {}).get("source_excerpt"))
    normalized_doc = " ".join(line for line in normalized_lines if line)

    def pack(index):
        start = max(0, index - 1)
        end = min(len(lines), index + 2)
        excerpt = _normalize_source_trace_text(" ".join(lines[start:end]))[:500]
        locator = f"Lineas {start + 1}-{end}"
        return excerpt, locator

    if candidate and candidate.casefold() in normalized_doc.casefold():
        candidate_terms = [token for token in re.findall(r"\w+", candidate.casefold()) if len(token) >= 4]
        best_index = None
        best_score = 0
        for index, line in enumerate(normalized_lines):
            folded = line.casefold()
            score = sum(1 for token in candidate_terms[:20] if token in folded)
            if score > best_score:
                best_index = index
                best_score = score
        if best_index is not None and best_score > 0:
            return pack(best_index)

    raw_terms = []
    for key in ("topic", "subtopic", "unit_name", "keywords"):
        value = _normalize_source_trace_text((item or {}).get(key))
        if value:
            raw_terms.extend(re.split(r"[,;:/|]+", value))

    terms = []
    for value in raw_terms:
        for token in re.findall(r"\w+", value.casefold()):
            if len(token) >= 4 and token not in terms:
                terms.append(token)

    best_index = None
    best_score = 0
    topic = _normalize_source_trace_text((item or {}).get("topic")).casefold()

    for index, line in enumerate(normalized_lines):
        folded = line.casefold()
        if not folded:
            continue
        score = sum(1 for term in terms[:20] if term in folded)
        if topic and topic in folded:
            score += 6
        if score > best_score:
            best_index = index
            best_score = score

    if best_index is None or best_score <= 0:
        return "", ""

    return pack(best_index)


def _attach_plan_source_traceability(raw_text, parsed):
    if not isinstance(parsed, dict):
        return parsed

    topics = parsed.get("topics")
    if not isinstance(topics, list):
        return parsed

    for item in topics:
        if not isinstance(item, dict):
            continue
        excerpt, locator = _locate_plan_source_evidence(raw_text, item)
        item["source_excerpt"] = excerpt
        item["source_locator"] = locator

    return parsed


def ensure_plan_source_traceability_schema():
    execute("ALTER TABLE IF EXISTS plan_topics ADD COLUMN IF NOT EXISTS source_excerpt TEXT")
    execute("ALTER TABLE IF EXISTS plan_topics ADD COLUMN IF NOT EXISTS source_locator TEXT")

def parse_plan_topics_with_ai(raw_text: str, title: str = "") -> dict:

    """Convierte un plan analítico en unidades/temas/resultados. Usa el servicio generativo si hay API; si no, fallback robusto."""

    raw_text = (raw_text or "")[:24000]

    fallback_topics = []

    # fallback por líneas con palabras académicas frecuentes

    for line in raw_text.splitlines():

        clean = re.sub(r"\s+", " ", line).strip(" -•\t")

        if 8 <= len(clean) <= 160 and any(w in clean.lower() for w in ["unidad", "tema", "resultado", "competencia", "contenido", "aprendizaje", "objetivo"]):

            fallback_topics.append({

                "unit_name": clean[:80],

                "topic": clean[:90],

                "subtopic": "",

                "learning_outcome": "Comprender y aplicar el contenido del plan analítico.",

                "bloom_level": "Comprender/Aplicar",

                "keywords": clean

            })

        if len(fallback_topics) >= 12:

            break

    if not fallback_topics:

        fallback_topics = [{

            "unit_name": "Unidad general",

            "topic": title or "Contenido del plan analítico",

            "subtopic": "",

            "learning_outcome": "Comprender, aplicar y evaluar los contenidos del plan.",

            "bloom_level": "Comprender/Aplicar",

            "keywords": title or "plan analítico"

        }]



    client = ai_client()

    if not client:

        return _attach_plan_source_traceability(raw_text, {"summary": "Plan cargado con extracción básica local.", "topics": fallback_topics})



    prompt = f"""

Actúa como diseñador curricular universitario.

Analiza este plan analítico y devuelve SOLO JSON válido con esta estructura:

{{

  "summary": "resumen breve",

  "topics": [

    {{

      "unit_name": "Unidad 1",

      "topic": "tema principal",

      "subtopic": "subtema",

      "learning_outcome": "resultado de aprendizaje evaluable",

      "bloom_level": "Recordar/Comprender/Aplicar/Analizar/Evaluar/Crear",

      "keywords": "palabras clave separadas por coma",
      "source_excerpt": "fragmento textual breve copiado literalmente del PLAN que sustenta este tema"

    }}

  ]

}}

Máximo 18 topics. No uses markdown.



Título: {title}

PLAN:

{raw_text}

"""

    try:

        resp = client.models.generate_content(model="gemini-2.5-flash-lite", contents=prompt)

        parsed = _safe_json_loads(resp.text, {})

        if isinstance(parsed, dict) and isinstance(parsed.get("topics"), list) and parsed["topics"]:

            return _attach_plan_source_traceability(raw_text, parsed)

    except Exception:

        pass

    return _attach_plan_source_traceability(raw_text, {"summary": "Plan cargado con extracción básica local.", "topics": fallback_topics})





def save_analytic_plan(title, course, teacher_id, filename, raw_text, parsed, academic_context=None):
    ensure_plan_source_traceability_schema()

    academic_context = academic_context or {}

    row = execute("""

        INSERT INTO analytic_plans(title, course, subject, course_level, parallel, shift, teacher_id, filename, raw_text, summary, created_at)

        VALUES(%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s) RETURNING id

    """, (

        title, course,

        academic_context.get("subject"), academic_context.get("course_level"),

        academic_context.get("parallel"), academic_context.get("shift"),

        teacher_id, filename, raw_text, parsed.get("summary",""), now()

    ), returning=True)

    plan_id = row["id"]

    for item in parsed.get("topics", []):

        execute("""

            INSERT INTO plan_topics(plan_id, unit_name, topic, subtopic, learning_outcome, bloom_level, keywords, source_excerpt, source_locator)

            VALUES(%s,%s,%s,%s,%s,%s,%s,%s,%s)

        """, (

            plan_id,

            item.get("unit_name",""),

            item.get("topic",""),

            item.get("subtopic",""),

            item.get("learning_outcome",""),

            item.get("bloom_level",""),

            item.get("keywords",""),
            item.get("source_excerpt",""),
            item.get("source_locator",""),

        ))

    return plan_id





def get_analytic_plans(teacher_id=None):

    if teacher_id is not None:

        return fetchall("""

            SELECT p.*, u.username AS teacher_username

            FROM analytic_plans p

            LEFT JOIN users u ON u.id=p.teacher_id

            WHERE p.teacher_id=%s

            ORDER BY p.id DESC

        """, (teacher_id,))

    return fetchall("""

        SELECT p.*, u.username AS teacher_username

        FROM analytic_plans p

        LEFT JOIN users u ON u.id=p.teacher_id

        ORDER BY p.id DESC

    """)





def get_plan_topics(plan_id=None):

    if plan_id:

        return fetchall("SELECT * FROM plan_topics WHERE plan_id=%s ORDER BY id", (plan_id,))

    return fetchall("""

        SELECT pt.*, p.title AS plan_title, p.course AS plan_course, p.subject AS plan_subject

        FROM plan_topics pt

        JOIN analytic_plans p ON p.id=pt.plan_id

        ORDER BY p.id DESC, pt.id

    """)





QUESTION_BANK_BLOOM_LEVELS = ("Recordar", "Comprender", "Aplicar", "Analizar", "Evaluar", "Crear")

QUESTION_BANK_DIFFICULTIES = ("Básico", "Intermedio", "Avanzado")





def get_plan_topic(plan_topic_id):

    """Return one curricular topic; this is the primary question-bank relation."""

    return fetchone("SELECT * FROM plan_topics WHERE id=%s", (plan_topic_id,))





def actor_can_manage_plan(plan_id, actor_id):

    row = fetchone("""

        SELECT p.id

        FROM analytic_plans p

        JOIN users actor ON actor.id=%s

        WHERE p.id=%s

          AND (p.teacher_id=%s OR LOWER(COALESCE(actor.role,''))='admin')

    """, (actor_id, plan_id, actor_id))

    return bool(row)





def get_plan_topic_for_actor(plan_topic_id, actor_id):

    topic = get_plan_topic(plan_topic_id)

    if not topic or not actor_can_manage_plan(topic["plan_id"], actor_id):

        return None

    return topic





def actor_can_manage_question_bank_item(item_id, actor_id):

    row = fetchone("SELECT plan_id FROM question_bank_items WHERE id=%s", (item_id,))

    return bool(row and actor_can_manage_plan(row["plan_id"], actor_id))





def get_question_bank_items(plan_id=None, plan_topic_id=None, status=None):

    clauses, params = [], []

    if plan_id is not None:

        clauses.append("plan_id=%s")

        params.append(plan_id)

    if plan_topic_id is not None:

        clauses.append("plan_topic_id=%s")

        params.append(plan_topic_id)

    if status is not None:

        clauses.append("status=%s")

        params.append(status)

    where = (" WHERE " + " AND ".join(clauses)) if clauses else ""

    return fetchall("SELECT * FROM question_bank_items" + where + " ORDER BY id DESC", tuple(params))





def validate_question_bank_payload(payload):

    """Validate a teacher-reviewable multiple-choice item without changing its curriculum."""

    if not isinstance(payload, dict):

        raise ValueError("La pregunta debe ser un objeto JSON.")

    question = normalize_spaces(payload.get("question", ""))

    options = [normalize_spaces(value) for value in (payload.get("options") or [])]

    options = [value for value in options if value]

    correct_answer = normalize_spaces(payload.get("correct_answer", ""))

    bloom_level = normalize_spaces(payload.get("bloom_level", ""))

    difficulty_level = normalize_spaces(payload.get("difficulty_level", ""))

    if not question:

        raise ValueError("La pregunta no puede estar vacía.")

    if len(options) != 4 or len(set(options)) != 4:

        raise ValueError("Cada pregunta debe tener exactamente cuatro opciones distintas.")

    if correct_answer not in options:

        raise ValueError("La respuesta correcta debe pertenecer a las cuatro opciones.")

    if bloom_level not in QUESTION_BANK_BLOOM_LEVELS:

        raise ValueError("Nivel Bloom inválido.")

    if difficulty_level not in QUESTION_BANK_DIFFICULTIES:

        raise ValueError("Dificultad inválida.")

    return {

        "question": question,

        "options": options,

        "correct_answer": correct_answer,

        "explanation": normalize_spaces(payload.get("explanation", "")),

        "bloom_level": bloom_level,

        "difficulty_level": difficulty_level,

    }





def create_question_bank_drafts(teacher_id, plan_topic_id, drafts, ai_model):

    topic_context = get_plan_topic_for_actor(plan_topic_id, teacher_id)

    if not topic_context:

        raise ValueError("No se encontró el tema curricular seleccionado.")

    if not isinstance(drafts, list) or not drafts:

        raise ValueError("No se recibieron preguntas para guardar.")

    created = []

    for draft in drafts:

        item = validate_question_bank_payload(draft)

        row = execute("""

            INSERT INTO question_bank_items(

                plan_id, plan_topic_id, created_by, question, options_json, correct_answer,

                explanation, unit_name, topic, subtopic, learning_outcome, bloom_level,

                difficulty_level, status, source, ai_model, created_at

            ) VALUES(%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,'draft','ai',%s,%s)

            RETURNING *

        """, (

            topic_context["plan_id"], topic_context["id"], teacher_id, item["question"],

            json.dumps(item["options"], ensure_ascii=False), item["correct_answer"], item["explanation"],

            topic_context.get("unit_name"), topic_context["topic"], topic_context.get("subtopic"),

            topic_context.get("learning_outcome"), item["bloom_level"], item["difficulty_level"], ai_model, now(),

        ), returning=True)

        created.append(row)

    return created





def update_question_bank_item(item_id, payload, actor_id):

    if not actor_can_manage_question_bank_item(item_id, actor_id):

        raise ValueError("No tienes autorización para modificar esta pregunta.")

    item = validate_question_bank_payload(payload)

    return execute("""

        UPDATE question_bank_items

        SET question=%s, options_json=%s, correct_answer=%s, explanation=%s,

            bloom_level=%s, difficulty_level=%s, updated_at=%s

        WHERE id=%s AND status='draft'

        RETURNING *

    """, (

        item["question"], json.dumps(item["options"], ensure_ascii=False), item["correct_answer"],

        item["explanation"], item["bloom_level"], item["difficulty_level"], now(), item_id,

    ), returning=True)





# BUNSEKI_R4_GOLD_GOVERNANCE_V1
def ensure_question_bank_gold_schema():
    execute(
        "ALTER TABLE IF EXISTS question_bank_items "
        "ADD COLUMN IF NOT EXISTS governance_level TEXT NOT NULL DEFAULT 'practice'"
    )
    execute(
        "ALTER TABLE IF EXISTS question_bank_items "
        "ADD COLUMN IF NOT EXISTS gold_by INTEGER REFERENCES users(id) ON DELETE SET NULL"
    )
    execute(
        "ALTER TABLE IF EXISTS question_bank_items "
        "ADD COLUMN IF NOT EXISTS gold_at TEXT"
    )
    execute(
        "UPDATE question_bank_items "
        "SET governance_level='validated' "
        "WHERE status='approved' AND governance_level='practice'"
    )
    execute(
        "DO $$ BEGIN "
        "IF NOT EXISTS (SELECT 1 FROM pg_constraint "
        "WHERE conname='question_bank_items_governance_level_check') THEN "
        "ALTER TABLE question_bank_items "
        "ADD CONSTRAINT question_bank_items_governance_level_check "
        "CHECK (governance_level IN ('practice','validated','gold')); "
        "END IF; END $$;"
    )


def promote_question_bank_item_to_gold(item_id, actor_id):
    ensure_question_bank_gold_schema()

    if not actor_can_manage_question_bank_item(item_id, actor_id):
        raise ValueError("No tienes autorización para promover esta pregunta a Gold.")

    item = fetchone(
        "SELECT q.*, pt.source_excerpt, pt.source_locator, ap.raw_text "
        "FROM question_bank_items q "
        "JOIN plan_topics pt ON pt.id=q.plan_topic_id "
        "JOIN analytic_plans ap ON ap.id=q.plan_id "
        "WHERE q.id=%s",
        (item_id,),
    )

    if not item:
        raise ValueError("No se encontró la pregunta o su trazabilidad curricular.")

    if item.get("status") != "approved":
        raise ValueError("Solo una pregunta aprobada puede promoverse a Gold.")

    if item.get("governance_level") == "gold":
        raise ValueError("La pregunta ya pertenece al Banco Gold.")

    if item.get("governance_level") != "validated":
        raise ValueError("La pregunta debe estar validada antes de promoverse a Gold.")

    required_fields = {
        "plan_topic_id": item.get("plan_topic_id"),
        "learning_outcome": item.get("learning_outcome"),
        "bloom_level": item.get("bloom_level"),
        "difficulty_level": item.get("difficulty_level"),
    }
    missing = [
        field
        for field, value in required_fields.items()
        if value is None or not str(value).strip()
    ]
    if missing:
        raise ValueError(
            "No puede promoverse a Gold: faltan campos curriculares: "
            + ", ".join(missing)
            + "."
        )

    excerpt = _normalize_source_trace_text(item.get("source_excerpt"))
    raw_text = _normalize_source_trace_text(item.get("raw_text"))
    locator = str(item.get("source_locator") or "").strip()

    if not excerpt or not raw_text or excerpt.casefold() not in raw_text.casefold():
        raise ValueError(
            "No puede promoverse a Gold: la evidencia documental no es verificable "
            "contra el plan analítico original."
        )

    if not re.fullmatch(r"Lineas \d+-\d+", locator):
        raise ValueError(
            "No puede promoverse a Gold: el localizador documental no es válido."
        )

    promoted = execute(
        "UPDATE question_bank_items "
        "SET governance_level='gold', gold_by=%s, gold_at=%s, updated_at=%s "
        "WHERE id=%s AND status='approved' AND governance_level='validated' "
        "RETURNING *",
        (actor_id, now(), now(), item_id),
        returning=True,
    )

    if not promoted:
        raise ValueError("La promoción a Gold no pudo completarse.")

    return promoted


# BUNSEKI_R5_MOODLE_XML_EXPORT_V1
# BUNSEKI_R7_MOODLE_PREVIEW_V1
def render_moodle_gold_preview(gold_items):
    st.markdown("#### Vista previa Moodle")
    st.caption(
        "Simulación docente de cómo se verá cada pregunta Gold antes de exportarla. "
        "La importación final puede variar ligeramente según el tema de Moodle."
    )

    for position, item in enumerate(gold_items, 1):
        try:
            options = json.loads(item.get("options_json") or "[]")
        except Exception:
            options = []

        if not isinstance(options, list):
            options = []

        options = [str(option) for option in options]
        correct_answer = re.sub(
            r"\s+",
            " ",
            str(item.get("correct_answer") or ""),
        ).strip().casefold()

        with st.expander(
            f"Pregunta {position} · {item.get('topic') or 'Sin tema'}",
            expanded=(position == 1),
        ):
            st.markdown(f"**{position}. {item.get('question') or ''}**")

            if options:
                for option_index, option in enumerate(options):
                    label = chr(65 + option_index)
                    st.write(f"○ {label}. {option}")
            else:
                st.warning("Esta pregunta no contiene opciones válidas.")

            st.caption(
                "Etiquetas Moodle: "
                f"Gold · Bloom:{item.get('bloom_level') or '-'} · "
                f"Dificultad:{item.get('difficulty_level') or '-'}"
            )

            correct_option = next(
                (
                    option
                    for option in options
                    if re.sub(r"\s+", " ", option).strip().casefold()
                    == correct_answer
                ),
                None,
            )

            st.markdown("**Clave docente**")
            if correct_option:
                st.success(f"Respuesta correcta: {correct_option}")
            else:
                st.warning(
                    "No se pudo identificar una respuesta correcta única "
                    "para la vista previa."
                )

            explanation = str(item.get("explanation") or "").strip()
            if explanation:
                st.info(f"Retroalimentación: {explanation}")
            else:
                st.caption("Sin retroalimentación general registrada.")

            st.caption(
                f"Banco: Gold · ID #{item.get('id')} · "
                f"Resultado de aprendizaje: {item.get('learning_outcome') or '-'}"
            )


def get_gold_question_bank_items_for_moodle(plan_id):
    ensure_question_bank_gold_schema()
    return fetchall(
        "SELECT q.*, pt.source_excerpt, pt.source_locator, "
        "ap.title AS plan_title "
        "FROM question_bank_items q "
        "JOIN plan_topics pt ON pt.id=q.plan_topic_id "
        "JOIN analytic_plans ap ON ap.id=q.plan_id "
        "WHERE q.plan_id=%s "
        "AND q.status='approved' "
        "AND q.governance_level='gold' "
        "ORDER BY q.id",
        (plan_id,),
    )


def build_moodle_xml_from_gold_items(plan_id):
    import xml.etree.ElementTree as ET

    rows = get_gold_question_bank_items_for_moodle(plan_id)
    if not rows:
        raise ValueError("No existen preguntas Gold para exportar.")

    plan_title = str(rows[0].get("plan_title") or f"Plan {plan_id}").strip()
    quiz = ET.Element("quiz")

    category_question = ET.SubElement(
        quiz,
        "question",
        {"type": "category"},
    )
    category = ET.SubElement(category_question, "category")
    ET.SubElement(category, "text").text = (
        f"$course$/top/BunsekiChat/{plan_title}"
    )

    exported_count = 0

    for item in rows:
        if item.get("status") != "approved":
            continue
        if item.get("governance_level") != "gold":
            continue

        try:
            options = json.loads(item.get("options_json") or "[]")
        except Exception as exc:
            raise ValueError(
                f"La pregunta #{item.get('id')} tiene opciones JSON inválidas."
            ) from exc

        if not isinstance(options, list) or len(options) < 2:
            raise ValueError(
                f"La pregunta #{item.get('id')} no tiene suficientes opciones."
            )

        options = [str(option) for option in options]

        def normalize_answer(value):
            return re.sub(r"\s+", " ", str(value or "")).strip().casefold()

        correct_answer = normalize_answer(item.get("correct_answer"))
        matches = [
            option
            for option in options
            if normalize_answer(option) == correct_answer
        ]

        if len(matches) != 1:
            raise ValueError(
                f"La respuesta correcta de la pregunta #{item.get('id')} "
                "no coincide de forma única con sus opciones."
            )

        correct_option = matches[0]

        question = ET.SubElement(
            quiz,
            "question",
            {"type": "multichoice"},
        )

        name = ET.SubElement(question, "name")
        ET.SubElement(name, "text").text = (
            f"Bunseki Gold #{item.get('id')} - "
            f"{item.get('topic') or 'Pregunta'}"
        )

        questiontext = ET.SubElement(
            question,
            "questiontext",
            {"format": "html"},
        )
        ET.SubElement(questiontext, "text").text = str(
            item.get("question") or ""
        )

        generalfeedback = ET.SubElement(
            question,
            "generalfeedback",
            {"format": "html"},
        )
        ET.SubElement(generalfeedback, "text").text = str(
            item.get("explanation") or ""
        )

        ET.SubElement(question, "defaultgrade").text = "1.0000000"
        ET.SubElement(question, "penalty").text = "0.3333333"
        ET.SubElement(question, "hidden").text = "0"
        ET.SubElement(question, "single").text = "true"
        ET.SubElement(question, "shuffleanswers").text = "true"
        ET.SubElement(question, "answernumbering").text = "abc"

        correctfeedback = ET.SubElement(
            question,
            "correctfeedback",
            {"format": "html"},
        )
        ET.SubElement(correctfeedback, "text").text = "Respuesta correcta."

        partiallycorrectfeedback = ET.SubElement(
            question,
            "partiallycorrectfeedback",
            {"format": "html"},
        )
        ET.SubElement(partiallycorrectfeedback, "text").text = (
            "Revisa la retroalimentación general."
        )

        incorrectfeedback = ET.SubElement(
            question,
            "incorrectfeedback",
            {"format": "html"},
        )
        ET.SubElement(incorrectfeedback, "text").text = (
            "Revisa el razonamiento y vuelve a intentarlo."
        )

        tags = ET.SubElement(question, "tags")
        tag_values = [
            "BunsekiChat",
            "Gold",
            f"Bloom:{item.get('bloom_level') or ''}",
            f"Dificultad:{item.get('difficulty_level') or ''}",
            f"Tema:{item.get('topic') or ''}",
        ]
        for tag_value in tag_values:
            if not tag_value.endswith(":") and tag_value.strip():
                tag = ET.SubElement(tags, "tag")
                ET.SubElement(tag, "text").text = tag_value.strip()

        for option in options:
            fraction = "100" if option == correct_option else "0"
            answer = ET.SubElement(
                question,
                "answer",
                {"fraction": fraction, "format": "html"},
            )
            ET.SubElement(answer, "text").text = option
            feedback = ET.SubElement(
                answer,
                "feedback",
                {"format": "html"},
            )
            ET.SubElement(feedback, "text").text = (
                "Respuesta correcta."
                if fraction == "100"
                else "Opción incorrecta."
            )

        exported_count += 1

    if exported_count == 0:
        raise ValueError("No se encontró ninguna pregunta Gold exportable.")

    xml_bytes = ET.tostring(
        quiz,
        encoding="utf-8",
        xml_declaration=True,
    )
    ET.fromstring(xml_bytes)

    safe_title = re.sub(
        r"[^A-Za-z0-9._-]+",
        "_",
        plan_title,
    ).strip("._") or f"plan_{plan_id}"

    filename = f"{safe_title}_Bunseki_Gold_Moodle.xml"
    return xml_bytes, filename, exported_count


def approve_question_bank_item(item_id, approved_by):
    ensure_question_bank_gold_schema()

    if not actor_can_manage_question_bank_item(item_id, approved_by):

        raise ValueError("No tienes autorización para aprobar esta pregunta.")

    return execute("""

        UPDATE question_bank_items

        SET status='approved', governance_level='validated', approved_by=%s, approved_at=%s, updated_at=%s

        WHERE id=%s AND status='draft'

        RETURNING *

    """, (approved_by, now(), now(), item_id), returning=True)





def reject_question_bank_item(item_id, actor_id):

    if not actor_can_manage_question_bank_item(item_id, actor_id):

        raise ValueError("No tienes autorización para rechazar esta pregunta.")

    return execute("""

        UPDATE question_bank_items SET status='rejected', updated_at=%s

        WHERE id=%s AND status='draft' RETURNING *

    """, (now(), item_id), returning=True)





def delete_question_bank_item(item_id, actor_id):

    """Soft delete only: teacher actions must never physically delete bank items."""

    if not actor_can_manage_question_bank_item(item_id, actor_id):

        raise ValueError("No tienes autorización para eliminar esta pregunta.")

    return execute("""

        UPDATE question_bank_items SET status='deleted', updated_at=%s

        WHERE id=%s AND status IN ('draft', 'rejected') RETURNING *

    """, (now(), item_id), returning=True)





def generate_topic_question_drafts(teacher_id, plan_topic_id, difficulty="Intermedio", n_questions=10):

    """Generate and persist AI drafts for one selected curricular topic, never for a student quiz."""

    if difficulty not in QUESTION_BANK_DIFFICULTIES:

        raise ValueError("Dificultad inválida.")

    n_questions = int(n_questions)

    if not 1 <= n_questions <= 20:

        raise ValueError("El número de preguntas debe estar entre 1 y 20.")

    topic_context = get_plan_topic_for_actor(plan_topic_id, teacher_id)

    if not topic_context:

        raise ValueError("No se encontró el tema curricular seleccionado.")

    client = ai_client()

    if not client:

        raise RuntimeError("No hay un modelo Gemini configurado para generar borradores.")

    model = "gemini-2.5-flash"

    prompt = f"""

Eres diseñador de evaluación universitaria. Genera EXACTAMENTE {n_questions} preguntas de opción múltiple.

Devuelve SOLO un arreglo JSON válido, sin markdown. Cada objeto debe tener: question, options,

correct_answer, explanation, bloom_level, difficulty_level. options debe contener exactamente 4 textos

distintos y correct_answer debe coincidir exactamente con una opción.



ALINEACIÓN OBLIGATORIA: no cambies ni inventes otro tema. Cada pregunta debe comprobar el resultado de

aprendizaje dentro del tema curricular indicado y respetar Bloom y dificultad solicitados.

Unidad: {topic_context.get('unit_name') or ''}

Tema obligatorio: {topic_context.get('topic') or ''}

Subtema: {topic_context.get('subtopic') or ''}

Resultado de aprendizaje: {topic_context.get('learning_outcome') or ''}

Bloom permitido: {topic_context.get('bloom_level') or ''} (usa uno de: {', '.join(QUESTION_BANK_BLOOM_LEVELS)})

Palabras clave: {topic_context.get('keywords') or ''}

Dificultad obligatoria: {difficulty}

"""

    try:

        response = client.models.generate_content(model=model, contents=prompt)

        generated = _safe_json_loads(response.text, [])

    except Exception as exc:

        raise RuntimeError("No se pudieron generar los borradores curriculares.") from exc

    if not isinstance(generated, list) or len(generated) != n_questions:

        raise ValueError("Gemini debe devolver exactamente el número de preguntas solicitado.")

    normalized = []

    for draft in generated:

        draft = dict(draft) if isinstance(draft, dict) else {}

        # Topic provenance is deliberately copied from plan_topic, never accepted from Gemini.

        draft["difficulty_level"] = difficulty

        if not draft.get("bloom_level"):

            draft["bloom_level"] = topic_context.get("bloom_level") or "Aplicar"

        normalized.append(validate_question_bank_payload(draft))

    return create_question_bank_drafts(teacher_id, plan_topic_id, normalized, model)





ASSESSMENT_AUDIENCE_MODES = ("academic_context", "targeted")





def _assessment_audience_mode(assessment):

    mode = str((assessment or {}).get("audience_mode") or "academic_context").strip().lower()

    return mode if mode in ASSESSMENT_AUDIENCE_MODES else "invalid"





def get_teacher_assessment_target_user_ids(assessment_id):

    rows = fetchall(

        "SELECT user_id FROM teacher_assessment_targets WHERE assessment_id=%s ORDER BY user_id",

        (assessment_id,),

    )

    return [int(row["user_id"]) for row in rows]





# P5A5-ELIGIBLE-ASSESSMENT-TARGETS

def get_teacher_assessment_eligible_students(assessment_id, actor_user_id):

    assessment = fetchone(

        "SELECT * FROM teacher_assessments WHERE id=%s",

        (assessment_id,),

    )

    if not assessment or int(assessment.get("created_by") or 0) != int(actor_user_id or 0):

        return []



    actor = fetchone(

        "SELECT role, active FROM users WHERE id=%s",

        (actor_user_id,),

    ) or {}

    if not bool(actor.get("active")):

        return []



    role = str(actor.get("role") or "").strip().lower()

    columns = """

        u.id,u.username,

        p.first_names,p.last_names,p.subject,p.course_level,

        p.parallel,p.shift,p.cohort,p.course

    """



    if role in {"teacher", "docente"}:

        rows = fetchall(

            f"""

            SELECT DISTINCT {columns}

            FROM users u

            JOIN enrollments e

              ON e.student_user_id=u.id

             AND e.status='active'

            JOIN teacher_parallels tp

              ON tp.parallel_id=e.parallel_id

            JOIN teachers t

              ON t.id=tp.teacher_id

             AND t.user_id=%s

             AND t.status='active'

            LEFT JOIN profiles p

              ON p.user_id=u.id

            WHERE u.role='student'

              AND u.active=TRUE

            ORDER BY u.id

            """,

            (actor_user_id,),

        )

    elif role == "admin":

        rows = fetchall(

            f"""

            SELECT DISTINCT {columns}

            FROM users u

            LEFT JOIN profiles p

              ON p.user_id=u.id

            WHERE u.role='student'

              AND u.active=TRUE

            ORDER BY u.id

            """

        )

    else:

        return []



    eligible = []

    for row in rows:

        context = academic_context_from_profile(row)

        if assessment_matches_academic_context(assessment, context):

            eligible.append(row)

    return eligible





def set_teacher_assessment_audience(assessment_id, teacher_id, audience_mode, target_user_ids=None):

    assessment = get_teacher_assessment(assessment_id)

    if not assessment or assessment["created_by"] != teacher_id or assessment["status"] != "draft":

        raise ValueError("Solo el docente creador puede configurar la audiencia de una evaluación en borrador.")

    mode = str(audience_mode or "").strip().lower()

    if mode not in ASSESSMENT_AUDIENCE_MODES:

        raise ValueError("Modo de audiencia inválido.")

    ids = list(dict.fromkeys(int(user_id) for user_id in (target_user_ids or []) if int(user_id) > 0))

    if mode == "targeted" and not ids:

        raise ValueError("Una evaluación dirigida necesita al menos un estudiante destinatario.")

    if mode == "academic_context" and ids:

        raise ValueError("El modo por contexto académico no admite destinatarios individuales.")

    if ids:

        eligible = fetchall(

            "SELECT id FROM users WHERE id = ANY(%s) AND role='student' AND active=TRUE",

            (ids,),

        )

        if {int(row["id"]) for row in eligible} != set(ids):

            raise ValueError("Todos los destinatarios deben ser estudiantes activos.")

        scoped_ids = {

            int(row["id"])

            for row in get_teacher_assessment_eligible_students(

                assessment_id,

                teacher_id,

            )

        }

        if set(ids) != (set(ids) & scoped_ids):

            raise ValueError(

                "Todos los destinatarios deben pertenecer al alcance del "

                "docente y coincidir con el contexto académico de la evaluación."

            )

    execute("DELETE FROM teacher_assessment_targets WHERE assessment_id=%s", (assessment_id,))

    execute(

        "UPDATE teacher_assessments SET audience_mode=%s WHERE id=%s AND status='draft'",

        (mode, assessment_id),

    )

    for user_id in ids:

        execute(

            """INSERT INTO teacher_assessment_targets(assessment_id,user_id,assigned_by,created_at)

               VALUES(%s,%s,%s,%s)""",

            (assessment_id, user_id, teacher_id, now()),

        )

    return get_teacher_assessment(assessment_id)





def assessment_allows_student(assessment, user_id, academic_context=None):

    mode = _assessment_audience_mode(assessment)

    if mode == "academic_context":

        return assessment_matches_academic_context(assessment, academic_context)

    if mode != "targeted" or not user_id:

        return False

    if not assessment_matches_academic_context(assessment, academic_context):

        return False

    target = fetchone(

        "SELECT 1 AS allowed FROM teacher_assessment_targets WHERE assessment_id=%s AND user_id=%s",

        (assessment.get("id"), user_id),

    )

    return bool(target)



def create_teacher_assessment(created_by, plan_id, title, academic_context=None):

    context = academic_context or {}

    if not actor_can_manage_plan(plan_id, created_by):

        raise ValueError("No tienes autorización para crear una evaluación con este plan analítico.")

    if not normalize_spaces(title):

        raise ValueError("La evaluación necesita un título.")

    return execute("""INSERT INTO teacher_assessments(created_by,plan_id,title,subject,course_level,parallel,shift,cohort,status,created_at)

        VALUES(%s,%s,%s,%s,%s,%s,%s,%s,'draft',%s) RETURNING *""", (created_by, plan_id, normalize_spaces(title), context.get("subject"), context.get("course_level"), context.get("parallel"), context.get("shift"), context.get("cohort"), now()), returning=True)





def get_teacher_assessments(created_by=None, status=None):

    clauses, params = [], []

    for field, value in (("created_by", created_by), ("status", status)):

        if value is not None: clauses.append(field + "=%s"); params.append(value)

    return fetchall("SELECT * FROM teacher_assessments" + (" WHERE " + " AND ".join(clauses) if clauses else "") + " ORDER BY id DESC", tuple(params))





def get_teacher_assessment(assessment_id):

    return fetchone("SELECT * FROM teacher_assessments WHERE id=%s", (assessment_id,))





def get_teacher_assessment_items(assessment_id):

    return fetchall("""SELECT tai.*, qbi.* FROM teacher_assessment_items tai JOIN question_bank_items qbi ON qbi.id=tai.question_bank_item_id

        WHERE tai.assessment_id=%s ORDER BY tai.position""", (assessment_id,))





def set_teacher_assessment_items(assessment_id, teacher_id, question_bank_item_ids):

    assessment = get_teacher_assessment(assessment_id)

    if not assessment or assessment["created_by"] != teacher_id or assessment["status"] != "draft":

        raise ValueError("Solo el docente creador puede editar una evaluación en borrador.")

    ids = list(dict.fromkeys(int(item_id) for item_id in question_bank_item_ids))

    if ids:

        approved = fetchall(

            "SELECT id FROM question_bank_items WHERE id = ANY(%s) AND status='approved' AND plan_id=%s",

            (ids, assessment["plan_id"]),

        )

        if {row["id"] for row in approved} != set(ids):

            raise ValueError("Solo se pueden agregar preguntas aprobadas del mismo plan analítico.")

    execute("DELETE FROM teacher_assessment_items WHERE assessment_id=%s", (assessment_id,))

    for position, item_id in enumerate(ids, 1):

        execute("INSERT INTO teacher_assessment_items(assessment_id,question_bank_item_id,position) VALUES(%s,%s,%s)", (assessment_id, item_id, position))





def publish_teacher_assessment(assessment_id, teacher_id):

    assessment = get_teacher_assessment(assessment_id)

    if not assessment or assessment["created_by"] != teacher_id or assessment["status"] != "draft":

        raise ValueError("La evaluación no se puede publicar.")

    if not get_teacher_assessment_items(assessment_id):

        raise ValueError("No se puede publicar una evaluación sin preguntas.")

    mode = _assessment_audience_mode(assessment)

    if mode == "invalid":

        raise ValueError("La evaluación tiene un modo de audiencia inválido.")

    if mode == "targeted":

        target_ids = get_teacher_assessment_target_user_ids(assessment_id)

        if not target_ids:

            raise ValueError("No se puede publicar una evaluación dirigida sin estudiantes destinatarios.")

        eligible_ids = {

            int(row["id"])

            for row in get_teacher_assessment_eligible_students(

                assessment_id,

                teacher_id,

            )

        }

        if not set(target_ids).issubset(eligible_ids):

            raise ValueError(

                "No se puede publicar una evaluación dirigida con destinatarios "

                "fuera del alcance docente o del contexto académico vigente."

            )

    return execute(

        "UPDATE teacher_assessments SET status='published', published_at=%s WHERE id=%s AND status='draft' RETURNING *",

        (now(), assessment_id),

        returning=True,

    )



def close_teacher_assessment(assessment_id, teacher_id):

    assessment = get_teacher_assessment(assessment_id)

    if not assessment or assessment["created_by"] != teacher_id or assessment["status"] != "published": raise ValueError("La evaluación no se puede cerrar.")

    return execute("UPDATE teacher_assessments SET status='closed', closed_at=%s WHERE id=%s AND status='published' RETURNING *", (now(), assessment_id), returning=True)





def assessment_matches_academic_context(assessment, context):

    return all(not assessment.get(key) or str(assessment.get(key)) == str((context or {}).get(key) or "") for key in ("subject", "course_level", "parallel", "shift", "cohort"))





def get_student_teacher_assessments(academic_context, user_id=None):

    rows = get_teacher_assessments(status="published")

    return [row for row in rows if assessment_allows_student(row, user_id, academic_context)]



def start_teacher_assessment_attempt(user_id, teacher_assessment_id, academic_context=None):

    assessment = get_teacher_assessment(teacher_assessment_id)

    if not assessment or assessment["status"] != "published":

        raise ValueError("La evaluación no está publicada.")

    # assessment_matches_academic_context remains enforced inside assessment_allows_student.

    if not assessment_allows_student(assessment, user_id, academic_context):

        raise ValueError("La evaluación no está asignada a este estudiante o no corresponde a su contexto académico.")

    existing = fetchone(

        "SELECT * FROM adaptive_quizzes WHERE user_id=%s AND teacher_assessment_id=%s",

        (user_id, teacher_assessment_id),

    )

    if existing: return existing

    items = get_teacher_assessment_items(teacher_assessment_id)

    if not items or any(item.get("status") != "approved" for item in items):

        raise ValueError("La evaluación contiene preguntas no aprobadas.")

    quiz = execute(

        """INSERT INTO adaptive_quizzes(user_id,plan_id,title,quiz_type,source_topic,difficulty,subject,course_level,parallel,shift,cohort,question_count,status,teacher_assessment_id,created_at)

           VALUES(%s,%s,%s,'bank_assessment',%s,'Banco aprobado',%s,%s,%s,%s,%s,%s,'generated',%s,%s) RETURNING *""",

        (

            user_id, assessment["plan_id"], assessment["title"], items[0].get("topic", ""),

            assessment.get("subject"), assessment.get("course_level"), assessment.get("parallel"),

            assessment.get("shift"), assessment.get("cohort"), len(items), teacher_assessment_id, now(),

        ),

        returning=True,

    )

    for item in items:

        execute(

            """INSERT INTO adaptive_questions(quiz_id,question,options_json,correct_answer,position,topic,subtopic,difficulty_level,explanation,bloom_level,question_bank_item_id,plan_topic_id,learning_outcome)

               VALUES(%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)""",

            (

                quiz["id"], item["question"], item["options_json"], item["correct_answer"], item["position"],

                item.get("topic"), item.get("subtopic"), item.get("difficulty_level"), item.get("explanation"),

                item.get("bloom_level"), item["question_bank_item_id"], item.get("plan_topic_id"),

                item.get("learning_outcome"),

            ),

        )

    return quiz



def grade_teacher_assessment_attempt(quiz_id, answers, elapsed_seconds=None):

    quiz, questions = load_adaptive_quiz(quiz_id)

    if not quiz or quiz.get("quiz_type") != "bank_assessment": raise ValueError("Intento de banco inválido.")

    correct = 0

    for question in questions:

        answer = (answers or {}).get(str(question["id"]), "")

        ok = answer == question.get("correct_answer"); correct += int(ok)

        execute("UPDATE adaptive_questions SET user_answer=%s,is_correct=%s,response_time_seconds=%s WHERE id=%s", (answer, ok, elapsed_seconds, question["id"]))

    score = round(correct / max(1, len(questions)) * 100, 2)

    return execute("UPDATE adaptive_quizzes SET score=%s,passed=%s,status='completed',completed_at=%s WHERE id=%s RETURNING *", (score, score >= 70, now(), quiz_id), returning=True)





def get_teacher_assessment_results(assessment_id):

    return fetchall("""SELECT q.user_id,q.score,q.status,q.completed_at,COUNT(aq.id) AS answered,COALESCE(SUM(CASE WHEN aq.is_correct THEN 1 ELSE 0 END),0) AS correct

        FROM adaptive_quizzes q LEFT JOIN adaptive_questions aq ON aq.quiz_id=q.id WHERE q.teacher_assessment_id=%s GROUP BY q.id ORDER BY q.id DESC""", (assessment_id,))





def get_teacher_assessment_item_results(assessment_id):

    return fetchall("SELECT aq.* FROM adaptive_questions aq JOIN adaptive_quizzes q ON q.id=aq.quiz_id WHERE q.teacher_assessment_id=%s", (assessment_id,))





def get_teacher_assessment_curricular_analytics(assessment_id):

    rows = get_teacher_assessment_item_results(assessment_id)

    result = {}

    for label, field in (("topic", "topic"), ("learning_outcome", "learning_outcome"), ("bloom_level", "bloom_level"), ("difficulty_level", "difficulty_level")):

        groups = {}

        for row in rows:

            key = row.get(field) or "Sin dato"; entry = groups.setdefault(key, {"total": 0, "correct": 0, "errors": 0})

            if row.get("user_answer") is not None:

                entry["total"] += 1; entry["correct"] += int(bool(row.get("is_correct"))); entry["errors"] += int(not bool(row.get("is_correct")))

        result[label] = [{label: key, **value, "percentage": round(value["correct"] / max(1, value["total"]) * 100, 2)} for key, value in groups.items()]

    return result





def get_student_consultation_profile(uid: int) -> dict:

    rows = interactions(uid)

    user_rows = [r for r in rows if str(r.get("role","")).lower() == "user"]

    topic_counts = {}

    subtopic_counts = {}

    recent_messages = []

    for r in user_rows:

        t = r.get("topic") or "General"

        s = r.get("subtopic") or ""

        topic_counts[t] = topic_counts.get(t, 0) + 1

        if s:

            subtopic_counts[s] = subtopic_counts.get(s, 0) + 1

        if r.get("message"):

            recent_messages.append(str(r.get("message"))[:500])

    ranked_topics = sorted(topic_counts, key=topic_counts.get, reverse=True)

    ranked_subtopics = sorted(subtopic_counts, key=subtopic_counts.get, reverse=True)

    return {

        "topic_counts": topic_counts,

        "ranked_topics": ranked_topics,

        "ranked_subtopics": ranked_subtopics,

        "recent_messages": recent_messages[-8:],

        "total_questions": len(user_rows),

    }





def is_research_quiz(quiz_type):

    return str(quiz_type or "").lower() in {"pretest", "posttest"}





def quiz_type_label(quiz_type):

    return RESEARCH_TEST_TYPES.get(str(quiz_type or "adaptive").lower(), "Evaluación adaptativa")





def research_dimension_counts(n_questions):

    n = int(n_questions)

    counts = {}

    assigned = 0

    for item in RESEARCH_DIMENSION_BLUEPRINT[:-1]:

        count = int(round(n * float(item["weight"])))

        counts[item["dimension"]] = count

        assigned += count

    last_dim = RESEARCH_DIMENSION_BLUEPRINT[-1]["dimension"]

    counts[last_dim] = max(0, n - assigned)

    return counts





def research_blueprint_sequence(n_questions):

    counts = research_dimension_counts(n_questions)

    sequence = []

    for item in RESEARCH_DIMENSION_BLUEPRINT:

        for _ in range(counts.get(item["dimension"], 0)):

            sequence.append(dict(item))

    while len(sequence) < int(n_questions):

        sequence.append(dict(RESEARCH_DIMENSION_BLUEPRINT[-1]))

    return sequence[:int(n_questions)]





def difficulty_for_position(position, total):

    ratio = position / max(1, total)

    if ratio <= 0.35:

        return "Fácil"

    if ratio <= 0.75:

        return "Medio"

    return "Difícil"





def assessment_version_from_seed(seed):

    versions = ["A", "B", "C", "D"]

    try:

        return versions[int(str(seed)[:2], 16) % len(versions)]

    except Exception:

        return random.choice(versions)





def research_form_version(uid, academic_context):

    context = academic_context or {}

    raw = "|".join([

        str(uid), str(context.get("subject", "")), str(context.get("course_level", "")),

        str(context.get("parallel", "")), str(context.get("shift", "")), RESEARCH_FOCUS_TOPIC,

    ])

    return assessment_version_from_seed(hashlib.sha256(raw.encode("utf-8")).hexdigest())





def stable_item_code(question_text):

    normalized = strip_accents(normalize_spaces(question_text)).lower()

    return "ITM-" + hashlib.sha256(normalized.encode("utf-8")).hexdigest()[:12].upper()





def enrich_research_questions(questions, n_questions):

    blueprint = research_blueprint_sequence(n_questions)

    enriched = []

    for idx, q in enumerate(questions[:int(n_questions)], start=1):

        item = dict(q)

        meta = blueprint[idx - 1] if idx - 1 < len(blueprint) else dict(RESEARCH_DIMENSION_BLUEPRINT[-1])

        item.setdefault("dimension", meta.get("dimension", "Aplicaciones"))

        item.setdefault("indicator", meta.get("indicator", RESEARCH_FOCUS_TOPIC))

        item.setdefault("competence", meta.get("competence", "Resuelve problemas aplicados"))

        item.setdefault("subtopic", item.get("topic") or RESEARCH_FOCUS_TOPIC)

        item.setdefault("difficulty_level", difficulty_for_position(idx, n_questions))

        enriched.append(item)

    return enriched





def conceptual_error_for_question(question, is_correct):

    if is_correct:

        return ""

    topic = strip_accents(question.get("topic") or question.get("subtopic") or "").lower()

    if "optimiz" in topic:

        return "Dificultad para formular función objetivo o analizar extremos"

    if "razon" in topic or "tasa" in topic or "velocidad" in topic:

        return "Confusión entre magnitud y tasa de cambio"

    if "concav" in topic:

        return "Confusión en interpretación de la segunda derivada"

    if "tangente" in topic or "pendiente" in topic:

        return "Dificultad para interpretar la derivada como pendiente"

    if "marginal" in topic:

        return "Confusión en interpretación marginal"

    return "Error conceptual o procedimental en aplicaciones de la derivada"





def cognitive_profile_from_score(score):

    if score >= 80:

        return "avanzado"

    if score >= 55:

        return "intermedio"

    return "novato"





def bloom_rank(level):

    order = {"Recordar": 1, "Comprender": 2, "Aplicar": 3, "Analizar": 4, "Evaluar": 5, "Crear": 6}

    return order.get(str(level or "").strip(), 0)





def build_quiz_diagnosis(score, questions):

    by_dimension = {}

    by_topic = {}

    errors = {}

    correct_blooms = []

    for q in questions:

        dim = q.get("dimension") or "Sin dimensión"

        topic = q.get("topic") or q.get("subtopic") or RESEARCH_FOCUS_TOPIC

        ok = bool(q.get("is_correct"))

        by_dimension.setdefault(dim, {"correct": 0, "total": 0})

        by_topic.setdefault(topic, {"correct": 0, "total": 0})

        by_dimension[dim]["total"] += 1

        by_topic[topic]["total"] += 1

        if ok:

            by_dimension[dim]["correct"] += 1

            by_topic[topic]["correct"] += 1

            correct_blooms.append(q.get("bloom_level"))

        else:

            err = q.get("conceptual_error") or conceptual_error_for_question(q, False)

            errors[err] = errors.get(err, 0) + 1



    dimension_scores = {

        dim: round(v["correct"] / max(1, v["total"]) * 100, 2)

        for dim, v in by_dimension.items()

    }

    topic_scores = {

        topic: round(v["correct"] / max(1, v["total"]) * 100, 2)

        for topic, v in by_topic.items()

    }

    dominated = [topic for topic, value in topic_scores.items() if value >= 70]

    deficient = [topic for topic, value in topic_scores.items() if value < 70]

    bloom_level = "Inicial"

    if correct_blooms:

        bloom_level = sorted(correct_blooms, key=bloom_rank)[-1]

    profile = cognitive_profile_from_score(score)

    learning_plan = {

        "focus_topic": RESEARCH_FOCUS_TOPIC,

        "profile": profile,

        "recommended_microclasses": deficient or [RESEARCH_FOCUS_TOPIC],

        "recommended_exercises": {"Fácil": 5, "Medio": 5, "Difícil": 3},

    }

    return {

        "dimension_scores": dimension_scores,

        "topic_scores": topic_scores,

        "frequent_errors": sorted(errors, key=errors.get, reverse=True)[:5],

        "dominated_concepts": dominated,

        "deficient_concepts": deficient,

        "bloom_level": bloom_level,

        "profile": profile,

        "learning_plan": learning_plan,

    }





def local_research_question_pool(subject):

    key = strip_accents(subject).lower()

    if "calculo diferencial" in key or "calculo" in key:

        focus_key = strip_accents(RESEARCH_FOCUS_TOPIC).lower()

        if "aplicaciones" in focus_key and "derivada" in focus_key:

            return [dict(q) for q in DERIVATIVE_APPLICATIONS_QUESTION_BANK]

        return [dict(q) for q in DIFFERENTIAL_CALCULUS_QUESTION_BANK]

    return []





ADAPTIVE_DIFFICULTIES = ("Inicial", "Básico", "Intermedio", "Avanzado")

# BUNSEKI_R8_20_V6_LEVEL_UX
PRACTICE_LEVEL_LABELS = {
    "Recomendado": "Automático (recomendado)",
    "Inicial": "Fundamentos",
    "Básico": "Básico",
    "Intermedio": "Intermedio",
    "Avanzado": "Avanzado",
}

PRACTICE_LEVEL_HELP = {
    "Recomendado": (
        "Automático: BunsekiChat selecciona y ajusta el nivel según el "
        "desempeño reciente del estudiante."
    ),
    "Inicial": (
        "Fundamentos: reconocimiento de conceptos, razones básicas y "
        "procedimientos directos de un paso."
    ),
    "Básico": (
        "Básico: aplicación directa del concepto en ejercicios de uno o "
        "dos pasos para consolidar el aprendizaje."
    ),
    "Intermedio": (
        "Intermedio: problemas contextualizados de varios pasos que exigen "
        "relacionar conceptos y elegir un procedimiento."
    ),
    "Avanzado": (
        "Avanzado: problemas no rutinarios que exigen análisis, transferencia, "
        "justificación y evaluación de estrategias."
    ),
}






def normalize_adaptive_difficulty(value):

    raw = normalize_spaces(value) or "Intermedio"

    aliases = {

        "inicial": "Inicial",

        "basico": "Básico",

        "básico": "Básico",

        "intermedio": "Intermedio",

        "avanzado": "Avanzado",

    }

    normalized = aliases.get(strip_accents(raw).lower())

    if normalized not in ADAPTIVE_DIFFICULTIES:

        raise ValueError(f"Dificultad adaptativa inválida: {raw}")

    return normalized





def adaptive_bloom_levels_for_difficulty(difficulty):

    difficulty = normalize_adaptive_difficulty(difficulty)

    return {

        "Inicial": {"Recordar", "Comprender"},

        "Básico": {"Recordar", "Comprender"},

        "Intermedio": {"Comprender", "Aplicar"},

        "Avanzado": {"Aplicar", "Analizar", "Evaluar", "Crear"},

    }[difficulty]





def adaptive_difficulty_instruction(difficulty):

    difficulty = normalize_adaptive_difficulty(difficulty)

    return {

        "Inicial": "conceptos fundamentales, reconocimiento e interpretación directa; evita problemas multietapa",

        "Básico": "comprensión conceptual y aplicación directa en uno o dos pasos, con datos explícitos",

        "Intermedio": "aplicación contextualizada y resolución de problemas de varios pasos con interpretación",

        "Avanzado": "análisis de situaciones no rutinarias, modelación, decisiones justificadas e interpretación crítica",

    }[difficulty]





def fallback_questions(n_questions, base_topics=None, assessment_context=None):

    assessment_context = assessment_context or {}

    pool = local_research_question_pool(assessment_context.get("subject", ""))

    if pool:

        quiz_type = str(assessment_context.get("quiz_type") or "adaptive").lower()

        if not is_research_quiz(quiz_type):

            requested_difficulty = normalize_adaptive_difficulty(

                assessment_context.get("difficulty") or "Intermedio"

            )

            allowed_bloom = adaptive_bloom_levels_for_difficulty(requested_difficulty)

            preferred = [

                dict(item)

                for item in pool

                if str(item.get("bloom_level") or "").strip() in allowed_bloom

            ]

            if preferred:

                remaining = [

                    dict(item)

                    for item in pool

                    if item not in preferred

                ]

                return preferred + remaining

        return pool

    base_topics = [x for x in (base_topics or []) if x] or [assessment_context.get("subject") or "Matemáticas"]

    fallback = []

    for i in range(int(n_questions)):

        t = base_topics[i % len(base_topics)] if base_topics else "Matemáticas"

        fallback.append({

            "question": f"Pregunta {i+1}: Resuelve o explica un concepto clave relacionado con {t}.",

            "options": [

                f"Aplicar correctamente el procedimiento de {t}",

                "Elegir una respuesta sin justificar",

                "Ignorar las condiciones del problema",

                "Usar una fórmula no relacionada",

            ],

            "correct_answer": f"Aplicar correctamente el procedimiento de {t}",

            "explanation": f"La respuesta correcta debe usar el procedimiento o concepto central de {t}.",

            "bloom_level": "Aplicar",

            "topic": t,

        })

    return fallback









def local_fast_adaptive_pool(subject):

    """Banco local validado para evaluaciones/prácticas rápidas sin esperar al generador externo."""

    key = strip_accents(subject or "").lower()

    if "calculo" not in key:

        return []

    combined = []

    seen = set()

    for source in (DERIVATIVE_APPLICATIONS_QUESTION_BANK, DIFFERENTIAL_CALCULUS_QUESTION_BANK):

        for raw in source:

            item = normalize_question_payload(raw)

            if not item:

                continue

            code = item.get("item_code") or stable_item_code(item.get("question", ""))

            if code in seen:

                continue

            seen.add(code)

            combined.append(item)

    return combined





def adaptive_questions_for_difficulty(questions, difficulty):

    difficulty = normalize_adaptive_difficulty(difficulty)

    allowed = adaptive_bloom_levels_for_difficulty(difficulty)

    aligned = []

    for raw in questions or []:

        item = normalize_question_payload(raw)

        if not item:

            continue

        bloom = str(item.get("bloom_level") or "").strip()

        if bloom in allowed:

            item["difficulty_level"] = difficulty

            aligned.append(item)

    return aligned





def adaptive_practice_next_difficulty(current_difficulty, correct, total):

    current = normalize_adaptive_difficulty(current_difficulty)

    ratio = float(correct or 0) / max(1, int(total or 0))

    idx = ADAPTIVE_DIFFICULTIES.index(current)

    if ratio >= 0.80 and idx < len(ADAPTIVE_DIFFICULTIES) - 1:

        return ADAPTIVE_DIFFICULTIES[idx + 1], "Dominaste este bloque; la siguiente práctica aumenta la exigencia cognitiva."

    if ratio < 0.50 and idx > 0:

        return ADAPTIVE_DIFFICULTIES[idx - 1], "Conviene consolidar fundamentos antes de volver a subir la dificultad."

    return current, "Mantendremos la dificultad para consolidar el desempeño antes de cambiar de nivel."






# BUNSEKI_R8_20_V3_DYNAMIC_AI_PRACTICE_BANK
AI_PRACTICE_PROMPT_VERSION = "r8.20-v4"
AI_PRACTICE_MODEL = "gemini-2.5-flash"
# BUNSEKI_R8_20_V4_AI_RESILIENCE
# BUNSEKI_R8_20_V5_1_SUBTOPIC_PROMPT_FOCUS
AI_PRACTICE_MAX_ATTEMPTS = 4


def _ai_practice_generate_content_resilient(
    client,
    prompt,
    purpose="generación",
):
    """Retry transient provider failures and surface a friendly error."""
    import time

    transient_tokens = (
        "503",
        "unavailable",
        "high demand",
        "temporarily",
        "timeout",
        "timed out",
        "429",
        "resource_exhausted",
        "rate limit",
        "rate_limit",
    )

    last_kind = "ProviderError"

    for attempt in range(1, AI_PRACTICE_MAX_ATTEMPTS + 1):
        try:
            return client.models.generate_content(
                model=AI_PRACTICE_MODEL,
                contents=prompt,
            )
        except Exception as exc:
            last_kind = type(exc).__name__
            message = str(exc).lower()
            transient = any(
                token in message
                for token in transient_tokens
            )

            if not transient:
                raise RuntimeError(
                    "No fue posible completar la "
                    f"{purpose} con IA. "
                    "Revise la configuración del proveedor "
                    f"({last_kind})."
                ) from exc

            if attempt < AI_PRACTICE_MAX_ATTEMPTS:
                delay_seconds = min(2 ** attempt, 8)
                time.sleep(delay_seconds)

    raise RuntimeError(
        "El servicio de IA está temporalmente saturado. "
        "BunsekiChat realizó varios reintentos sin éxito. "
        "Espere unos segundos y vuelva a intentar."
    )


# BUNSEKI_R8_21_2D_EXACT_SCHEMA_CACHE_ENSURE_AI_PRACTICE_BANK_SCHEMA
@st.cache_resource(show_spinner=False)
def ensure_ai_practice_bank_schema():
    execute(
        """
        CREATE TABLE IF NOT EXISTS ai_practice_question_bank (
            id BIGSERIAL PRIMARY KEY,
            topic TEXT NOT NULL,
            subtopic TEXT NOT NULL,
            difficulty_level TEXT NOT NULL,
            question TEXT NOT NULL,
            options_json TEXT NOT NULL,
            correct_answer TEXT NOT NULL,
            explanation TEXT,
            bloom_level TEXT,
            item_code TEXT NOT NULL UNIQUE,
            ai_model TEXT NOT NULL,
            prompt_version TEXT NOT NULL,
            verification_status TEXT NOT NULL DEFAULT 'needs_review',
            verifier_answer TEXT,
            verifier_reason TEXT,
            usage_count INTEGER NOT NULL DEFAULT 0,
            correct_count INTEGER NOT NULL DEFAULT 0,
            incorrect_count INTEGER NOT NULL DEFAULT 0,
            created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
            updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
            active BOOLEAN NOT NULL DEFAULT TRUE
        )
        """
    )
    execute(
        """
        CREATE INDEX IF NOT EXISTS idx_ai_practice_bank_scope
        ON ai_practice_question_bank(
            topic,
            subtopic,
            difficulty_level,
            verification_status,
            active
        )
        """
    )
    execute(
        """
        CREATE TABLE IF NOT EXISTS ai_practice_question_attempts (
            id BIGSERIAL PRIMARY KEY,
            bank_question_id BIGINT NOT NULL
                REFERENCES ai_practice_question_bank(id) ON DELETE CASCADE,
            user_id INTEGER NOT NULL
                REFERENCES users(id) ON DELETE CASCADE,
            attempt_key TEXT NOT NULL UNIQUE,
            topic TEXT NOT NULL,
            subtopic TEXT NOT NULL,
            difficulty_level TEXT NOT NULL,
            user_answer TEXT,
            is_correct BOOLEAN NOT NULL,
            created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
        )
        """
    )
    execute(
        """
        CREATE INDEX IF NOT EXISTS idx_ai_practice_attempt_user_scope
        ON ai_practice_question_attempts(
            user_id,
            topic,
            subtopic,
            difficulty_level,
            created_at
        )
        """
    )


def _strict_ai_practice_item(raw, topic, subtopic, difficulty):
    if not isinstance(raw, dict):
        return None

    question = normalize_spaces(raw.get("question", ""))
    options = [
        normalize_spaces(value)
        for value in (raw.get("options") or [])
        if normalize_spaces(value)
    ]
    options = list(dict.fromkeys(options))
    correct_answer = normalize_spaces(raw.get("correct_answer", ""))

    if not question or len(options) != 4 or correct_answer not in options:
        return None

    bloom = normalize_spaces(raw.get("bloom_level", "")) or "Aplicar"
    allowed = adaptive_bloom_levels_for_difficulty(difficulty)

    # BUNSEKI_R8_20_V6_BLOOM_SET_HOTFIX
    bloom_order = (
        "Recordar",
        "Comprender",
        "Aplicar",
        "Analizar",
        "Evaluar",
        "Crear",
    )
    allowed_values = [
        level
        for level in bloom_order
        if level in (allowed or set())
    ]

    if bloom not in allowed_values:
        bloom = allowed_values[0] if allowed_values else "Aplicar"

    return {
        "question": question,
        "options": options,
        "correct_answer": correct_answer,
        "explanation": normalize_spaces(raw.get("explanation", "")),
        "bloom_level": bloom,
        "difficulty_level": difficulty,
        "topic": topic,
        "subtopic": subtopic,
        "item_code": stable_item_code(
            f"{topic}|{subtopic}|{difficulty}|{question}"
        ),
    }


def _verify_ai_practice_items(client, items, topic, subtopic):
    verification_input = [
        {
            "index": index,
            "question": item["question"],
            "options": item["options"],
        }
        for index, item in enumerate(items)
    ]

    prompt = f"""
Actúa como verificador matemático independiente.

Tema o materia obligatorio: {topic}
Subtema obligatorio: {subtopic}

Para cada pregunta:
1. Resuélvela de forma independiente.
2. Elige EXACTAMENTE una opción proporcionada.
3. Comprueba que pertenece exclusivamente al subtema indicado.
4. valid=true solo si el enunciado es claro y tiene una respuesta inequívoca.
5. Devuelve SOLO JSON válido.

Formato:
[
  {{
    "index": 0,
    "valid": true,
    "correct_answer": "texto exacto de una opción",
    "reason": "justificación breve"
  }}
]

Preguntas:
{json.dumps(verification_input, ensure_ascii=False)}
"""

    response = _ai_practice_generate_content_resilient(
        client,
        prompt,
        purpose="verificación matemática",
    )
    parsed = _safe_json_loads(response.text, [])
    if not isinstance(parsed, list):
        return {}

    result = {}
    for row in parsed:
        if not isinstance(row, dict):
            continue
        try:
            index = int(row.get("index"))
        except Exception:
            continue
        result[index] = {
            "valid": bool(row.get("valid")),
            "correct_answer": normalize_spaces(
                row.get("correct_answer", "")
            ),
            "reason": normalize_spaces(row.get("reason", "")),
        }
    return result


# BUNSEKI_R8_21_BANK_SUPPLY_UNIQUE_ASSIGNMENT
AI_PRACTICE_SUPPLY_TARGET_DEFAULT = 40
AI_PRACTICE_NEAR_DUPLICATE_THRESHOLD = 0.86


def _ai_practice_normalize_question_text(value):
    import re
    import unicodedata
    text = normalize_spaces(value or "").lower()
    text = unicodedata.normalize("NFKD", text)
    text = "".join(c for c in text if not unicodedata.combining(c))
    text = re.sub(r"[^\\w\\s]", " ", text)
    return normalize_spaces(text)


def _ai_practice_question_tokens(value):
    return {
        token
        for token in _ai_practice_normalize_question_text(value).split()
        if len(token) > 2
    }


def _ai_practice_question_similarity(left, right):
    a = _ai_practice_question_tokens(left)
    b = _ai_practice_question_tokens(right)
    union = a | b
    return (len(a & b) / len(union)) if union else 0.0


# BUNSEKI_R8_21_2D_EXACT_SCHEMA_CACHE_ENSURE_AI_PRACTICE_SUPPLY_SCHEMA
@st.cache_resource(show_spinner=False)
def ensure_ai_practice_supply_schema():
    ensure_ai_practice_bank_schema()
    execute(
        """
        CREATE TABLE IF NOT EXISTS ai_practice_question_serves (
            id BIGSERIAL PRIMARY KEY,
            bank_question_id BIGINT NOT NULL
                REFERENCES ai_practice_question_bank(id) ON DELETE CASCADE,
            user_id INTEGER NOT NULL
                REFERENCES users(id) ON DELETE CASCADE,
            serve_batch_key TEXT NOT NULL,
            topic TEXT NOT NULL,
            subtopic TEXT NOT NULL,
            difficulty_level TEXT NOT NULL,
            served_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
            UNIQUE(serve_batch_key, bank_question_id)
        )
        """
    )
    execute(
        """
        CREATE INDEX IF NOT EXISTS idx_ai_practice_serves_user_scope
        ON ai_practice_question_serves(
            user_id, topic, subtopic, difficulty_level, served_at
        )
        """
    )
    execute(
        """
        CREATE INDEX IF NOT EXISTS idx_ai_practice_serves_question_recent
        ON ai_practice_question_serves(bank_question_id, served_at)
        """
    )


def ai_practice_verified_count(topic, subtopic, difficulty):
    ensure_ai_practice_supply_schema()
    row = fetchone(
        """
        SELECT COUNT(*) AS n
        FROM ai_practice_question_bank
        WHERE topic=%s
          AND subtopic=%s
          AND difficulty_level=%s
          AND verification_status='verified'
          AND active=TRUE
        """,
        (topic, subtopic, normalize_adaptive_difficulty(difficulty)),
    )
    return int((row or {}).get("n") or 0)


def get_ai_practice_bank_supply_coverage():
    ensure_ai_practice_supply_schema()
    return fetchall(
        """
        SELECT
            topic,
            subtopic,
            difficulty_level,
            COUNT(*) FILTER (
                WHERE verification_status='verified' AND active=TRUE
            ) AS verified,
            COUNT(*) FILTER (
                WHERE verification_status='needs_review' AND active=TRUE
            ) AS needs_review,
            COALESCE(SUM(usage_count),0) AS uses
        FROM ai_practice_question_bank
        GROUP BY topic, subtopic, difficulty_level
        ORDER BY topic, subtopic, difficulty_level
        """
    )


def ensure_ai_practice_bank_target(
    topic,
    subtopic,
    difficulty,
    target_verified=AI_PRACTICE_SUPPLY_TARGET_DEFAULT,
    max_batches=12,
):
    difficulty = normalize_adaptive_difficulty(difficulty)
    initial = ai_practice_verified_count(topic, subtopic, difficulty)
    current = initial
    batches = 0
    stagnant = 0
    error = ""
    while current < int(target_verified) and batches < int(max_batches):
        before = current
        try:
            generate_ai_practice_bank_batch(
                0,
                topic,
                subtopic,
                difficulty,
                batch_size=max(10, min(20, int(target_verified) - current + 4)),
            )
        except Exception as exc:
            error = str(exc)
            break
        batches += 1
        current = ai_practice_verified_count(topic, subtopic, difficulty)
        stagnant = stagnant + 1 if current <= before else 0
        if stagnant >= 2:
            break
    return {
        "initial_verified": initial,
        "verified": current,
        "target": int(target_verified),
        "batches": batches,
        "reached": current >= int(target_verified),
        "error": error,
    }


def _ai_practice_rows_to_items(rows, topic, subtopic, difficulty):
    output = []
    for row in rows:
        item = _strict_ai_practice_item(
            {
                "question": row.get("question"),
                "options": _safe_json_loads(row.get("options_json"), []),
                "correct_answer": row.get("correct_answer"),
                "explanation": row.get("explanation"),
                "bloom_level": row.get("bloom_level"),
            },
            topic,
            subtopic,
            difficulty,
        )
        if not item:
            continue
        item["bank_question_id"] = row.get("id")
        item["item_code"] = row.get("item_code") or item["item_code"]
        item["verification_status"] = row.get("verification_status")
        item["generation_source"] = "gemini_verified_bank"
        output.append(item)
    return output


# BUNSEKI_R8_21_1B_REALDICT_COUNT_HOTFIX
def _ai_practice_count_value(row):
    """Read COUNT(*) safely from tuple, RealDictRow, or mapping cursors."""
    if row is None:
        return 0

    if hasattr(row, "get"):
        for key in ("n", "count"):
            value = row.get(key)
            if value is not None:
                return int(value or 0)

        values = list(row.values())
        return int(values[0] or 0) if values else 0

    return int(row[0] or 0)


def reserve_ai_practice_bank_questions(
    uid,
    topic,
    subtopic,
    difficulty,
    n_questions=5,
    round_no=1,
):
    """No-repeat-first allocation; stable within a practice round."""
    ensure_ai_practice_supply_schema()
    difficulty = normalize_adaptive_difficulty(difficulty)
    n_questions = max(1, int(n_questions))
    round_no = max(1, int(round_no))
    batch_key = hashlib.sha256(
        f"r8.21|{int(uid)}|{topic}|{subtopic}|{difficulty}|{round_no}".encode("utf-8")
    ).hexdigest()

    connection = conn()
    try:
        with connection.cursor() as cur:
            cur.execute(
                "SELECT COUNT(*) FROM ai_practice_question_serves WHERE serve_batch_key=%s",
                (batch_key,),
            )
            current = int(_ai_practice_count_value(cur.fetchone()) or 0)
            missing = max(0, n_questions - current)

            if missing:
                cur.execute(
                    """
                    WITH candidate AS (
                        SELECT q.id
                        FROM ai_practice_question_bank q
                        WHERE q.topic=%s
                          AND q.subtopic=%s
                          AND q.difficulty_level=%s
                          AND q.verification_status='verified'
                          AND q.active=TRUE
                          AND NOT EXISTS (
                              SELECT 1
                              FROM ai_practice_question_serves mine
                              WHERE mine.user_id=%s
                                AND mine.bank_question_id=q.id
                          )
                          AND NOT EXISTS (
                              SELECT 1
                              FROM ai_practice_question_serves recent
                              WHERE recent.bank_question_id=q.id
                                AND recent.user_id<>%s
                                AND recent.served_at >= CURRENT_TIMESTAMP - INTERVAL '30 minutes'
                          )
                        ORDER BY
                            (
                                SELECT COUNT(*)
                                FROM ai_practice_question_serves recent24
                                WHERE recent24.bank_question_id=q.id
                                  AND recent24.served_at >= CURRENT_TIMESTAMP - INTERVAL '24 hours'
                            ) ASC,
                            q.usage_count ASC,
                            RANDOM()
                        FOR UPDATE SKIP LOCKED
                        LIMIT %s
                    )
                    INSERT INTO ai_practice_question_serves(
                        bank_question_id,user_id,serve_batch_key,
                        topic,subtopic,difficulty_level
                    )
                    SELECT id,%s,%s,%s,%s,%s FROM candidate
                    ON CONFLICT DO NOTHING
                    """,
                    (
                        topic, subtopic, difficulty,
                        int(uid), int(uid), int(missing),
                        int(uid), batch_key, topic, subtopic, difficulty,
                    ),
                )

            cur.execute(
                "SELECT COUNT(*) FROM ai_practice_question_serves WHERE serve_batch_key=%s",
                (batch_key,),
            )
            current = int(_ai_practice_count_value(cur.fetchone()) or 0)
            missing = max(0, n_questions - current)

            if missing:
                cur.execute(
                    """
                    WITH candidate AS (
                        SELECT q.id
                        FROM ai_practice_question_bank q
                        WHERE q.topic=%s
                          AND q.subtopic=%s
                          AND q.difficulty_level=%s
                          AND q.verification_status='verified'
                          AND q.active=TRUE
                          AND NOT EXISTS (
                              SELECT 1
                              FROM ai_practice_question_serves same_batch
                              WHERE same_batch.serve_batch_key=%s
                                AND same_batch.bank_question_id=q.id
                          )
                        ORDER BY
                            (
                                SELECT MAX(previous.served_at)
                                FROM ai_practice_question_serves previous
                                WHERE previous.bank_question_id=q.id
                                  AND previous.user_id=%s
                            ) ASC NULLS FIRST,
                            q.usage_count ASC,
                            RANDOM()
                        FOR UPDATE SKIP LOCKED
                        LIMIT %s
                    )
                    INSERT INTO ai_practice_question_serves(
                        bank_question_id,user_id,serve_batch_key,
                        topic,subtopic,difficulty_level
                    )
                    SELECT id,%s,%s,%s,%s,%s FROM candidate
                    ON CONFLICT DO NOTHING
                    """,
                    (
                        topic, subtopic, difficulty, batch_key,
                        int(uid), int(missing),
                        int(uid), batch_key, topic, subtopic, difficulty,
                    ),
                )
        connection.commit()
    except Exception:
        connection.rollback()
        raise
    finally:
        connection.close()

    rows = fetchall(
        """
        SELECT q.*
        FROM ai_practice_question_serves s
        JOIN ai_practice_question_bank q ON q.id=s.bank_question_id
        WHERE s.serve_batch_key=%s
        ORDER BY s.id
        LIMIT %s
        """,
        (batch_key, n_questions),
    )
    return _ai_practice_rows_to_items(rows, topic, subtopic, difficulty)


def render_admin_ai_practice_bank_manager(user):
    ensure_ai_practice_supply_schema()
    with st.expander("ðŸ§  Abastecimiento del banco IA de prÃ¡ctica", expanded=False):
        st.caption(
            "Precarga preguntas verificadas para que el estudiante dependa menos de la IA en vivo. "
            "La asignaciÃ³n prioriza preguntas no vistas y minimiza coincidencias recientes."
        )
        topic = st.selectbox(
            "Tema / materia",
            list(TOPICS),
            key="admin_ai_supply_topic",
        )
        subtopics = list(TOPICS.get(topic, [])) or [topic]
        subtopic = st.selectbox(
            "Subtema",
            subtopics,
            key="admin_ai_supply_subtopic",
        )
        difficulty = st.selectbox(
            "Nivel",
            ADAPTIVE_DIFFICULTIES,
            format_func=lambda value: PRACTICE_LEVEL_LABELS.get(value, value),
            key="admin_ai_supply_difficulty",
        )
        target = st.selectbox(
            "Meta de preguntas verificadas",
            [20, 40, 60, 80],
            index=1,
            key="admin_ai_supply_target",
        )
        current = ai_practice_verified_count(topic, subtopic, difficulty)
        c1, c2 = st.columns(2)
        c1.metric("Verificadas actuales", current)
        c2.metric("Meta", int(target))

        if st.button(
            "Generar y verificar hasta alcanzar la meta",
            key="admin_ai_supply_generate",
            use_container_width=True,
        ):
            with st.spinner("BunsekiChat estÃ¡ abasteciendo y verificando el banco..."):
                result = ensure_ai_practice_bank_target(
                    topic, subtopic, difficulty, int(target)
                )
            if result["reached"]:
                st.success(f"Banco abastecido: {result['verified']} preguntas verificadas.")
            else:
                st.warning(
                    f"Abastecimiento parcial: {result['verified']} de {result['target']} verificadas."
                )
            st.rerun()

        coverage = get_ai_practice_bank_supply_coverage()
        if coverage:
            st.markdown("**Cobertura actual del banco IA**")
            df = pd.DataFrame(coverage).rename(
                columns={
                    "topic": "Tema",
                    "subtopic": "Subtema",
                    "difficulty_level": "Nivel interno",
                    "verified": "Verificadas",
                    "needs_review": "Revisar",
                    "uses": "Usos",
                }
            )
            st.dataframe(df, use_container_width=True, hide_index=True)


def _store_ai_practice_items(items, verifications):
    ensure_ai_practice_supply_schema()
    verified = 0

    for index, item in enumerate(items):
        verification = verifications.get(index, {})
        verifier_answer = normalize_spaces(verification.get("correct_answer", ""))
        verifier_reason = normalize_spaces(verification.get("reason", ""))

        status = "needs_review"
        if (
            verification.get("valid")
            and verifier_answer
            and verifier_answer == item["correct_answer"]
            and verifier_answer in item["options"]
        ):
            status = "verified"

        existing = fetchall(
            """
            SELECT question
            FROM ai_practice_question_bank
            WHERE topic=%s
              AND subtopic=%s
              AND difficulty_level=%s
              AND active=TRUE
            """,
            (item["topic"], item["subtopic"], item["difficulty_level"]),
        )
        if any(
            _ai_practice_question_similarity(item["question"], row.get("question"))
            >= AI_PRACTICE_NEAR_DUPLICATE_THRESHOLD
            for row in existing
        ):
            continue

        inserted = execute(
            """
            INSERT INTO ai_practice_question_bank(
                topic,subtopic,difficulty_level,question,options_json,
                correct_answer,explanation,bloom_level,item_code,ai_model,
                prompt_version,verification_status,verifier_answer,
                verifier_reason,updated_at
            )
            VALUES(%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,CURRENT_TIMESTAMP)
            ON CONFLICT(item_code) DO NOTHING
            RETURNING id
            """,
            (
                item["topic"], item["subtopic"], item["difficulty_level"],
                item["question"], json.dumps(item["options"], ensure_ascii=False),
                item["correct_answer"], item.get("explanation", ""),
                item.get("bloom_level", "Aplicar"), item["item_code"],
                AI_PRACTICE_MODEL, AI_PRACTICE_PROMPT_VERSION, status,
                verifier_answer, verifier_reason,
            ),
            returning=True,
        )
        if inserted and status == "verified":
            verified += 1

    return verified




def generate_ai_practice_bank_batch(
    uid,
    topic,
    subtopic,
    difficulty,
    batch_size=10,
):
    difficulty = normalize_adaptive_difficulty(difficulty)
    client = ai_client()
    if not client:
        raise RuntimeError(
            "La API de IA no está disponible y no existen suficientes "
            "preguntas verificadas para este subtema."
        )

    difficulty_instruction = adaptive_difficulty_instruction(difficulty)

    prompt = f"""
Eres un experto en evaluación matemática universitaria y diseño de ítems.

Genera exactamente {int(batch_size)} preguntas NUEVAS de opción múltiple.

TEMA O MATERIA:
{topic}

SUBTEMA OBLIGATORIO:
{subtopic}

DIFICULTAD:
{difficulty}

CRITERIO COGNITIVO:
{difficulty_instruction}

REGLAS ESTRICTAS:
- Evalúa EXCLUSIVAMENTE el subtema "{subtopic}".
- No mezcles otros temas o subtemas.
- Cada pregunta debe tener exactamente 4 opciones distintas.
- Debe existir una sola respuesta inequívocamente correcta.
- correct_answer debe copiar exactamente una de las opciones.
- Incluye una explicación matemática breve y verificable.
- Ajusta Bloom y razonamiento a la dificultad.
- Evita repetir enunciados dentro del lote.
- Devuelve SOLO JSON válido, sin markdown.

Formato:
[
  {{
    "question": "enunciado",
    "options": ["A", "B", "C", "D"],
    "correct_answer": "A",
    "explanation": "procedimiento o verificación breve",
    "bloom_level": "Aplicar"
  }}
]
"""

    response = _ai_practice_generate_content_resilient(
        client,
        prompt,
        purpose="generación de preguntas",
    )
    parsed = _safe_json_loads(response.text, [])
    if not isinstance(parsed, list) or not parsed:
        raise RuntimeError(
            "La IA no devolvió un lote JSON válido de preguntas."
        )

    clean = []
    seen = set()
    for raw in parsed[: int(batch_size)]:
        item = _strict_ai_practice_item(
            raw,
            topic,
            subtopic,
            difficulty,
        )
        if not item or item["item_code"] in seen:
            continue
        seen.add(item["item_code"])
        clean.append(item)

    if not clean:
        raise RuntimeError(
            "Las preguntas generadas no superaron la validación estructural."
        )

    try:
        verifications = _verify_ai_practice_items(
            client,
            clean,
            topic,
            subtopic,
        )
    except Exception as exc:
        verifications = {
            index: {
                "valid": False,
                "correct_answer": "",
                "reason": (
                    "Verificación IA no disponible: "
                    f"{type(exc).__name__}"
                ),
            }
            for index in range(len(clean))
        }

    verified = _store_ai_practice_items(clean, verifications)
    return {
        "generated": len(clean),
        "verified": verified,
    }


def get_ai_practice_bank_questions(
    uid,
    topic,
    subtopic,
    difficulty,
    limit=40,
):
    ensure_ai_practice_bank_schema()
    difficulty = normalize_adaptive_difficulty(difficulty)

    rows = fetchall(
        """
        SELECT
            q.*,
            COALESCE(seen.seen_count, 0) AS user_seen_count
        FROM ai_practice_question_bank q
        LEFT JOIN (
            SELECT bank_question_id, COUNT(*) AS seen_count
            FROM ai_practice_question_attempts
            WHERE user_id=%s
            GROUP BY bank_question_id
        ) seen
          ON seen.bank_question_id=q.id
        WHERE q.topic=%s
          AND q.subtopic=%s
          AND q.difficulty_level=%s
          AND q.verification_status='verified'
          AND q.active=TRUE
        ORDER BY
            COALESCE(seen.seen_count, 0) ASC,
            q.usage_count ASC,
            q.created_at DESC
        LIMIT %s
        """,
        (
            int(uid),
            topic,
            subtopic,
            difficulty,
            int(limit),
        ),
    )

    output = []
    for row in rows:
        options = _safe_json_loads(row.get("options_json"), [])
        item = _strict_ai_practice_item(
            {
                "question": row.get("question"),
                "options": options,
                "correct_answer": row.get("correct_answer"),
                "explanation": row.get("explanation"),
                "bloom_level": row.get("bloom_level"),
            },
            topic,
            subtopic,
            difficulty,
        )
        if not item:
            continue
        item["bank_question_id"] = row.get("id")
        item["item_code"] = row.get("item_code") or item["item_code"]
        item["user_seen_count"] = int(row.get("user_seen_count") or 0)
        item["verification_status"] = row.get("verification_status")
        item["generation_source"] = "gemini_verified_bank"
        output.append(item)

    return output


def record_ai_practice_bank_result(
    uid,
    item,
    user_answer,
    is_correct,
    topic,
    subtopic,
    difficulty,
    attempt_key,
):
    bank_question_id = item.get("bank_question_id")
    if not bank_question_id:
        return False

    ensure_ai_practice_bank_schema()

    inserted = execute(
        """
        INSERT INTO ai_practice_question_attempts(
            bank_question_id, user_id, attempt_key,
            topic, subtopic, difficulty_level,
            user_answer, is_correct
        )
        VALUES(%s,%s,%s,%s,%s,%s,%s,%s)
        ON CONFLICT(attempt_key) DO NOTHING
        RETURNING id
        """,
        (
            int(bank_question_id),
            int(uid),
            str(attempt_key),
            topic,
            subtopic,
            normalize_adaptive_difficulty(difficulty),
            normalize_spaces(user_answer),
            bool(is_correct),
        ),
        returning=True,
    )

    if not inserted:
        return False

    execute(
        """
        UPDATE ai_practice_question_bank
        SET
            usage_count=usage_count+1,
            correct_count=correct_count+%s,
            incorrect_count=incorrect_count+%s,
            updated_at=CURRENT_TIMESTAMP
        WHERE id=%s
        """,
        (
            1 if is_correct else 0,
            0 if is_correct else 1,
            int(bank_question_id),
        ),
    )
    return True


def adaptive_practice_questions(
    uid,
    current_topic,
    difficulty,
    n_questions=5,
    round_no=1,
    current_subtopic=None,
):
    """Bank-first practice: IA is used only when verified stock is insufficient."""
    difficulty = normalize_adaptive_difficulty(difficulty)

    if current_subtopic:
        reserved = reserve_ai_practice_bank_questions(
            uid,
            current_topic,
            current_subtopic,
            difficulty,
            n_questions=n_questions,
            round_no=round_no,
        )
        if len(reserved) >= int(n_questions):
            return reserved[: int(n_questions)]

        generation_rounds = 0
        while len(reserved) < int(n_questions) and generation_rounds < 2:
            generate_ai_practice_bank_batch(
                uid,
                current_topic,
                current_subtopic,
                difficulty,
                batch_size=max(10, int(n_questions) * 2),
            )
            generation_rounds += 1
            reserved = reserve_ai_practice_bank_questions(
                uid,
                current_topic,
                current_subtopic,
                difficulty,
                n_questions=n_questions,
                round_no=round_no,
            )

        if len(reserved) < int(n_questions):
            count = ai_practice_verified_count(
                current_topic, current_subtopic, difficulty
            )
            raise RuntimeError(
                "El banco verificado todavÃ­a no tiene suficientes preguntas para "
                f"{current_topic} â†’ {current_subtopic} â†’ {difficulty}. "
                f"Disponibles: {count}. El administrador puede abastecer previamente este banco."
            )
        return reserved[: int(n_questions)]

    pool = local_fast_adaptive_pool(current_topic)
    candidates = adaptive_questions_for_difficulty(pool, difficulty)
    if len(candidates) < int(n_questions):
        for q_topic, qdata in adaptive_quiz_for_user(uid, current_topic):
            question, options, correct_idx = qdata
            if not options:
                continue
            bloom = {
                "Inicial": "Comprender",
                "BÃ¡sico": "Comprender",
                "Intermedio": "Aplicar",
                "Avanzado": "Analizar",
            }[difficulty]
            candidates.append(
                {
                    "question": question,
                    "options": list(options),
                    "correct_answer": options[correct_idx],
                    "explanation": f"Repasa el concepto central relacionado con {q_topic} y justifica la respuesta.",
                    "bloom_level": bloom,
                    "difficulty_level": difficulty,
                    "topic": q_topic,
                    "item_code": stable_item_code(question),
                }
            )
    unique = []
    seen = set()
    for item in candidates:
        code = item.get("item_code") or stable_item_code(item.get("question", ""))
        if code in seen:
            continue
        seen.add(code)
        unique.append(dict(item))
    rng = random.Random(
        hashlib.sha256(
            f"practice|{uid}|{current_topic}|{difficulty}|{int(round_no)}".encode("utf-8")
        ).hexdigest()
    )
    rng.shuffle(unique)
    return unique[: int(n_questions)]









def adaptive_learning_prompts(weak_topics, next_difficulty, fallback_topic):

    topics = [normalize_spaces(x) for x in (weak_topics or []) if normalize_spaces(x)]

    focus = topics[0] if topics else (normalize_spaces(fallback_topic) or "el tema evaluado")

    level = normalize_adaptive_difficulty(next_difficulty)

    return [

        f"Explícame {focus} con un ejemplo universitario de nivel {level}. Después hazme una pregunta guiada sin darme la respuesta.",

        f"Dame 3 ejercicios progresivos sobre {focus}, desde el nivel que domino hasta {level}, y espera mi respuesta antes de corregirme.",

        f"Voy a resolver un problema de {focus}. Evalúa mi procedimiento paso a paso, identifica mi error conceptual y dime qué debo dominar para avanzar a {level}.",

    ]





def adaptive_complexity_evidence(questions, difficulty):

    difficulty = normalize_adaptive_difficulty(difficulty)

    allowed = adaptive_bloom_levels_for_difficulty(difficulty)

    distribution = {}

    aligned = 0

    for q in questions or []:

        bloom = str(q.get("bloom_level") or "Sin nivel").strip() or "Sin nivel"

        distribution[bloom] = distribution.get(bloom, 0) + 1

        if bloom in allowed:

            aligned += 1

    summary = ", ".join(f"{k}: {v}" for k, v in sorted(distribution.items(), key=lambda kv: bloom_rank(kv[0]))) or "Sin datos"

    return {

        "aligned": aligned,

        "total": len(questions or []),

        "distribution": summary,

        "instruction": adaptive_difficulty_instruction(difficulty),

    }



def make_assessment_seed(uid, quiz_type, academic_context, salt=None):

    context = academic_context or {}

    raw = "|".join([

        str(uid),

        str(quiz_type or "adaptive"),

        str(context.get("subject", "")),

        str(context.get("course_level", "")),

        str(context.get("parallel", "")),

        str(context.get("shift", "")),

        str(salt or time.time_ns()),

    ])

    return hashlib.sha256(raw.encode("utf-8")).hexdigest()





def normalize_question_payload(question):

    if not isinstance(question, dict):

        return None

    opts = [normalize_spaces(o) for o in question.get("options", []) if normalize_spaces(o)]

    correct = normalize_spaces(question.get("correct_answer", ""))

    if correct and correct not in opts:

        opts.append(correct)

    opts = list(dict.fromkeys(opts))

    if len(opts) > 4:

        opts = opts[:4] if correct in opts[:4] else opts[:3] + [correct]

    if len(opts) < 2 or not correct:

        return None

    question_text = normalize_spaces(question.get("question", ""))

    if not question_text:

        return None

    payload = {

        "question": question_text,

        "options": opts,

        "correct_answer": correct,

        "explanation": normalize_spaces(question.get("explanation", "")),

        "bloom_level": normalize_spaces(question.get("bloom_level", "Aplicar")) or "Aplicar",

        "topic": normalize_spaces(question.get("topic", "")),

    }

    for key in ["subtopic", "dimension", "indicator", "competence", "difficulty_level"]:

        if question.get(key):

            payload[key] = normalize_spaces(question.get(key))

    payload["item_code"] = normalize_spaces(question.get("item_code")) or stable_item_code(question_text)

    return payload





def standardized_research_form(questions, quiz_type, version_code, n_questions, academic_context):

    clean, seen = [], set()

    for question in questions:

        item = normalize_question_payload(question)

        if not item or item["item_code"] in seen:

            continue

        seen.add(item["item_code"])

        clean.append(item)

    form_raw = "|".join([

        str((academic_context or {}).get("subject", "")), RESEARCH_FOCUS_TOPIC,

        str(quiz_type), str(version_code), str(n_questions),

    ])

    form_rng = random.Random(hashlib.sha256(form_raw.encode("utf-8")).hexdigest())

    form_rng.shuffle(clean)

    selected = clean[:int(n_questions)]

    return enrich_research_questions(selected, n_questions)





def randomize_questions_for_student(questions, uid, quiz_type, n_questions, seed):

    rng = random.Random(seed)

    clean = []

    seen = set()

    for q in questions:

        item = normalize_question_payload(q)

        if not item:

            continue

        key = strip_accents(item["question"]).lower()

        if key in seen:

            continue

        seen.add(key)

        opts = item["options"][:]

        rng.shuffle(opts)

        item["options"] = opts

        clean.append(item)

    rng.shuffle(clean)

    return clean[:int(n_questions)]





def generate_questions_with_ai(uid: int, plan_id=None, difficulty="Intermedio", n_questions=10, retry_focus=None, assessment_context=None):

    assessment_context = assessment_context or {}

    difficulty = normalize_adaptive_difficulty(difficulty)

    difficulty_instruction = adaptive_difficulty_instruction(difficulty)

    quiz_type = assessment_context.get("quiz_type", "adaptive")

    profile = get_student_consultation_profile(uid)

    plan_topics = get_plan_topics(plan_id) if plan_id else get_plan_topics()

    plan_context = "\n".join([

        f"- {x.get('unit_name','')} | {x.get('topic','')} | {x.get('subtopic','')} | Resultado: {x.get('learning_outcome','')} | Bloom: {x.get('bloom_level','')}"

        for x in plan_topics[:60]

    ])

    consulted = ", ".join(profile["ranked_topics"][:5] + profile["ranked_subtopics"][:5]) or "sin historial suficiente"

    recent = "\n".join([f"- {m}" for m in profile["recent_messages"]])

    base_topics = profile["ranked_topics"] or [x.get("topic") for x in plan_topics if x.get("topic")] or list(TOPICS.keys())

    fallback = fallback_questions(n_questions, base_topics, assessment_context)



    if is_research_quiz(quiz_type) and local_research_question_pool(assessment_context.get("subject", "")):

        return fallback



    # Para la evaluación adaptativa normal, prioriza banco local validado si hay

    # suficientes ítems alineados. Esto reduce drásticamente el tiempo de espera.

    if not is_research_quiz(quiz_type):

        fast_pool = local_fast_adaptive_pool(assessment_context.get("subject", ""))

        fast_aligned = adaptive_questions_for_difficulty(fast_pool, difficulty)

        if len(fast_aligned) >= int(n_questions):

            return fast_aligned



    client = ai_client()

    if not client:

        return fallback



    research_rules = ""

    if is_research_quiz(quiz_type):

        research_rules = f"""

MODO INVESTIGACIÓN:

- Tipo de instrumento: {quiz_type_label(quiz_type)}.

- Título del estudio: {RESEARCH_TITLE}.

- Materia: {assessment_context.get('subject') or 'Cálculo diferencial'}.

- Tema específico obligatorio: {RESEARCH_FOCUS_TOPIC}.

- Curso: {assessment_context.get('course_level') or ''}.

- Paralelo: {assessment_context.get('parallel') or ''}.

- Jornada: {assessment_context.get('shift') or ''}.

- Genera exactamente {int(n_questions)} preguntas equivalentes en dificultad para comparar pretest y posttest.

- Todas las preguntas deben evaluar aplicaciones de la derivada: tasas de cambio, recta tangente, optimización, crecimiento/decrecimiento, concavidad, marginalidad y razones relacionadas.

- Evita preguntas puramente mecánicas de derivación sin contexto aplicado, salvo que sean necesarias dentro de un problema aplicado.

- Si es pretest, evalúa saberes previos sin depender del historial de consultas.

- Si es posttest, evalúa las mismas competencias con enunciados no idénticos al pretest.

"""



    prompt = f"""

Eres un experto en evaluación educativa adaptativa, matemática universitaria y taxonomía de Bloom.

Genera {int(n_questions)} preguntas tipo test para BunsekiChat. Deben evaluar lo que el estudiante ha consultado y alinearse al plan analítico.



CONDICIONES:

- Devuelve SOLO JSON válido, sin markdown.

- Cada pregunta debe tener 4 opciones.

- Debe existir correct_answer igual a una de las opciones.

- Nivel de dificultad obligatorio: {difficulty}.

- Criterio cognitivo para este nivel: {difficulty_instruction}.

- Ajusta complejidad, cantidad de pasos y nivel Bloom al nivel solicitado.

- Si el estudiante no obtiene 70%, estas preguntas se usarán para reforzar y generar nuevos intentos.

- Incluye explicación pedagógica breve.

- Usa lenguaje claro en español.

{research_rules}



JSON:

[

  {{

    "question": "texto de la pregunta",

    "options": ["A", "B", "C", "D"],

    "correct_answer": "A",

    "explanation": "explicación",

    "bloom_level": "Aplicar",

    "difficulty_level": "{difficulty}",

    "topic": "tema evaluado"

  }}

]



PLAN ANALÍTICO:

{plan_context}



TEMAS CONSULTADOS POR EL ESTUDIANTE:

{consulted}



CONSULTAS RECIENTES:

{recent}



FOCO DE REINTENTO SI APLICA:

{retry_focus or "Ninguno"}

"""

    try:

        resp = client.models.generate_content(model="gemini-2.5-flash", contents=prompt)

        parsed = _safe_json_loads(resp.text, [])

        if isinstance(parsed, list) and parsed:

            clean = []

            for q in parsed[:int(n_questions)]:

                opts = q.get("options") or []

                if len(opts) >= 2 and q.get("correct_answer") in opts:

                    bloom_level = str(q.get("bloom_level", "Aplicar")).strip() or "Aplicar"

                    if (

                        not is_research_quiz(quiz_type)

                        and bloom_level not in adaptive_bloom_levels_for_difficulty(difficulty)

                    ):

                        continue

                    clean.append({

                        "question": str(q.get("question","")).strip(),

                        "options": [str(o) for o in opts[:4]],

                        "correct_answer": str(q.get("correct_answer","")),

                        "explanation": str(q.get("explanation","")),

                        "bloom_level": bloom_level,

                        "difficulty_level": difficulty,

                        "topic": str(q.get("topic","")),

                    })

            if clean:

                return clean

    except Exception:

        pass

    return fallback





def create_adaptive_quiz(uid, plan_id=None, difficulty="Intermedio", n_questions=10, parent_quiz_id=None, retry_focus=None, quiz_type="adaptive", academic_context=None):

    academic_context = academic_context or {}

    quiz_type = str(quiz_type or "adaptive").lower()

    difficulty = normalize_adaptive_difficulty(difficulty)

    if is_research_quiz(quiz_type):

        n_questions = RESEARCH_QUESTION_COUNT

    prior_attempts = 1

    if parent_quiz_id:

        parent = fetchone("SELECT attempt_no FROM adaptive_quizzes WHERE id=%s", (parent_quiz_id,))

        prior_attempts = int(parent.get("attempt_no", 1)) + 1 if parent else 2



    assessment_context = {

        **academic_context,

        "quiz_type": quiz_type,

        "difficulty": difficulty,

    }

    questions = generate_questions_with_ai(

        uid,

        plan_id,

        difficulty,

        n_questions,

        retry_focus,

        assessment_context,

    )

    if len(questions) < int(n_questions):

        questions.extend(

            fallback_questions(

                n_questions,

                assessment_context=assessment_context,

            )

        )

    if not is_research_quiz(quiz_type):

        aligned_questions = adaptive_questions_for_difficulty(questions, difficulty)

        if len(aligned_questions) < int(n_questions):

            local_candidates = adaptive_questions_for_difficulty(

                local_fast_adaptive_pool(academic_context.get("subject", "")),

                difficulty,

            )

            existing_codes = {

                q.get("item_code") or stable_item_code(q.get("question", ""))

                for q in aligned_questions

            }

            for candidate in local_candidates:

                code = candidate.get("item_code") or stable_item_code(candidate.get("question", ""))

                if code not in existing_codes:

                    aligned_questions.append(candidate)

                    existing_codes.add(code)

                if len(aligned_questions) >= int(n_questions):

                    break

        if len(aligned_questions) >= int(n_questions):

            questions = aligned_questions

        for question in questions:

            if isinstance(question, dict):

                question["difficulty_level"] = difficulty

    random_seed = make_assessment_seed(uid, quiz_type, academic_context)

    if is_research_quiz(quiz_type):

        version_code = research_form_version(uid, academic_context)

        questions = standardized_research_form(questions, quiz_type, version_code, n_questions, academic_context)

        questions = randomize_questions_for_student(questions, uid, quiz_type, n_questions, random_seed)

    else:

        version_code = ""

        questions = randomize_questions_for_student(questions, uid, quiz_type, n_questions, random_seed)

    title = "Evaluación adaptativa"

    if is_research_quiz(quiz_type):

        title = f"{quiz_type_label(quiz_type)} - {academic_context.get('subject') or 'Materia'}"

    source_topic = RESEARCH_FOCUS_TOPIC if is_research_quiz(quiz_type) else ", ".join(sorted(set([q.get("topic","") for q in questions if q.get("topic")]))[:3])

    row = execute("""

        INSERT INTO adaptive_quizzes(

            user_id, plan_id, title, quiz_type, source_topic, difficulty,

            subject, course_level, parallel, shift, cohort, research_title,

            question_count, random_seed, version_code, attempt_no, parent_quiz_id, status, created_at

        )

        VALUES(%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s) RETURNING id

    """, (

        uid, plan_id, title, quiz_type, source_topic, difficulty,

        academic_context.get("subject"), academic_context.get("course_level"),

        academic_context.get("parallel"), academic_context.get("shift"),

        academic_context.get("cohort") or academic_context.get("course"),

        RESEARCH_TITLE if is_research_quiz(quiz_type) else "",

        int(n_questions), random_seed, version_code, prior_attempts, parent_quiz_id, "generated", now()

    ), returning=True)

    quiz_id = row["id"]

    for position, q in enumerate(questions, start=1):

        execute("""

            INSERT INTO adaptive_questions(

                quiz_id, question, options_json, correct_answer, item_code, position, topic, subtopic,

                dimension, indicator, competence, difficulty_level, explanation, bloom_level

            )

            VALUES(%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)

        """, (

            quiz_id, q["question"], json.dumps(q["options"], ensure_ascii=False),

            q["correct_answer"], q.get("item_code") or stable_item_code(q["question"]),

            position, q.get("topic",""), q.get("subtopic",""),

            q.get("dimension",""), q.get("indicator",""), q.get("competence",""),

            q.get("difficulty_level",""), q.get("explanation",""), q.get("bloom_level","Aplicar")

        ))

    if not is_research_quiz(quiz_type):

        # Defensa adicional: la cabecera y todas las preguntas deben conservar

        # exactamente la dificultad solicitada por el estudiante.

        execute(

            "UPDATE adaptive_quizzes SET difficulty=%s WHERE id=%s",

            (difficulty, quiz_id),

        )

        execute(

            "UPDATE adaptive_questions SET difficulty_level=%s WHERE quiz_id=%s",

            (difficulty, quiz_id),

        )

    if is_research_quiz(quiz_type):

        set_research_assessment_state(uid, quiz_type, "iniciado")

    return quiz_id





def load_adaptive_quiz(quiz_id):

    quiz = fetchone("SELECT * FROM adaptive_quizzes WHERE id=%s", (quiz_id,))

    questions = fetchall("SELECT * FROM adaptive_questions WHERE quiz_id=%s ORDER BY COALESCE(position, id), id", (quiz_id,))

    for q in questions:

        q["options"] = _safe_json_loads(q.get("options_json"), [])

    return quiz, questions





def get_latest_adaptive_quiz(uid, quiz_type=None):

    if quiz_type:

        return fetchone("""

            SELECT * FROM adaptive_quizzes

            WHERE user_id=%s

              AND COALESCE(quiz_type,'adaptive')=%s

              AND LOWER(COALESCE(status,'')) NOT IN ('superseded','invalidado_metodologia','invalidated_methodology')

            ORDER BY id DESC LIMIT 1

        """, (uid, quiz_type))

    return fetchone("""

        SELECT * FROM adaptive_quizzes

        WHERE user_id=%s

          AND LOWER(COALESCE(status,'')) NOT IN ('superseded','invalidado_metodologia','invalidated_methodology')

        ORDER BY id DESC LIMIT 1

    """, (uid,))





def quiz_matches_research_focus(quiz):

    if not quiz or not is_research_quiz(quiz.get("quiz_type")):

        return True

    source = strip_accents(quiz.get("source_topic") or "").lower()

    focus = strip_accents(RESEARCH_FOCUS_TOPIC).lower()

    if "aplicaciones" in focus and "derivada" in focus:

        return "aplicaciones" in source and "derivada" in source

    return focus in source





def supersede_adaptive_quiz(quiz_id):

    with DB_LOCK:

        execute("""

            UPDATE adaptive_quizzes

            SET status='superseded', recommendation=%s

            WHERE id=%s AND status<>'completed'

        """, ("Instrumento archivado al regenerarse por actualización del tema; se conserva para auditoría.", quiz_id))





def grade_adaptive_quiz(quiz_id, answers: dict, elapsed_seconds=None):

    quiz, questions = load_adaptive_quiz(quiz_id)

    total = max(1, len(questions))

    correct = 0

    weak_topics = []

    per_question_time = round(float(elapsed_seconds or 0) / total, 2) if elapsed_seconds else None

    graded_questions = []

    for q in questions:

        ans = answers.get(str(q["id"]), "")

        ok = str(ans).strip() == str(q.get("correct_answer","")).strip()

        if ok:

            correct += 1

        else:

            weak_topics.append(q.get("question","")[:120])

        conceptual_error = conceptual_error_for_question(q, ok)

        estimated_confidence = 0.85 if ok else 0.35

        execute("""

            UPDATE adaptive_questions

            SET user_answer=%s, is_correct=%s, attempts=%s, response_time_seconds=%s,

                conceptual_error=%s, estimated_confidence=%s

            WHERE id=%s

        """, (ans, ok, 1, per_question_time, conceptual_error, estimated_confidence, q["id"]))

        q["user_answer"] = ans

        q["is_correct"] = ok

        q["conceptual_error"] = conceptual_error

        q["estimated_confidence"] = estimated_confidence

        q["response_time_seconds"] = per_question_time

        graded_questions.append(q)

    score = round(correct / total * 100, 2)

    passed = score >= 70

    diagnosis = build_quiz_diagnosis(score, graded_questions)

    recommendation = "Aprobado. Puedes avanzar al siguiente reto." if passed else "Refuerzo automático sugerido: revisa las preguntas falladas y estudia las microclases sugeridas."

    execute("""

        UPDATE adaptive_quizzes

        SET score=%s, passed=%s, status=%s, recommendation=%s, completed_at=%s,

            dimension_scores_json=%s, diagnosis_json=%s, cognitive_profile=%s,

            learning_plan_json=%s, total_time_seconds=%s

        WHERE id=%s

    """, (

        score, passed, "completed", recommendation, now(),

        json.dumps(diagnosis.get("dimension_scores", {}), ensure_ascii=False),

        json.dumps(diagnosis, ensure_ascii=False),

        diagnosis.get("profile", ""),

        json.dumps(diagnosis.get("learning_plan", {}), ensure_ascii=False),

        float(elapsed_seconds or 0) if elapsed_seconds else None,

        quiz_id,

    ))

    if quiz and is_research_quiz(quiz.get("quiz_type")):

        set_research_assessment_state(quiz.get("user_id"), quiz.get("quiz_type"), "completado")

    return score, passed, weak_topics, recommendation





def fallback_derivative_applications_lesson(weak_questions=None):

    weak_questions = weak_questions or []

    weak_block = ""

    if weak_questions:

        weak_block = "\n\n**Puntos a reforzar según el pretest**\n" + "\n".join([f"- {q}" for q in weak_questions[:5]])

    return f"""

**Clase de refuerzo: {RESEARCH_FOCUS_TOPIC}**



**1. Idea central**

La derivada permite medir cómo cambia una cantidad en un instante. En aplicaciones, no basta con derivar: hay que interpretar qué significa la derivada dentro del problema.



**2. Tasas de cambio**

Si una función representa posición, temperatura, costo, ingreso o área, su derivada representa la rapidez con la que cambia esa cantidad.

Ejemplo: si `s(t)` es posición, entonces `s'(t)` es velocidad instantánea.



**3. Recta tangente**

La pendiente de la tangente en `x=a` es `f'(a)`. La ecuación se escribe:

`y - f(a) = f'(a)(x-a)`.



**4. Optimización**

Para maximizar o minimizar:

1. Define la función objetivo.

2. Usa la restricción para dejar una sola variable.

3. Deriva.

4. Resuelve `f'(x)=0`.

5. Verifica si es máximo o mínimo.



**5. Crecimiento, decrecimiento y concavidad**

Si `f'(x)>0`, la función crece. Si `f'(x)<0`, decrece.

Si `f''(x)>0`, es cóncava hacia arriba. Si `f''(x)<0`, es cóncava hacia abajo.



**6. Razones relacionadas**

Cuando varias cantidades cambian con el tiempo, se deriva implícitamente respecto de `t`.

Ejemplo: si `A=πr²`, entonces `dA/dt = 2πr dr/dt`.



**7. Antes del posttest**

Lee el enunciado, identifica variables, escribe la función, deriva e interpreta la respuesta con unidades.

{weak_block}

"""





def render_quiz_diagnosis(quiz):

    diagnosis = _safe_json_loads(quiz.get("diagnosis_json"), {}) if quiz else {}

    if not diagnosis:

        return

    st.markdown("<div class='eval-card'><h3>Diagnóstico automático</h3></div>", unsafe_allow_html=True)

    d1, d2, d3 = st.columns(3)

    d1.markdown(f"<div class='metric'><b>{diagnosis.get('profile','N/D').title()}</b><br><span>Perfil cognitivo</span></div>", unsafe_allow_html=True)

    d2.markdown(f"<div class='metric'><b>{diagnosis.get('bloom_level','N/D')}</b><br><span>Nivel Bloom alcanzado</span></div>", unsafe_allow_html=True)

    d3.markdown(f"<div class='metric'><b>{len(diagnosis.get('deficient_concepts', []))}</b><br><span>Conceptos a reforzar</span></div>", unsafe_allow_html=True)

    dimension_scores = diagnosis.get("dimension_scores") or {}

    if dimension_scores:

        dim_df = pd.DataFrame([{"dimensión": k, "porcentaje": v} for k, v in dimension_scores.items()])

        st.plotly_chart(px.bar(dim_df, x="dimensión", y="porcentaje", range_y=[0, 100], title="Porcentaje por dimensión"), use_container_width=True)

    if diagnosis.get("frequent_errors"):

        st.warning("Errores frecuentes: " + "; ".join(diagnosis.get("frequent_errors", [])))

    if diagnosis.get("deficient_concepts"):

        st.info("Plan recomendado: estudiar " + ", ".join(diagnosis.get("deficient_concepts", [])))



    if str(quiz.get("quiz_type") or "adaptive").lower() == "adaptive":

        current_difficulty = normalize_adaptive_difficulty(quiz.get("difficulty") or "Intermedio")

        next_difficulty, reason = adaptive_practice_next_difficulty(

            current_difficulty,

            int(round(float(quiz.get("score") or 0) / 100 * max(1, int(quiz.get("question_count") or 1)))),

            max(1, int(quiz.get("question_count") or 1)),

        )

        weak_topics = diagnosis.get("deficient_concepts") or []

        fallback_topic = quiz.get("source_topic") or "el tema evaluado"

        st.markdown("<div class='eval-card'><h3>Ruta para elevar tu nivel</h3></div>", unsafe_allow_html=True)

        st.info(f"Siguiente dificultad recomendada: {next_difficulty}. {reason}")

        st.caption("Usa uno de estos prompts en el Tutor para estudiar antes de generar la siguiente evaluación:")

        for prompt_text in adaptive_learning_prompts(weak_topics, next_difficulty, fallback_topic):

            st.code(prompt_text, language=None)





def generate_derivative_applications_lesson(uid, academic_context, pretest_quiz_id):

    quiz, questions = load_adaptive_quiz(pretest_quiz_id)

    weak_questions = [q.get("question", "") for q in questions if not q.get("is_correct")]

    fallback = fallback_derivative_applications_lesson(weak_questions)

    client = ai_client()

    if not client:

        return fallback



    weak_context = "\n".join([f"- {q}" for q in weak_questions[:8]]) or "El estudiante no tuvo errores registrados; reforzar con ejemplos integradores."

    prompt = f"""

Actúa como tutor universitario de Cálculo Diferencial.

Genera una clase breve, clara y práctica para estudiar antes del posttest.



Tema obligatorio: {RESEARCH_FOCUS_TOPIC}

Materia: {academic_context.get('subject') or 'Cálculo diferencial'}

Curso/cohorte: {academic_context.get('cohort') or ''}



Debes incluir:

1. Explicación conceptual.

2. Procedimiento paso a paso.

3. Dos ejemplos resueltos: uno de optimización y uno de razones relacionadas o tasa de cambio.

4. Errores comunes.

5. Mini-guía para resolver el posttest.



Errores o preguntas falladas del pretest:

{weak_context}



Devuelve solo texto en español, con formato Markdown simple.

"""

    try:

        resp = client.models.generate_content(model="gemini-2.5-flash", contents=prompt)

        text = (resp.text or "").strip()

        return text if text else fallback

    except Exception:

        return fallback





def render_research_lesson_panel(user, academic_context, pretest_quiz):

    if not pretest_quiz or pretest_quiz.get("status") != "completed":

        return

    lesson_key = f"research_lesson_{user['id']}_{pretest_quiz['id']}"

    st.markdown(

        f"<div class='eval-card'><h3>Clase de refuerzo</h3>"

        f"<span class='eval-chip'>{RESEARCH_FOCUS_TOPIC}</span>"

        f"<span class='eval-chip'>Antes del posttest</span>"

        f"<p class='small'>Estudia esta clase después del pretest y antes de iniciar el posttest.</p></div>",

        unsafe_allow_html=True,

    )

    if st.button("Generar clase de refuerzo", use_container_width=True, key=f"btn_{lesson_key}"):

        with st.spinner("Generando clase de refuerzo..."):

            st.session_state[lesson_key] = generate_derivative_applications_lesson(user["id"], academic_context, pretest_quiz["id"])

        log_interaction(

            user["id"],

            "assistant",

            st.session_state[lesson_key],

            topic="Cálculo diferencial",

            subtopic=RESEARCH_FOCUS_TOPIC,

            level=academic_context.get("course_level", ""),

            model="gemini-2.5-flash",

            latency=0,

        )

        log_research_learning_event(

            user["id"], "class_generated", quiz_id=pretest_quiz["id"],

            metadata={"topic": RESEARCH_FOCUS_TOPIC, "model": "gemini-2.5-flash"},

        )

        st.rerun()

    if st.session_state.get(lesson_key):

        st.markdown(st.session_state[lesson_key])





def guided_exercise_bank():

    return [

        ("Fácil", "Si s(t)=4t+3, ¿qué representa s'(t)=4?", ["Velocidad constante", "Aceleración variable", "Área total", "Tiempo máximo"], "Velocidad constante", "La derivada de posición representa velocidad."),

        ("Fácil", "Si C'(50)=12, ¿qué interpretación es correcta?", ["Producir una unidad adicional cerca de 50 cuesta aproximadamente 12", "El costo total es 12", "La utilidad es 50", "La producción máxima es 12"], "Producir una unidad adicional cerca de 50 cuesta aproximadamente 12", "La derivada del costo es costo marginal."),

        ("Fácil", "Si f'(x)>0 en un intervalo, la función:", ["Crece", "Decrece", "No existe", "Es cóncava abajo siempre"], "Crece", "Derivada positiva implica crecimiento."),

        ("Fácil", "La pendiente de la tangente a f en x=a es:", ["f'(a)", "f(a)", "a", "f''(a)+1"], "f'(a)", "La derivada en el punto es la pendiente."),

        ("Fácil", "Si f''(x)>0, la gráfica es:", ["Cóncava hacia arriba", "Cóncava hacia abajo", "Horizontal", "Discontinua"], "Cóncava hacia arriba", "La segunda derivada positiva indica concavidad hacia arriba."),

        ("Medio", "Para maximizar A(x), el primer candidato interior se obtiene resolviendo:", ["A'(x)=0", "A(x)=0", "A''(x)=0 siempre", "x=0 siempre"], "A'(x)=0", "Los extremos interiores se buscan con puntos críticos."),

        ("Medio", "Si R(x)=80x-0.5x², entonces R'(x) es:", ["80-x", "80x-x²", "80-0.5x", "x-80"], "80-x", "Se deriva término a término."),

        ("Medio", "Si A=πr² y dr/dt=2, entonces dA/dt es:", ["4πr", "2πr", "πr²", "2r"], "4πr", "dA/dt=2πr dr/dt=4πr."),

        ("Medio", "Si f'(a)=0 y f''(a)<0, entonces hay:", ["Máximo local", "Mínimo local", "Punto de inflexión siempre", "No hay conclusión"], "Máximo local", "Punto crítico con segunda derivada negativa indica máximo local."),

        ("Medio", "La recta tangente en x=a usa la fórmula:", ["y-f(a)=f'(a)(x-a)", "y=f(a)+a", "y=x/a", "y=f''(a)x"], "y-f(a)=f'(a)(x-a)", "Es la forma punto-pendiente."),

        ("Difícil", "Una caja sin tapa se fabrica con una lámina cuadrada recortando esquinas. El objetivo de derivar el volumen es:", ["Encontrar el corte que maximiza el volumen", "Encontrar solo el perímetro", "Eliminar la restricción", "Calcular velocidad"], "Encontrar el corte que maximiza el volumen", "Es un problema de optimización geométrica."),

        ("Difícil", "Si la demanda p(x)=100-2x e ingreso R=xp(x), para maximizar ingreso se debe:", ["Formar R(x)=100x-2x² y resolver R'(x)=0", "Resolver p(x)=0 solamente", "Derivar p y detenerse", "Usar solo x=0"], "Formar R(x)=100x-2x² y resolver R'(x)=0", "Primero se modela el ingreso y luego se optimiza."),

        ("Difícil", "En un problema de escalera que se desliza, si x e y cambian con el tiempo, el método correcto es:", ["Derivar la relación x²+y²=L² respecto de t", "Derivar solo x", "Hacer x=y siempre", "Ignorar dx/dt"], "Derivar la relación x²+y²=L² respecto de t", "Razones relacionadas usan derivación implícita respecto al tiempo."),

    ]





def render_guided_exercises_panel(user, academic_context, pretest_quiz):

    if not pretest_quiz or pretest_quiz.get("status") != "completed":

        return

    exercises = guided_exercise_bank()

    st.markdown(

        f"<div class='eval-card'><h3>Ejercicios guiados adaptativos</h3>"

        f"<span class='eval-chip'>5 fáciles</span><span class='eval-chip'>5 medios</span><span class='eval-chip'>3 difíciles</span>"

        f"<p class='small'>Responde para recibir retroalimentación inmediata antes del posttest.</p></div>",

        unsafe_allow_html=True,

    )

    form_key = f"guided_exercises_{user['id']}_{pretest_quiz['id']}"

    with st.form(form_key):

        answers = {}

        for idx, (difficulty, text, options, correct, feedback) in enumerate(exercises, start=1):

            st.markdown(f"<div class='eval-question'><b>{idx}. {text}</b><br><span class='eval-chip'>{difficulty}</span></div>", unsafe_allow_html=True)

            answers[str(idx)] = st.radio("Selecciona una respuesta", options, key=f"{form_key}_{idx}", label_visibility="collapsed")

        submitted = st.form_submit_button("Revisar ejercicios guiados", use_container_width=True)

    if submitted:

        correct_count = 0

        for idx, (difficulty, text, options, correct, feedback) in enumerate(exercises, start=1):

            user_answer = answers.get(str(idx), "")

            ok = user_answer == correct

            correct_count += 1 if ok else 0

            execute("""

                INSERT INTO research_exercise_attempts(

                    user_id, quiz_id, topic, subtopic, exercise_text, options_json,

                    correct_answer, user_answer, difficulty_level, is_correct, feedback, created_at

                )

                VALUES(%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)

            """, (

                user["id"], pretest_quiz["id"], "Cálculo diferencial", RESEARCH_FOCUS_TOPIC,

                text, json.dumps(options, ensure_ascii=False), correct, user_answer,

                difficulty, ok, feedback, now()

            ))

        log_research_learning_event(

            user["id"], "guided_exercises_completed", quiz_id=pretest_quiz["id"],

            metadata={"exercise_count": len(exercises), "correct_count": correct_count, "feedback_count": len(exercises)},

        )

        st.success(f"Ejercicios revisados: {correct_count}/{len(exercises)} correctos.")

        if correct_count < len(exercises) * 0.7:

            st.warning("Recomendación adaptativa: vuelve a revisar la clase de refuerzo y practica ejercicios similares de dificultad equivalente.")

        else:

            st.info("Buen avance: puedes intentar el posttest con ejercicios de mayor contexto.")





def survey_already_submitted(uid, quiz_id):

    row = fetchone("SELECT COUNT(*) AS n FROM research_survey_responses WHERE user_id=%s AND quiz_id=%s", (uid, quiz_id))

    return bool(row and int(row.get("n") or 0) >= len(RESEARCH_SURVEY_ITEMS))





def render_final_survey(user, posttest_quiz):

    if not posttest_quiz or posttest_quiz.get("status") != "completed":

        return

    st.markdown("<div class='eval-card'><h3>Encuesta final de percepción</h3><p class='small'>Escala Likert: 1 = totalmente en desacuerdo, 5 = totalmente de acuerdo.</p></div>", unsafe_allow_html=True)

    if survey_already_submitted(user["id"], posttest_quiz["id"]):

        st.success("Encuesta final registrada. Gracias.")

        return

    with st.form(f"survey_{user['id']}_{posttest_quiz['id']}"):

        scores = {}

        for idx, (dimension, item) in enumerate(RESEARCH_SURVEY_ITEMS, start=1):

            scores[idx] = st.slider(f"{idx}. {item}", 1, 5, 3, key=f"survey_item_{posttest_quiz['id']}_{idx}")

            st.caption(dimension)

        submitted = st.form_submit_button("Enviar encuesta final", use_container_width=True)

    if submitted:

        for idx, (dimension, item) in enumerate(RESEARCH_SURVEY_ITEMS, start=1):

            execute("""

                INSERT INTO research_survey_responses(user_id, quiz_id, item_no, dimension, item_text, score, created_at)

                VALUES(%s,%s,%s,%s,%s,%s,%s)

            """, (user["id"], posttest_quiz["id"], idx, dimension, item, int(scores[idx]), now()))

        st.success("Encuesta final registrada correctamente.")

        st.rerun()





def render_teacher_plan_manager(user):
    ensure_question_bank_gold_schema()

    st.markdown("""

    <div class='eval-hero'>

        <h2>📚 Planes analíticos inteligentes</h2>

        <p>Carga planes analíticos, sílabos o contenidos. BunsekiChat extrae temas, resultados de aprendizaje y niveles Bloom para crear evaluaciones adaptativas.</p>

    </div>

    """, unsafe_allow_html=True)



    with st.container():

        st.markdown("<div class='plan-upload-box'><b>Subir nuevo plan analítico</b></div>", unsafe_allow_html=True)

        c1, c2 = st.columns([1.2, 1])

        title = c1.text_input("Título del plan", placeholder="Ej. Cálculo diferencial - Unidad 1")

        subject = c2.selectbox("Materia", unique_values(ACADEMIC_SUBJECTS), index=0)

        p1, p2, p3 = st.columns(3)

        with p1:

            course_level = academic_selectbox("Curso / nivel", ACADEMIC_COURSE_LEVELS, ACADEMIC_COURSE_LEVELS[0], "plan_course_level", normalize_course_level)

        with p2:

            parallel = academic_selectbox("Paralelo", ACADEMIC_PARALLELS, ACADEMIC_PARALLELS[0], "plan_parallel", normalize_parallel)

        with p3:

            shift = academic_selectbox("Jornada", ACADEMIC_SHIFTS, ACADEMIC_SHIFTS[0], "plan_shift", normalize_shift)

        course = build_course_label(course_level, parallel, shift)

        uploaded = st.file_uploader("Archivo del plan analítico", type=["txt", "md", "docx", "pdf"])

        if st.button("Procesar y guardar plan", use_container_width=True):

            raw_text = read_uploaded_plan_text(uploaded)

            if not title.strip():

                st.warning("Escribe un título para el plan.")

            elif not raw_text.strip():

                st.warning("No se pudo extraer texto del archivo. Prueba con TXT/DOCX o instala pypdf para PDF.")

            else:

                parsed = parse_plan_topics_with_ai(raw_text, title)

                academic_context = {

                    "subject": normalize_academic_text(subject),

                    "course_level": normalize_course_level(course_level),

                    "parallel": normalize_parallel(parallel),

                    "shift": normalize_shift(shift),

                    "cohort": course,

                }

                plan_id = save_analytic_plan(title.strip(), course.strip(), user.get("id"), uploaded.name if uploaded else "", raw_text, parsed, academic_context)

                st.success(f"Plan guardado correctamente. ID: {plan_id}. Temas extraídos: {len(parsed.get('topics', []))}")

                st.rerun()



    role = str(user.get("role") or "").lower()

    plans = pd.DataFrame(get_analytic_plans() if role == "admin" else get_analytic_plans(user["id"]))

    if plans.empty:

        st.info("Aún no hay planes analíticos cargados.")

        return



    st.markdown("<div class='eval-card'><h3>Planes cargados</h3></div>", unsafe_allow_html=True)

    plan_cols = [c for c in ["id","title","subject","course","course_level","parallel","shift","teacher_username","created_at","summary"] if c in plans.columns]

    st.dataframe(plans[plan_cols], use_container_width=True, height=280)



    plan_id = st.selectbox("Ver temas de un plan", plans["id"].tolist(), format_func=lambda x: f"{x} · {plans.loc[plans['id']==x,'title'].iloc[0]}")

    topics_df = pd.DataFrame(get_plan_topics(int(plan_id)))

    if topics_df.empty:

        st.info("Este plan todavía no tiene temas curriculares extraídos.")

        return



    st.dataframe(topics_df[["unit_name","topic","subtopic","learning_outcome","bloom_level","keywords"]], use_container_width=True, height=260)

    selected_topic_id = st.selectbox(

        "Selecciona un tema curricular",

        topics_df["id"].tolist(),

        format_func=lambda x: f"{x} · {topics_df.loc[topics_df['id'] == x, 'topic'].iloc[0]}",

        key=f"question_bank_topic_{plan_id}",

    )

    selected_topic = get_plan_topic(int(selected_topic_id))

    st.markdown("### Tema seleccionado")

    st.write(f"**Unidad:** {selected_topic.get('unit_name') or '—'}")

    st.write(f"**Tema:** {selected_topic.get('topic') or '—'}")

    st.write(f"**Subtema:** {selected_topic.get('subtopic') or '—'}")

    st.write(f"**Resultado de aprendizaje:** {selected_topic.get('learning_outcome') or '—'}")

    st.write(f"**Bloom:** {selected_topic.get('bloom_level') or '—'}")
    st.write(f"**Fuente documental:** {selected_topic.get('source_locator') or '-'}")
    if selected_topic.get("source_excerpt"):
        st.caption("Evidencia documental: " + str(selected_topic.get("source_excerpt")))



    generation_left, generation_right = st.columns(2)

    difficulty = generation_left.selectbox("Dificultad", QUESTION_BANK_DIFFICULTIES, index=1, key=f"question_bank_difficulty_{selected_topic_id}")

    n_questions = generation_right.number_input("Número de preguntas", min_value=1, max_value=20, value=10, step=1, key=f"question_bank_count_{selected_topic_id}")

    if st.button("Generar preguntas en borrador", use_container_width=True, key=f"generate_question_bank_{selected_topic_id}"):

        try:

            with st.spinner("Generando borradores curriculares con Gemini..."):

                created = generate_topic_question_drafts(user["id"], int(selected_topic_id), difficulty, int(n_questions))

            st.success(f"Se guardaron {len(created)} preguntas en borrador para revisión docente.")

            st.rerun()

        except (ValueError, RuntimeError) as exc:

            st.error(str(exc))



    st.markdown("### Preguntas en revisión")

    drafts = get_question_bank_items(plan_topic_id=int(selected_topic_id), status="draft")

    if not drafts:

        st.caption("No hay borradores pendientes para este tema.")

    for item in drafts:

        item_id = item["id"]

        with st.expander(f"Borrador #{item_id}: {item['question'][:90]}", expanded=False):

            question = st.text_area("Pregunta", item["question"], key=f"qb_question_{item_id}")

            raw_options = st.text_area("Opciones (JSON: arreglo de 4 textos)", item["options_json"], key=f"qb_options_{item_id}")

            correct = st.text_input("Respuesta correcta", item["correct_answer"], key=f"qb_correct_{item_id}")

            explanation = st.text_area("Explicación", item.get("explanation") or "", key=f"qb_explanation_{item_id}")

            edit_left, edit_right = st.columns(2)

            bloom = edit_left.selectbox("Bloom", QUESTION_BANK_BLOOM_LEVELS, index=QUESTION_BANK_BLOOM_LEVELS.index(item.get("bloom_level")) if item.get("bloom_level") in QUESTION_BANK_BLOOM_LEVELS else 2, key=f"qb_bloom_{item_id}")

            item_difficulty = edit_right.selectbox("Dificultad", QUESTION_BANK_DIFFICULTIES, index=QUESTION_BANK_DIFFICULTIES.index(item.get("difficulty_level")) if item.get("difficulty_level") in QUESTION_BANK_DIFFICULTIES else 1, key=f"qb_difficulty_{item_id}")

            action_save, action_approve, action_reject, action_delete = st.columns(4)

            payload = {"question": question, "options": _safe_json_loads(raw_options, []), "correct_answer": correct, "explanation": explanation, "bloom_level": bloom, "difficulty_level": item_difficulty}

            if action_save.button("Guardar cambios", key=f"qb_save_{item_id}"):

                try:

                    update_question_bank_item(item_id, payload, user["id"])

                    st.success("Cambios guardados.")

                    st.rerun()

                except ValueError as exc:

                    st.error(str(exc))

            if action_approve.button("Aprobar", key=f"qb_approve_{item_id}"):

                try:

                    update_question_bank_item(item_id, payload, user["id"])

                    approve_question_bank_item(item_id, user["id"])

                    st.success("Pregunta aprobada y enviada al banco validado.")

                    st.rerun()

                except ValueError as exc:

                    st.error(str(exc))

            if action_reject.button("Rechazar", key=f"qb_reject_{item_id}"):

                reject_question_bank_item(item_id, user["id"])

                st.info("Pregunta rechazada.")

                st.rerun()

            if action_delete.button("Eliminar", key=f"qb_delete_{item_id}"):

                delete_question_bank_item(item_id, user["id"])

                st.info("Pregunta marcada como eliminada.")

                st.rerun()



    st.markdown("### Banco de preguntas aprobadas")

    approved = get_question_bank_items(plan_id=int(plan_id), status="approved")

    if not approved:

        st.caption("Aún no hay preguntas aprobadas para este plan.")

    else:

        approved_df = pd.DataFrame(approved)

        visible = [column for column in ["unit_name", "topic", "learning_outcome", "bloom_level", "difficulty_level", "question", "governance_level", "approved_at", "gold_at"] if column in approved_df.columns]

        st.dataframe(approved_df[visible], use_container_width=True, height=300)



    st.markdown("### Gobernanza Gold")
    st.caption(
        "Gold es una capa adicional sobre preguntas aprobadas. "
        "Exige trazabilidad curricular y evidencia documental verificable."
    )

    gold_items = [
        item for item in approved
        if (item.get("governance_level") or "") == "gold"
    ]
    gold_candidates = [
        item for item in approved
        if (item.get("governance_level") or "") == "validated"
    ]

    if gold_candidates:
        gold_candidate_id = st.selectbox(
            "Pregunta validada para promover a Gold",
            [item["id"] for item in gold_candidates],
            format_func=lambda item_id: next(
                f"#{item_id} · {item['topic']} · {item['question'][:70]}"
                for item in gold_candidates
                if item["id"] == item_id
            ),
            key=f"gold_candidate_{plan_id}",
        )
        if st.button(
            "Promover a Banco Gold",
            key=f"promote_gold_{plan_id}",
        ):
            try:
                promote_question_bank_item_to_gold(
                    int(gold_candidate_id),
                    user["id"],
                )
                st.success(
                    "Pregunta promovida a Gold con trazabilidad curricular verificada."
                )
                st.rerun()
            except ValueError as exc:
                st.error(str(exc))
    else:
        st.caption("No hay preguntas validadas pendientes de promoción a Gold.")

    if gold_items:
        gold_df = pd.DataFrame(gold_items)
        gold_visible = [
            column
            for column in [
                "unit_name",
                "topic",
                "learning_outcome",
                "bloom_level",
                "difficulty_level",
                "question",
                "gold_at",
            ]
            if column in gold_df.columns
        ]
        st.markdown("#### Banco Gold")
        st.dataframe(
            gold_df[gold_visible],
            use_container_width=True,
            height=260,
        )
        render_moodle_gold_preview(gold_items)

        try:
            moodle_xml, moodle_filename, moodle_count = (
                build_moodle_xml_from_gold_items(int(plan_id))
            )
            st.download_button(
                f"Descargar Moodle XML ({moodle_count} Gold)",
                moodle_xml,
                moodle_filename,
                "application/xml",
                key=f"download_moodle_gold_{plan_id}",
                use_container_width=True,
            )
            st.caption(
                "Exporta únicamente preguntas Gold del plan en formato "
                "Moodle XML multichoice."
            )
        except ValueError as exc:
            st.warning(f"No se pudo preparar Moodle XML: {exc}")
    else:
        st.caption("El Banco Gold todavía no contiene preguntas.")


    st.markdown("### Crear evaluación desde banco aprobado")

    bank_items = get_question_bank_items(plan_id=int(plan_id), status="approved")

    selected_bank_ids = st.multiselect("Preguntas aprobadas", [item["id"] for item in bank_items], format_func=lambda item_id: next(f"#{item_id} · {item['topic']} · {item['question'][:65]}" for item in bank_items if item["id"] == item_id), key=f"assessment_bank_items_{plan_id}")

    assessment_title = st.text_input("Título de evaluación", key=f"assessment_title_{plan_id}")

    if st.button("Crear evaluación", key=f"create_assessment_{plan_id}"):

        plan = next((row for row in plans.to_dict("records") if row["id"] == int(plan_id)), {})

        context = {key: plan.get(key) for key in ("subject", "course_level", "parallel", "shift", "cohort")}

        context["cohort"] = context.get("cohort") or plan.get("course")

        try:

            assessment = create_teacher_assessment(user["id"], int(plan_id), assessment_title, context)

            set_teacher_assessment_items(assessment["id"], user["id"], selected_bank_ids)

            st.success("Evaluación creada como borrador."); st.rerun()

        except ValueError as exc: st.error(str(exc))

    for assessment in get_teacher_assessments(created_by=user["id"]):

        with st.expander(f"Evaluación #{assessment['id']} · {assessment['title']} · {assessment['status']}"):

            assessment_id = int(assessment["id"])

            st.write(f"Contexto: {assessment.get('subject') or '—'} · {assessment.get('course_level') or '—'} · {assessment.get('parallel') or '—'} · {assessment.get('shift') or '—'}")

            st.write(f"Preguntas: {len(get_teacher_assessment_items(assessment_id))}")



            audience_mode = _assessment_audience_mode(assessment)

            target_ids = get_teacher_assessment_target_user_ids(assessment_id)

            audience_label = (

                "Dirigida a estudiantes seleccionados"

                if audience_mode == "targeted"

                else "Por contexto académico"

            )

            st.write(f"**Audiencia:** {audience_label}")



            eligible_students = get_teacher_assessment_eligible_students(

                assessment_id,

                user["id"],

            )

            eligible_by_id = {

                int(row["id"]): row

                for row in eligible_students

            }



            def _assessment_student_label(student_id):

                row = eligible_by_id.get(int(student_id), {})

                full_name = normalize_spaces(

                    f"{row.get('first_names') or ''} {row.get('last_names') or ''}"

                )

                username = row.get("username") or f"usuario_{student_id}"

                return f"{full_name or username} · {username} · ID {student_id}"



            if target_ids:

                visible_targets = [

                    _assessment_student_label(student_id)

                    for student_id in target_ids

                ]

                st.caption(

                    "Destinatarios actuales: " + " | ".join(visible_targets)

                )

            elif audience_mode == "targeted":

                st.warning(

                    "La evaluación está en modo dirigido pero no tiene "

                    "destinatarios. No puede publicarse."

                )



            if assessment["status"] == "draft":

                mode_options = {

                    "academic_context": "Por contexto académico",

                    "targeted": "Dirigida a estudiantes seleccionados",

                }

                selected_mode = st.selectbox(

                    "Modo de audiencia",

                    list(mode_options),

                    index=(

                        list(mode_options).index(audience_mode)

                        if audience_mode in mode_options

                        else 0

                    ),

                    format_func=lambda value: mode_options[value],

                    key=f"assessment_audience_mode_{assessment_id}",

                )



                selected_targets = []

                if selected_mode == "targeted":

                    eligible_ids = list(eligible_by_id)

                    selected_targets = st.multiselect(

                        "Estudiantes destinatarios",

                        eligible_ids,

                        default=[

                            student_id

                            for student_id in target_ids

                            if student_id in eligible_by_id

                        ],

                        format_func=_assessment_student_label,

                        key=f"assessment_targets_{assessment_id}",

                    )

                    if not eligible_ids:

                        st.info(

                            "No existen estudiantes elegibles dentro de tus "

                            "asignaciones y del contexto de esta evaluación."

                        )



                if st.button(

                    "Guardar audiencia",

                    key=f"save_assessment_audience_{assessment_id}",

                    use_container_width=True,

                ):

                    try:

                        set_teacher_assessment_audience(

                            assessment_id,

                            user["id"],

                            selected_mode,

                            selected_targets,

                        )

                        st.success("Audiencia guardada correctamente.")

                        st.rerun()

                    except ValueError as exc:

                        st.error(str(exc))



                if st.button(

                    "Publicar evaluación",

                    key=f"publish_assessment_{assessment_id}",

                ):

                    try:

                        publish_teacher_assessment(assessment_id, user["id"])

                        st.success("Evaluación publicada.")

                        st.rerun()

                    except ValueError as exc:

                        st.error(str(exc))



            if assessment["status"] == "published" and st.button("Cerrar evaluación", key=f"close_assessment_{assessment_id}"):

                try: close_teacher_assessment(assessment_id, user["id"]); st.success("Evaluación cerrada."); st.rerun()

                except ValueError as exc: st.error(str(exc))

            if assessment["status"] in ("published", "closed"):

                st.dataframe(pd.DataFrame(get_teacher_assessment_results(assessment_id)), use_container_width=True)

                analytics = get_teacher_assessment_curricular_analytics(assessment_id)

                for label, rows in analytics.items():

                    st.caption(label); st.dataframe(pd.DataFrame(rows), use_container_width=True)









def _p5a6_transaction_connection():

    connection = conn()

    connection.autocommit = False

    return connection





def _p5a6_elapsed_seconds(connection, attempt_id, user_id):

    with connection.cursor() as cur:

        cur.execute(

            """

            SELECT GREATEST(

                0,

                FLOOR(

                    EXTRACT(

                        EPOCH FROM (

                            CURRENT_TIMESTAMP - started_at

                        )

                    )

                )

            )::INTEGER AS elapsed_seconds

            FROM public.assessment_attempts

            WHERE id=%s

              AND user_id=%s

            """,

            (attempt_id, user_id),

        )

        row = cur.fetchone()



    if not row:

        raise ValueError("Intento reanudable no encontrado.")



    return int(row.get("elapsed_seconds") or 0)





def _p5a6_format_elapsed_seconds(value):

    try:

        total = max(0, int(value or 0))

    except (TypeError, ValueError):

        total = 0



    hours, remainder = divmod(total, 3600)

    minutes, seconds = divmod(remainder, 60)



    if hours:

        return f"{hours:d}:{minutes:02d}:{seconds:02d}"



    return f"{minutes:02d}:{seconds:02d}"





def _p5a6_prepare_teacher_attempt(user_id, quiz, register_resume=False):

    if not quiz or quiz.get("status") != "generated":

        return None



    teacher_assessment_id = quiz.get("teacher_assessment_id")

    if not teacher_assessment_id:

        raise ValueError("La evaluacion no tiene identificador docente.")



    connection = _p5a6_transaction_connection()

    try:

        attempt = resumable_get_or_create_attempt(

            connection,

            user_id=user_id,

            assessment_kind="teacher_assessment",

            source_key=f"teacher_assessment:{teacher_assessment_id}",

            adaptive_quiz_id=quiz["id"],

            teacher_assessment_id=teacher_assessment_id,

            version_code=quiz.get("version_code"),

            metadata={

                "teacher_assessment_id": teacher_assessment_id,

                "adaptive_quiz_id": quiz["id"],

            },

        )



        if register_resume and not attempt.get("created"):

            attempt = resumable_resume_attempt(

                connection,

                attempt_id=attempt["id"],

                user_id=user_id,

            )



        progress = resumable_load_attempt_progress(

            connection,

            attempt_id=attempt["id"],

            user_id=user_id,

        )

        progress["server_elapsed_seconds"] = _p5a6_elapsed_seconds(

            connection,

            attempt["id"],

            user_id,

        )

        connection.commit()

        return progress

    except Exception:

        connection.rollback()

        raise

    finally:

        connection.close()





def _p5a6_autosave_teacher_answer(user_id, attempt_id, question_id, position, widget_key):

    answer = st.session_state.get(widget_key)

    if answer is None:

        return



    connection = _p5a6_transaction_connection()

    try:

        elapsed_seconds = _p5a6_elapsed_seconds(connection, attempt_id, user_id)

        resumable_save_attempt_answer(

            connection,

            attempt_id=attempt_id,

            user_id=user_id,

            question_key=f"adaptive_question:{question_id}",

            question_id=question_id,

            answer_payload={"value": answer},

            elapsed_seconds=elapsed_seconds,

            current_position=max(0, int(position or 0)),

        )

        connection.commit()

        st.session_state["teacher_assessment_autosave_error"] = None

        st.session_state["teacher_assessment_last_saved_question"] = question_id

    except Exception as exc:

        connection.rollback()

        st.session_state["teacher_assessment_autosave_error"] = str(exc)

    finally:

        connection.close()





def _p5a6_grade_teacher_assessment_resumable(user_id, quiz_id, attempt_id, answers):

    quiz, questions = load_adaptive_quiz(quiz_id)



    if not quiz or quiz.get("quiz_type") != "bank_assessment":

        raise ValueError("Intento de banco invalido.")

    if int(quiz.get("user_id")) != int(user_id):

        raise ValueError("La evaluacion no pertenece al estudiante autenticado.")



    connection = _p5a6_transaction_connection()

    try:

        with connection.cursor() as cur:

            cur.execute(

                """

                SELECT status, adaptive_quiz_id

                FROM public.assessment_attempts

                WHERE id=%s AND user_id=%s

                FOR UPDATE

                """,

                (attempt_id, user_id),

            )

            attempt_row = cur.fetchone()



        if not attempt_row:

            raise ValueError("Intento reanudable no encontrado.")



        attempt_status = attempt_row.get("status")

        attempt_quiz_id = attempt_row.get("adaptive_quiz_id")



        if attempt_quiz_id is None or int(attempt_quiz_id) != int(quiz_id):

            raise ValueError("El intento reanudable no corresponde a esta evaluacion.")

        if attempt_status == "submitted":

            connection.rollback()

            return quiz

        if attempt_status != "in_progress":

            raise ValueError("El intento ya no esta activo.")

        if quiz.get("status") != "generated":

            raise ValueError("La evaluacion ya fue finalizada.")



        elapsed_seconds = _p5a6_elapsed_seconds(connection, attempt_id, user_id)

        correct = 0



        for ordinal, question in enumerate(questions, start=1):

            question_id = question["id"]

            answer = (answers or {}).get(str(question_id))



            if answer is None or str(answer).strip() == "":

                raise ValueError("Debes responder todas las preguntas antes de enviar.")



            position = question.get("position") or ordinal

            resumable_save_attempt_answer(

                connection,

                attempt_id=attempt_id,

                user_id=user_id,

                question_key=f"adaptive_question:{question_id}",

                question_id=question_id,

                answer_payload={"value": answer},

                elapsed_seconds=elapsed_seconds,

                current_position=position,

            )



            is_correct = answer == question.get("correct_answer")

            correct += int(is_correct)



            with connection.cursor() as cur:

                cur.execute(

                    """

                    UPDATE public.adaptive_questions

                    SET user_answer=%s,

                        is_correct=%s,

                        response_time_seconds=%s

                    WHERE id=%s AND quiz_id=%s

                    """,

                    (answer, is_correct, elapsed_seconds, question_id, quiz_id),

                )

                if cur.rowcount != 1:

                    raise ValueError("No se pudo actualizar una pregunta.")



        score = round(correct / max(1, len(questions)) * 100, 2)



        with connection.cursor() as cur:

            cur.execute(

                """

                UPDATE public.adaptive_quizzes

                SET score=%s,

                    passed=%s,

                    status='completed',

                    completed_at=%s

                WHERE id=%s

                  AND user_id=%s

                  AND status='generated'

                """,

                (score, score >= 70, now(), quiz_id, user_id),

            )

            if cur.rowcount != 1:

                raise ValueError("No se pudo finalizar la evaluacion.")



        resumable_submit_attempt(

            connection,

            attempt_id=attempt_id,

            user_id=user_id,

            elapsed_seconds=elapsed_seconds,

        )

        connection.commit()

    except Exception:

        connection.rollback()

        raise

    finally:

        connection.close()



    completed_quiz, _ = load_adaptive_quiz(quiz_id)

    return completed_quiz





def _p5a6_prepare_research_attempt(

    user_id,

    quiz,

    register_resume=False,

):

    if not quiz or not is_research_quiz(quiz.get("quiz_type")):

        raise ValueError(

            "El instrumento no corresponde a pretest/posttest."

        )



    quiz_type = str(quiz.get("quiz_type") or "").lower()

    if quiz_type not in {"pretest", "posttest"}:

        raise ValueError(

            "Tipo de instrumento de investigacion no soportado."

        )



    connection = _p5a6_transaction_connection()

    try:

        attempt = resumable_get_or_create_attempt(

            connection,

            user_id=user_id,

            assessment_kind=quiz_type,

            source_key=f"research:{quiz_type}:adaptive_quiz:{quiz['id']}",

            adaptive_quiz_id=quiz["id"],

            version_code=quiz.get("version_code"),

            metadata={

                "quiz_type": quiz_type,

                "adaptive_quiz_id": quiz["id"],

                "version_code": quiz.get("version_code"),

                "subject": quiz.get("subject"),

                "course_level": quiz.get("course_level"),

                "parallel": quiz.get("parallel"),

                "shift": quiz.get("shift"),

                "cohort": quiz.get("cohort"),

                "source_topic": quiz.get("source_topic"),

                "research_title": quiz.get("research_title"),

            },

        )



        if register_resume and not attempt.get("created"):

            attempt = resumable_resume_attempt(

                connection,

                attempt_id=attempt["id"],

                user_id=user_id,

            )



        progress = resumable_load_attempt_progress(

            connection,

            attempt_id=attempt["id"],

            user_id=user_id,

        )

        progress["server_elapsed_seconds"] = _p5a6_elapsed_seconds(

            connection,

            attempt["id"],

            user_id,

        )

        connection.commit()

        return progress

    except Exception:

        connection.rollback()

        raise

    finally:

        connection.close()





def _p5a6_autosave_research_answer(

    user_id,

    attempt_id,

    quiz_id,

    question_id,

    position,

    widget_key,

):

    answer = st.session_state.get(widget_key)

    if answer is None:

        return



    error_key = f"research_assessment_autosave_error_{quiz_id}"

    connection = _p5a6_transaction_connection()

    try:

        elapsed_seconds = _p5a6_elapsed_seconds(

            connection,

            attempt_id,

            user_id,

        )

        resumable_save_attempt_answer(

            connection,

            attempt_id=attempt_id,

            user_id=user_id,

            question_key=f"adaptive_question:{question_id}",

            question_id=question_id,

            answer_payload={"value": answer},

            elapsed_seconds=elapsed_seconds,

            current_position=max(0, int(position or 0)),

        )

        connection.commit()

        st.session_state[error_key] = None

        st.session_state[

            f"research_last_saved_question_{quiz_id}"

        ] = question_id

    except Exception as exc:

        connection.rollback()

        st.session_state[error_key] = str(exc)

    finally:

        connection.close()





def _p5a6_grade_research_quiz_resumable(

    user_id,

    quiz_id,

    attempt_id,

    answers,

):

    quiz, questions = load_adaptive_quiz(quiz_id)

    if not quiz:

        raise ValueError(

            "Instrumento de investigacion no encontrado."

        )



    quiz_type = str(quiz.get("quiz_type") or "").lower()

    if quiz_type not in {"pretest", "posttest"}:

        raise ValueError(

            "El instrumento no es pretest/posttest."

        )

    if int(quiz.get("user_id")) != int(user_id):

        raise ValueError(

            "El instrumento no pertenece al estudiante autenticado."

        )



    connection = _p5a6_transaction_connection()

    try:

        with connection.cursor() as cur:

            cur.execute(

                """

                SELECT status, adaptive_quiz_id, assessment_kind

                FROM public.assessment_attempts

                WHERE id=%s

                  AND user_id=%s

                FOR UPDATE

                """,

                (attempt_id, user_id),

            )

            attempt_row = cur.fetchone()



        if not attempt_row:

            raise ValueError(

                "Intento reanudable no encontrado."

            )



        attempt_status = attempt_row.get("status")

        attempt_quiz_id = attempt_row.get("adaptive_quiz_id")

        attempt_kind = str(

            attempt_row.get("assessment_kind") or ""

        ).lower()



        if (

            attempt_quiz_id is None

            or int(attempt_quiz_id) != int(quiz_id)

        ):

            raise ValueError(

                "El intento reanudable no corresponde "

                "a este instrumento."

            )

        if attempt_kind != quiz_type:

            raise ValueError(

                "El tipo de intento no coincide con el instrumento."

            )



        with connection.cursor() as cur:

            cur.execute(

                """

                SELECT status, score, passed, recommendation

                FROM public.adaptive_quizzes

                WHERE id=%s

                  AND user_id=%s

                FOR UPDATE

                """,

                (quiz_id, user_id),

            )

            quiz_row = cur.fetchone()



        if not quiz_row:

            raise ValueError(

                "Instrumento no encontrado para el estudiante."

            )



        quiz_status = str(

            quiz_row.get("status") or ""

        ).lower()



        if (

            attempt_status == "submitted"

            and quiz_status == "completed"

        ):

            connection.rollback()

            return (

                float(quiz_row.get("score") or 0),

                bool(quiz_row.get("passed")),

                [],

                quiz_row.get("recommendation") or "",

            )



        if attempt_status != "in_progress":

            raise ValueError(

                "El intento ya no esta activo."

            )

        if quiz_status != "generated":

            raise ValueError(

                "El instrumento ya no esta disponible para envio."

            )



        elapsed_seconds = _p5a6_elapsed_seconds(

            connection,

            attempt_id,

            user_id,

        )

        total = max(1, len(questions))

        per_question_time = (

            round(float(elapsed_seconds or 0) / total, 2)

            if elapsed_seconds

            else None

        )



        correct = 0

        weak_topics = []

        graded_questions = []



        for ordinal, question in enumerate(

            questions,

            start=1,

        ):

            question_id = question["id"]

            answer = (answers or {}).get(str(question_id))

            if answer is None or str(answer).strip() == "":

                raise ValueError(

                    "Debes responder todas las preguntas "

                    "antes de enviar."

                )



            position = question.get("position") or ordinal

            resumable_save_attempt_answer(

                connection,

                attempt_id=attempt_id,

                user_id=user_id,

                question_key=f"adaptive_question:{question_id}",

                question_id=question_id,

                answer_payload={"value": answer},

                elapsed_seconds=elapsed_seconds,

                current_position=position,

            )



            is_correct = (

                str(answer).strip()

                == str(question.get("correct_answer", "")).strip()

            )

            if is_correct:

                correct += 1

            else:

                weak_topics.append(

                    question.get("question", "")[:120]

                )



            conceptual_error = conceptual_error_for_question(

                question,

                is_correct,

            )

            estimated_confidence = 0.85 if is_correct else 0.35



            with connection.cursor() as cur:

                cur.execute(

                    """

                    UPDATE public.adaptive_questions

                    SET user_answer=%s,

                        is_correct=%s,

                        attempts=%s,

                        response_time_seconds=%s,

                        conceptual_error=%s,

                        estimated_confidence=%s

                    WHERE id=%s

                      AND quiz_id=%s

                    """,

                    (

                        answer,

                        is_correct,

                        1,

                        per_question_time,

                        conceptual_error,

                        estimated_confidence,

                        question_id,

                        quiz_id,

                    ),

                )

                if cur.rowcount != 1:

                    raise ValueError(

                        "No se pudo actualizar una pregunta."

                    )



            graded = dict(question)

            graded["user_answer"] = answer

            graded["is_correct"] = is_correct

            graded["conceptual_error"] = conceptual_error

            graded["estimated_confidence"] = estimated_confidence

            graded["response_time_seconds"] = per_question_time

            graded_questions.append(graded)



        score = round(correct / total * 100, 2)

        passed = score >= 70

        diagnosis = build_quiz_diagnosis(

            score,

            graded_questions,

        )

        recommendation = (

            "Aprobado. Puedes avanzar al siguiente reto."

            if passed

            else (

                "Refuerzo automatico sugerido: revisa las "

                "preguntas falladas y estudia las microclases "

                "sugeridas."

            )

        )



        with connection.cursor() as cur:

            cur.execute(

                """

                UPDATE public.adaptive_quizzes

                SET score=%s,

                    passed=%s,

                    status='completed',

                    recommendation=%s,

                    completed_at=%s,

                    dimension_scores_json=%s,

                    diagnosis_json=%s,

                    cognitive_profile=%s,

                    learning_plan_json=%s,

                    total_time_seconds=%s

                WHERE id=%s

                  AND user_id=%s

                  AND status='generated'

                """,

                (

                    score,

                    passed,

                    recommendation,

                    now(),

                    json.dumps(

                        diagnosis.get("dimension_scores", {}),

                        ensure_ascii=False,

                    ),

                    json.dumps(

                        diagnosis,

                        ensure_ascii=False,

                    ),

                    diagnosis.get("profile", ""),

                    json.dumps(

                        diagnosis.get("learning_plan", {}),

                        ensure_ascii=False,

                    ),

                    float(elapsed_seconds or 0),

                    quiz_id,

                    user_id,

                ),

            )

            if cur.rowcount != 1:

                raise ValueError(

                    "No se pudo finalizar el instrumento."

                )



            profile_field = (

                "pretest_estado"

                if quiz_type == "pretest"

                else "posttest_estado"

            )

            cur.execute(

                f"""

                UPDATE public.profiles

                SET {profile_field}='completado'

                WHERE user_id=%s

                """,

                (user_id,),

            )

            if cur.rowcount != 1:

                raise ValueError(

                    "No se pudo actualizar el estado "

                    "de investigacion del estudiante."

                )



        resumable_submit_attempt(

            connection,

            attempt_id=attempt_id,

            user_id=user_id,

            elapsed_seconds=elapsed_seconds,

        )

        connection.commit()



        return (

            score,

            passed,

            weak_topics,

            recommendation,

        )

    except Exception:

        connection.rollback()

        raise

    finally:

        connection.close()



def render_student_adaptive_evaluation(user, prof, topic, subtopic):

    academic_context = academic_context_from_profile(prof)

    is_control_group = str(prof.get("research_group") or "").strip().lower() == "control"



    st.markdown("### Evaluaciones del docente")

    published_assessments = get_student_teacher_assessments(

        academic_context,

        user["id"],

    )



    if not published_assessments:

        st.caption(

            "No hay evaluaciones docentes publicadas para tu contexto academico."

        )



    for assessment in published_assessments:

        if st.button(

            f"Iniciar / continuar: {assessment['title']}",

            key=f"start_teacher_assessment_{assessment['id']}",

        ):

            try:

                bank_quiz = start_teacher_assessment_attempt(

                    user["id"],

                    assessment["id"],

                    academic_context,

                )

                st.session_state["teacher_assessment_quiz_id"] = bank_quiz["id"]



                if bank_quiz.get("status") == "generated":

                    progress = _p5a6_prepare_teacher_attempt(

                        user["id"],

                        bank_quiz,

                        register_resume=True,

                    )

                    st.session_state["teacher_assessment_attempt_id"] = progress["attempt"]["id"]



                st.rerun()

            except Exception as exc:

                st.error(str(exc))



    bank_quiz_id = st.session_state.get("teacher_assessment_quiz_id")



    if bank_quiz_id:

        bank_quiz, bank_questions = load_adaptive_quiz(bank_quiz_id)



        if bank_quiz and bank_quiz.get("status") == "generated":

            try:

                progress = _p5a6_prepare_teacher_attempt(

                    user["id"],

                    bank_quiz,

                    register_resume=False,

                )

            except Exception as exc:

                st.error(f"No fue posible recuperar el intento: {exc}")

                progress = None



            if progress:

                attempt = progress["attempt"]

                attempt_id = attempt["id"]

                st.session_state["teacher_assessment_attempt_id"] = attempt_id



                total_questions = len(bank_questions)

                answered_count = progress["answered_count"]

                pending_count = max(0, total_questions - answered_count)

                server_elapsed_seconds = progress.get(

                    "server_elapsed_seconds",

                    attempt.get("elapsed_seconds", 0),

                )



                ux1, ux2, ux3 = st.columns(3)

                ux1.metric(

                    "Respondidas",

                    f"{answered_count}/{total_questions}",

                )

                ux2.metric("Pendientes", pending_count)

                ux3.metric(

                    "Tiempo registrado",

                    _p5a6_format_elapsed_seconds(server_elapsed_seconds),

                )

                st.progress(answered_count / max(1, total_questions))

                st.caption(

                    "Guardado automático en base de datos. "

                    "Puedes cerrar esta página y continuar más tarde "

                    "sin perder las respuestas confirmadas."

                )



                if attempt.get("resume_count", 0) > 0:

                    st.info(

                        "Intento recuperado correctamente. "

                        f"Reingresos registrados: {int(attempt.get('resume_count') or 0)}."

                    )



                autosave_error = st.session_state.get(

                    "teacher_assessment_autosave_error"

                )

                if autosave_error:

                    st.error(

                        "No se pudo confirmar el guardado automático de la última "

                        f"respuesta. Mantén esta página abierta y vuelve a intentarlo. "

                        f"Detalle: {autosave_error}"

                    )

                elif answered_count:

                    st.success("Guardado automático activo.")



                answers = {}



                for ordinal, question in enumerate(bank_questions, start=1):

                    question_id = question["id"]

                    options = _safe_json_loads(question.get("options_json"), [])

                    question_key = f"adaptive_question:{question_id}"

                    widget_key = f"bank_answer_{question_id}"



                    saved_row = progress["answers_by_key"].get(question_key)

                    saved_value = None

                    if saved_row:

                        payload = saved_row.get("answer_json") or {}

                        if isinstance(payload, dict):

                            saved_value = payload.get("value")



                    if widget_key not in st.session_state and saved_value in options:

                        st.session_state[widget_key] = saved_value



                    position = question.get("position") or ordinal

                    answers[str(question_id)] = st.radio(

                        question["question"],

                        options,

                        index=None,

                        key=widget_key,

                        on_change=_p5a6_autosave_teacher_answer,

                        args=(

                            user["id"],

                            attempt_id,

                            question_id,

                            position,

                            widget_key,

                        ),

                    )

                    if saved_row:

                        st.caption("✓ Respuesta guardada")



                st.caption(

                    "El envío final cerrará el intento y las respuestas "

                    "ya no podrán modificarse."

                )

                if st.button(

                    "Enviar evaluacion del docente",

                    key=f"grade_teacher_assessment_{bank_quiz_id}",

                ):

                    unanswered = [

                        qid

                        for qid, answer in answers.items()

                        if answer is None or str(answer).strip() == ""

                    ]



                    if unanswered:

                        st.warning(

                            "Debes responder todas las preguntas antes de enviar la evaluacion."

                        )

                    else:

                        try:

                            _p5a6_grade_teacher_assessment_resumable(

                                user["id"],

                                bank_quiz_id,

                                attempt_id,

                                answers,

                            )

                            st.session_state["teacher_assessment_autosave_error"] = None

                            st.success("Evaluacion enviada correctamente.")

                            st.rerun()

                        except Exception as exc:

                            st.error(str(exc))



        elif bank_quiz:

            st.info(

                f"Evaluacion completada. Puntaje: {bank_quiz.get('score', 0)}%"

            )



    st.markdown('### 🧠 Evaluación e investigación')
    st.caption('Selecciona evaluación adaptativa, pretest o posttest. Los instrumentos de investigación usan 20 preguntas y quedan vinculados a materia, curso, paralelo y jornada.')



    official_student = is_official_profile(prof)

    if official_student and (not prof.get("consentimiento_informado") or not prof.get("usar_en_investigacion")):

        st.error("Debes completar el consentimiento informado antes de acceder a los instrumentos oficiales.")

        return

    if official_student and str(prof.get("research_group") or "").strip().lower() not in {"experimental", "control"}:

        st.error("El docente debe asignarte al grupo Experimental o Control antes de iniciar el pretest.")

        return



    if official_student and str(prof.get("pretest_estado") or "pendiente").lower() != "completado":

        mode_options = ["Pretest"]

    elif official_student:

        mode_options = ["Pretest", "Posttest"]

    else:

        mode_options = ["Evaluación adaptativa", "Pretest", "Posttest"]

    if st.session_state.get("assessment_mode") not in mode_options:

        st.session_state["assessment_mode"] = mode_options[0]

    mode_label = st.radio(

        "Tipo de evaluación",

        mode_options,

        horizontal=True,

        key="assessment_mode",

    )

    quiz_type = {

        "Evaluación adaptativa": "adaptive",

        "Pretest": "pretest",

        "Posttest": "posttest",

    }[mode_label]

    research_mode = is_research_quiz(quiz_type)

    current_key = f"current_adaptive_quiz_id_{quiz_type}"



    profile = get_student_consultation_profile(user["id"])

    a,b,c = st.columns(3)

    a.markdown(f"<div class='metric'><b>{profile['total_questions']}</b><br><span>Consultas usadas</span></div>", unsafe_allow_html=True)

    b.markdown(f"<div class='metric'><b>{academic_context.get('cohort')}</b><br><span>Cohorte</span></div>", unsafe_allow_html=True)

    last_quiz = get_latest_adaptive_quiz(user["id"], quiz_type)

    completed_pretest = get_latest_adaptive_quiz(user["id"], "pretest")

    last_score = "N/D" if not last_quiz or last_quiz.get("score") is None else f"{float(last_quiz.get('score')):.0f}%"

    c.markdown(f"<div class='metric'><b>{last_score}</b><br><span>{quiz_type_label(quiz_type)}</span></div>", unsafe_allow_html=True)



    plans = pd.DataFrame(get_analytic_plans())

    if not plans.empty and research_mode and "subject" in plans.columns:

        subject_key = strip_accents(academic_context.get("subject", "")).lower()

        filtered_plans = plans[

            plans["subject"].fillna("").astype(str).map(lambda x: strip_accents(x).lower()).isin(["", subject_key])

        ].copy()

        if not filtered_plans.empty:

            plans = filtered_plans

    plan_options = [None] + (plans["id"].tolist() if not plans.empty else [])

    plan_id = st.selectbox(

        "Plan analítico base",

        plan_options,

        format_func=lambda x: "Todos los planes disponibles" if x is None else f"{x} · {plans.loc[plans['id']==x,'title'].iloc[0]}",

    )

    difficulty_key = f"adaptive_difficulty_{quiz_type}"

    difficulty = st.selectbox(

        "Dificultad para la próxima evaluación",

        ADAPTIVE_DIFFICULTIES,

        index=2,

        disabled=research_mode,

        key=difficulty_key,

    )

    difficulty = normalize_adaptive_difficulty(difficulty)

    n_questions = RESEARCH_QUESTION_COUNT if research_mode else st.slider(

        "Número de preguntas", 5, 20, 10, key=f"adaptive_question_count_{quiz_type}"

    )



    if research_mode:

        st.markdown(

            f"<div class='eval-card'><b>{quiz_type_label(quiz_type)} de investigación</b><br>"

            f"<span class='eval-chip'>{academic_context.get('subject')}</span>"

            f"<span class='eval-chip'>{RESEARCH_FOCUS_TOPIC}</span>"

            f"<span class='eval-chip'>{academic_context.get('cohort')}</span>"

            f"<span class='eval-chip'>{RESEARCH_QUESTION_COUNT} preguntas aleatorias</span></div>",

            unsafe_allow_html=True,

        )



    focus_mismatch = research_mode and last_quiz and not quiz_matches_research_focus(last_quiz)

    if focus_mismatch:

        st.warning(

            f"Este {quiz_type_label(quiz_type).lower()} fue generado con un tema anterior "

            f"({last_quiz.get('source_topic') or 'sin tema'}). Debe regenerarse con {RESEARCH_FOCUS_TOPIC}."

        )

        if last_quiz.get("status") == "completed":

            st.error("Este instrumento ya fue completado. Para una prueba limpia usa un estudiante nuevo o elimina ese intento desde Supabase.")

            return

        if st.button(f"Regenerar {quiz_type_label(quiz_type)} con {RESEARCH_FOCUS_TOPIC}", use_container_width=True):

            try:

                with st.spinner(f"Regenerando {quiz_type_label(quiz_type).lower()}..."):

                    supersede_adaptive_quiz(last_quiz["id"])

                    st.session_state.pop(current_key, None)

                    quiz_id = create_adaptive_quiz(

                        user["id"],

                        plan_id=plan_id,

                        difficulty=difficulty,

                        n_questions=n_questions,

                        quiz_type=quiz_type,

                        academic_context=academic_context,

                    )

                st.session_state[current_key] = quiz_id

                st.rerun()

            except Exception as e:

                st.error("No se pudo regenerar el instrumento.")

                st.exception(e)

        return



    if quiz_type == "posttest":

        if not completed_pretest or completed_pretest.get("status") != "completed":

            st.warning("El posttest se habilita cuando el estudiante haya completado el pretest.")

            return

        if is_control_group:

            st.info("Grupo control: continúa con el postest según el protocolo, sin intervención del tutor adaptativo.")

        else:

            render_research_lesson_panel(user, academic_context, completed_pretest)

            render_guided_exercises_panel(user, academic_context, completed_pretest)

            if official_student:

                lesson_ready = research_learning_event_exists(user["id"], "class_generated", completed_pretest["id"])

                exercises_ready = research_learning_event_exists(user["id"], "guided_exercises_completed", completed_pretest["id"])

                if not lesson_ready or not exercises_ready:

                    pending_steps = []

                    if not lesson_ready:

                        pending_steps.append("generar y estudiar la clase de refuerzo")

                    if not exercises_ready:

                        pending_steps.append("completar los ejercicios guiados")

                    st.warning("Antes del posttest debes " + " y ".join(pending_steps) + ".")

                    return



    if not research_mode:

        st.caption("Carga rápida: BunsekiChat usa primero el banco validado local y recurre al generador externo solo cuando faltan ítems adecuados.")



    col1, col2 = st.columns(2)

    can_generate = not (research_mode and last_quiz)

    generate_label = (

        f"Iniciar {quiz_type_label(quiz_type)}"

        if research_mode

        else f"Generar nueva evaluación · {difficulty}"

    )

    if col1.button(generate_label, use_container_width=True, disabled=not can_generate):

        try:

            with st.spinner(f"Generando {quiz_type_label(quiz_type).lower()}..."):

                quiz_id = create_adaptive_quiz(

                        user["id"],

                        plan_id=plan_id,

                        difficulty=difficulty,

                        n_questions=n_questions,

                        quiz_type=quiz_type,

                        academic_context=academic_context,

                    )

            st.session_state[current_key] = quiz_id

            st.success("Instrumento generado. Si no ves las preguntas, baja un poco en la página.")

            time.sleep(0.4)

            st.rerun()

        except Exception as e:

            st.error("No se pudo generar el instrumento. Revisa el detalle técnico o envía esta captura.")

            st.exception(e)

            return

    if research_mode and not can_generate:

        if last_quiz.get("status") == "completed":

            st.info(f"{quiz_type_label(quiz_type)} ya fue completado. El resultado queda registrado para la investigación.")

        else:

            st.info(f"{quiz_type_label(quiz_type)} ya fue generado. Completa el instrumento pendiente para conservar la trazabilidad.")



    if not research_mode and last_quiz and last_quiz.get("status") == "completed" and not last_quiz.get("passed"):

        if col2.button("Reintento automático por no alcanzar 70%", use_container_width=True):

            _, questions = load_adaptive_quiz(last_quiz["id"])

            weak = [q.get("question","") for q in questions if not q.get("is_correct")]

            quiz_id = create_adaptive_quiz(

                user["id"],

                plan_id=last_quiz.get("plan_id"),

                difficulty=difficulty,

                n_questions=n_questions,

                parent_quiz_id=last_quiz["id"],

                retry_focus="\n".join(weak[:5]),

                quiz_type=quiz_type,

                academic_context=academic_context

            )

            st.session_state[current_key] = quiz_id

            st.rerun()



    quiz_id = st.session_state.get(current_key) or (last_quiz.get("id") if last_quiz else None)

    if not quiz_id:

        st.info("Inicia una evaluación para comenzar.")

        return



    quiz, questions = load_adaptive_quiz(int(quiz_id))

    if not quiz or not questions:

        st.warning("No se pudo cargar la evaluación.")

        return



    if not research_mode:

        visible_difficulty = normalize_adaptive_difficulty(

            quiz.get("difficulty") or "Intermedio"

        )

        if visible_difficulty != difficulty:

            st.info(

                f"La evaluación visible fue generada en nivel {visible_difficulty}. "

                f"Has seleccionado {difficulty} para la próxima evaluación. "

                "Pulsa el botón de generación para crear un instrumento nuevo con ese nivel."

            )



    st.markdown(

        f"<div class='eval-card'><h3>{quiz.get('title','Evaluación adaptativa')}</h3>"

        f"<span class='eval-chip'>Intento: {quiz.get('attempt_no',1)}</span>"

        f"<span class='eval-chip'>Tipo: {quiz_type_label(quiz.get('quiz_type') or 'adaptive')}</span>"

        f"<span class='eval-chip'>Dificultad: {quiz.get('difficulty','')}</span>"

        f"<span class='eval-chip'>Estado: {quiz.get('status','')}</span>"

        f"<span class='eval-chip'>Cohorte: {quiz.get('cohort') or academic_context.get('cohort')}</span>"

        f"<span class='eval-chip'>Tema: {quiz.get('source_topic') or 'Adaptativo'}</span></div>",

        unsafe_allow_html=True,

    )



    if not research_mode:

        complexity = adaptive_complexity_evidence(

            questions,

            quiz.get("difficulty") or difficulty,

        )

        st.caption(

            f"Evidencia adaptativa: {complexity['aligned']}/{complexity['total']} preguntas "

            f"alineadas al nivel {normalize_adaptive_difficulty(quiz.get('difficulty') or difficulty)}. "

            f"Bloom: {complexity['distribution']}."

        )

        if complexity["total"] and complexity["aligned"] < complexity["total"]:

            st.warning("Algunas preguntas no coinciden con la demanda cognitiva esperada; genera un nuevo intento antes de usar este resultado como evidencia.")



    if quiz.get("status") == "completed":

        score = float(quiz.get("score") or 0)

        cls = "eval-result-pass" if quiz.get("passed") else "eval-result-fail"

        st.markdown(f"<div class='{cls}'>Resultado: {score:.0f}% · {'Aprobado' if quiz.get('passed') else 'No aprobado'} · {quiz.get('recommendation') or ''}</div>", unsafe_allow_html=True)

        render_quiz_diagnosis(quiz)

        for q in questions:

            icon = "✅" if q.get("is_correct") else "❌"

            st.markdown(f"<div class='eval-question'><b>{icon} {q.get('question')}</b><br><span class='small'>Tu respuesta: {q.get('user_answer') or 'Sin respuesta'} · Correcta: {q.get('correct_answer')}</span><br><br>{q.get('explanation') or ''}</div>", unsafe_allow_html=True)

        if (quiz.get("quiz_type") or quiz_type) == "pretest":

            if is_control_group:

                st.info("Grupo control: espera el momento establecido por el docente para realizar el postest.")

            else:

                render_research_lesson_panel(user, academic_context, quiz)

                render_guided_exercises_panel(user, academic_context, quiz)

        if (quiz.get("quiz_type") or quiz_type) == "posttest":

            if not is_control_group:

                render_final_survey(user, quiz)

        return



    if research_mode:

        session_attempt_key = f"research_attempt_id_{quiz_id}"

        register_resume = (

            session_attempt_key not in st.session_state

        )



        try:

            progress = _p5a6_prepare_research_attempt(

                user["id"],

                quiz,

                register_resume=register_resume,

            )

        except Exception as exc:

            st.error(

                "No fue posible recuperar "

                f"el instrumento: {exc}"

            )

            return



        attempt = progress["attempt"]

        attempt_id = attempt["id"]

        st.session_state[session_attempt_key] = attempt_id



        total_questions = len(questions)

        answered_count = progress["answered_count"]

        pending_count = max(0, total_questions - answered_count)

        server_elapsed_seconds = progress.get(

            "server_elapsed_seconds",

            attempt.get("elapsed_seconds", 0),

        )



        ux1, ux2, ux3 = st.columns(3)

        ux1.metric(

            "Respondidas",

            f"{answered_count}/{total_questions}",

        )

        ux2.metric("Pendientes", pending_count)

        ux3.metric(

            "Tiempo registrado",

            _p5a6_format_elapsed_seconds(server_elapsed_seconds),

        )

        st.progress(

            answered_count / max(1, total_questions)

        )

        st.caption(

            "Guardado automático en base de datos. "

            "Puedes cerrar esta página y continuar más tarde "

            "sin perder las respuestas confirmadas."

        )



        if attempt.get("resume_count", 0) > 0:

            st.info(

                "Intento recuperado correctamente. "

                f"Reingresos registrados: {int(attempt.get('resume_count') or 0)}."

            )



        autosave_error_key = (

            f"research_assessment_autosave_error_{quiz_id}"

        )

        autosave_error = st.session_state.get(

            autosave_error_key

        )

        if autosave_error:

            st.error(

                "No se pudo confirmar el guardado automático de la última "

                f"respuesta. Mantén esta página abierta y vuelve a intentarlo. "

                f"Detalle: {autosave_error}"

            )

        elif answered_count:

            st.success("Guardado automático activo.")



        answers = {}

        for i, q in enumerate(

            questions,

            start=1,

        ):

            opts = (

                q.get("options")

                or _safe_json_loads(

                    q.get("options_json"),

                    [],

                )

            )

            st.markdown(

                f"<div class='eval-question'>"

                f"<b>{i}. {q.get('question')}</b><br>"

                f"<span class='eval-chip'>"

                f"{q.get('bloom_level') or 'Aplicar'}"

                f"</span>"

                f"<span class='eval-chip'>"

                f"{q.get('dimension') or 'Aplicaciones'}"

                f"</span>"

                f"<span class='eval-chip'>"

                f"{q.get('difficulty_level') or 'Medio'}"

                f"</span></div>",

                unsafe_allow_html=True,

            )



            question_id = q["id"]

            question_key = f"adaptive_question:{question_id}"

            widget_key = f"research_q_{quiz_id}_{question_id}"



            saved_row = progress[

                "answers_by_key"

            ].get(question_key)

            saved_value = None

            if saved_row:

                payload = (

                    saved_row.get("answer_json")

                    or {}

                )

                if isinstance(payload, dict):

                    saved_value = payload.get("value")



            if (

                widget_key not in st.session_state

                and saved_value in opts

            ):

                st.session_state[

                    widget_key

                ] = saved_value



            position = q.get("position") or i

            answers[str(question_id)] = st.radio(

                "Selecciona una respuesta",

                opts,

                index=None,

                key=widget_key,

                label_visibility="collapsed",

                on_change=_p5a6_autosave_research_answer,

                args=(

                    user["id"],

                    attempt_id,

                    quiz_id,

                    question_id,

                    position,

                    widget_key,

                ),

            )

            if saved_row:

                st.caption("✓ Respuesta guardada")



        st.caption(

            "El envío final cerrará el intento y las respuestas "

            "ya no podrán modificarse."

        )

        if st.button(

            f"Enviar {quiz_type_label(quiz_type)}",

            key=f"submit_research_quiz_{quiz_id}",

            type="primary",

            use_container_width=True,

        ):

            unanswered = [

                question_id

                for question_id, answer

                in answers.items()

                if answer is None

                or str(answer).strip() == ""

            ]

            if unanswered:

                st.warning(

                    "Debes responder todas las preguntas "

                    "antes de enviar."

                )

            else:

                try:

                    (

                        score,

                        passed,

                        weak_topics,

                        recommendation,

                    ) = _p5a6_grade_research_quiz_resumable(

                        user["id"],

                        int(quiz_id),

                        attempt_id,

                        answers,

                    )



                    event_type = f"{quiz_type}_submitted"

                    gps_actual = capture_browser_gps(

                        user["id"],

                        page="evaluacion_ia",

                        topic=topic,

                        subtopic=subtopic,

                        event_type=event_type,

                        show_status=False,

                    )

                    log_location_event(

                        user["id"],

                        event_type,

                        page="evaluacion_ia",

                        topic=topic,

                        subtopic=subtopic,

                        gps=gps_actual,

                    )



                    if passed:

                        st.success(

                            f"Resultado: {score:.0f}%. "

                            "Aprobaste la evaluacion."

                        )

                    else:

                        st.warning(

                            f"Resultado registrado: "

                            f"{score:.0f}%."

                        )



                    st.rerun()

                except Exception as exc:

                    st.error(str(exc))

        return



    with st.form(f"adaptive_quiz_form_{quiz_id}"):

        timer_key = f"quiz_started_at_{quiz_id}"

        if timer_key not in st.session_state:

            st.session_state[timer_key] = time.time()

        answers = {}

        for i, q in enumerate(questions, start=1):

            opts = q.get("options") or _safe_json_loads(q.get("options_json"), [])

            st.markdown(

                f"<div class='eval-question'><b>{i}. {q.get('question')}</b><br>"

                f"<span class='eval-chip'>{q.get('bloom_level') or 'Aplicar'}</span>"

                f"<span class='eval-chip'>{q.get('dimension') or 'Aplicaciones'}</span>"

                f"<span class='eval-chip'>{q.get('difficulty_level') or quiz.get('difficulty') or 'Intermedio'}</span></div>",

                unsafe_allow_html=True

            )

            answers[str(q["id"])] = st.radio("Selecciona una respuesta", opts, key=f"adaptive_q_{quiz_id}_{q['id']}", label_visibility="collapsed")

        submitted = st.form_submit_button("Calificar evaluación", use_container_width=True)



    if submitted:

        elapsed_seconds = round(time.time() - float(st.session_state.get(f"quiz_started_at_{quiz_id}", time.time())), 2)

        score, passed, weak_topics, recommendation = grade_adaptive_quiz(int(quiz_id), answers, elapsed_seconds=elapsed_seconds)

        event_type = f"{quiz_type}_submitted"

        gps_actual = capture_browser_gps(user['id'], page='evaluacion_ia', topic=topic, subtopic=subtopic, event_type=event_type, show_status=False)

        log_location_event(user['id'], event_type, page='evaluacion_ia', topic=topic, subtopic=subtopic, gps=gps_actual)

        if passed:

            st.success(f"Resultado: {score:.0f}%. Aprobaste la evaluación.")

        else:

            if research_mode:

                st.warning(f"Resultado registrado: {score:.0f}%.")

            else:

                st.warning(f"Resultado: {score:.0f}%. No alcanzaste el 70%. Se habilitó el reintento automático.")

        st.rerun()





# ---------------- Sidebar SaaS PRO por roles ----------------

def sidebar_brand(role_label: str, subtitle: str):

    st.sidebar.markdown(f"""

    <div class='saas-sidebar-brand'>

        <div class='logo-row'>

            <div class='logo-mini'>BΣ</div>

            <div>

                <div class='title'>BunsekiChat</div>

                <div class='subtitle'>{subtitle}</div>

            </div>

        </div>

        <span class='saas-role-chip'>{role_label}</span>

    </div>

    """, unsafe_allow_html=True)



def render_student_sidebar(prof):

    sidebar_brand("Estudiante", "Tutor matemático universitario")

    st.sidebar.markdown("<div class='saas-section-title'>Navegación</div>", unsafe_allow_html=True)

    is_control_group = str(prof.get("research_group") or "").strip().lower() == "control"

    official_student = is_official_profile(prof)

    pretest_completed = str(prof.get("pretest_estado") or "pendiente").strip().lower() == "completado"

    if official_student and not pretest_completed:

        student_pages = ["🧠 Evaluación"]

    elif official_student and is_control_group:

        student_pages = ["🧠 Evaluación", "📈 Mi progreso"]

    else:

        student_pages = ["💬 Tutor", "🧠 Evaluación", "📘 Descargar aprendizaje", "🎮 Prueba de nivel", "📈 Mi progreso"]

    if st.session_state.get("student_nav") not in student_pages:

        st.session_state["student_nav"] = student_pages[0]

    page = st.sidebar.radio(

        "Menú del estudiante",

        student_pages,

        label_visibility="collapsed",

        key="student_nav"

    )

    st.sidebar.markdown(

        "<div class='saas-help-card'><b>¿Buscas el posttest?</b><br>Selecciona <b>Evaluación</b> y luego elige <b>Posttest</b>.</div>",

        unsafe_allow_html=True,

    )

    st.sidebar.markdown("<div class='saas-section-title'>Configuración académica</div>", unsafe_allow_html=True)

    topic = st.sidebar.selectbox("Área", list(TOPICS.keys()), key="student_topic")

    subtopic = st.sidebar.selectbox("Subtema", TOPICS[topic], key="student_subtopic")

    level = prof.get("level") or "Inicial"

    academic_context = academic_context_from_profile(prof)

    st.sidebar.markdown(

        f"<div class='saas-help-card'><b>Nivel actual:</b> {level}<br><b>Materia:</b> {academic_context.get('subject') or 'Sin materia'}<br><b>Curso:</b> {academic_context.get('cohort') or 'Sin curso'}<br><b>Grupo:</b> {prof.get('research_group') or 'Sin asignar'}</div>",

        unsafe_allow_html=True

    )

    st.sidebar.markdown("<div class='saas-section-title'>Sesión</div>", unsafe_allow_html=True)

    if st.sidebar.button("Cerrar sesión", key="student_logout"):

        st.session_state.clear()

        st.rerun()



    def _select_student_page(target_page):

        if target_page in student_pages:

            st.session_state["student_nav"] = target_page



    with st.expander("☰ Navegación alternativa", expanded=False):

        st.caption(

            "Respaldo de navegación. Úsalo solo si el navegador oculta el panel lateral."

        )

        fallback_columns = st.columns(2)

        for idx, page_name in enumerate(student_pages):

            fallback_columns[idx % 2].button(

                page_name,

                key=f"student_nav_fallback_{idx}",

                use_container_width=True,

                on_click=_select_student_page,

                args=(page_name,),

            )



    return page, topic, subtopic, level



TEACHER_NAV_PAGES = (

    "📊 Dashboard docente PRO",

    "👥 Matrícula masiva",

    "📚 Planes analíticos",

    "🧪 Investigación pre/post",

    "📥 Exportación",

    "👤 Seguimiento individual",

    "🧾 Datos completos",

    "⚙️ Configuración",

)





def render_teacher_sidebar():

    # Stable teacher navigation with a main-area fallback.

    current = st.session_state.get("teacher_nav_page", TEACHER_NAV_PAGES[0])

    if current not in TEACHER_NAV_PAGES:

        current = TEACHER_NAV_PAGES[0]

        st.session_state["teacher_nav_page"] = current



    sidebar_brand("Docente / Admin", "Analítica y exportación")

    st.sidebar.markdown(

        "<div class='saas-section-title'>Navegación</div>",

        unsafe_allow_html=True,

    )



    for idx, page_name in enumerate(TEACHER_NAV_PAGES):

        if st.sidebar.button(

            page_name,

            key=f"teacher_nav_btn_{idx}",

            use_container_width=True,

            type="primary" if page_name == current else "secondary",

        ):

            st.session_state["teacher_nav_page"] = page_name

            st.rerun()



    st.sidebar.markdown(

        "<div class='saas-help-card'>Panel exclusivo para rol docente/admin. "

        "Exporta reportes por estudiante en CSV, Word y PDF.</div>",

        unsafe_allow_html=True,

    )

    if st.sidebar.button("Cerrar sesión", key="teacher_logout"):

        st.session_state.clear()

        st.rerun()



    current = st.session_state.get("teacher_nav_page", TEACHER_NAV_PAGES[0])

    current_index = TEACHER_NAV_PAGES.index(current)



    with st.expander("☰ Navegación docente alternativa", expanded=False):

        st.caption(

            "Respaldo de navegación para navegadores que oculten el panel lateral."

        )

        main_choice = st.selectbox(

            "Menú docente alternativo",

            TEACHER_NAV_PAGES,

            index=current_index,

            key=f"teacher_nav_fallback_{current_index}",

            label_visibility="collapsed",

        )

        if main_choice != current:

            st.session_state["teacher_nav_page"] = main_choice

            st.rerun()



    return current





# ---------------- Pages ----------------

def landing_page():
    inject_bunseki_frontend_skin()

    landing_css = """<style>
    .block-container{padding-top:.35rem!important;padding-bottom:.45rem!important}
    .bunseki-home{width:min(1120px,96vw);margin:0 auto}
    .bunseki-home-hero{
        overflow:hidden;
        background:
            radial-gradient(circle at 92% 8%,rgba(224,23,125,.09),transparent 26%),
            radial-gradient(circle at 7% 8%,rgba(42,27,77,.08),transparent 28%),
            linear-gradient(135deg,#fff 0%,#fff8fc 100%);
        border:1px solid rgba(42,27,77,.10);
        border-radius:26px;
        padding:1rem 1.15rem .9rem;
        box-shadow:0 14px 36px rgba(42,27,77,.075);
        text-align:center
    }
    .bunseki-home-logo{width:min(390px,82vw);height:auto;display:block;margin:0 auto .15rem}
    .bunseki-home-kicker{color:#E0177D;font-weight:800;font-size:.76rem;letter-spacing:.075em;text-transform:uppercase;margin:0 0 .25rem}
    .bunseki-home-title{color:#2A1B4D;font-size:clamp(1.75rem,3.7vw,3rem);line-height:1.04;font-weight:850;max-width:900px;margin:0 auto .42rem}
    .bunseki-home-copy{color:#655d78;font-size:clamp(.94rem,1.45vw,1.06rem);line-height:1.48;max-width:790px;margin:0 auto}
    .bunseki-home-grid{width:min(1120px,96vw);margin:.58rem auto 0;display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:.58rem}
    .bunseki-home-card{background:rgba(255,255,255,.96);border:1px solid rgba(42,27,77,.09);border-radius:17px;padding:.68rem .78rem;min-height:98px;box-shadow:0 6px 18px rgba(42,27,77,.035)}
    .bunseki-home-card h3{color:#2A1B4D;font-size:.96rem;margin:0 0 .22rem}
    .bunseki-home-card p{color:#6d657e;font-size:.84rem;line-height:1.38;margin:0}
    .bunseki-home-note{text-align:center;color:#746c85;font-size:.82rem;margin-top:.18rem}
    .bunseki-home-footer{text-align:center;color:#7b738a;font-size:.76rem;line-height:1.3;margin:.28rem auto 0}
    div[data-testid="stButton"] button[kind="primary"]{min-height:45px!important;border-radius:14px!important;font-size:.98rem!important;font-weight:800!important}
    @media(max-width:760px){
        .bunseki-home{width:94vw}
        .bunseki-home-hero{border-radius:21px;padding:.72rem}
        .bunseki-home-logo{width:min(295px,80vw)}
        .bunseki-home-title{font-size:1.6rem}
        .bunseki-home-copy{font-size:.9rem}
        .bunseki-home-grid{width:94vw;grid-template-columns:1fr;gap:.42rem;margin-top:.42rem}
        .bunseki-home-card{min-height:auto;padding:.58rem .68rem}
    }
    </style>"""

    hero_html = (
        '<main class="bunseki-home">'
        '<section class="bunseki-home-hero">'
        f'<img class="bunseki-home-logo" src="{BUNSEKI_FRONTEND_LOGO_DATA_URI}" alt="BunsekiChat">'
        '<div class="bunseki-home-kicker">Aprendizaje matemático personalizado</div>'
        '<div class="bunseki-home-title">Comprende, practica y demuestra tu progreso.</div>'
        '<div class="bunseki-home-copy">'
        'BunsekiChat acompaña tu aprendizaje en matemáticas universitarias, '
        'adapta la práctica a tu nivel y convierte cada intento en '
        'retroalimentación útil para seguir avanzando.'
        '</div>'
        '</section>'
        '</main>'
    )

    cards_html = (
        '<section class="bunseki-home-grid">'
        '<article class="bunseki-home-card">'
        '<h3>💬 Aprende con acompañamiento</h3>'
        '<p>Consulta conceptos, ejercicios y procedimientos con explicaciones orientadas a comprender el razonamiento.</p>'
        '</article>'
        '<article class="bunseki-home-card">'
        '<h3>🎯 Practica a tu nivel</h3>'
        '<p>Elige tema y dificultad o usa el modo recomendado para adaptar progresivamente la práctica a tu desempeño.</p>'
        '</article>'
        '<article class="bunseki-home-card">'
        '<h3>📈 Comprueba tu avance</h3>'
        '<p>Recibe retroalimentación, identifica qué reforzar y conserva evidencia de evaluaciones y progreso.</p>'
        '</article>'
        '</section>'
    )

    st.markdown(landing_css, unsafe_allow_html=True)
    st.markdown(hero_html, unsafe_allow_html=True)
    st.markdown(cards_html, unsafe_allow_html=True)

    _left, _center, _right = st.columns([1.15, 1.7, 1.15])
    with _center:
        if st.button(
            "Comenzar con BunsekiChat",
            key="landing_start",
            type="primary",
            use_container_width=True,
        ):
            st.session_state["landing_entered"] = True
            st.rerun()

    st.markdown(
        '<div class="bunseki-home-note">Accede con tu cuenta institucional o registrada.</div>'
        '<div class="bunseki-home-footer">'
        'Formación inteligente para un mundo en transformación · '
        'práctica, retroalimentación y trazabilidad del aprendizaje.'
        '</div>',
        unsafe_allow_html=True,
    )




def login_page():

    inject_bunseki_frontend_skin()
    # BUNSEKI_LOGIN_LOGO_FINAL_CLOSE_V2
    _bunseki_login_logo_html = (
        '<div class="bunseki-login-logo-final-wrap">'
        f'<img class="bunseki-login-logo-final" '
        f'src="{BUNSEKI_FRONTEND_LOGO_DATA_URI}" '
        'alt="BunsekiChat brand">'
        '</div>'
    )
    st.markdown(
        """<style>
        .bunseki-login-logo-final-wrap{
            width:100%;
            display:flex;
            align-items:center;
            justify-content:center;
            margin:.05rem auto .65rem;
            padding:0;
        }
        img.bunseki-login-logo-final,
        .login-card-premium img.bunseki-login-logo-final{
            display:block!important;
            visibility:visible!important;
            opacity:1!important;
            width:min(390px,90vw)!important;
            max-width:100%!important;
            height:auto!important;
            object-fit:contain!important;
            margin:0 auto!important;
            padding:0!important;
        }
        .bunseki-login-classic-mark,
        .bunseki-login-mark,
        .login-card-premium .bunseki-login-mark,
        .bunseki-login-name,
        .login-card-premium .bunseki-login-name,
        .login-card-premium .logo-orb{
            display:none!important;
        }
        @media(max-width:640px){
            img.bunseki-login-logo-final,
            .login-card-premium img.bunseki-login-logo-final{
                width:min(330px,92vw)!important;
            }
        }
        </style>""",
        unsafe_allow_html=True,
    )
    st.markdown(_bunseki_login_logo_html, unsafe_allow_html=True)


    # BUNSEKI_LOGIN_VISUAL_FINAL_V2
    st.markdown(
        """<style>
        .block-container{padding-top:.35rem!important}
        .logo-orb,
        .bunseki-brand-logo,
        .login-logo,
        img[alt="BunsekiChat"],
        div[data-testid="stImage"]{
            display:none!important
        }
        .bunseki-login-classic-mark{
            width:62px;height:62px;
            margin:.1rem auto .5rem;
            border-radius:17px;
            background:#2A1B4D;
            color:#fff;
            display:flex;
            align-items:center;
            justify-content:center;
            font-size:1.42rem;
            font-weight:900;
            line-height:1;
            letter-spacing:-.06em;
            box-shadow:0 8px 22px rgba(42,27,77,.16)
        }
        .login-title{
            display:block!important;
            color:#E0177D!important;
            font-size:1.72rem!important;
            line-height:1.1!important;
            font-weight:850!important;
            margin:.05rem 0 .35rem!important;
            text-align:center!important
        }
        .login-subtitle{
            color:#655d78!important;
            font-size:.93rem!important;
            line-height:1.45!important;
            max-width:25rem!important;
            margin:0 auto .72rem!important;
            text-align:center!important
        }
        @media(max-width:640px){
            .bunseki-login-classic-mark{width:56px;height:56px;font-size:1.28rem}
            .login-title{font-size:1.52rem!important}
        }
        </style>
        """,
        unsafe_allow_html=True,
    )


    # BUNSEKI_LOGIN_CLASSIC_FINAL
    st.markdown(
        """
        <style>
        /* Login clásico: la marca horizontal nueva queda exclusivamente en la portada */
        .login-card-premium img,
        .login-card-premium .bunseki-brand-logo,
        .login-card-premium .bunseki-brand-compact,
        .login-card-premium .login-logo,
        .login-card-premium [class*="wordmark"] {
            display: none !important;
        }

        .login-card-premium .logo-orb {
            display: none !important;
        }

        .login-card-premium {
            width: min(500px, 94vw) !important;
            max-width: 500px !important;
            margin: .45rem auto .7rem !important;
            padding: .95rem 1rem .9rem !important;
            border-radius: 24px !important;
        }

        .login-card-premium .login-title {
            display: block !important;
            color: #E0177D !important;
            font-size: 1.72rem !important;
            line-height: 1.1 !important;
            font-weight: 850 !important;
            margin: 0 0 .35rem !important;
            text-align: center !important;
        }

        .login-card-premium .login-title::before {
            content: "BΣ";
            width: 62px;
            height: 62px;
            margin: 0 auto .48rem;
            border-radius: 17px;
            background: #2A1B4D;
            color: #ffffff;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.42rem;
            font-weight: 900;
            line-height: 1;
            letter-spacing: -.06em;
            box-shadow: 0 8px 22px rgba(42,27,77,.16);
        }

        .login-card-premium .login-subtitle {
            display: block !important;
            color: #655d78 !important;
            font-size: .93rem !important;
            line-height: 1.45 !important;
            max-width: 25rem !important;
            margin: 0 auto .72rem !important;
            text-align: center !important;
        }

        .login-card-premium [data-baseweb="tab-list"] {
            margin-top: .15rem !important;
        }

        @media (max-width: 640px) {
            .login-card-premium {
                width: 94vw !important;
                max-width: 94vw !important;
                margin-top: .25rem !important;
                padding: .78rem .78rem .8rem !important;
            }

            .login-card-premium .login-title {
                font-size: 1.52rem !important;
            }

            .login-card-premium .login-title::before {
                width: 56px;
                height: 56px;
                font-size: 1.28rem;
            }
        }
        </style>
        """,
        unsafe_allow_html=True,
    )

    st.markdown(

        """

        <style>

        /* LOGIN BUNSEKICHAT · identidad analítica + conversación */

        html, body, [class*="css"]{

            font-family:"Poppins","Segoe UI",Arial,sans-serif !important;

        }

        .stApp{

            background:

                radial-gradient(circle at 15% 10%, rgba(224,23,125,.08), transparent 34%),

                radial-gradient(circle at 88% 88%, rgba(255,200,87,.10), transparent 28%),

                #f8f6fb !important;

        }

        .block-container{

            width:min(92vw, 470px) !important;

            max-width:470px !important;

            margin:clamp(24px,7vh,72px) auto 28px !important;

            padding:30px clamp(20px,5vw,34px) 24px !important;

            background:#ffffff !important;

            border:1px solid rgba(42,27,77,.16) !important;

            border-radius:26px !important;

            box-shadow:none !important;

        }

        header[data-testid="stHeader"]{

            height:0 !important; min-height:0 !important;

            pointer-events:none !important; background:transparent !important;

        }

        .bunseki-login-brand{ text-align:center; margin:0 0 18px; }

        .bunseki-login-mark{

            width:78px; height:78px; margin:0 auto 12px; display:block;

        }

        .bunseki-login-name{

            color:#2A1B4D !important; font-size:2rem; line-height:1; font-weight:800;

            letter-spacing:-.04em; margin:0;

        }

        .bunseki-login-name span{ color:#E0177D !important; }

        .bunseki-login-subtitle{

            color:#625a72; font-size:.91rem; line-height:1.5; margin:10px auto 2px;

            max-width:340px;

        }

        div[data-testid="stTabs"] [data-baseweb="tab-list"]{

            gap:4px; border-bottom:1px solid #eee8f2;

        }

        div[data-testid="stTabs"] button[role="tab"]{

            color:#655c74 !important; font-weight:700 !important;

            border-bottom:3px solid transparent !important;

        }

        div[data-testid="stTabs"] button[role="tab"][aria-selected="true"]{

            color:#E0177D !important; border-bottom-color:#E0177D !important;

        }

        div[data-testid="stTabs"] [data-baseweb="tab-highlight"]{

            background:#E0177D !important;

        }

        .stTextInput label{ color:#2A1B4D !important; font-weight:700 !important; }

        .stTextInput input{

            height:46px !important; min-height:46px !important;

            border-radius:12px !important; background:#fbfafc !important;

            border:1px solid #d8d1df !important; color:#2A1B4D !important;

            padding:0 14px !important;

        }

        .stTextInput input:focus{

            border-color:#E0177D !important;

            box-shadow:0 0 0 3px rgba(224,23,125,.12) !important;

        }

        .stButton > button{

            height:44px !important; min-height:44px !important; border-radius:12px !important;

            background:#E0177D !important; color:#fff !important; border:0 !important;

            font-weight:800 !important; box-shadow:none !important;

        }

        .stButton > button:hover{ background:#c81370 !important; }

        .bunseki-login-help{

            color:#746c80; text-align:center; font-size:.78rem; line-height:1.45;

            margin:15px 0 0;

        }

        .bunseki-login-signature{

            color:#8a8293; text-align:center; font-size:.72rem; margin-top:8px;

        }

        @media(max-width:560px){

            .block-container{

                width:calc(100vw - 24px) !important; margin:12px auto !important;

                padding:24px 18px 20px !important; border-radius:20px !important;

            }

            .bunseki-login-name{font-size:1.72rem;}

            .bunseki-login-mark{width:68px;height:68px;}

        }


        .bunseki-login-wordmark-dark{
            color:#2A1B4D !important;
        }

        .bunseki-login-wordmark-accent{
            color:#E0177D !important;
        }

        </style>

        """,
        unsafe_allow_html=True,
    )

    # Runtime-safe login brand:
    # use the certified frontend logo as IMG instead of inline indented SVG.
    st.caption("Aprendizaje adaptativo con trazabilidad académica.")



    tab1, tab2 = st.tabs(["Ingresar", "Registrarse"])



    with tab1:

        u = st.text_input(

            "Usuario",

            key="lu",

            placeholder="tu usuario o correo",

        )

        p = st.text_input(

            "Contraseña",

            type="password",

            key="lp",

            placeholder="tu contraseña",

        )

        if st.button("Ingresar", use_container_width=True, key="login_submit"):

            user = authenticate(u, p)

            if user:

                st.session_state.user = user

                st.session_state.last_activity = datetime.now()

                st.rerun()

            else:

                st.error("Credenciales inválidas o usuario inactivo.")



    with tab2:

        u = st.text_input(

            "Nuevo usuario",

            key="ru",

            placeholder="elige un usuario",

        )

        p = st.text_input(

            "Nueva contraseña",

            type="password",

            key="rp",

            placeholder="mínimo 6 caracteres",

        )

        if st.button("Crear cuenta", use_container_width=True, key="register_submit"):

            if len(u) < 4 or len(p) < 6:

                st.warning("El usuario requiere 4 caracteres y la contraseña al menos 6.")

            elif user_by_username(u):

                st.warning("Ese usuario ya existe.")

            else:

                create_user(u, p)

                st.success("Cuenta creada. Ya puedes ingresar.")



    st.caption("Acceso académico seguro · Tus avances se conservan en tu cuenta.")
    st.caption("BunsekiChat · Christian Tomalá")





def render_official_first_login(user, prof):

    sidebar_brand("Estudiante oficial", "Activación de cuenta")

    if st.sidebar.button("Cerrar sesión", key="official_onboarding_logout"):

        st.session_state.clear()

        st.rerun()



    st.markdown(

        "<div class='hero'>" + bunseki_logo_html(

            "Activación de cuenta oficial",

            "Confirma tus datos, acepta el consentimiento y protege tu cuenta antes del pretest",

        ) + "</div>",

        unsafe_allow_html=True,

    )

    st.info(

        f"Cohorte: {prof.get('cohort') or OFFICIAL_COHORT_CODE} · "

        f"Grupo: {prof.get('research_group') or 'Sin asignar'} · "

        "Tus resultados no ingresarán al análisis hasta completar este proceso."

    )



    with st.form("official_first_login_form"):

        st.markdown("### 1. Confirma tus datos")

        c1, c2 = st.columns(2)

        with c1:

            cedula = st.text_input("Cédula / usuario", value=str(prof.get("cedula") or user.get("username") or ""), disabled=True)

            first_names = st.text_input("Nombres", value=str(prof.get("first_names") or ""))

            last_names = st.text_input("Apellidos", value=str(prof.get("last_names") or ""))

            correo = st.text_input("Correo", value=str(prof.get("correo") or ""))

        with c2:

            subject = st.text_input("Materia", value=str(prof.get("subject") or "Cálculo I"))

            course_level = st.text_input("Curso / nivel", value=str(prof.get("course_level") or "1"))

            parallel = st.text_input("Paralelo", value=str(prof.get("parallel") or ""))

            shift = st.selectbox(

                "Jornada",

                ACADEMIC_SHIFTS,

                index=ACADEMIC_SHIFTS.index(normalize_shift(prof.get("shift"))) if normalize_shift(prof.get("shift")) in ACADEMIC_SHIFTS else 0,

            )

            st.text_input("Cohorte de investigación", value=str(prof.get("cohort") or OFFICIAL_COHORT_CODE), disabled=True)

            st.text_input("Grupo de investigación", value=str(prof.get("research_group") or "Sin asignar"), disabled=True)

        st.caption("La cédula, cohorte y grupo son controlados por el docente. Si alguno es incorrecto, no continúes y comunícalo al responsable de la investigación.")



        st.markdown("### 2. Consentimiento informado")

        st.markdown(INFORMED_CONSENT_TEXT)

        consent = st.checkbox(

            "He leído la información y acepto voluntariamente participar en esta investigación.",

            value=False,

        )



        st.markdown("### 3. Cambia la contraseña temporal")

        p1, p2 = st.columns(2)

        new_password = p1.text_input("Nueva contraseña", type="password", help="Mínimo 8 caracteres, con letras, número y símbolo.")

        confirm_password = p2.text_input("Confirmar nueva contraseña", type="password")

        submitted = st.form_submit_button("Confirmar datos y activar cuenta", use_container_width=True)



    if not submitted:

        return



    clean_names = normalize_person_name(first_names)

    clean_surnames = normalize_person_name(last_names)

    clean_email = str(correo or "").strip().lower()

    errors = []

    if not clean_names or not clean_surnames:

        errors.append("Completa nombres y apellidos oficiales.")

    if not re.match(r"^[^\s@]+@[^\s@]+\.[^\s@]+$", clean_email):

        errors.append("Ingresa un correo válido.")

    duplicate_email = fetchone(

        "SELECT user_id FROM profiles WHERE LOWER(COALESCE(correo,''))=%s AND user_id<>%s LIMIT 1",

        (clean_email, user["id"]),

    )

    if duplicate_email:

        errors.append("El correo ya pertenece a otro estudiante.")

    if not subject.strip() or not course_level.strip() or not parallel.strip() or not shift:

        errors.append("Completa materia, curso, paralelo y jornada.")

    if not consent:

        errors.append("Debes aceptar el consentimiento para participar y acceder al pretest oficial.")

    if len(new_password) < 8 or not re.search(r"[A-Za-z]", new_password) or not re.search(r"\d", new_password) or not re.search(r"[^A-Za-z0-9]", new_password):

        errors.append("La nueva contraseña debe tener al menos 8 caracteres, letras, número y símbolo.")

    if new_password != confirm_password:

        errors.append("Las contraseñas no coinciden.")

    if new_password and verify_password(new_password, user.get("password_hash") or ""):

        errors.append("La nueva contraseña debe ser diferente de la temporal.")

    if errors:

        for error in errors:

            st.error(error)

        return



    clean_subject = normalize_academic_text(subject)

    clean_course = normalize_course_level(course_level)

    clean_parallel = normalize_parallel(parallel)

    clean_shift = normalize_shift(shift)

    course_label = build_course_label(clean_course, clean_parallel, clean_shift)

    consent_date = now()

    c = conn()

    c.autocommit = False

    try:

        with c.cursor() as cur:

            cur.execute("""

                UPDATE profiles SET

                    first_names=%s,last_names=%s,correo=%s,subject=%s,course_level=%s,parallel=%s,shift=%s,

                    course=%s,full_name_normalized=%s,profile_completed=1,consentimiento_informado=TRUE,

                    fecha_consentimiento=%s,usar_en_investigacion=TRUE,pretest_estado=COALESCE(NULLIF(pretest_estado,''),'pendiente'),

                    posttest_estado=COALESCE(NULLIF(posttest_estado,''),'pendiente')

                WHERE user_id=%s

            """, (

                clean_names, clean_surnames, clean_email, clean_subject, clean_course, clean_parallel, clean_shift,

                course_label, normalize_spaces(f"{clean_names} {clean_surnames}"), consent_date, user["id"],

            ))

            cur.execute("""

                UPDATE users SET password_hash=%s,password_temporal=FALSE,primer_ingreso=FALSE WHERE id=%s

            """, (hash_password(new_password), user["id"]))

        c.commit()

    except Exception:

        c.rollback()

        raise

    finally:

        c.close()



    st.session_state.user = get_user(user["id"])

    st.session_state["student_nav"] = "🧠 Evaluación"

    st.session_state["assessment_mode"] = "Pretest"

    st.success("Cuenta activada. Serás dirigido al pretest oficial.")

    st.rerun()



def profile_page(user):

    prof=get_profile(user['id'])

    academic_defaults = academic_context_from_profile(prof)

    gps_actual = capture_browser_gps(user['id'], page='Ficha inicial', event_type='app_open', show_status=True)

    st.markdown("<div class='card'><h3>Ficha inicial del estudiante</h3><p class='small'>Registra datos oficiales. La app normaliza nombres, apellidos, materia, curso, paralelo y jornada para la investigación y los reportes.</p></div>", unsafe_allow_html=True)

    with st.form("perfil"):

        c1,c2,c3=st.columns(3)

        data={}

        with c1:

            data['first_names']=st.text_input("Nombres", prof.get('first_names') or '')

            data['last_names']=st.text_input("Apellidos", prof.get('last_names') or '')

            data['birth_date']=str(st.date_input("Fecha de nacimiento", value=birth_date_value(prof.get('birth_date'))))

            data['subject']=academic_selectbox("Materia", ACADEMIC_SUBJECTS, academic_defaults["subject"], "profile_subject")

        with c2:

            data['course_level']=academic_selectbox("Curso / nivel", ACADEMIC_COURSE_LEVELS, academic_defaults["course_level"], "profile_course_level", normalize_course_level)

            data['parallel']=academic_selectbox("Paralelo", ACADEMIC_PARALLELS, academic_defaults["parallel"], "profile_parallel", normalize_parallel)

            data['shift']=academic_selectbox("Jornada", ACADEMIC_SHIFTS, academic_defaults["shift"], "profile_shift", normalize_shift)

            data['teacher']=st.text_input("Docente", prof.get('teacher') or '')

            data['phone']=st.text_input("Número celular", prof.get('phone') or '')

        with c3:

            data['province']=st.text_input("Provincia", prof.get('province') or '')

            data['city']=st.text_input("Ciudad", prof.get('city') or '')

            data['canton']=st.text_input("Cantón", prof.get('canton') or '')

            data['address']=st.text_area("Dirección", prof.get('address') or '')

            auto_lat = (gps_actual or {}).get('lat') if gps_actual else None

            auto_lon = (gps_actual or {}).get('lon') if gps_actual else None

            data['gps_lat']=st.number_input("GPS latitud", value=float(auto_lat if auto_lat is not None else (prof.get('gps_lat') or 0.0)), format="%.6f")

            data['gps_lon']=st.number_input("GPS longitud", value=float(auto_lon if auto_lon is not None else (prof.get('gps_lon') or 0.0)), format="%.6f")

            data['gps_accuracy']=str((gps_actual or {}).get('accuracy') or prof.get('gps_accuracy') or '')

        submitted=st.form_submit_button("Guardar ficha")

        if submitted:

            data['first_names'] = normalize_person_name(data.get('first_names'))

            data['last_names'] = normalize_person_name(data.get('last_names'))

            data['subject'] = normalize_academic_text(data.get('subject'))

            data['course_level'] = normalize_course_level(data.get('course_level'))

            data['parallel'] = normalize_parallel(data.get('parallel'))

            data['shift'] = normalize_shift(data.get('shift'))

            data['cohort'] = build_course_label(data['course_level'], data['parallel'], data['shift'])

            data['course'] = data['cohort']

            data['full_name_normalized'] = normalize_spaces(f"{data['first_names']} {data['last_names']}")

            if not data['first_names'] or not data['last_names']:

                st.warning("Ingresa nombres y apellidos oficiales del estudiante.")

            elif not data['subject'] or not data['course_level'] or not data['parallel'] or not data['shift']:

                st.warning("Completa materia, curso, paralelo y jornada.")

            else:

                data['profile_completed']=1

                update_profile(user['id'], data)

                st.success(f"Ficha guardada: {data['full_name_normalized']} · {data['subject']} · {data['cohort']}")

                st.rerun()

    st.info("GPS automático activado con consentimiento del navegador. En producción debe ejecutarse en HTTPS; en desarrollo funciona en localhost.")



def student_page(user):
    inject_student_layout_cleanup_v2()

    inject_bunseki_frontend_skin()
    prof=get_profile(user['id'])

    if is_official_profile(prof) and not official_profile_ready(user, prof):

        return render_official_first_login(user, prof)

    if not prof.get('profile_completed'): return profile_page(user)

    academic_context = academic_context_from_profile(prof)

    hero_html = (

        "<div class='hero'>"

        + bunseki_logo_html(APP_NAME, 'Bienvenido/a, ' + str(prof.get('first_names') or '') + ' · ' + academic_context.get('subject','Matemáticas universitarias'))

        + f"<div style='margin-top:12px'><span class='badge'>Nivel: {prof.get('level','Inicial')}</span> <span class='badge'>{academic_context.get('cohort','Sin curso')}</span></div>"

        + "</div>"

    )

    st.markdown(hero_html, unsafe_allow_html=True)



    pretest_completed = str(prof.get("pretest_estado") or "pendiente").strip().lower() == "completado"

    quick_assessment_mode = "Posttest" if pretest_completed else "Pretest"

    st.markdown(

        f"<div class='saas-help-card'><b>Acceso rápido a la evaluación</b><br>"

        f"Pulsa el botón para abrir directamente el <b>{quick_assessment_mode}</b>.</div>",

        unsafe_allow_html=True,

    )

    if st.button(

        f"Abrir {quick_assessment_mode}",

        key="student_assessment_shortcut",

        type="primary",

        use_container_width=True,

    ):

        st.session_state["student_nav"] = "🧠 Evaluación"

        st.session_state["assessment_mode"] = quick_assessment_mode



    page, topic, subtopic, level = render_student_sidebar(prof)

    gps_actual = capture_browser_gps(user['id'], page=page, topic=topic, subtopic=subtopic, event_type='app_open', show_status=False)

    rows=interactions(user['id']); user_q=[r for r in rows if r['role']=='user']

    m1,m2,m3,m4=st.columns(4)

    m1.markdown(f"<div class='metric'><b>{len(user_q)}</b><br><span>Consultas</span></div>", unsafe_allow_html=True)

    m2.markdown(f"<div class='metric'><b>{len(set([r['topic'] for r in user_q if r['topic']]))}</b><br><span>Áreas trabajadas</span></div>", unsafe_allow_html=True)

    m3.markdown(f"<div class='metric'><b>{sum(r['tokens_est'] for r in rows)}</b><br><span>Tokens estimados</span></div>", unsafe_allow_html=True)

    m4.markdown(f"<div class='metric'><b>{level}</b><br><span>Nivel actual</span></div>", unsafe_allow_html=True)



    if page == "🧠 Evaluación":

        render_student_adaptive_evaluation(user, prof, topic, subtopic)

        return



    if page == "📈 Mi progreso":

        st.markdown("<div class='card'><h3>📈 Mi progreso académico</h3><p class='small'>Resumen de tus consultas, áreas trabajadas y evolución.</p></div>", unsafe_allow_html=True)

        if user_q:

            df_prog = pd.DataFrame(user_q)

            c1, c2 = st.columns(2)

            c1.plotly_chart(px.histogram(df_prog, x='topic', title='Mis consultas por área'), use_container_width=True)

            c2.plotly_chart(px.histogram(df_prog, x='created_at', title='Mi actividad en el tiempo'), use_container_width=True)

            st.dataframe(df_prog, use_container_width=True, height=320)

        else:

            st.info("Aún no tienes consultas registradas.")

        return



    # Descarga individual para estudiantes: evidencia de lo investigado y aprendido

    st.markdown("### 📘 Descargar mi aprendizaje")
    st.caption("Genera un reporte personal con tus consultas, respuestas recibidas, temas trabajados y trazabilidad académica.")

    exp1, exp2, exp3 = st.columns(3)

    profile_export, report_df_export = build_student_summary(user['id'])

    exp1.download_button(

        "📊 Descargar CSV",

        report_df_export.to_csv(index=False).encode("utf-8-sig"),

        f"mi_aprendizaje_{profile_export.get('username','estudiante')}.csv",

        "text/csv",

        use_container_width=True,

    )

    try:

        exp2.download_button(

            "📝 Descargar Word",

            export_student_docx(user['id']),

            f"mi_aprendizaje_{profile_export.get('username','estudiante')}.docx",

            "application/vnd.openxmlformats-officedocument.wordprocessingml.document",

            use_container_width=True,

        )

    except Exception as e:

        exp2.warning(f"Word no disponible: {e}")

    try:

        exp3.download_button(

            "📄 Descargar PDF",

            export_student_pdf(user['id']),

            f"mi_aprendizaje_{profile_export.get('username','estudiante')}.pdf",

            "application/pdf",

            use_container_width=True,

        )

    except Exception as e:

        exp3.warning(f"PDF no disponible: {e}")



    if page == "📘 Descargar aprendizaje":

        st.markdown("<div class='card'><h3>Vista previa de mi aprendizaje</h3></div>", unsafe_allow_html=True)

        st.dataframe(report_df_export, use_container_width=True, height=360)

        return



    left,right=st.columns([1.5,1])

    with left:

        st.markdown("<div class='card'><h3>💬 Tutor matemático</h3><p class='small'>Sugerencias para preguntar mejor:</p></div>", unsafe_allow_html=True)

        with st.expander("💡 Ver sugerencias de prompt", expanded=False):

            for sug in prompt_suggestions(topic, subtopic, level):

                st.markdown(f"- {sug}")

        for r in rows[-12:]: render_message(r['role'], r['clean_message'] or r['message'])

        q=st.chat_input("Escribe tu pregunta de matemáticas...")

        if q:

            if contains_bad_language(q):

                warning = "Tu consulta contiene lenguaje inapropiado. Reformúlala con respeto para poder ayudarte mejor."

                gps_actual = capture_browser_gps(user['id'], page=page, topic=topic, subtopic=subtopic, event_type='interaction', show_status=False)

                log_location_event(user['id'], 'chat_message_blocked', page=page, topic=topic, subtopic=subtopic, gps=gps_actual)

                log_interaction(user['id'],'user',q,topic,subtopic,level,gps=gps_actual)

                log_interaction(user['id'],'assistant',warning,topic,subtopic,level,"moderacion",0,gps=gps_actual)

                st.rerun()

            gps_actual = capture_browser_gps(user['id'], page=page, topic=topic, subtopic=subtopic, event_type='interaction', show_status=False)

            log_location_event(user['id'], 'chat_message', page=page, topic=topic, subtopic=subtopic, gps=gps_actual)

            log_interaction(user['id'],'user',q,topic,subtopic,level,gps=gps_actual)

            ans,model,ms=ask_ai(q,topic,subtopic,level,prof)

            log_interaction(user['id'],'assistant',ans,topic,subtopic,level,model,ms,gps=gps_actual)

            st.rerun()

    with right:

        st.markdown(

            "<div class='card'><h3>🎯 Configura tu práctica</h3>"

            "<p class='small'>Elige el área que quieres reforzar. En <b>Recomendado</b>, BunsekiChat ajusta la dificultad según tu desempeño; también puedes escoger un nivel manualmente.</p></div>",

            unsafe_allow_html=True,

        )



        practice_topics = list(TOPICS.keys())

        default_practice_topic = topic if topic in practice_topics else practice_topics[0]

        topic_state_key = f"adaptive_practice_topic_{user['id']}"

        # BUNSEKI_R8_21_4B_DURABLE_SELECTOR_STATE
        topic_value_key = f"adaptive_practice_topic_value_{user['id']}"
        persisted_topic = st.session_state.get(topic_value_key)
        if (
            st.session_state.get(topic_state_key) not in practice_topics
            and persisted_topic in practice_topics
        ):
            st.session_state[topic_state_key] = persisted_topic

        if st.session_state.get(topic_state_key) not in practice_topics:

            st.session_state[topic_state_key] = default_practice_topic

        practice_topic = st.selectbox(

            "Tema de práctica",

            practice_topics,

            key=topic_state_key,

            help="Puedes cambiar el área de práctica sin modificar tu materia, pretest o nivel oficial.",

        )

        st.session_state[topic_value_key] = practice_topic

        practice_subtopics = list(TOPICS.get(practice_topic, []))
        if not practice_subtopics:
            practice_subtopics = [practice_topic]

        default_practice_subtopic = (
            subtopic
            if subtopic in practice_subtopics
            else practice_subtopics[0]
        )

        subtopic_state_key = (
            f"adaptive_practice_subtopic_{user['id']}_"
            f"{strip_accents(practice_topic).lower()}"
        )

        subtopic_value_key = (
            f"adaptive_practice_subtopic_value_{user['id']}_"
            f"{strip_accents(practice_topic).lower()}"
        )
        persisted_subtopic = st.session_state.get(subtopic_value_key)
        if (
            st.session_state.get(subtopic_state_key) not in practice_subtopics
            and persisted_subtopic in practice_subtopics
        ):
            st.session_state[subtopic_state_key] = persisted_subtopic

        if st.session_state.get(subtopic_state_key) not in practice_subtopics:
            st.session_state[subtopic_state_key] = default_practice_subtopic

        practice_subtopic = st.selectbox(
            "Subtema de práctica",
            practice_subtopics,
            key=subtopic_state_key,
            help=(
                "El subtema permite focalizar la práctica. "
                "En Matemáticas básicas puedes elegir Álgebra elemental, "
                "Trigonometría o Geometría analítica."
            ),
        )

        st.session_state[subtopic_value_key] = practice_subtopic

        subtopic = practice_subtopic



        level_choices = ["Recomendado", *ADAPTIVE_DIFFICULTIES]

        level_state_key = (
            f"adaptive_practice_level_choice_{user['id']}_"
            f"{strip_accents(practice_topic).lower()}_"
            f"{strip_accents(practice_subtopic).lower()}"
        )

        level_value_key = (
            f"adaptive_practice_level_value_{user['id']}_"
            f"{strip_accents(practice_topic).lower()}_"
            f"{strip_accents(practice_subtopic).lower()}"
        )
        persisted_level = st.session_state.get(level_value_key)
        if (
            st.session_state.get(level_state_key) not in level_choices
            and persisted_level in level_choices
        ):
            st.session_state[level_state_key] = persisted_level

        if st.session_state.get(level_state_key) not in level_choices:

            st.session_state[level_state_key] = "Recomendado"

        practice_level_choice = st.selectbox(

            "Nivel de práctica",

            level_choices,

            key=level_state_key,

            help="Recomendado parte de tu nivel actual y se ajusta con tus resultados. Los otros niveles permiten práctica libre.",

                                            format_func=lambda value: PRACTICE_LEVEL_LABELS.get(value, value),
)

        st.session_state[level_value_key] = practice_level_choice
        st.caption(PRACTICE_LEVEL_HELP.get(practice_level_choice, ""))



        practice_key = (
            f"adaptive_practice_state_{user['id']}_"
            f"{strip_accents(practice_topic).lower()}_"
            f"{strip_accents(practice_subtopic).lower()}"
        )

        if practice_key not in st.session_state:

            profile_level = prof.get("level") if prof.get("level") in ADAPTIVE_DIFFICULTIES else "Básico"

            st.session_state[practice_key] = {

                "round": 1,

                "difficulty": normalize_adaptive_difficulty(profile_level),

                "result": None,

                "selection_signature": None,

            }



        practice_state = st.session_state[practice_key]

        selection_signature = f"{practice_topic}|{practice_subtopic}|{practice_level_choice}"

        if practice_state.get("selection_signature") != selection_signature:

            practice_state["round"] = 1

            practice_state["result"] = None
            practice_state["items"] = []
            practice_state["items_signature"] = None

            practice_state["selection_signature"] = selection_signature

            if practice_level_choice != "Recomendado":

                practice_state["difficulty"] = normalize_adaptive_difficulty(practice_level_choice)

            else:

                profile_level = prof.get("level") if prof.get("level") in ADAPTIVE_DIFFICULTIES else "Básico"

                practice_state["difficulty"] = normalize_adaptive_difficulty(profile_level)

            st.session_state[practice_key] = practice_state



        if practice_level_choice == "Recomendado":

            practice_difficulty = normalize_adaptive_difficulty(practice_state.get("difficulty") or "Básico")

            practice_mode_label = "Adaptativo automático"

        else:

            practice_difficulty = normalize_adaptive_difficulty(practice_level_choice)

            practice_state["difficulty"] = practice_difficulty

            st.session_state[practice_key] = practice_state

            practice_mode_label = "Nivel elegido por el estudiante"



        practice_round = int(practice_state.get("round") or 1)

        try:
                        # BUNSEKI_R8_21_3_ACTIVE_PRACTICE_PERSISTENCE
            practice_items_signature = (
                f"{practice_topic}|{practice_subtopic}|"
                f"{practice_difficulty}|{practice_round}"
            )
            cached_practice_items = practice_state.get("items") or []
            cached_items_signature = practice_state.get("items_signature")
            if cached_practice_items and cached_items_signature == practice_items_signature:
                practice_items = cached_practice_items
            else:
                practice_items = adaptive_practice_questions(
                                user["id"],
                                practice_topic,
                                practice_difficulty,
                                n_questions=5,
                                round_no=practice_round,
                                current_subtopic=practice_subtopic,
                            )
                practice_state["items"] = practice_items
                practice_state["items_signature"] = practice_items_signature
        except Exception as practice_generation_error:
            st.error(
                "No se pudo preparar una práctica verificada para "
                f"{practice_topic} → {practice_subtopic}: "
                f"{practice_generation_error}"
            )
            st.stop()



        st.markdown(

            "<div class='quiz-card-premium'><h3>🎯 Práctica adaptativa</h3>"

            "<p class='small'>5 preguntas para entrenarte antes de una evaluación. No genera nota, no altera el pretest/posttest y no cambia tu nivel oficial.</p>"

            f"<span class='quiz-topic-chip'>Tema: {practice_topic}</span>"
              f"<span class='quiz-topic-chip'>Subtema: {practice_subtopic}</span>"

            f"<span class='quiz-topic-chip'>Nivel: {practice_difficulty}</span>"

            f"<span class='quiz-topic-chip'>Modo: {practice_mode_label}</span></div>",

            unsafe_allow_html=True,

        )



        with st.form(f"adaptive_practice_form_{practice_round}_{practice_difficulty}"):

            practice_answers = {}

            for i, item in enumerate(practice_items, start=1):

                options = item.get("options") or []

                practice_answers[str(i)] = st.radio(

                    f"{i}. [{item.get('bloom_level') or 'Comprender'}] {item.get('question')}",

                    options,

                    index=None,

                    key=f"practice_{user['id']}_{practice_round}_{i}",

                )

            practice_submit = st.form_submit_button("Revisar práctica", use_container_width=True)



        if practice_submit:

            unanswered = [key for key, value in practice_answers.items() if value is None]

            if unanswered:

                st.warning("Responde las 5 preguntas para recibir una ruta de práctica adaptada.")

            else:

                reviewed = []

                correct_count = 0

                weak_topics = []

                for i, item in enumerate(practice_items, start=1):

                    answer = practice_answers[str(i)]

                    ok = str(answer).strip() == str(item.get("correct_answer") or "").strip()

                    correct_count += int(ok)

                    if not ok:

                        weak_topics.append(item.get("topic") or topic)

                    reviewed.append({

                        "question": item.get("question"),

                        "answer": answer,

                        "correct_answer": item.get("correct_answer"),

                        "explanation": item.get("explanation"),

                        "topic": item.get("topic") or practice_topic,
                        "subtopic": item.get("subtopic") or practice_subtopic,

                        "is_correct": ok,

                    })
                    record_ai_practice_bank_result(
                        user["id"],
                        item,
                        answer,
                        ok,
                        practice_topic,
                        practice_subtopic,
                        practice_difficulty,
                        attempt_key=(
                            f"ai_practice|{user['id']}|{practice_topic}|{practice_subtopic}|"
                            f"{practice_difficulty}|{practice_round}|{item.get('item_code') or stable_item_code(item.get('question', ''))}"
                        ),
                    )

                next_difficulty, reason = adaptive_practice_next_difficulty(

                    practice_difficulty,

                    correct_count,

                    len(practice_items),

                )

                practice_state["result"] = {

                    "correct": correct_count,

                    "total": len(practice_items),

                    "reviewed": reviewed,

                    "weak_topics": list(dict.fromkeys(weak_topics)),

                    "next_difficulty": next_difficulty,

                    "reason": reason,

                }

                st.session_state[practice_key] = practice_state

                log_location_event(

                    user["id"],

                    "adaptive_practice_reviewed",

                    page=page,

                    topic=practice_topic,

                    subtopic=practice_subtopic,

                    gps=None,

                )

                st.rerun()



        practice_result = practice_state.get("result")

        if practice_result:

            st.success(

                f"Práctica revisada: {practice_result['correct']} de {practice_result['total']} respuestas correctas. "

                "Este dato orienta la siguiente práctica y no es una calificación."

            )

            st.info(

                f"Siguiente práctica recomendada: {practice_result['next_difficulty']}. "

                f"{practice_result['reason']}"

            )

            if practice_level_choice != "Recomendado":

                st.caption(

                    f"Estás en modo manual ({practice_difficulty}). La recomendación no cambia tu selección; puedes modificar el nivel arriba cuando quieras."

                )

            for reviewed in practice_result.get("reviewed", []):

                icon = "✅" if reviewed.get("is_correct") else "❌"

                st.markdown(

                    f"**{icon} {reviewed.get('question')}**  \n"

                    f"Tu respuesta: {reviewed.get('answer')}  \n"

                    f"Respuesta esperada: {reviewed.get('correct_answer')}  \n"

                    f"{reviewed.get('explanation') or ''}"

                )

            st.markdown("**Prompts recomendados para elevar tu nivel**")

            for prompt_text in adaptive_learning_prompts(
                    [practice_subtopic],
                    practice_result.get("next_difficulty"),
                    practice_subtopic,
                ):

                st.code(prompt_text, language=None)



            if practice_level_choice == "Recomendado":

                next_button_label = f"Nueva práctica adaptada · {practice_result['next_difficulty']}"

            else:

                next_button_label = f"Nueva práctica · {practice_difficulty} (nivel elegido)"



            if st.button(

                next_button_label,

                key=f"next_practice_{user['id']}_{practice_round}",

                use_container_width=True,

            ):

                practice_state["round"] = practice_round + 1
                practice_state["items"] = []
                practice_state["items_signature"] = None

                if practice_level_choice == "Recomendado":

                    practice_state["difficulty"] = practice_result["next_difficulty"]

                else:

                    practice_state["difficulty"] = practice_difficulty

                practice_state["result"] = None

                st.session_state[practice_key] = practice_state

                st.rerun()



        df = pd.DataFrame(user_q)

        if not df.empty:

            fig = px.histogram(df, x='topic', title='Consultas por área')

            st.plotly_chart(fig, use_container_width=True)





def render_teacher_gps_heatmap(logs: pd.DataFrame, filtered_students: pd.DataFrame | None = None):

    """Mapa de calor docente basado en las coordenadas GPS guardadas en interactions.

    Usa únicamente registros con gps_lat/gps_lon válidos y permite respetar los filtros

    académicos activos del dashboard.

    """

    st.markdown('### 🗺️ Mapa de calor GPS por interacciones')
    st.caption('Visualiza la concentración geográfica de uso de BunsekiChat según las coordenadas guardadas en cada interacción.')



    if logs is None or logs.empty:

        st.info("Aún no existen interacciones para generar el mapa de calor GPS.")

        return



    gps_df = logs.copy()



    # Respeta los filtros de curso/docente/nivel aplicados al dashboard.

    if filtered_students is not None and not filtered_students.empty and "id" in filtered_students.columns:

        allowed_ids = set(filtered_students["id"].dropna().astype(int).tolist())

        gps_df = gps_df[gps_df["user_id"].isin(allowed_ids)]



    # El mapa se construye desde la tabla interactions, no desde el registro manual del perfil.

    if "role" in gps_df.columns:

        gps_df = gps_df[gps_df["role"].astype(str).str.lower().eq("user")]



    required_cols = {"gps_lat", "gps_lon"}

    if not required_cols.issubset(set(gps_df.columns)):

        st.warning("La tabla interactions todavía no tiene columnas gps_lat/gps_lon disponibles.")

        return



    gps_df["gps_lat"] = pd.to_numeric(gps_df["gps_lat"], errors="coerce")

    gps_df["gps_lon"] = pd.to_numeric(gps_df["gps_lon"], errors="coerce")

    gps_df = gps_df.dropna(subset=["gps_lat", "gps_lon"])

    gps_df = gps_df[gps_df["gps_lat"].between(-90, 90) & gps_df["gps_lon"].between(-180, 180)]



    if gps_df.empty:

        st.warning("No hay interacciones con GPS válido. Verifica que el navegador haya autorizado la ubicación y que se esté ejecutando en localhost o HTTPS.")

        return



    # Columnas auxiliares para un hover docente más claro.

    gps_df["estudiante"] = gps_df.get("username", "Sin usuario")

    if "first_names" in gps_df.columns or "last_names" in gps_df.columns:

        gps_df["nombre"] = (

            gps_df.get("first_names", "").fillna("").astype(str).str.strip()

            + " "

            + gps_df.get("last_names", "").fillna("").astype(str).str.strip()

        ).str.strip()

        gps_df.loc[gps_df["nombre"].eq(""), "nombre"] = gps_df["estudiante"]

    else:

        gps_df["nombre"] = gps_df["estudiante"]



    center_lat = float(gps_df["gps_lat"].mean())

    center_lon = float(gps_df["gps_lon"].mean())

    zoom = 11 if len(gps_df) <= 10 else 10



    h1, h2, h3 = st.columns(3)

    h1.markdown(f"<div class='metric'><b>{len(gps_df)}</b><br><span>Interacciones con GPS</span></div>", unsafe_allow_html=True)

    h2.markdown(f"<div class='metric'><b>{gps_df['user_id'].nunique()}</b><br><span>Estudiantes geolocalizados</span></div>", unsafe_allow_html=True)

    last_gps = gps_df["created_at"].max() if "created_at" in gps_df.columns else "N/D"

    h3.markdown(f"<div class='metric'><b>{last_gps}</b><br><span>Último GPS registrado</span></div>", unsafe_allow_html=True)



    fig_heat = px.density_mapbox(

        gps_df,

        lat="gps_lat",

        lon="gps_lon",

        radius=28,

        center={"lat": center_lat, "lon": center_lon},

        zoom=zoom,

        mapbox_style="open-street-map",

        hover_name="nombre",

        hover_data={

            "estudiante": True,

            "topic": True,

            "subtopic": True,

            "created_at": True,

            "gps_lat": ":.6f",

            "gps_lon": ":.6f",

            "gps_accuracy": True if "gps_accuracy" in gps_df.columns else False,

        },

        title="Mapa de calor de uso por coordenadas GPS de interacciones",

    )

    fig_heat.update_layout(margin={"r":0,"t":45,"l":0,"b":0}, height=560)

    st.plotly_chart(fig_heat, use_container_width=True)



    fig_points = px.scatter_mapbox(

        gps_df,

        lat="gps_lat",

        lon="gps_lon",

        color="topic" if "topic" in gps_df.columns else None,

        hover_name="nombre",

        hover_data={

            "estudiante": True,

            "topic": True if "topic" in gps_df.columns else False,

            "subtopic": True if "subtopic" in gps_df.columns else False,

            "created_at": True if "created_at" in gps_df.columns else False,

            "gps_lat": ":.6f",

            "gps_lon": ":.6f",

        },

        center={"lat": center_lat, "lon": center_lon},

        zoom=zoom,

        mapbox_style="open-street-map",

        title="Puntos GPS individuales por interacción",

    )

    fig_points.update_layout(margin={"r":0,"t":45,"l":0,"b":0}, height=500)

    with st.expander("Ver puntos GPS individuales"):

        st.plotly_chart(fig_points, use_container_width=True)

        cols = [c for c in ["id", "user_id", "username", "first_names", "last_names", "course", "teacher", "topic", "subtopic", "created_at", "gps_lat", "gps_lon", "gps_accuracy", "gps_source"] if c in gps_df.columns]

        st.dataframe(gps_df[cols].sort_values("created_at", ascending=False) if "created_at" in cols else gps_df[cols], use_container_width=True, height=280)

        st.download_button(

            "📍 Descargar interacciones con GPS",

        gps_df[cols].to_csv(index=False).encode("utf-8-sig"),

            "interacciones_gps_mapa_calor.csv",

            "text/csv",

            use_container_width=True,

        )





def get_research_results():

    return fetchall("""

        SELECT

            aq.id, aq.user_id, aq.quiz_type, aq.title, aq.subject,

            aq.course_level, aq.parallel, aq.shift, aq.cohort,

            aq.score, aq.passed, aq.question_count, aq.status,

            aq.version_code, aq.dimension_scores_json, aq.diagnosis_json,

            aq.cognitive_profile, aq.learning_plan_json, aq.total_time_seconds,

            aq.created_at, aq.completed_at, aq.research_title, aq.random_seed,

            u.username,

            p.first_names, p.last_names, p.full_name_normalized,

            p.cedula, p.correo, COALESCE(NULLIF(p.estado_dato,''), 'piloto') AS estado_dato,

            COALESCE(p.usar_en_investigacion,FALSE) AS usar_en_investigacion,

            COALESCE(p.consentimiento_informado,FALSE) AS consentimiento_informado,

            p.fecha_consentimiento, p.pretest_estado, p.posttest_estado,

            COALESCE(NULLIF(p.research_group,''), 'Sin asignar') AS research_group,

            COALESCE(aq.subject, p.subject) AS profile_subject,

            COALESCE(aq.course_level, p.course_level) AS profile_course_level,

            COALESCE(aq.parallel, p.parallel) AS profile_parallel,

            COALESCE(aq.shift, p.shift) AS profile_shift,

            COALESCE(aq.cohort, p.cohort, p.course) AS profile_cohort

        FROM adaptive_quizzes aq

        JOIN users u ON u.id=aq.user_id

        LEFT JOIN profiles p ON p.user_id=aq.user_id

        WHERE COALESCE(aq.quiz_type,'adaptive') IN ('pretest','posttest')

        ORDER BY aq.id DESC

    """)





def get_research_students():

    return fetchall("""

        SELECT u.id AS user_id, u.username, p.first_names, p.last_names,

               p.full_name_normalized, p.subject, p.course_level, p.parallel,

               p.shift, p.cohort, p.course, p.cedula, p.correo,

               COALESCE(NULLIF(p.estado_dato,''), 'piloto') AS estado_dato,

               COALESCE(p.usar_en_investigacion,FALSE) AS usar_en_investigacion,

               COALESCE(p.consentimiento_informado,FALSE) AS consentimiento_informado,

               p.fecha_consentimiento,p.pretest_estado,p.posttest_estado,

               COALESCE(NULLIF(p.research_group,''), 'Sin asignar') AS research_group

        FROM users u

        JOIN profiles p ON p.user_id=u.id

        WHERE LOWER(COALESCE(u.role,'student'))='student'

        ORDER BY p.last_names, p.first_names, u.username

    """)





def get_research_item_responses():

    return fetchall("""

        SELECT q.id, q.quiz_id, aq.user_id, aq.quiz_type, aq.version_code,

               aq.subject, aq.course_level, aq.parallel, aq.shift, aq.cohort,

               COALESCE(NULLIF(p.research_group,''), 'Sin asignar') AS research_group,

               COALESCE(NULLIF(q.item_code,''), 'LEGACY-' || q.id::text) AS item_code,

               q.question, q.position, q.dimension, q.difficulty_level, q.bloom_level,

               q.user_answer, q.is_correct, q.response_time_seconds, q.conceptual_error

        FROM adaptive_questions q

        JOIN adaptive_quizzes aq ON aq.id=q.quiz_id

        LEFT JOIN profiles p ON p.user_id=aq.user_id

        WHERE COALESCE(aq.quiz_type,'adaptive') IN ('pretest','posttest')

          AND aq.status='completed'

        ORDER BY q.quiz_id, q.position, q.id

    """)





def get_research_learning_events():

    return fetchall("""

        SELECT rle.*, COALESCE(NULLIF(p.research_group,''), 'Sin asignar') AS research_group

        FROM research_learning_events rle

        LEFT JOIN profiles p ON p.user_id=rle.user_id

        ORDER BY rle.id

    """)





def get_research_interactions():

    return fetchall("""

        SELECT i.id, i.user_id, i.role, i.topic, i.subtopic, i.latency_ms, i.created_at

        FROM interactions i

        WHERE EXISTS (

            SELECT 1 FROM adaptive_quizzes aq

            WHERE aq.user_id=i.user_id AND COALESCE(aq.quiz_type,'adaptive') IN ('pretest','posttest')

        )

        ORDER BY i.user_id, i.created_at, i.id

    """)





def get_research_session_events():

    return fetchall("""

        SELECT le.id, le.user_id, le.event_type, le.page, le.created_at

        FROM location_events le

        WHERE le.event_type='app_open'

          AND EXISTS (

              SELECT 1 FROM adaptive_quizzes aq

              WHERE aq.user_id=le.user_id AND COALESCE(aq.quiz_type,'adaptive') IN ('pretest','posttest')

          )

        ORDER BY le.user_id, le.created_at, le.id

    """)





def get_research_exercise_attempts():

    return fetchall("""

        SELECT rea.*, u.username, p.first_names, p.last_names, p.cohort

        FROM research_exercise_attempts rea

        JOIN users u ON u.id=rea.user_id

        LEFT JOIN profiles p ON p.user_id=rea.user_id

        ORDER BY rea.id DESC

    """)





def get_research_survey_responses():

    return fetchall("""

        SELECT rsr.*, u.username, p.first_names, p.last_names, p.cohort

        FROM research_survey_responses rsr

        JOIN users u ON u.id=rsr.user_id

        LEFT JOIN profiles p ON p.user_id=rsr.user_id

        ORDER BY rsr.id DESC

    """)





def build_research_export_bundle(student_df, result_df, item_df, event_df, interaction_df, exercise_df, session_df=None):

    students = student_df.copy() if student_df is not None else pd.DataFrame()

    results = result_df.copy() if result_df is not None else pd.DataFrame()

    items = item_df.copy() if item_df is not None else pd.DataFrame()

    events = event_df.copy() if event_df is not None else pd.DataFrame()

    interactions_df = interaction_df.copy() if interaction_df is not None else pd.DataFrame()

    exercises = exercise_df.copy() if exercise_df is not None else pd.DataFrame()

    sessions = session_df.copy() if session_df is not None else pd.DataFrame()

    if students.empty:

        empty = pd.DataFrame()

        return {"summary": empty, "items": empty, "events": empty, "exercises": empty}



    for frame, columns in [

        (results, ["user_id", "score", "total_time_seconds"]),

        (items, ["user_id", "response_time_seconds"]),

        (events, ["user_id", "duration_seconds"]),

        (interactions_df, ["user_id"]),

        (exercises, ["user_id"]),

        (sessions, ["user_id"]),

    ]:

        for column in columns:

            if column in frame.columns:

                frame[column] = pd.to_numeric(frame[column], errors="coerce")



    completed = results.copy()

    if not completed.empty:

        completed = completed[completed.get("status", "").astype(str).eq("completed")]

        completed = completed.sort_values([column for column in ["user_id", "quiz_type", "id"] if column in completed.columns])



    def latest_quiz(user_results, quiz_type):

        if user_results.empty or "quiz_type" not in user_results.columns:

            return {}

        subset = user_results[user_results["quiz_type"].astype(str).eq(quiz_type)]

        return subset.iloc[-1].to_dict() if not subset.empty else {}



    def frame_for_user(frame, user_id):

        if frame.empty or "user_id" not in frame.columns:

            return frame.iloc[0:0].copy()

        return frame[pd.to_numeric(frame["user_id"], errors="coerce").eq(int(user_id))].copy()



    summary_rows = []

    students = students.sort_values([column for column in ["last_names", "first_names", "user_id"] if column in students.columns])

    for _, student in students.drop_duplicates("user_id", keep="last").iterrows():

        user_id = int(student["user_id"])

        user_results = frame_for_user(completed, user_id)

        user_items = frame_for_user(items, user_id)

        user_events = frame_for_user(events, user_id)

        user_interactions = frame_for_user(interactions_df, user_id)

        user_exercises = frame_for_user(exercises, user_id)

        user_sessions = frame_for_user(sessions, user_id)

        pretest = latest_quiz(user_results, "pretest")

        posttest = latest_quiz(user_results, "posttest")

        pre_score = pd.to_numeric(pretest.get("score"), errors="coerce")

        post_score = pd.to_numeric(posttest.get("score"), errors="coerce")

        gain = post_score - pre_score if pd.notna(pre_score) and pd.notna(post_score) else None



        session_dates = set()

        for frame in [user_results, user_events, user_interactions, user_exercises]:

            if "created_at" in frame.columns:

                session_dates.update(str(value)[:10] for value in frame["created_at"].dropna() if str(value).strip())

        answered = user_items[user_items.get("user_answer", pd.Series(index=user_items.index, dtype=str)).fillna("").astype(str).str.strip().ne("")]

        correct_count = int(answered.get("is_correct", pd.Series(index=answered.index, dtype=bool)).fillna(False).astype(bool).sum())

        feedback_count = int(user_exercises.get("feedback", pd.Series(index=user_exercises.index, dtype=str)).fillna("").astype(str).str.strip().ne("").sum())

        bloom_counts = answered.get("bloom_level", pd.Series(dtype=str)).fillna("Sin clasificar").astype(str).value_counts().to_dict()

        difficulty_counts = answered.get("difficulty_level", pd.Series(dtype=str)).fillna("Sin clasificar").astype(str).value_counts().to_dict()

        quiz_seconds = pd.to_numeric(user_results.get("total_time_seconds", pd.Series(dtype=float)), errors="coerce").fillna(0).sum()

        event_seconds = pd.to_numeric(user_events.get("duration_seconds", pd.Series(dtype=float)), errors="coerce").fillna(0).sum()

        dates = [value for value in [pretest.get("completed_at"), posttest.get("completed_at")] if value]

        summary_rows.append({

            "user_id": user_id,

            "cedula": student.get("cedula"),

            "nombres": student.get("first_names"),

            "apellidos": student.get("last_names"),

            "correo": student.get("correo"),

            "materia": student.get("subject"),

            "curso": student.get("course_level"),

            "paralelo": student.get("parallel"),

            "jornada": student.get("shift"),

            "grupo_investigacion": student.get("research_group"),

            "cohorte": student.get("cohort"),

            "estado_dato": student.get("estado_dato"),

            "usar_en_investigacion": student.get("usar_en_investigacion"),

            "consentimiento_informado": student.get("consentimiento_informado"),

            "fecha_consentimiento": student.get("fecha_consentimiento"),

            "pretest_estado": student.get("pretest_estado"),

            "posttest_estado": student.get("posttest_estado"),

            "pretest": None if pd.isna(pre_score) else float(pre_score),

            "posttest": None if pd.isna(post_score) else float(post_score),

            "ganancia_aprendizaje": None if gain is None or pd.isna(gain) else float(gain),

            "tiempo_uso_segundos": float(quiz_seconds + event_seconds),

            "numero_sesiones": len(user_sessions) if not user_sessions.empty else len(session_dates),

            "preguntas_respondidas": len(answered),

            "aciertos": correct_count,

            "errores": int(len(answered) - correct_count),

            "nivel_bloom": json.dumps(bloom_counts, ensure_ascii=False),

            "dificultad": json.dumps(difficulty_counts, ensure_ascii=False),

            "uso_retroalimentacion": feedback_count,

            "fecha_pretest": pretest.get("completed_at"),

            "fecha_posttest": posttest.get("completed_at"),

            "fecha_aplicacion": max(dates) if dates else None,

        })



    summary = pd.DataFrame(summary_rows)

    identity_columns = ["user_id", "cedula", "first_names", "last_names", "cohort", "research_group", "estado_dato"]

    identity = students[[column for column in identity_columns if column in students.columns]].drop_duplicates("user_id")



    def attach_identity(frame):

        if frame.empty or "user_id" not in frame.columns:

            return frame

        clean = frame.drop(columns=[column for column in identity.columns if column != "user_id" and column in frame.columns], errors="ignore")

        return clean.merge(identity, on="user_id", how="left")



    return {

        "summary": summary,

        "items": attach_identity(items),

        "events": attach_identity(events),

        "exercises": attach_identity(exercises),

        "sessions": attach_identity(sessions),

    }





def research_export_excel(bundle):

    output = BytesIO()

    with pd.ExcelWriter(output, engine="openpyxl") as writer:

        sheet_map = {

            "summary": "Resumen_estudiantes",

            "items": "Respuestas_items",

            "events": "Eventos_aprendizaje",

            "exercises": "Ejercicios_feedback",

            "sessions": "Sesiones",

        }

        for key, sheet_name in sheet_map.items():

            frame = bundle.get(key, pd.DataFrame())

            frame.to_excel(writer, sheet_name=sheet_name, index=False)

            worksheet = writer.book[sheet_name]

            worksheet.freeze_panes = "A2"

            worksheet.auto_filter.ref = worksheet.dimensions

    return output.getvalue()





def render_teacher_research_dashboard(user):

    st.markdown("""

    <div class='teacher-card'>

        <h3>🧪 Investigación pretest/posttest</h3>

        <p class='small'>Resultados parametrizados por materia, curso, paralelo y jornada para análisis del efecto del tutor inteligente.</p>

    </div>

    """, unsafe_allow_html=True)



    research_students = pd.DataFrame(get_research_students())

    with st.expander("Asignación de grupo experimental/control", expanded=False):

        st.caption("La asignación la realiza el docente. El estudiante no puede modificar su grupo de investigación.")

        if research_students.empty:

            st.info("Aún no existen estudiantes registrados.")

        else:

            def safe_student_text(value):

                if value is None or pd.isna(value):

                    return ""

                return str(value).strip()



            student_ids = research_students["user_id"].tolist()

            labels = {}

            for _, row in research_students.iterrows():

                full_name = safe_student_text(row.get("full_name_normalized"))

                if not full_name:

                    full_name = normalize_spaces(f"{safe_student_text(row.get('first_names'))} {safe_student_text(row.get('last_names'))}")

                full_name = full_name or safe_student_text(row.get("username")) or f"Estudiante {row['user_id']}"

                cohort_label = safe_student_text(row.get("cohort")) or "Sin cohorte"

                group_label = safe_student_text(row.get("research_group")) or "Sin asignar"

                labels[row["user_id"]] = f"{full_name} · {cohort_label} · {group_label}"

            with st.form("research_group_assignment"):

                selected_student_id = st.selectbox("Estudiante", student_ids, format_func=lambda value: labels.get(value, str(value)))

                current_group = safe_student_text(research_students.loc[research_students["user_id"].eq(selected_student_id), "research_group"].iloc[0]) or "Sin asignar"

                selected_student_row = research_students.loc[research_students["user_id"].eq(selected_student_id)].iloc[0]

                selected_pretest_state = safe_student_text(selected_student_row.get("pretest_estado")) or "pendiente"

                selected_data_status = safe_student_text(selected_student_row.get("estado_dato")) or "piloto"

                selected_group = st.selectbox("Grupo de investigación", RESEARCH_GROUPS, index=RESEARCH_GROUPS.index(current_group) if current_group in RESEARCH_GROUPS else 0)

                assign_group = st.form_submit_button("Guardar asignación", use_container_width=True)

            if assign_group:

                if selected_data_status.lower() == "oficial" and selected_pretest_state.lower() != "pendiente":

                    st.error("No se puede cambiar el grupo de un estudiante oficial después de iniciar el pretest.")

                else:

                    update_profile(int(selected_student_id), {"research_group": selected_group})

                    st.success(f"Grupo actualizado: {selected_group}.")

                    st.rerun()



    df = pd.DataFrame(get_research_results())

    required_result_columns = [

        "id", "user_id", "quiz_type", "status", "score", "profile_subject", "profile_course_level",

        "profile_parallel", "profile_shift", "profile_cohort", "research_group", "estado_dato",

        "usar_en_investigacion", "consentimiento_informado",

    ]

    for column in required_result_columns:

        if column not in df.columns:

            df[column] = pd.Series(dtype=float if column in {"id", "user_id", "score"} else object)

    df["score"] = pd.to_numeric(df["score"], errors="coerce")

    df["student"] = df.apply(

        lambda r: r.get("full_name_normalized") or f"{r.get('first_names') or ''} {r.get('last_names') or ''}".strip() or r.get("username"),

        axis=1,

    )



    def opts(result_column, student_column=None):

        values = set(str(x).strip() for x in df[result_column].dropna().unique() if str(x).strip())

        student_column = student_column or result_column

        if not research_students.empty and student_column in research_students.columns:

            values.update(str(x).strip() for x in research_students[student_column].dropna().unique() if str(x).strip())

        values = sorted(values)

        return ["Todos"] + values



    scope_options = [

        "Oficiales con consentimiento (análisis principal)",

        "Todos los oficiales (auditoría)",

        "Datos piloto",

        "Todos los registros (auditoría)",

    ]

    f0, f00 = st.columns(2)

    selected_scope = f0.selectbox("Conjunto de datos", scope_options, index=0, key="research_data_scope")

    sel_cohort = f00.selectbox("Cohorte", opts("profile_cohort", "cohort"), key="research_cohort_filter")

    f1, f2, f3 = st.columns(3)

    sel_subject = f1.selectbox("Materia", opts("profile_subject", "subject"), key="research_subject_filter")

    sel_course = f2.selectbox("Curso", opts("profile_course_level", "course_level"), key="research_course_filter")

    sel_parallel = f3.selectbox("Paralelo", opts("profile_parallel", "parallel"), key="research_parallel_filter")

    f4, f5, f6 = st.columns(3)

    sel_shift = f4.selectbox("Jornada", opts("profile_shift", "shift"), key="research_shift_filter")

    sel_group = f5.selectbox("Grupo", opts("research_group", "research_group"), key="research_group_filter")

    sel_type = f6.selectbox("Instrumento", ["Todos", "pretest", "posttest"], key="research_type_filter")



    def truthy(series):

        return series.fillna(False).astype(str).str.lower().isin({"true", "1", "t", "yes", "si", "sí"})



    def apply_research_scope(frame):

        if frame.empty:

            return frame.copy()

        status = frame.get("estado_dato", pd.Series("piloto", index=frame.index)).fillna("piloto").astype(str).str.lower()

        if selected_scope == scope_options[0]:

            return frame[status.eq("oficial") & truthy(frame.get("usar_en_investigacion", pd.Series(False, index=frame.index))) & truthy(frame.get("consentimiento_informado", pd.Series(False, index=frame.index)))].copy()

        if selected_scope == scope_options[1]:

            return frame[status.eq("oficial")].copy()

        if selected_scope == scope_options[2]:

            return frame[status.eq("piloto")].copy()

        return frame.copy()



    cohort_filtered = apply_research_scope(df)

    student_filtered = apply_research_scope(research_students)

    if sel_cohort != "Todos":

        cohort_filtered = cohort_filtered[cohort_filtered["profile_cohort"].astype(str) == sel_cohort]

        student_filtered = student_filtered[student_filtered["cohort"].astype(str) == sel_cohort]

    if sel_subject != "Todos": cohort_filtered = cohort_filtered[cohort_filtered["profile_subject"].astype(str) == sel_subject]

    if sel_course != "Todos": cohort_filtered = cohort_filtered[cohort_filtered["profile_course_level"].astype(str) == sel_course]

    if sel_parallel != "Todos": cohort_filtered = cohort_filtered[cohort_filtered["profile_parallel"].astype(str) == sel_parallel]

    if sel_shift != "Todos": cohort_filtered = cohort_filtered[cohort_filtered["profile_shift"].astype(str) == sel_shift]

    if sel_group != "Todos": cohort_filtered = cohort_filtered[cohort_filtered["research_group"].astype(str) == sel_group]

    if not student_filtered.empty:

        if sel_subject != "Todos": student_filtered = student_filtered[student_filtered["subject"].astype(str) == sel_subject]

        if sel_course != "Todos": student_filtered = student_filtered[student_filtered["course_level"].astype(str) == sel_course]

        if sel_parallel != "Todos": student_filtered = student_filtered[student_filtered["parallel"].astype(str) == sel_parallel]

        if sel_shift != "Todos": student_filtered = student_filtered[student_filtered["shift"].astype(str) == sel_shift]

        if sel_group != "Todos": student_filtered = student_filtered[student_filtered["research_group"].astype(str) == sel_group]



    if selected_scope == scope_options[0]:

        st.success("Análisis principal protegido: solo datos oficiales con consentimiento y autorización de uso.")

    else:

        st.warning("Vista de auditoría o piloto. Estos registros no forman parte del análisis principal oficial.")



    with st.expander("Repetir posttest por incidencia metodologica", expanded=False):

        st.caption(

            "Use esta opcion solo si hubo problemas de comprension de la metodologia, conectividad o aplicacion. "

            "Los posttest anteriores se conservan para auditoria, pero no cuentan en el analisis principal."

        )

        reset_candidates = student_filtered.copy()

        if reset_candidates.empty or "user_id" not in reset_candidates.columns:

            st.info("No hay estudiantes en el alcance de filtros actual.")

        else:

            def reset_student_label(row):

                raw_name = str(row.get("full_name_normalized") or "").strip()

                if not raw_name:

                    raw_name = f"{row.get('first_names') or ''} {row.get('last_names') or ''}"

                name = normalize_spaces(raw_name) or str(row.get("username") or f"Estudiante {row.get('user_id')}")

                cohort = normalize_spaces(row.get("cohort")) or "Sin cohorte"

                group = normalize_spaces(row.get("research_group")) or "Sin grupo"

                post_state = normalize_spaces(row.get("posttest_estado")) or "pendiente"

                return f"{name} - {cohort} - {group} - posttest: {post_state}"



            reset_scope = st.radio(

                "Alcance de repeticion",

                ["Todos los estudiantes filtrados", "Un estudiante"],

                horizontal=True,

                key="repeat_posttest_scope",

            )

            if reset_scope == "Un estudiante":

                candidate_ids = reset_candidates["user_id"].tolist()

                label_map = {row["user_id"]: reset_student_label(row) for _, row in reset_candidates.iterrows()}

                selected_repeat_id = st.selectbox(

                    "Estudiante",

                    candidate_ids,

                    format_func=lambda value: label_map.get(value, str(value)),

                    key="repeat_posttest_student",

                )

                reset_user_ids = [int(selected_repeat_id)]

            else:

                reset_user_ids = pd.to_numeric(reset_candidates["user_id"], errors="coerce").dropna().astype(int).drop_duplicates().tolist()

                st.info(f"Se habilitara repeticion para {len(reset_user_ids)} estudiante(s) del filtro actual.")



            reset_reason = st.text_area(

                "Motivo metodologico",

                value="Los estudiantes no comprendieron la metodologia de aplicacion del posttest.",

                key="repeat_posttest_reason",

            )

            reset_confirm = st.text_input(

                "Confirmacion",

                placeholder="Escriba REPETIR POSTTEST",

                key="repeat_posttest_confirm",

            )

            if st.button("Habilitar nuevo posttest", key="repeat_posttest_button", type="primary", use_container_width=True):

                if reset_confirm.strip().upper() != "REPETIR POSTTEST":

                    st.error("Para confirmar, escriba exactamente: REPETIR POSTTEST")

                elif not reset_user_ids:

                    st.error("No hay estudiantes seleccionados.")

                else:

                    result = invalidate_research_posttests_for_repeat(reset_user_ids, reset_reason, user)

                    st.success(

                        f"Posttest habilitado nuevamente para {result['students']} estudiante(s). "

                        f"Intentos anteriores anulados para analisis principal: {result['attempts']}."

                    )

                    st.info("Indique a los estudiantes que ingresen a Evaluación > Posttest > Iniciar Posttest.")

                    st.rerun()



    # La vista puede mostrar un instrumento, pero el informe inferencial siempre

    # conserva pretest y postest de la cohorte para mantener el emparejamiento.

    filtered = cohort_filtered.copy()

    if sel_type != "Todos": filtered = filtered[filtered["quiz_type"].astype(str) == sel_type]



    completed = filtered[filtered["status"].astype(str).eq("completed")].copy()

    pre_avg = completed.loc[completed["quiz_type"].eq("pretest"), "score"].mean()

    post_avg = completed.loc[completed["quiz_type"].eq("posttest"), "score"].mean()

    latest = completed.sort_values(["user_id", "quiz_type", "id"]).drop_duplicates(["user_id", "quiz_type"], keep="last")

    pivot = latest.pivot_table(index="user_id", columns="quiz_type", values="score", aggfunc="last")

    gain = (pivot["posttest"] - pivot["pretest"]).mean() if {"pretest", "posttest"}.issubset(set(pivot.columns)) else None



    m1, m2, m3, m4 = st.columns(4)

    m1.markdown(f"<div class='metric'><b>{student_filtered['user_id'].nunique() if 'user_id' in student_filtered.columns else 0}</b><br><span>Estudiantes en alcance</span></div>", unsafe_allow_html=True)

    m2.markdown(f"<div class='metric'><b>{0 if pd.isna(pre_avg) else pre_avg:.1f}%</b><br><span>Promedio pretest</span></div>", unsafe_allow_html=True)

    m3.markdown(f"<div class='metric'><b>{0 if pd.isna(post_avg) else post_avg:.1f}%</b><br><span>Promedio posttest</span></div>", unsafe_allow_html=True)

    m4.markdown(f"<div class='metric'><b>{'N/D' if gain is None or pd.isna(gain) else f'{gain:.1f} pts'}</b><br><span>Ganancia media</span></div>", unsafe_allow_html=True)



    if not completed.empty:

        fig = px.box(completed, x="quiz_type", y="score", color="profile_cohort", points="all", title="Distribución de resultados pre/post")

        fig.update_layout(height=430, margin={"r":10,"t":60,"l":10,"b":80})

        st.plotly_chart(fig, use_container_width=True)



        dim_rows = []

        for _, row in completed.iterrows():

            scores = _safe_json_loads(row.get("dimension_scores_json"), {})

            for dim, value in scores.items():

                dim_rows.append({

                    "student": row.get("student"),

                    "quiz_type": row.get("quiz_type"),

                    "dimension": dim,

                    "score": float(value or 0),

                })

        if dim_rows:

            dim_df = pd.DataFrame(dim_rows)

            heat = dim_df.pivot_table(index="dimension", columns="quiz_type", values="score", aggfunc="mean").fillna(0)

            st.plotly_chart(px.imshow(heat, aspect="auto", title="Heatmap de rendimiento por dimensión"), use_container_width=True)

            radar_df = dim_df.groupby(["quiz_type", "dimension"], as_index=False)["score"].mean()

            st.plotly_chart(px.line_polar(radar_df, r="score", theta="dimension", color="quiz_type", line_close=True, range_r=[0, 100], title="Radar de competencias pre/post"), use_container_width=True)



    view_cols = [c for c in [

        "id", "student", "username", "quiz_type", "score", "passed", "status",

        "profile_subject", "profile_cohort", "profile_course_level", "profile_parallel", "profile_shift", "research_group",

        "question_count", "version_code", "cognitive_profile", "total_time_seconds",

        "dimension_scores_json", "created_at", "completed_at", "random_seed"

    ] if c in filtered.columns]

    if filtered.empty:

        st.info("No existen pretest o postest con los filtros seleccionados.")

    else:

        st.dataframe(filtered[view_cols], use_container_width=True, height=420)

    st.download_button(

        "Descargar CSV investigación",

        filtered[view_cols].to_csv(index=False).encode("utf-8-sig"),

        "investigacion_pretest_posttest_bunsekichat.csv",

        "text/csv",

        use_container_width=True,

    )

    st.download_button(

        "Descargar JSON investigación",

        filtered[view_cols].to_json(orient="records", force_ascii=False, indent=2).encode("utf-8"),

        "investigacion_pretest_posttest_bunsekichat.json",

        "application/json",

        use_container_width=True,

    )

    all_exercise_df = pd.DataFrame(get_research_exercise_attempts())

    all_survey_df = pd.DataFrame(get_research_survey_responses())

    all_item_df = pd.DataFrame(get_research_item_responses())

    all_event_df = pd.DataFrame(get_research_learning_events())

    all_interaction_df = pd.DataFrame(get_research_interactions())

    all_session_df = pd.DataFrame(get_research_session_events())

    allowed_user_ids = set(pd.to_numeric(student_filtered.get("user_id", pd.Series(dtype=float)), errors="coerce").dropna().astype(int))

    allowed_quiz_ids = set(pd.to_numeric(cohort_filtered.get("id", pd.Series(dtype=float)), errors="coerce").dropna().astype(int))



    def related_research_rows(source_df):

        if source_df.empty:

            return source_df

        user_ids = pd.to_numeric(source_df.get("user_id", pd.Series(index=source_df.index, dtype=float)), errors="coerce")

        user_mask = user_ids.isin(allowed_user_ids)

        if "quiz_id" not in source_df.columns:

            return source_df[user_mask].copy()

        quiz_ids = pd.to_numeric(source_df["quiz_id"], errors="coerce")

        return source_df[quiz_ids.isin(allowed_quiz_ids) | (quiz_ids.isna() & user_mask)].copy()



    exercise_df = related_research_rows(all_exercise_df)

    survey_df = related_research_rows(all_survey_df)

    item_df = related_research_rows(all_item_df)

    event_df = related_research_rows(all_event_df)

    interaction_df = related_research_rows(all_interaction_df)

    session_df = related_research_rows(all_session_df)

    export_bundle = build_research_export_bundle(

        student_filtered,

        cohort_filtered,

        item_df,

        event_df,

        interaction_df,

        exercise_df,

        session_df,

    )

    st.markdown("### Exportación para análisis estadístico")

    st.caption("El Excel incluye resumen por estudiante, respuestas por ítem, eventos y uso de retroalimentación. El CSV contiene el resumen consolidado.")

    ex1, ex2 = st.columns(2)

    summary_export = export_bundle.get("summary", pd.DataFrame())

    ex1.download_button(

        "Descargar Excel de investigación",

        research_export_excel(export_bundle),

        "datos_investigacion_bunsekichat.xlsx",

        "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",

        use_container_width=True,

        disabled=summary_export.empty,

    )

    ex2.download_button(

        "Descargar CSV oficial",

        summary_export.to_csv(index=False).encode("utf-8-sig"),

        "datos_investigacion_bunsekichat.csv",

        "text/csv",

        use_container_width=True,

        disabled=summary_export.empty,

    )

    report_filters = {

        "data_scope": selected_scope,

        "cohort": sel_cohort,

        "subject": sel_subject,

        "course": sel_course,

        "parallel": sel_parallel,

        "shift": sel_shift,

        "group": sel_group,

    }

    report_state_cols = [col for col in ["id", "user_id", "quiz_type", "status", "score", "completed_at", "dimension_scores_json"] if col in cohort_filtered.columns]

    signature_payload = {

        "filters": report_filters,

        "research": cohort_filtered[report_state_cols].fillna("").astype(str).to_dict(orient="records"),

        "exercise_ids": sorted(pd.to_numeric(exercise_df.get("id", pd.Series(dtype=float)), errors="coerce").dropna().astype(int).tolist()),

        "survey_ids": sorted(pd.to_numeric(survey_df.get("id", pd.Series(dtype=float)), errors="coerce").dropna().astype(int).tolist()),

        "item_ids": sorted(pd.to_numeric(item_df.get("id", pd.Series(dtype=float)), errors="coerce").dropna().astype(int).tolist()),

        "event_ids": sorted(pd.to_numeric(event_df.get("id", pd.Series(dtype=float)), errors="coerce").dropna().astype(int).tolist()),

        "interaction_ids": sorted(pd.to_numeric(interaction_df.get("id", pd.Series(dtype=float)), errors="coerce").dropna().astype(int).tolist()),

    }

    report_signature = hashlib.sha256(json.dumps(signature_payload, sort_keys=True).encode("utf-8")).hexdigest()



    st.markdown("<div class='teacher-card'><h3>Motor Estadístico-Predictivo</h3><p class='small'>EDA, psicometría, hipótesis pretest-posttest para grupo experimental único, normalidad, tamaño del efecto, correlaciones, predicción y exportación científica reproducible.</p></div>", unsafe_allow_html=True)

    safe_group = re.sub(r"[^A-Za-z0-9_-]+", "_", f"{sel_course}_{sel_parallel}_{sel_shift}_{sel_group}").strip("_") or "cohorte"

    if build_scientific_package is not None:

        if st.button("Generar paquete científico completo", key="generate_scientific_package", use_container_width=True):

            try:

                with st.spinner("Validando datos, ejecutando pruebas y construyendo informes..."):

                    st.session_state["scientific_package"] = build_scientific_package(

                        cohort_filtered,

                        exercise_df=exercise_df,

                        survey_df=survey_df,

                        interaction_df=interaction_df,

                        event_df=event_df,

                        item_df=item_df,

                        filters=report_filters,

                        research_title=RESEARCH_TITLE,

                    )

                    st.session_state["scientific_package_signature"] = report_signature

                st.success("Paquete científico generado correctamente.")

            except Exception as report_error:

                st.error(f"No se pudo generar el paquete científico: {report_error}")

        package = st.session_state.get("scientific_package") if st.session_state.get("scientific_package_signature") == report_signature else None

        if package:

            summary = package.get("summary", {})

            s1, s2, s3, s4 = st.columns(4)

            s1.metric("Estudiantes", summary.get("students", 0))

            s2.metric("Pares completos", summary.get("paired", 0))

            s3.metric("Formas psicométricas", summary.get("psychometric_forms", 0))

            s4.metric("Contrastes", summary.get("group_comparisons", 0))

            d1, d2, d3 = st.columns(3)

            d1.download_button("Descargar paquete ZIP", package["zip"], f"paquete_cientifico_{safe_group}.zip", "application/zip", use_container_width=True)

            d2.download_button("Descargar informe Word", package["word"], f"informe_predictivo_{safe_group}.docx", "application/vnd.openxmlformats-officedocument.wordprocessingml.document", use_container_width=True)

            d3.download_button("Descargar informe PDF", package["pdf"], f"informe_predictivo_{safe_group}.pdf", "application/pdf", use_container_width=True)

            d4, d5, d6 = st.columns(3)

            d4.download_button("Descargar tablas Excel", package["excel"], f"tablas_dataset_{safe_group}.xlsx", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", use_container_width=True)

            d5.download_button("Descargar dataset limpio", package["csv"], f"dataset_procesado_{safe_group}.csv", "text/csv", use_container_width=True)

            d6.download_button("Descargar reporte JSON", package["json"], f"reporte_estadistico_{safe_group}.json", "application/json", use_container_width=True)

            for warning in summary.get("warnings", []):

                st.info(warning)

    elif build_research_word_report is not None:

        st.warning(f"El motor predictivo no está disponible; se mantiene el informe estadístico base. Detalle: {RESEARCH_ENGINE_IMPORT_ERROR}")

        if st.button("Generar análisis EDA e inferencial en Word", key="generate_research_word", use_container_width=True):

            st.session_state["research_word_report"] = build_research_word_report(cohort_filtered, exercise_df, survey_df, report_filters, RESEARCH_TITLE)

            st.session_state["research_word_signature"] = report_signature

        if st.session_state.get("research_word_signature") == report_signature and st.session_state.get("research_word_report"):

            st.download_button("Descargar informe estadístico Word", st.session_state["research_word_report"], f"informe_estadistico_{safe_group}.docx", "application/vnd.openxmlformats-officedocument.wordprocessingml.document", use_container_width=True)

    else:

        st.error(f"Los módulos estadísticos no están disponibles: {RESEARCH_ANALYTICS_IMPORT_ERROR}; {RESEARCH_ENGINE_IMPORT_ERROR}")

    st.caption("El análisis usa los filtros académicos y de grupo seleccionados; integra pretest y postest aunque la tabla visible muestre un instrumento.")



    e1, e2 = st.columns(2)

    if not exercise_df.empty:

        e1.download_button("CSV ejercicios guiados", exercise_df.to_csv(index=False).encode("utf-8-sig"), "ejercicios_guiados_bunsekichat.csv", "text/csv", use_container_width=True)

    else:

        e1.info("Aún no hay ejercicios guiados registrados.")

    if not survey_df.empty:

        e2.download_button("CSV encuesta final", survey_df.to_csv(index=False).encode("utf-8-sig"), "encuesta_final_bunsekichat.csv", "text/csv", use_container_width=True)

    else:

        e2.info("Aún no hay encuestas finales registradas.")





def render_bulk_enrollment_page(user):

    st.markdown("""

    <div class='teacher-card'>

        <h3>Matrícula masiva de investigación</h3>

        <p class='small'>Crea cuentas oficiales desde Excel o CSV, valida cédula/correo y entrega credenciales temporales para el primer ingreso.</p>

    </div>

    """, unsafe_allow_html=True)



    c1, c2, c3 = st.columns(3)

    c1.metric("Cohorte oficial", "2026")

    c2.metric("Estado inicial", "Oficial")

    c3.metric("Análisis", "Después del consentimiento")

    st.code(OFFICIAL_COHORT_CODE, language=None)

    st.download_button(

        "Descargar plantilla Excel",

        build_enrollment_template_excel(),

        "plantilla_matricula_bunsekichat_2026.xlsx",

        "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",

        use_container_width=True,

    )

    st.caption("Use la plantilla y mantenga la cédula como texto de 10 dígitos. Los grupos permitidos son Experimental y Control.")



    uploaded = st.file_uploader("Subir listado de estudiantes", type=["xlsx", "csv"], key="bulk_enrollment_file")

    checked = pd.DataFrame()

    preview_issues = pd.DataFrame()

    if uploaded is not None:

        try:

            raw_df = read_enrollment_upload(uploaded)

            normalized, file_issues = prepare_enrollment_dataframe(raw_df)

            checked, database_issues = validate_enrollment_against_database(normalized)

            preview_issues = pd.concat([file_issues, database_issues], ignore_index=True).drop_duplicates()

            valid_count = int(checked["valido"].sum()) if not checked.empty else 0

            invalid_count = len(checked) - valid_count

            m1, m2, m3 = st.columns(3)

            m1.metric("Filas leídas", len(checked))

            m2.metric("Listas para crear", valid_count)

            m3.metric("Con observaciones", invalid_count)

            visible_columns = [column for column in [

                "fila_origen", "cedula", "nombres", "apellidos", "correo", "materia", "curso",

                "paralelo", "jornada", "grupo_investigacion", "cohorte", "valido", "errores",

            ] if column in checked.columns]

            st.dataframe(checked[visible_columns], use_container_width=True, height=360)

            if not preview_issues.empty:

                with st.expander("Ver observaciones de validación", expanded=True):

                    st.dataframe(preview_issues, use_container_width=True, height=220)

                    st.download_button(

                        "Descargar errores CSV",

                        preview_issues.to_csv(index=False).encode("utf-8-sig"),

                        "errores_matricula_bunsekichat.csv",

                        "text/csv",

                        use_container_width=True,

                    )

            confirm = st.checkbox(

                "Confirmo que revisé la cohorte, los grupos y los datos personales.",

                key="confirm_bulk_enrollment",

            )

            if st.button(

                "Crear estudiantes oficiales",

                disabled=valid_count == 0 or not confirm,

                use_container_width=True,

                key="create_bulk_enrollment",

            ):

                with st.spinner("Creando usuarios y registrando la auditoría de matrícula..."):

                    result = import_bulk_enrollment(checked, user["id"], uploaded.name)

                st.session_state["bulk_enrollment_result"] = result

                st.success(f"Lote {result['batch_id']} procesado: {result['created']} creados y {result['rejected']} rechazados.")

                st.rerun()

        except Exception as error:

            st.error(f"No se pudo procesar el archivo: {error}")



    result = st.session_state.get("bulk_enrollment_result")

    if result:

        credentials = result.get("credentials", pd.DataFrame())

        errors = result.get("errors", pd.DataFrame())

        st.markdown("### Resultado del último lote")

        if not credentials.empty:

            st.warning("Descargue y entregue estas credenciales de forma privada. Las contraseñas temporales no pueden recuperarse después de cerrar esta sesión.")

            st.dataframe(credentials, use_container_width=True, height=260)

            output = BytesIO()

            with pd.ExcelWriter(output, engine="openpyxl") as writer:

                credentials.to_excel(writer, sheet_name="Credenciales", index=False)

                if not errors.empty:

                    errors.to_excel(writer, sheet_name="Rechazados", index=False)

            d1, d2 = st.columns(2)

            d1.download_button(

                "Descargar credenciales Excel",

                output.getvalue(),

                f"credenciales_lote_{result['batch_id']}.xlsx",

                "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",

                use_container_width=True,

            )

            d2.download_button(

                "Descargar credenciales CSV",

                credentials.to_csv(index=False).encode("utf-8-sig"),

                f"credenciales_lote_{result['batch_id']}.csv",

                "text/csv",

                use_container_width=True,

            )

        if not errors.empty:

            st.markdown("#### Filas rechazadas")

            st.dataframe(errors, use_container_width=True, height=220)



    batches = pd.DataFrame(get_recent_enrollment_batches())

    if not batches.empty:

        st.markdown("### Historial de matrículas")

        st.dataframe(batches, use_container_width=True, height=260)



# BUNSEKI_R8_5_ADMIN_PASSWORD_RESET_V1
# BUNSEKI_R8_6_RAW_HTML_UI_FIX_V2
# BUNSEKI_R8_9_V2_BRAND_MATH_SCIENCE
# BUNSEKI_R8_11_TEACHER_INTERVENTION_V1
# BUNSEKI_R8_14_V3_TEACHER_ASSIGNMENT_ADMIN
def admin_reset_student_password(admin_user, target_user_id, new_password, force_change=False):
    admin_role = str((admin_user or {}).get("role") or "").strip().lower()
    if admin_role != "admin":
        raise ValueError("Solo un administrador puede restablecer contraseñas.")

    target = get_user(int(target_user_id))
    if not target or str(target.get("role") or "").strip().lower() != "student":
        raise ValueError("El usuario seleccionado no es un estudiante válido.")

    if not target.get("active"):
        raise ValueError("El estudiante seleccionado está inactivo.")

    password = str(new_password or "")
    if (
        len(password) < 8
        or not re.search(r"[A-Za-z]", password)
        or not re.search(r"\d", password)
        or not re.search(r"[^A-Za-z0-9]", password)
    ):
        raise ValueError(
            "La contraseña debe tener al menos 8 caracteres, "
            "incluyendo letras, número y símbolo."
        )

    update_user(
        int(target_user_id),
        {
            "password_hash": hash_password(password),
            "password_temporal": bool(force_change),
            "primer_ingreso": bool(force_change),
            "last_login": None,
        },
    )
    return get_user(int(target_user_id))


def render_admin_password_reset(admin_user):
    st.markdown("### Restablecer contraseña de estudiante")
    st.caption(
        "El administrador asigna una contraseña nueva. "
        "La contraseña anterior nunca se muestra ni se puede recuperar."
    )

    students = fetchall(
        "SELECT u.id,u.username,u.active,p.first_names,p.last_names "
        "FROM users u "
        "LEFT JOIN profiles p ON p.user_id=u.id "
        "WHERE LOWER(COALESCE(u.role,'student'))='student' "
        "ORDER BY u.username"
    )

    if not students:
        st.info("No hay estudiantes registrados.")
        return

    by_id = {int(row["id"]): row for row in students}

    def _student_label(user_id):
        row = by_id[int(user_id)]
        full_name = normalize_spaces(
            f"{row.get('first_names') or ''} {row.get('last_names') or ''}"
        )
        username = row.get("username") or f"usuario_{user_id}"
        state = "activo" if row.get("active") else "inactivo"
        return f"{full_name or username} · {username} · ID {user_id} · {state}"

    selected_id = st.selectbox(
        "Estudiante",
        list(by_id),
        format_func=_student_label,
        key="admin_password_reset_student",
    )

    new_password = st.text_input(
        "Nueva contraseña",
        type="password",
        key="admin_password_reset_new",
        help="Mínimo 8 caracteres, con letras, número y símbolo.",
    )
    confirm_password = st.text_input(
        "Confirmar nueva contraseña",
        type="password",
        key="admin_password_reset_confirm",
    )

    force_change = st.checkbox(
        "Forzar cambio de contraseña en el próximo ingreso",
        value=False,
        key="admin_password_reset_force_change",
        help=(
            "Para la cuenta de prueba puede dejarlo desmarcado. "
            "Si se activa, el estudiante pasará por el flujo de primer ingreso."
        ),
    )

    confirmation = st.text_input(
        "Confirmación de seguridad",
        key="admin_password_reset_confirmation",
        placeholder="Escriba RESTABLECER",
    )

    if st.button(
        "Restablecer contraseña",
        key="admin_password_reset_button",
        use_container_width=True,
    ):
        if new_password != confirm_password:
            st.error("Las contraseñas no coinciden.")
            return

        if confirmation.strip().upper() != "RESTABLECER":
            st.error("Escriba RESTABLECER para confirmar la operación.")
            return

        try:
            updated = admin_reset_student_password(
                admin_user,
                int(selected_id),
                new_password,
                force_change=force_change,
            )
            st.success(
                f"Contraseña restablecida para {updated.get('username')}. "
                "La contraseña no se mostrará nuevamente."
            )
        except ValueError as exc:
            st.error(str(exc))



# BUNSEKI_R8_9_V2_BRAND_MATH_SCIENCE
def inject_student_layout_cleanup_v2():
    st.markdown(
        """
        <style>
        .block-container{
            width:100% !important;
            max-width:1180px !important;
            margin-left:auto !important;
            margin-right:auto !important;
            box-sizing:border-box !important;
        }
        .student-export-card,
        .card,
        .saas-help-card,
        .quiz-card-premium{
            width:100% !important;
            max-width:100% !important;
            box-sizing:border-box !important;
            overflow:hidden !important;
        }
        div[data-testid="stMarkdownContainer"] pre,
        div[data-testid="stMarkdownContainer"] code{
            max-width:100% !important;
            white-space:pre-wrap !important;
            overflow-wrap:anywhere !important;
            overflow-x:hidden !important;
        }
        </style>
        """,
        unsafe_allow_html=True,
    )


def _science_dt(series):
    return pd.to_datetime(series, errors="coerce", utc=True)


def _science_risk_band(value):
    score = float(value or 0)
    if score >= 60:
        return "Alta"
    if score >= 35:
        return "Media"
    return "Baja"


def _science_recommendation(row):
    days = float(row.get("Días sin actividad") or 0)
    interactions = float(row.get("Interacciones 14d") or 0)
    attempts = float(row.get("Prácticas 30d") or 0)
    accuracy = row.get("Precisión 30d (%)")

    if days >= 14:
        return "Contacto de reenganche y plan breve de retorno"
    if pd.notna(accuracy) and attempts >= 5 and float(accuracy) < 60:
        return "Refuerzo focalizado en temas de menor precisión"
    if interactions <= 2:
        return "Actividad guiada corta y seguimiento en 7 días"
    return "Seguimiento ordinario"


def render_scientific_learning_analytics_dashboard():
    st.markdown("## 🧭 Learning Analytics Science Lab")
    st.caption(
        "Analítica educativa para acompañamiento docente. Las alertas son heurísticas "
        "transparentes: orientan intervención temprana y no sustituyen el juicio académico."
    )

    students = pd.DataFrame(
        fetchall(
            """
            SELECT
                u.id AS user_id,
                u.username,
                u.last_login,
                u.active,
                p.first_names,
                p.last_names,
                p.subject,
                p.course,
                p.course_level,
                p.parallel,
                p.shift,
                p.level
            FROM users u
            LEFT JOIN profiles p ON p.user_id=u.id
            WHERE LOWER(COALESCE(u.role,'student'))='student'
              AND COALESCE(u.active,TRUE)=TRUE
            ORDER BY u.username
            """
        )
    )

    if students.empty:
        st.info("No hay estudiantes activos para construir indicadores.")
        return

    for col in ["subject", "course_level", "parallel", "shift"]:
        if col not in students.columns:
            students[col] = None

    f1, f2, f3, f4 = st.columns(4)

    def _filter_options(column):
        values = sorted(
            {
                str(v).strip()
                for v in students[column].dropna().tolist()
                if str(v).strip()
            }
        )
        return ["Todos"] + values

    subject = f1.selectbox(
        "Materia",
        _filter_options("subject"),
        key="science_kpi_subject_v2",
    )
    course_level = f2.selectbox(
        "Nivel/curso",
        _filter_options("course_level"),
        key="science_kpi_level_v2",
    )
    parallel = f3.selectbox(
        "Paralelo",
        _filter_options("parallel"),
        key="science_kpi_parallel_v2",
    )
    shift = f4.selectbox(
        "Jornada",
        _filter_options("shift"),
        key="science_kpi_shift_v2",
    )

    filtered = students.copy()
    for column, value in {
        "subject": subject,
        "course_level": course_level,
        "parallel": parallel,
        "shift": shift,
    }.items():
        if value != "Todos":
            filtered = filtered[
                filtered[column].fillna("").astype(str).str.strip() == value
            ]

    if filtered.empty:
        st.warning("No hay estudiantes que coincidan con los filtros seleccionados.")
        return

    selected_ids = set(filtered["user_id"].dropna().astype(int).tolist())

    interactions = pd.DataFrame(
        fetchall(
            """
            SELECT user_id, topic, subtopic, level, latency_ms, created_at
            FROM interactions
            """
        )
    )
    practice = pd.DataFrame(
        fetchall(
            """
            SELECT user_id, topic, subtopic, difficulty_level, is_correct, created_at
            FROM research_exercise_attempts
            """
        )
    )
    adaptive = pd.DataFrame(
        fetchall(
            """
            SELECT topic, subtopic, difficulty_level, is_correct,
                   response_time_seconds, attempts
            FROM adaptive_questions
            """
        )
    )
    assessment_attempts = pd.DataFrame(
        fetchall(
            """
            SELECT user_id, teacher_assessment_id, status, resume_count,
                   created_at, updated_at
            FROM assessment_attempts
            WHERE teacher_assessment_id IS NOT NULL
            """
        )
    )
    location_events = pd.DataFrame(
        fetchall(
            """
            SELECT user_id, created_at
            FROM location_events
            """
        )
    )

    now_utc = pd.Timestamp.now(tz="UTC")
    cut_14 = now_utc - pd.Timedelta(days=14)
    cut_28 = now_utc - pd.Timedelta(days=28)
    cut_30 = now_utc - pd.Timedelta(days=30)
    cut_56 = now_utc - pd.Timedelta(days=56)

    if interactions.empty:
        interactions = pd.DataFrame(
            columns=["user_id", "topic", "subtopic", "created_at", "created_dt"]
        )
    else:
        interactions = interactions[
            interactions["user_id"].isin(selected_ids)
        ].copy()
        interactions["created_dt"] = _science_dt(interactions["created_at"])

    if practice.empty:
        practice = pd.DataFrame(
            columns=[
                "user_id", "topic", "subtopic", "is_correct",
                "created_at", "created_dt", "correct_num"
            ]
        )
    else:
        practice = practice[
            practice["user_id"].isin(selected_ids)
        ].copy()
        practice["created_dt"] = _science_dt(practice["created_at"])
        practice["correct_num"] = practice["is_correct"].fillna(False).astype(int)

    if assessment_attempts.empty:
        assessment_attempts = pd.DataFrame(
            columns=[
                "user_id", "status", "resume_count", "created_dt", "updated_dt"
            ]
        )
    else:
        assessment_attempts = assessment_attempts[
            assessment_attempts["user_id"].isin(selected_ids)
        ].copy()
        assessment_attempts["created_dt"] = _science_dt(
            assessment_attempts["created_at"]
        )
        assessment_attempts["updated_dt"] = _science_dt(
            assessment_attempts["updated_at"]
        )

    if location_events.empty:
        location_events = pd.DataFrame(columns=["user_id", "created_dt"])
    else:
        location_events = location_events[
            location_events["user_id"].isin(selected_ids)
        ].copy()
        location_events["created_dt"] = _science_dt(location_events["created_at"])

    filtered["last_login_dt"] = _science_dt(filtered["last_login"])

    recent14 = interactions[
        interactions["created_dt"].notna()
        & (interactions["created_dt"] >= cut_14)
    ]
    prev14 = interactions[
        interactions["created_dt"].notna()
        & (interactions["created_dt"] >= cut_28)
        & (interactions["created_dt"] < cut_14)
    ]
    practice30 = practice[
        practice["created_dt"].notna()
        & (practice["created_dt"] >= cut_30)
    ]

    active_ids = set(recent14["user_id"].dropna().astype(int).tolist())
    active_ids.update(practice30["user_id"].dropna().astype(int).tolist())

    if not assessment_attempts.empty:
        recent_assessment = assessment_attempts[
            assessment_attempts["updated_dt"].notna()
            & (assessment_attempts["updated_dt"] >= cut_14)
        ]
        active_ids.update(
            recent_assessment["user_id"].dropna().astype(int).tolist()
        )

    overall_accuracy = (
        float(practice30["correct_num"].mean() * 100)
        if not practice30.empty
        else float("nan")
    )

    interactions_14 = int(len(recent14))
    interactions_prev = int(len(prev14))
    momentum_delta = interactions_14 - interactions_prev

    gps_recent_ids = set()
    if not location_events.empty:
        gps_recent_ids = set(
            location_events.loc[
                location_events["created_dt"].notna()
                & (location_events["created_dt"] >= cut_30),
                "user_id",
            ]
            .dropna()
            .astype(int)
            .tolist()
        )
    gps_coverage = 100.0 * len(gps_recent_ids) / max(1, len(selected_ids))

    base = filtered[
        [
            "user_id", "username", "first_names", "last_names",
            "last_login_dt", "subject", "course_level", "parallel", "shift"
        ]
    ].copy()

    base["Estudiante"] = (
        base["first_names"].fillna("").astype(str).str.strip()
        + " "
        + base["last_names"].fillna("").astype(str).str.strip()
    ).str.strip()
    base["Estudiante"] = base.apply(
        lambda row: row["Estudiante"] or str(row["username"]),
        axis=1,
    )

    if not interactions.empty:
        last_interaction = interactions.groupby("user_id")["created_dt"].max()
        count14 = recent14.groupby("user_id").size()
        count_prev14 = prev14.groupby("user_id").size()
    else:
        last_interaction = pd.Series(dtype="datetime64[ns, UTC]")
        count14 = pd.Series(dtype="int64")
        count_prev14 = pd.Series(dtype="int64")

    if not practice.empty:
        last_practice = practice.groupby("user_id")["created_dt"].max()
    else:
        last_practice = pd.Series(dtype="datetime64[ns, UTC]")

    if not practice30.empty:
        practice_count30 = practice30.groupby("user_id").size()
        practice_accuracy30 = (
            practice30.groupby("user_id")["correct_num"].mean() * 100
        )
    else:
        practice_count30 = pd.Series(dtype="int64")
        practice_accuracy30 = pd.Series(dtype="float64")

    completed_statuses = {
        "completed", "submitted", "graded", "finished", "complete"
    }

    if not assessment_attempts.empty:
        last_assessment = assessment_attempts.groupby("user_id")["updated_dt"].max()
        resume_sum = (
            assessment_attempts.groupby("user_id")["resume_count"]
            .sum(min_count=1)
            .fillna(0)
        )
        open_mask = ~assessment_attempts["status"].fillna("").astype(str).str.lower().isin(
            completed_statuses
        )
        open_attempts = assessment_attempts[open_mask].groupby("user_id").size()
    else:
        last_assessment = pd.Series(dtype="datetime64[ns, UTC]")
        resume_sum = pd.Series(dtype="float64")
        open_attempts = pd.Series(dtype="int64")

    base["Interacciones 14d"] = base["user_id"].map(count14).fillna(0).astype(int)
    base["Interacciones previas 14d"] = (
        base["user_id"].map(count_prev14).fillna(0).astype(int)
    )
    base["Prácticas 30d"] = (
        base["user_id"].map(practice_count30).fillna(0).astype(int)
    )
    base["Precisión 30d (%)"] = base["user_id"].map(practice_accuracy30)
    base["Reanudaciones"] = base["user_id"].map(resume_sum).fillna(0).astype(int)
    base["Intentos abiertos"] = base["user_id"].map(open_attempts).fillna(0).astype(int)

    base["last_interaction"] = base["user_id"].map(last_interaction)
    base["last_practice"] = base["user_id"].map(last_practice)
    base["last_assessment"] = base["user_id"].map(last_assessment)

    base["Última actividad"] = pd.concat(
        [
            base["last_login_dt"],
            base["last_interaction"],
            base["last_practice"],
            base["last_assessment"],
        ],
        axis=1,
    ).max(axis=1)

    base["Días sin actividad"] = (
        (now_utc - base["Última actividad"]).dt.total_seconds() / 86400
    )
    base["Días sin actividad"] = (
        base["Días sin actividad"].fillna(999).clip(lower=0).round(1)
    )

    base["Momentum"] = (
        (base["Interacciones 14d"] - base["Interacciones previas 14d"])
        / (base["Interacciones previas 14d"] + 1)
    )

    def _academic_risk(row):
        score = 0.0
        attempts = float(row["Prácticas 30d"])
        accuracy = row["Precisión 30d (%)"]
        days = float(row["Días sin actividad"])
        interactions14 = float(row["Interacciones 14d"])
        open_count = float(row["Intentos abiertos"])

        if attempts >= 5 and pd.notna(accuracy):
            score += min(48.0, max(0.0, (60.0 - float(accuracy)) * 1.2))
        elif attempts < 3:
            score += 8.0

        if days >= 21:
            score += 28.0
        elif days >= 14:
            score += 22.0
        elif days >= 7:
            score += 12.0
        elif days >= 3:
            score += 5.0

        if interactions14 == 0:
            score += 18.0
        elif interactions14 <= 2:
            score += 10.0

        if open_count > 0:
            score += min(12.0, open_count * 6.0)

        return min(100.0, round(score, 1))

    def _disconnect_risk(row):
        score = 0.0
        days = float(row["Días sin actividad"])
        interactions14 = float(row["Interacciones 14d"])
        momentum = float(row["Momentum"])

        if days >= 21:
            score += 60.0
        elif days >= 14:
            score += 48.0
        elif days >= 7:
            score += 30.0
        elif days >= 3:
            score += 15.0

        if interactions14 == 0:
            score += 25.0
        elif interactions14 <= 2:
            score += 15.0

        if momentum <= -0.50:
            score += 15.0

        return min(100.0, round(score, 1))

    base["Riesgo académico"] = base.apply(_academic_risk, axis=1)
    base["Riesgo desconexión"] = base.apply(_disconnect_risk, axis=1)
    base["Alerta académica"] = base["Riesgo académico"].map(_science_risk_band)
    base["Alerta desconexión"] = base["Riesgo desconexión"].map(_science_risk_band)
    base["Acción sugerida"] = base.apply(_science_recommendation, axis=1)

    high_academic = int((base["Riesgo académico"] >= 60).sum())
    high_disconnect = int((base["Riesgo desconexión"] >= 60).sum())

    m1, m2, m3 = st.columns(3)
    m1.metric("Estudiantes activos 14d", f"{len(active_ids)}/{len(selected_ids)}")
    m2.metric(
        "Interacciones 14d",
        interactions_14,
        delta=momentum_delta,
        help="Variación frente a los 14 días anteriores.",
    )
    m3.metric(
        "Precisión práctica 30d",
        "Sin datos" if pd.isna(overall_accuracy) else f"{overall_accuracy:.1f}%",
    )

    m4, m5, m6 = st.columns(3)
    m4.metric("Alerta académica alta", high_academic)
    m5.metric("Riesgo desconexión alto", high_disconnect)
    m6.metric(
        "Cobertura GPS 30d",
        f"{gps_coverage:.1f}%",
        help=(
            "Solo cobertura agregada. La ubicación exacta no entra en el puntaje "
            "individual de riesgo."
        ),
    )

    st.markdown("### 🔬 Fricción de aprendizaje por tema")

    interactions30 = interactions[
        interactions["created_dt"].notna()
        & (interactions["created_dt"] >= cut_30)
    ].copy()

    if not interactions30.empty:
        interactions30["Tema"] = (
            interactions30["topic"]
            .fillna("Sin tema")
            .astype(str)
            .str.strip()
            .replace("", "Sin tema")
        )
        topic_i = (
            interactions30.groupby("Tema")
            .agg(
                **{
                    "Interacciones 30d": ("user_id", "size"),
                    "Estudiantes activos": ("user_id", "nunique"),
                }
            )
            .reset_index()
        )
    else:
        topic_i = pd.DataFrame(
            columns=["Tema", "Interacciones 30d", "Estudiantes activos"]
        )

    if not practice30.empty:
        practice30 = practice30.copy()
        practice30["Tema"] = (
            practice30["topic"]
            .fillna("Sin tema")
            .astype(str)
            .str.strip()
            .replace("", "Sin tema")
        )
        topic_p = (
            practice30.groupby("Tema")
            .agg(
                **{
                    "Prácticas 30d": ("correct_num", "size"),
                    "Precisión (%)": ("correct_num", "mean"),
                }
            )
            .reset_index()
        )
        topic_p["Precisión (%)"] = topic_p["Precisión (%)"] * 100
    else:
        topic_p = pd.DataFrame(
            columns=["Tema", "Prácticas 30d", "Precisión (%)"]
        )

    if not adaptive.empty:
        adaptive = adaptive.copy()
        adaptive["Tema"] = (
            adaptive["topic"]
            .fillna("Sin tema")
            .astype(str)
            .str.strip()
            .replace("", "Sin tema")
        )
        adaptive["response_time_seconds"] = pd.to_numeric(
            adaptive["response_time_seconds"], errors="coerce"
        )
        adaptive["attempts"] = pd.to_numeric(
            adaptive["attempts"], errors="coerce"
        )
        topic_a = (
            adaptive.groupby("Tema")
            .agg(
                **{
                    "Tiempo respuesta (s)": ("response_time_seconds", "mean"),
                    "Reintentos promedio": ("attempts", "mean"),
                }
            )
            .reset_index()
        )
    else:
        topic_a = pd.DataFrame(
            columns=["Tema", "Tiempo respuesta (s)", "Reintentos promedio"]
        )

    topic = topic_i.merge(topic_p, on="Tema", how="outer").merge(
        topic_a,
        on="Tema",
        how="outer",
    )

    if topic.empty:
        st.info("Todavía no hay suficiente información temática.")
    else:
        accuracy_gap = (
            100 - pd.to_numeric(topic["Precisión (%)"], errors="coerce")
            .fillna(50)
            .clip(lower=0, upper=100)
        )
        latency = pd.to_numeric(
            topic["Tiempo respuesta (s)"], errors="coerce"
        )
        retry = pd.to_numeric(
            topic["Reintentos promedio"], errors="coerce"
        )
        latency_rank = latency.fillna(latency.median()).rank(pct=True) * 100
        retry_rank = retry.fillna(retry.median()).rank(pct=True) * 100

        topic["Índice fricción"] = (
            0.60 * accuracy_gap
            + 0.25 * latency_rank.fillna(0)
            + 0.15 * retry_rank.fillna(0)
        ).round(1)

        topic = topic.sort_values(
            ["Índice fricción", "Interacciones 30d"],
            ascending=[False, False],
        )

        st.caption(
            "Índice de fricción = baja precisión + mayor tiempo de respuesta + "
            "reintentos. Se usa para priorizar apoyo didáctico."
        )
        st.dataframe(
            topic.head(20),
            use_container_width=True,
            hide_index=True,
        )

        chart = topic.head(10).set_index("Tema")[["Índice fricción"]]
        if not chart.empty:
            st.bar_chart(chart)

    st.markdown("### 📈 Evolución de KPIs · últimas 8 semanas")

    interaction_window = interactions[
        interactions["created_dt"].notna()
        & (interactions["created_dt"] >= cut_56)
    ].copy()
    practice_window = practice[
        practice["created_dt"].notna()
        & (practice["created_dt"] >= cut_56)
    ].copy()

    c1, c2 = st.columns(2)

    with c1:
        st.caption("Interacciones semanales")
        if interaction_window.empty:
            st.info("Sin datos de interacción.")
        else:
            interaction_window["Semana"] = (
                interaction_window["created_dt"]
                .dt.tz_convert(None)
                .dt.to_period("W")
                .astype(str)
            )
            weekly_i = (
                interaction_window.groupby("Semana")
                .size()
                .rename("Interacciones")
                .to_frame()
            )
            st.line_chart(weekly_i)

    with c2:
        st.caption("Precisión semanal de práctica")
        if practice_window.empty:
            st.info("Sin datos de precisión.")
        else:
            practice_window["Semana"] = (
                practice_window["created_dt"]
                .dt.tz_convert(None)
                .dt.to_period("W")
                .astype(str)
            )
            weekly_p = (
                practice_window.groupby("Semana")["correct_num"]
                .mean()
                .mul(100)
                .rename("Precisión (%)")
                .to_frame()
            )
            st.line_chart(weekly_p)

    st.markdown("### 🚦 Matriz de acompañamiento")

    alerts = base[
        [
            "Estudiante",
            "username",
            "Días sin actividad",
            "Interacciones 14d",
            "Prácticas 30d",
            "Precisión 30d (%)",
            "Reanudaciones",
            "Intentos abiertos",
            "Riesgo académico",
            "Riesgo desconexión",
            "Alerta académica",
            "Alerta desconexión",
            "Acción sugerida",
        ]
    ].copy()

    alerts["Precisión 30d (%)"] = pd.to_numeric(
        alerts["Precisión 30d (%)"],
        errors="coerce",
    ).round(1)

    alerts = alerts.sort_values(
        ["Riesgo académico", "Riesgo desconexión", "Días sin actividad"],
        ascending=[False, False, False],
    )

    st.dataframe(
        alerts.head(30),
        use_container_width=True,
        hide_index=True,
    )

    with st.expander("Metodología y límites de las alertas"):
        st.markdown(
            """
            **Riesgo académico:** combina desempeño reciente, práctica, actividad
            e intentos pendientes.

            **Riesgo de desconexión:** combina recencia, volumen de interacción
            y pérdida de momentum.

            **Ubicación:** se usa únicamente como indicador agregado de cobertura.
            La distancia o ubicación exacta del estudiante no se utiliza para
            clasificar su riesgo individual.

            **Uso:** estas señales priorizan acompañamiento; no deben emplearse
            como única base para decisiones académicas.
            """
        )



# BUNSEKI_R8_14_TEACHER_ASSIGNMENT_ADMIN_V1
def _teacher_assignment_admin_write(statements):
    """Execute a small admin-approved transaction without exposing credentials."""
    dsn = (
        globals().get("DATABASE_URL")
        or os.getenv("DATABASE_URL")
        or os.getenv("BUNSEKI_TEST_DATABASE_URL")
        or ""
    )
    dsn = str(dsn or "").strip()
    if not dsn:
        raise RuntimeError("DATABASE_URL no configurada.")

    import psycopg2

    conn = psycopg2.connect(dsn)
    try:
        conn.autocommit = False
        with conn.cursor() as cur:
            for statement, params in statements:
                cur.execute(statement, params)
        conn.commit()
    except Exception:
        conn.rollback()
        raise
    finally:
        conn.close()


def _teacher_assignment_catalog():
    courses = pd.DataFrame(
        fetchall(
            """
            SELECT
                c.id,
                c.code,
                c.name,
                c.status,
                c.academic_period_id,
                s.name AS subject_name
            FROM courses c
            LEFT JOIN subjects s ON s.id=c.subject_id
            ORDER BY COALESCE(s.name,''), COALESCE(c.name,''), c.id
            """
        )
    )

    parallels = pd.DataFrame(
        fetchall(
            """
            SELECT
                p.id,
                p.course_id,
                p.code,
                p.name,
                p.active,
                c.code AS course_code,
                c.name AS course_name,
                s.name AS subject_name
            FROM parallels p
            JOIN courses c ON c.id=p.course_id
            LEFT JOIN subjects s ON s.id=c.subject_id
            ORDER BY COALESCE(s.name,''), COALESCE(c.name,''), COALESCE(p.name,''), p.id
            """
        )
    )

    return courses, parallels


def _teacher_assignment_rows(teacher_id):
    course_rows = pd.DataFrame(
        fetchall(
            """
            SELECT
                tc.course_id,
                tc.role,
                c.code,
                c.name,
                s.name AS subject_name
            FROM teacher_courses tc
            JOIN courses c ON c.id=tc.course_id
            LEFT JOIN subjects s ON s.id=c.subject_id
            WHERE tc.teacher_id=%s
            ORDER BY COALESCE(s.name,''), COALESCE(c.name,''), c.id
            """,
            (teacher_id,),
        )
    )

    parallel_rows = pd.DataFrame(
        fetchall(
            """
            SELECT
                tp.parallel_id,
                p.course_id,
                p.code,
                p.name,
                c.code AS course_code,
                c.name AS course_name,
                s.name AS subject_name
            FROM teacher_parallels tp
            JOIN parallels p ON p.id=tp.parallel_id
            JOIN courses c ON c.id=p.course_id
            LEFT JOIN subjects s ON s.id=c.subject_id
            WHERE tp.teacher_id=%s
            ORDER BY COALESCE(s.name,''), COALESCE(c.name,''), COALESCE(p.name,''), p.id
            """,
            (teacher_id,),
        )
    )

    return course_rows, parallel_rows


def _teacher_assignment_students(teacher_id):
    return pd.DataFrame(
        fetchall(
            """
            SELECT DISTINCT
                u.id AS user_id,
                u.username,
                COALESCE(
                    NULLIF(TRIM(CONCAT(COALESCE(pr.first_names,''),' ',COALESCE(pr.last_names,''))), ''),
                    u.username
                ) AS student_name,
                c.code AS course_code,
                c.name AS course_name,
                p.code AS parallel_code,
                p.name AS parallel_name,
                e.status AS enrollment_status
            FROM teachers t
            JOIN teacher_courses tc
              ON tc.teacher_id=t.id
            JOIN teacher_parallels tp
              ON tp.teacher_id=t.id
            JOIN enrollments e
              ON e.course_id=tc.course_id
             AND e.parallel_id=tp.parallel_id
            JOIN users u
              ON u.id=e.student_user_id
            LEFT JOIN profiles pr
              ON pr.user_id=u.id
            JOIN courses c
              ON c.id=e.course_id
            JOIN parallels p
              ON p.id=e.parallel_id
             AND p.course_id=e.course_id
            WHERE t.id=%s
            ORDER BY student_name, course_name, parallel_name
            """,
            (teacher_id,),
        )
    )


def render_admin_teacher_assignment_manager(admin_user):
    role = str((admin_user or {}).get("role") or "").strip().lower()
    if role != "admin":
        st.error("Solo un administrador puede gestionar asignaciones docentes.")
        return

    st.markdown("### 👨‍🏫 Gestión docente y asignaciones")
    st.caption(
        "Administra la relación Docente → Materia/Curso → Paralelo. "
        "El dashboard docente y sus indicadores usan estas asignaciones "
        "como fuente de verdad y mantienen acceso deny-by-default."
    )

    teacher_accounts = pd.DataFrame(
        fetchall(
            """
            SELECT
                u.id AS user_id,
                u.username,
                u.active,
                u.role,
                t.id AS teacher_id,
                t.status AS teacher_status
            FROM users u
            LEFT JOIN teachers t
              ON t.user_id=u.id
            WHERE LOWER(COALESCE(u.role,'')) IN ('teacher','docente')
            ORDER BY u.username
            """
        )
    )

    if teacher_accounts.empty:
        st.warning(
            "No existen cuentas con rol teacher/docente. "
            "Crea o habilita primero la cuenta de usuario docente y vuelve a esta sección."
        )
        return

    account_labels = {}
    for _, row in teacher_accounts.iterrows():
        uid = int(row["user_id"])
        status = "registrado" if pd.notna(row.get("teacher_id")) else "sin estructura docente"
        account_labels[uid] = f"{row['username']} · {status}"

    selected_user_id = st.selectbox(
        "Cuenta docente",
        list(account_labels.keys()),
        format_func=lambda value: account_labels[value],
        key="admin_teacher_assignment_user",
    )

    selected_row = teacher_accounts[
        teacher_accounts["user_id"].astype(int) == int(selected_user_id)
    ].iloc[0]

    teacher_id = (
        int(selected_row["teacher_id"])
        if pd.notna(selected_row.get("teacher_id"))
        else None
    )

    if not bool(selected_row.get("active")):
        st.warning("La cuenta seleccionada está inactiva.")

    if teacher_id is None:
        st.info(
            "La cuenta tiene rol docente, pero aún no está registrada en la tabla teachers."
        )
        confirm_register = st.checkbox(
            "Confirmo el alta estructural de este usuario como docente.",
            key=f"confirm_teacher_structure_{selected_user_id}",
        )
        if st.button(
            "Registrar estructura docente",
            disabled=not confirm_register,
            use_container_width=True,
            key=f"register_teacher_structure_{selected_user_id}",
        ):
            _teacher_assignment_admin_write(
                [
                    (
                        """
                        INSERT INTO teachers(user_id,status,created_at,updated_at)
                        VALUES (%s,'active',NOW(),NOW())
                        ON CONFLICT (user_id)
                        DO UPDATE SET status='active', updated_at=NOW()
                        """,
                        (int(selected_user_id),),
                    )
                ]
            )
            st.success("Docente registrado correctamente.")
            st.rerun()
        return

    courses, parallels = _teacher_assignment_catalog()
    assigned_courses, assigned_parallels = _teacher_assignment_rows(teacher_id)

    if courses.empty:
        st.warning("No existen cursos en el catálogo académico.")
        return

    current_course_ids = (
        assigned_courses["course_id"].dropna().astype(int).tolist()
        if not assigned_courses.empty
        else []
    )
    current_parallel_ids = (
        assigned_parallels["parallel_id"].dropna().astype(int).tolist()
        if not assigned_parallels.empty
        else []
    )

    course_labels = {}
    for _, row in courses.iterrows():
        cid = int(row["id"])
        subject = str(row.get("subject_name") or "").strip()
        code = str(row.get("code") or "").strip()
        name = str(row.get("name") or "").strip()
        pieces = [value for value in [subject, code, name] if value]
        course_labels[cid] = " · ".join(pieces) or f"Curso {cid}"

    selected_course_ids = st.multiselect(
        "Materias / cursos asignados",
        options=list(course_labels.keys()),
        default=[
            cid for cid in current_course_ids
            if cid in course_labels
        ],
        format_func=lambda value: course_labels[value],
        key=f"teacher_courses_{teacher_id}",
        help=(
            "Selecciona únicamente los cursos que el docente debe visualizar. "
            "Los indicadores no incluirán cursos no asignados."
        ),
    )

    eligible_parallels = parallels[
        parallels["course_id"].astype(int).isin(
            [int(value) for value in selected_course_ids]
        )
    ].copy()

    parallel_labels = {}
    for _, row in eligible_parallels.iterrows():
        pid = int(row["id"])
        subject = str(row.get("subject_name") or "").strip()
        course_name = str(row.get("course_name") or "").strip()
        pcode = str(row.get("code") or "").strip()
        pname = str(row.get("name") or "").strip()
        course_text = " · ".join(
            value for value in [subject, course_name] if value
        )
        parallel_text = " / ".join(
            value for value in [pcode, pname] if value
        )
        parallel_labels[pid] = (
            f"{course_text} → {parallel_text}"
            if course_text
            else parallel_text or f"Paralelo {pid}"
        )

    selected_parallel_ids = st.multiselect(
        "Paralelos asignados",
        options=list(parallel_labels.keys()),
        default=[
            pid for pid in current_parallel_ids
            if pid in parallel_labels
        ],
        format_func=lambda value: parallel_labels[value],
        key=f"teacher_parallels_{teacher_id}",
        help=(
            "El docente verá estudiantes e indicadores únicamente de estos paralelos "
            "dentro de los cursos seleccionados."
        ),
    )

    c1, c2, c3 = st.columns(3)
    c1.metric("Cursos actuales", len(current_course_ids))
    c2.metric("Paralelos actuales", len(current_parallel_ids))
    current_students = _teacher_assignment_students(teacher_id)
    c3.metric(
        "Estudiantes visibles",
        0 if current_students.empty else current_students["user_id"].nunique(),
    )

    with st.expander("Vista previa del alcance actual", expanded=False):
        if current_students.empty:
            st.info("No hay estudiantes matriculados dentro del alcance actual.")
        else:
            st.dataframe(
                current_students,
                use_container_width=True,
                hide_index=True,
            )

    changes_exist = (
        set(map(int, selected_course_ids)) != set(map(int, current_course_ids))
        or set(map(int, selected_parallel_ids)) != set(map(int, current_parallel_ids))
    )

    st.markdown("#### Guardar alcance docente")
    st.caption(
        "La operación reemplaza solamente las asignaciones del docente seleccionado. "
        "No elimina cursos, paralelos, estudiantes, evaluaciones ni registros históricos."
    )

    confirmation = st.checkbox(
        "Confirmo que revisé cursos y paralelos antes de guardar.",
        key=f"confirm_teacher_assignments_{teacher_id}",
    )

    if st.button(
        "Guardar asignaciones docente",
        disabled=(not confirmation or not changes_exist),
        use_container_width=True,
        key=f"save_teacher_assignments_{teacher_id}",
    ):
        selected_course_ids = [int(value) for value in selected_course_ids]
        selected_parallel_ids = [int(value) for value in selected_parallel_ids]

        if selected_parallel_ids:
            validation = pd.DataFrame(
                fetchall(
                    """
                    SELECT id, course_id
                    FROM parallels
                    WHERE id = ANY(%s)
                    """,
                    (selected_parallel_ids,),
                )
            )
            if len(validation) != len(set(selected_parallel_ids)):
                raise ValueError("Uno o más paralelos seleccionados ya no existen.")

            invalid_course_links = validation[
                ~validation["course_id"].astype(int).isin(selected_course_ids)
            ]
            if not invalid_course_links.empty:
                raise ValueError(
                    "Todos los paralelos deben pertenecer a un curso seleccionado."
                )

        statements = [
            (
                "DELETE FROM teacher_parallels WHERE teacher_id=%s",
                (teacher_id,),
            ),
            (
                "DELETE FROM teacher_courses WHERE teacher_id=%s",
                (teacher_id,),
            ),
        ]

        for course_id in selected_course_ids:
            statements.append(
                (
                    """
                    INSERT INTO teacher_courses(teacher_id,course_id,role,created_at)
                    VALUES (%s,%s,'teacher',NOW())
                    ON CONFLICT DO NOTHING
                    """,
                    (teacher_id, course_id),
                )
            )

        for parallel_id in selected_parallel_ids:
            statements.append(
                (
                    """
                    INSERT INTO teacher_parallels(teacher_id,parallel_id,created_at)
                    VALUES (%s,%s,NOW())
                    ON CONFLICT DO NOTHING
                    """,
                    (teacher_id, parallel_id),
                )
            )

        statements.append(
            (
                """
                UPDATE teachers
                SET status='active', updated_at=NOW()
                WHERE id=%s
                """,
                (teacher_id,),
            )
        )

        _teacher_assignment_admin_write(statements)
        st.success(
            "Asignaciones actualizadas. El próximo acceso del docente "
            "usará automáticamente este nuevo alcance."
        )
        st.rerun()


def admin_page(user):
    render_admin_ai_practice_bank_manager(user)
    with st.expander("👨‍🏫 Gestión docente y asignaciones", expanded=False):
        render_admin_teacher_assignment_manager(user)

    render_admin_password_reset(user)


    teacher_page = render_teacher_sidebar()



    # CSS específico para que el dashboard docente no se corte y ocupe todo el ancho.

    st.markdown("""

    <style>

    .block-container{

        max-width:100% !important;

        padding-left:2rem !important;

        padding-right:2rem !important;

        padding-bottom:5rem !important;

    }

    div[data-testid="stVerticalBlock"]{overflow:visible !important;}

    .stPlotlyChart{width:100% !important; overflow:visible !important;}

    div[data-testid="stDataFrame"]{width:100% !important;}

    </style>

    """, unsafe_allow_html=True)



    st.markdown("<div class='hero'>" + bunseki_logo_html("Dashboard docente PRO", "Analítica, trazabilidad, exportación y seguimiento académico") + "</div>", unsafe_allow_html=True)



    users, logs, quizzes = get_teacher_tables()

    students = users[users["role"].astype(str).str.lower().eq("student")].copy() if not users.empty and "role" in users.columns else pd.DataFrame()



    # Configuración solo cuando se elige el menú correspondiente

    if teacher_page == "⚙️ Configuración":

        st.markdown("<div class='teacher-card'><h3>⚙️ Configuración</h3><p class='small'>Ajustes generales de sesión y parámetros administrativos.</p></div>", unsafe_allow_html=True)

        timeout = st.number_input("Inactividad (minutos)", 5, 180, get_timeout())

        if st.button("Guardar configuración", use_container_width=True):

            set_timeout(int(timeout)); st.success("Configuración guardada.")

        return



    if teacher_page == "📚 Planes analíticos":

        render_teacher_plan_manager(user)

        return



    if teacher_page == "👥 Matrícula masiva":

        render_bulk_enrollment_page(user)

        return



    if teacher_page == "🧪 Investigación pre/post":

        render_teacher_research_dashboard(user)

        return



    render_scientific_learning_analytics_dashboard()
    st.divider()
    st.caption("Indicadores operativos existentes")

    total_students = len(students)

    user_logs_all = logs[logs["role"].astype(str).str.lower().eq("user")].copy() if not logs.empty and "role" in logs.columns else pd.DataFrame()

    total_questions = len(user_logs_all)

    active_students = user_logs_all["user_id"].nunique() if not user_logs_all.empty and "user_id" in user_logs_all.columns else 0



    if not quizzes.empty and "score" in quizzes.columns:

        quizzes["score"] = pd.to_numeric(quizzes["score"], errors="coerce")

        avg_score = quizzes["score"].dropna().mean()

        if pd.isna(avg_score):

            avg_score = 0

    else:

        avg_score = 0



    k1,k2,k3,k4 = st.columns(4)

    k1.markdown(f"<div class='metric'><b>{total_students}</b><br><span>Estudiantes</span></div>", unsafe_allow_html=True)

    k2.markdown(f"<div class='metric'><b>{total_questions}</b><br><span>Consultas realizadas</span></div>", unsafe_allow_html=True)

    k3.markdown(f"<div class='metric'><b>{active_students}</b><br><span>Estudiantes activos</span></div>", unsafe_allow_html=True)

    k4.markdown(f"<div class='metric'><b>{avg_score:.0f}%</b><br><span>Promedio pruebas</span></div>", unsafe_allow_html=True)



    st.markdown("<div class='teacher-card'><h3>Filtros académicos</h3></div>", unsafe_allow_html=True)

    f1,f2,f3,f4,f5,f6 = st.columns(6)



    def opts(df, col):

        if df.empty or col not in df.columns:

            return ["Todos"]

        vals = sorted([str(x).strip() for x in df[col].dropna().unique() if str(x).strip()])

        return ["Todos"] + vals



    sel_subject = f1.selectbox("Materia", opts(students, "subject"))

    sel_course = f2.selectbox("Curso", opts(students, "course"))

    sel_parallel = f3.selectbox("Paralelo", opts(students, "parallel"))

    sel_shift = f4.selectbox("Jornada", opts(students, "shift"))

    sel_teacher = f5.selectbox("Docente", opts(students, "teacher"))

    sel_level = f6.selectbox("Nivel", opts(students, "level"))

    f7, f8, f9 = st.columns(3)

    sel_data_status = f7.selectbox("Tipo de dato", ["Todos", "Solo oficiales", "Solo piloto"], key="admin_data_status")

    sel_cohort = f8.selectbox("Cohorte de investigación", opts(students, "cohort"), key="admin_cohort")

    sel_research_group = f9.selectbox("Grupo de investigación", opts(students, "research_group"), key="admin_research_group")



    filtered_students = students.copy()

    if not filtered_students.empty:

        if sel_subject != "Todos": filtered_students = filtered_students[filtered_students["subject"].astype(str) == sel_subject]

        if sel_course != "Todos": filtered_students = filtered_students[filtered_students["course"].astype(str) == sel_course]

        if sel_parallel != "Todos": filtered_students = filtered_students[filtered_students["parallel"].astype(str) == sel_parallel]

        if sel_shift != "Todos": filtered_students = filtered_students[filtered_students["shift"].astype(str) == sel_shift]

        if sel_teacher != "Todos": filtered_students = filtered_students[filtered_students["teacher"].astype(str) == sel_teacher]

        if sel_level != "Todos": filtered_students = filtered_students[filtered_students["level"].astype(str) == sel_level]

        if sel_data_status == "Solo oficiales": filtered_students = filtered_students[filtered_students["estado_dato"].fillna("piloto").astype(str).str.lower().eq("oficial")]

        if sel_data_status == "Solo piloto": filtered_students = filtered_students[filtered_students["estado_dato"].fillna("piloto").astype(str).str.lower().eq("piloto")]

        if sel_cohort != "Todos": filtered_students = filtered_students[filtered_students["cohort"].astype(str) == sel_cohort]

        if sel_research_group != "Todos": filtered_students = filtered_students[filtered_students["research_group"].astype(str) == sel_research_group]



    allowed_ids = set(filtered_students["id"].dropna().astype(int).tolist()) if not filtered_students.empty and "id" in filtered_students.columns else set()



    filtered_logs = logs.copy()

    if not filtered_logs.empty and "user_id" in filtered_logs.columns:

        filtered_logs = filtered_logs[filtered_logs["user_id"].isin(allowed_ids)]



    filtered_quizzes = quizzes.copy()

    if not filtered_quizzes.empty and "user_id" in filtered_quizzes.columns:

        filtered_quizzes = filtered_quizzes[filtered_quizzes["user_id"].isin(allowed_ids)]



    st.markdown(f"<div class='teacher-card'><b>Vista seleccionada en menú:</b> {teacher_page}</div>", unsafe_allow_html=True)



    if teacher_page == "📊 Dashboard docente PRO":

        st.markdown("<div class='teacher-card'><h3>📊 Dashboard docente PRO</h3><p class='small'>Resumen visual del desempeño, participación, cursos y resultados de pruebas.</p></div>", unsafe_allow_html=True)



        user_logs = filtered_logs[filtered_logs["role"].astype(str).str.lower().eq("user")].copy() if not filtered_logs.empty and "role" in filtered_logs.columns else pd.DataFrame()



        if user_logs.empty:

            st.info("Aún no existen consultas registradas con los filtros seleccionados.")

        else:

            c1, c2 = st.columns(2, gap="large")

            fig1 = px.histogram(user_logs, x="topic", color="course" if "course" in user_logs.columns else None, title="Consultas por área y curso")

            fig1.update_layout(height=430, margin={"r":10,"t":60,"l":10,"b":80})

            c1.plotly_chart(fig1, use_container_width=True)



            fig2 = px.histogram(user_logs, x="username" if "username" in user_logs.columns else "user_id", title="Participación por estudiante")

            fig2.update_layout(height=430, margin={"r":10,"t":60,"l":10,"b":100})

            c2.plotly_chart(fig2, use_container_width=True)



        if not filtered_quizzes.empty and "score" in filtered_quizzes.columns:

            fig3 = px.box(filtered_quizzes, x="topic", y="score", color="course" if "course" in filtered_quizzes.columns else None, title="Distribución de puntajes por tema")

            fig3.update_layout(height=430, margin={"r":10,"t":60,"l":10,"b":80})

            st.plotly_chart(fig3, use_container_width=True)

        else:

            st.info("Aún no existen pruebas registradas con los filtros seleccionados.")



        render_teacher_gps_heatmap(filtered_logs, filtered_students)

        return



    if teacher_page == "📥 Exportación":

        st.markdown("""

        <div class='teacher-card'>

            <h3>📥 Exportación por estudiante</h3>

            <p class='small'>Botones independientes para exportar CSV, Word y PDF con las consultas reales del estudiante.</p>

        </div>

        """, unsafe_allow_html=True)

        if filtered_students.empty:

            st.warning("No hay estudiantes con los filtros seleccionados.")

            return



        filtered_students = filtered_students.copy()

        filtered_students["display"] = filtered_students.apply(lambda r: f"{r.get('username','')} · {r.get('first_names') or ''} {r.get('last_names') or ''} · {r.get('subject') or ''} · {r.get('course') or ''}", axis=1)

        selected_label = st.selectbox("Selecciona estudiante para exportar", filtered_students["display"].tolist(), key="export_student_select")

        selected_id = int(filtered_students.loc[filtered_students["display"] == selected_label, "id"].iloc[0])

        profile, report_df = build_student_summary(selected_id)



        st.markdown(

            f"<div class='teacher-card'><h3>{profile.get('first_names') or ''} {profile.get('last_names') or profile.get('username')}</h3>"

            f"<span class='teacher-chip'>Usuario: {profile.get('username') or ''}</span>"

            f"<span class='teacher-chip'>Materia: {profile.get('subject') or 'Sin materia'}</span>"

            f"<span class='teacher-chip'>Curso: {profile.get('course') or 'Sin curso'}</span>"

            f"<span class='teacher-chip'>Docente: {profile.get('teacher') or 'Sin docente'}</span>"

            f"<span class='teacher-chip'>Nivel: {profile.get('level') or 'Sin nivel'}</span>"

            f"<span class='teacher-chip'>Registros: {len(report_df)}</span></div>",

            unsafe_allow_html=True,

        )



        if report_df.empty:

            st.warning("Este estudiante no tiene consultas registradas. El archivo se generará con encabezados, pero sin filas.")

        b1, b2, b3 = st.columns(3)

        b1.download_button("📊 Exportar CSV", report_df.to_csv(index=False).encode("utf-8-sig"), f"consultas_{profile.get('username','estudiante')}_{selected_id}.csv", "text/csv", use_container_width=True)

        try:

            b2.download_button("📝 Exportar Word", export_student_docx(selected_id), f"reporte_{profile.get('username','estudiante')}_{selected_id}.docx", "application/vnd.openxmlformats-officedocument.wordprocessingml.document", use_container_width=True)

        except Exception as e:

            b2.warning(f"Word no disponible: {e}")

        try:

            b3.download_button("📄 Exportar PDF", export_student_pdf(selected_id), f"reporte_{profile.get('username','estudiante')}_{selected_id}.pdf", "application/pdf", use_container_width=True)

        except Exception as e:

            b3.warning(f"PDF no disponible: {e}")



        st.markdown("<div class='teacher-card'><h3>Vista previa de consultas del estudiante</h3></div>", unsafe_allow_html=True)

        st.dataframe(report_df, use_container_width=True, height=420)

        return



    if teacher_page == "👤 Seguimiento individual":

        st.markdown("<div class='teacher-card'><h3>👤 Seguimiento individual</h3><p class='small'>Revisa el comportamiento académico de cada estudiante antes de exportar.</p></div>", unsafe_allow_html=True)

        if filtered_students.empty:

            st.warning("No hay estudiantes con los filtros seleccionados.")

            return



        filtered_students = filtered_students.copy()

        filtered_students["display"] = filtered_students.apply(lambda r: f"{r.get('username','')} · {r.get('first_names') or ''} {r.get('last_names') or ''} · {r.get('subject') or ''} · {r.get('course') or ''}", axis=1)

        selected_label = st.selectbox("Selecciona estudiante para seguimiento", filtered_students["display"].tolist(), key="follow_student_select")

        selected_id = int(filtered_students.loc[filtered_students["display"] == selected_label, "id"].iloc[0])

        profile, report_df = build_student_summary(selected_id)

        st.markdown(

            f"<div class='teacher-card'><h3>{profile.get('first_names') or ''} {profile.get('last_names') or profile.get('username')}</h3>"

            f"<span class='teacher-chip'>Usuario: {profile.get('username') or ''}</span>"

            f"<span class='teacher-chip'>Materia: {profile.get('subject') or 'Sin materia'}</span>"

            f"<span class='teacher-chip'>Curso: {profile.get('course') or 'Sin curso'}</span>"

            f"<span class='teacher-chip'>Nivel: {profile.get('level') or 'Sin nivel'}</span></div>",

            unsafe_allow_html=True,

        )

        if report_df.empty:

            st.info("Este estudiante aún no registra consultas.")

        else:

            g1, g2 = st.columns(2, gap="large")

            user_df = report_df[report_df['role'].astype(str).str.lower().eq('user')].copy()

            if not user_df.empty:

                fig_a = px.histogram(user_df, x='topic', title='Temas consultados')

                fig_a.update_layout(height=420, margin={"r":10,"t":60,"l":10,"b":80})

                g1.plotly_chart(fig_a, use_container_width=True)

            fig_b = px.histogram(report_df, x='created_at', title='Actividad en el tiempo')

            fig_b.update_layout(height=420, margin={"r":10,"t":60,"l":10,"b":80})

            g2.plotly_chart(fig_b, use_container_width=True)

            st.dataframe(report_df, use_container_width=True, height=380)

        return



    if teacher_page == "🧾 Datos completos":

        st.markdown("<div class='teacher-card'><h3>🧾 Datos completos</h3><p class='small'>Tablas completas y descargas generales.</p></div>", unsafe_allow_html=True)

        st.subheader("Estudiantes")

        st.dataframe(filtered_students.drop(columns=["display"], errors="ignore"), use_container_width=True, height=300)

        st.subheader("Trazabilidad de interacciones")

        st.dataframe(filtered_logs, use_container_width=True, height=360)

        loc_df = pd.DataFrame(get_location_events())

        st.subheader("Eventos GPS para mapas de calor")

        if not loc_df.empty:

            st.dataframe(loc_df, use_container_width=True, height=300)

            st.download_button("📍 CSV eventos GPS", loc_df.to_csv(index=False).encode('utf-8-sig'), "eventos_gps_bunsekichat.csv", "text/csv", use_container_width=True)

        else:

            st.info("Aún no hay eventos GPS registrados.")

        st.subheader("Pruebas y gamificación")

        st.dataframe(filtered_quizzes, use_container_width=True, height=300)

        x1, x2, x3 = st.columns(3)

        x1.download_button("📊 CSV interacciones", filtered_logs.to_csv(index=False).encode('utf-8-sig'), "interacciones_bunsekichat.csv", "text/csv", use_container_width=True)

        x2.download_button("👥 CSV estudiantes", filtered_students.drop(columns=["display"], errors="ignore").to_csv(index=False).encode('utf-8-sig'), "estudiantes_bunsekichat.csv", "text/csv", use_container_width=True)

        x3.download_button("🎮 CSV pruebas", filtered_quizzes.to_csv(index=False).encode('utf-8-sig'), "pruebas_bunsekichat.csv", "text/csv", use_container_width=True)

        return



        # ============================================================

# BUNSEKI-018A.5B.1C

# Teacher scoped dashboard / deny-by-default

# ============================================================



def _teacher_record_for_user(user_id):

    """Devuelve el docente asociado al usuario autenticado."""

    if not user_id:

        return None



    return fetchone(

        """

        SELECT id, user_id

        FROM teachers

        WHERE user_id=%s

        """,

        (user_id,),

    )





def _teacher_scope_summary(user_id):

    """

    Resumen restringido exclusivamente a las asignaciones

    del docente autenticado.

    """

    teacher = _teacher_record_for_user(user_id)



    if not teacher:

        return None



    teacher_id = teacher["id"]



    courses = fetchone(

        """

        SELECT COUNT(*) AS total

        FROM teacher_courses

        WHERE teacher_id=%s

        """,

        (teacher_id,),

    ) or {}



    parallels = fetchone(

        """

        SELECT COUNT(*) AS total

        FROM teacher_parallels

        WHERE teacher_id=%s

        """,

        (teacher_id,),

    ) or {}



    enrollments = fetchone(

        """

        SELECT COUNT(*) AS total

        FROM enrollments e

        WHERE EXISTS (

            SELECT 1

            FROM teacher_parallels tp

            WHERE tp.teacher_id=%s

              AND tp.parallel_id=e.parallel_id

        )

        """,

        (teacher_id,),

    ) or {}



    return {

        "teacher_id": teacher_id,

        "courses": int(courses.get("total") or 0),

        "parallels": int(parallels.get("total") or 0),

        "enrollments": int(enrollments.get("total") or 0),

    }





def teacher_can_access_course(user_id, course_id):

    """Deny-by-default para recursos asociados a un curso."""

    if not user_id or not course_id:

        return False



    row = fetchone(

        """

        SELECT EXISTS (

            SELECT 1

            FROM teachers t

            JOIN teacher_courses tc

              ON tc.teacher_id=t.id

            WHERE t.user_id=%s

              AND tc.course_id=%s

        ) AS allowed

        """,

        (user_id, course_id),

    ) or {}



    return bool(row.get("allowed"))





def teacher_can_access_parallel(user_id, parallel_id):

    """Deny-by-default para recursos asociados a un paralelo."""

    if not user_id or not parallel_id:

        return False



    row = fetchone(

        """

        SELECT EXISTS (

            SELECT 1

            FROM teachers t

            JOIN teacher_parallels tp

              ON tp.teacher_id=t.id

            WHERE t.user_id=%s

              AND tp.parallel_id=%s

        ) AS allowed

        """,

        (user_id, parallel_id),

    ) or {}



    return bool(row.get("allowed"))





# BUNSEKI_R8_11_TEACHER_INTERVENTION_V1
def _teacher_intervention_row_allowed(user, row):
    """Deny-by-default reuse of the existing teacher scope guards."""
    course_candidates = [
        row.get("course"),
        row.get("course_level"),
        row.get("subject"),
    ]
    course_candidates = [
        value for value in course_candidates
        if value is not None and str(value).strip()
    ]

    course_ok = False
    for candidate in course_candidates:
        try:
            if teacher_can_access_course(user, candidate):
                course_ok = True
                break
        except Exception:
            continue

    parallel_value = row.get("parallel")
    parallel_ok = False

    parallel_calls = []
    if parallel_value is not None and str(parallel_value).strip():
        parallel_calls.append((user, parallel_value))
        for course_candidate in course_candidates:
            parallel_calls.append(
                (user, course_candidate, parallel_value)
            )

    for args in parallel_calls:
        try:
            if teacher_can_access_parallel(*args):
                parallel_ok = True
                break
        except Exception:
            continue

    return bool(course_ok and parallel_ok)


def _teacher_intervention_dt(series):
    return pd.to_datetime(series, errors="coerce", utc=True)


def _teacher_intervention_band(value):
    score = float(value or 0)
    if score >= 60:
        return "Alta"
    if score >= 35:
        return "Media"
    return "Baja"


def _teacher_intervention_action(row):
    days = float(row.get("Días sin actividad") or 0)
    interactions = float(row.get("Interacciones 14d") or 0)
    attempts = float(row.get("Prácticas 30d") or 0)
    accuracy = row.get("Precisión 30d (%)")

    if days >= 14:
        return "Contacto de reenganche y plan breve de retorno"
    if pd.notna(accuracy) and attempts >= 5 and float(accuracy) < 60:
        return "Refuerzo focalizado en temas de menor precisión"
    if interactions <= 2:
        return "Actividad guiada corta y seguimiento en 7 días"
    return "Seguimiento ordinario"


def render_teacher_intervention_dashboard(user):
    # BUNSEKI_R8_18_VISIBLE_MATRIX_RECORDS_V1
    # BUNSEKI_R8_19_MATRIX_READABILITY_POLISH_V1
    st.markdown("## 🧠 Intervención académica")
    st.caption(
        "Panel gráfico para priorizar acompañamiento dentro de tus cursos y "
        "paralelos asignados. Las alertas son heurísticas de apoyo docente."
    )

    students = pd.DataFrame(
        fetchall(
            """
            SELECT DISTINCT
                u.id AS user_id,
                u.username,
                u.last_login,
                u.active,
                p.first_names,
                p.last_names,
                p.subject,
                COALESCE(c.name, p.course) AS course,
                p.course_level,
                COALESCE(pr.name, p.parallel) AS parallel,
                p.shift,
                p.level,
                e.course_id,
                e.parallel_id,
                c.code AS assigned_course_code,
                c.name AS assigned_course_name,
                pr.code AS assigned_parallel_code,
                pr.name AS assigned_parallel_name
            FROM teachers t
            JOIN teacher_courses tc
              ON tc.teacher_id=t.id
            JOIN teacher_parallels tp
              ON tp.teacher_id=t.id
            JOIN enrollments e
              ON e.course_id=tc.course_id
             AND e.parallel_id=tp.parallel_id
            JOIN users u
              ON u.id=e.student_user_id
            LEFT JOIN profiles p
              ON p.user_id=u.id
            JOIN courses c
              ON c.id=e.course_id
            JOIN parallels pr
              ON pr.id=e.parallel_id
             AND pr.course_id=e.course_id
            WHERE t.user_id=%s
              AND COALESCE(u.active,TRUE)=TRUE
            ORDER BY u.username
            """,
            (int(user["id"]),),
        )
    )

    if students.empty:
        st.info(
            "No hay estudiantes matriculados dentro de tus cursos y paralelos asignados."
        )
        return

    allowed_ids = set(
        students["user_id"].dropna().astype(int).tolist()
    )

    interactions = pd.DataFrame(
        fetchall(
            """
            SELECT user_id, topic, subtopic, level, latency_ms, created_at
            FROM interactions
            """
        )
    )
    practice = pd.DataFrame(
        fetchall(
            """
            SELECT user_id, topic, subtopic, difficulty_level,
                   is_correct, created_at
            FROM research_exercise_attempts
            """
        )
    )
    assessment_attempts = pd.DataFrame(
        fetchall(
            """
            SELECT user_id, teacher_assessment_id, status, resume_count,
                   created_at, updated_at
            FROM assessment_attempts
            WHERE teacher_assessment_id IS NOT NULL
            """
        )
    )

    now_utc = pd.Timestamp.now(tz="UTC")
    cut_14 = now_utc - pd.Timedelta(days=14)
    cut_28 = now_utc - pd.Timedelta(days=28)
    cut_30 = now_utc - pd.Timedelta(days=30)

    if interactions.empty:
        interactions = pd.DataFrame(
            columns=[
                "user_id", "topic", "subtopic",
                "created_at", "created_dt"
            ]
        )
    else:
        interactions = interactions[
            interactions["user_id"].isin(allowed_ids)
        ].copy()
        interactions["created_dt"] = _teacher_intervention_dt(
            interactions["created_at"]
        )

    if practice.empty:
        practice = pd.DataFrame(
            columns=[
                "user_id", "topic", "subtopic", "is_correct",
                "created_at", "created_dt", "correct_num"
            ]
        )
    else:
        practice = practice[
            practice["user_id"].isin(allowed_ids)
        ].copy()
        practice["created_dt"] = _teacher_intervention_dt(
            practice["created_at"]
        )
        practice["correct_num"] = (
            practice["is_correct"].fillna(False).astype(int)
        )

    if assessment_attempts.empty:
        assessment_attempts = pd.DataFrame(
            columns=[
                "user_id", "status", "resume_count",
                "created_at", "updated_at",
                "created_dt", "updated_dt"
            ]
        )
    else:
        assessment_attempts = assessment_attempts[
            assessment_attempts["user_id"].isin(allowed_ids)
        ].copy()
        assessment_attempts["created_dt"] = _teacher_intervention_dt(
            assessment_attempts["created_at"]
        )
        assessment_attempts["updated_dt"] = _teacher_intervention_dt(
            assessment_attempts["updated_at"]
        )

    students["last_login_dt"] = _teacher_intervention_dt(
        students["last_login"]
    )

    recent14 = interactions[
        interactions["created_dt"].notna()
        & (interactions["created_dt"] >= cut_14)
    ]
    previous14 = interactions[
        interactions["created_dt"].notna()
        & (interactions["created_dt"] >= cut_28)
        & (interactions["created_dt"] < cut_14)
    ]
    practice30 = practice[
        practice["created_dt"].notna()
        & (practice["created_dt"] >= cut_30)
    ]

    base = students[
        [
            "user_id",
            "username",
            "first_names",
            "last_names",
            "last_login_dt",
            "subject",
            "course",
            "course_level",
            "parallel",
        ]
    ].copy()

    base["Estudiante"] = (
        base["first_names"].fillna("").astype(str).str.strip()
        + " "
        + base["last_names"].fillna("").astype(str).str.strip()
    ).str.strip()
    base["Estudiante"] = base.apply(
        lambda row: (
            row["Estudiante"]
            if row["Estudiante"]
            else str(row["username"])
        ),
        axis=1,
    )

    last_interaction = (
        interactions.groupby("user_id")["created_dt"].max()
        if not interactions.empty
        else pd.Series(dtype="datetime64[ns, UTC]")
    )
    last_practice = (
        practice.groupby("user_id")["created_dt"].max()
        if not practice.empty
        else pd.Series(dtype="datetime64[ns, UTC]")
    )

    count14 = (
        recent14.groupby("user_id").size()
        if not recent14.empty
        else pd.Series(dtype="int64")
    )
    count_prev14 = (
        previous14.groupby("user_id").size()
        if not previous14.empty
        else pd.Series(dtype="int64")
    )

    practice_count30 = (
        practice30.groupby("user_id").size()
        if not practice30.empty
        else pd.Series(dtype="int64")
    )
    practice_accuracy30 = (
        practice30.groupby("user_id")["correct_num"].mean() * 100
        if not practice30.empty
        else pd.Series(dtype="float64")
    )

    completed_statuses = {
        "completed",
        "submitted",
        "graded",
        "finished",
        "complete",
    }

    if not assessment_attempts.empty:
        last_assessment = (
            assessment_attempts.groupby("user_id")["updated_dt"].max()
        )
        resume_sum = (
            assessment_attempts.groupby("user_id")["resume_count"]
            .sum(min_count=1)
            .fillna(0)
        )
        open_mask = ~assessment_attempts[
            "status"
        ].fillna("").astype(str).str.lower().isin(
            completed_statuses
        )
        open_attempts = (
            assessment_attempts[open_mask]
            .groupby("user_id")
            .size()
        )
    else:
        last_assessment = pd.Series(
            dtype="datetime64[ns, UTC]"
        )
        resume_sum = pd.Series(dtype="float64")
        open_attempts = pd.Series(dtype="int64")

    base["Interacciones 14d"] = (
        base["user_id"].map(count14).fillna(0).astype(int)
    )
    base["Interacciones previas 14d"] = (
        base["user_id"].map(count_prev14).fillna(0).astype(int)
    )
    base["Prácticas 30d"] = (
        base["user_id"].map(practice_count30).fillna(0).astype(int)
    )
    base["Precisión 30d (%)"] = (
        base["user_id"].map(practice_accuracy30)
    )
    base["Reanudaciones"] = (
        base["user_id"].map(resume_sum).fillna(0).astype(int)
    )
    base["Intentos abiertos"] = (
        base["user_id"].map(open_attempts).fillna(0).astype(int)
    )

    # BUNSEKI_R8_15_V3_TEACHER_DATETIME_MAP_HOTFIX
    # Avoid pandas Series.map(Series[datetime64, tz]) casting edge cases.
    # Normalize user ids and map through plain Python dictionaries instead.
    _last_interaction_map = {
        int(key): value
        for key, value in last_interaction.items()
        if pd.notna(key)
    }
    _last_practice_map = {
        int(key): value
        for key, value in last_practice.items()
        if pd.notna(key)
    }
    _last_assessment_map = {
        int(key): value
        for key, value in last_assessment.items()
        if pd.notna(key)
    }

    _base_user_ids = pd.to_numeric(
        base["user_id"],
        errors="coerce",
    )

    base["last_interaction"] = pd.to_datetime(
        _base_user_ids.apply(
            lambda value: (
                _last_interaction_map.get(int(value), pd.NaT)
                if pd.notna(value)
                else pd.NaT
            )
        ),
        errors="coerce",
        utc=True,
    )

    base["last_practice"] = pd.to_datetime(
        _base_user_ids.apply(
            lambda value: (
                _last_practice_map.get(int(value), pd.NaT)
                if pd.notna(value)
                else pd.NaT
            )
        ),
        errors="coerce",
        utc=True,
    )

    base["last_assessment"] = pd.to_datetime(
        _base_user_ids.apply(
            lambda value: (
                _last_assessment_map.get(int(value), pd.NaT)
                if pd.notna(value)
                else pd.NaT
            )
        ),
        errors="coerce",
        utc=True,
    )

    base["Última actividad"] = pd.concat(
        [
            base["last_login_dt"],
            base["last_interaction"],
            base["last_practice"],
            base["last_assessment"],
        ],
        axis=1,
    ).max(axis=1)

    base["Días sin actividad"] = (
        (now_utc - base["Última actividad"])
        .dt.total_seconds()
        / 86400
    )
    base["Días sin actividad"] = (
        base["Días sin actividad"]
        .fillna(999)
        .clip(lower=0)
        .round(1)
    )

    base["Momentum"] = (
        (
            base["Interacciones 14d"]
            - base["Interacciones previas 14d"]
        )
        / (base["Interacciones previas 14d"] + 1)
    )

    def academic_risk(row):
        score = 0.0
        attempts = float(row["Prácticas 30d"])
        accuracy = row["Precisión 30d (%)"]
        days = float(row["Días sin actividad"])
        interactions14 = float(row["Interacciones 14d"])
        open_count = float(row["Intentos abiertos"])

        if attempts >= 5 and pd.notna(accuracy):
            score += min(
                48.0,
                max(
                    0.0,
                    (60.0 - float(accuracy)) * 1.2,
                ),
            )
        elif attempts < 3:
            score += 8.0

        if days >= 21:
            score += 28.0
        elif days >= 14:
            score += 22.0
        elif days >= 7:
            score += 12.0
        elif days >= 3:
            score += 5.0

        if interactions14 == 0:
            score += 18.0
        elif interactions14 <= 2:
            score += 10.0

        if open_count > 0:
            score += min(
                12.0,
                open_count * 6.0,
            )

        return min(
            100.0,
            round(score, 1),
        )

    def disconnect_risk(row):
        score = 0.0
        days = float(row["Días sin actividad"])
        interactions14 = float(row["Interacciones 14d"])
        momentum = float(row["Momentum"])

        if days >= 21:
            score += 60.0
        elif days >= 14:
            score += 48.0
        elif days >= 7:
            score += 30.0
        elif days >= 3:
            score += 15.0

        if interactions14 == 0:
            score += 25.0
        elif interactions14 <= 2:
            score += 15.0

        if momentum <= -0.50:
            score += 15.0

        return min(
            100.0,
            round(score, 1),
        )

    base["Riesgo académico"] = base.apply(
        academic_risk,
        axis=1,
    )
    base["Riesgo desconexión"] = base.apply(
        disconnect_risk,
        axis=1,
    )
    base["Alerta académica"] = base[
        "Riesgo académico"
    ].map(_teacher_intervention_band)
    base["Alerta desconexión"] = base[
        "Riesgo desconexión"
    ].map(_teacher_intervention_band)
    base["Acción sugerida"] = base.apply(
        _teacher_intervention_action,
        axis=1,
    )

    high_academic = int(
        (base["Riesgo académico"] >= 60).sum()
    )
    medium_academic = int(
        (
            (base["Riesgo académico"] >= 35)
            & (base["Riesgo académico"] < 60)
        ).sum()
    )
    high_disconnect = int(
        (base["Riesgo desconexión"] >= 60).sum()
    )
    inactive_7 = int(
        (base["Días sin actividad"] >= 7).sum()
    )
    avg_accuracy = pd.to_numeric(
        base["Precisión 30d (%)"],
        errors="coerce",
    ).mean()

    k1, k2, k3 = st.columns(3)
    k1.metric("Estudiantes acompañados", len(base))
    k2.metric("Riesgo académico alto", high_academic)
    k3.metric("Riesgo académico medio", medium_academic)

    k4, k5, k6 = st.columns(3)
    k4.metric("Riesgo desconexión alto", high_disconnect)
    k5.metric("Sin actividad ≥ 7 días", inactive_7)
    k6.metric(
        "Precisión media 30d",
        "Sin datos" if pd.isna(avg_accuracy) else f"{avg_accuracy:.1f}%",
    )

    # BUNSEKI_R8_17_TEACHER_INTERVENTION_VIEW_SWITCH_V1
    # Explicit view selector avoids tab-panel CSS collisions elsewhere in the app.
    intervention_view = st.radio(
        "Vista de intervención",
        [
            "📊 Panorama de intervención",
            "🚦 Matriz de acompañamiento",
            "📋 Registros",
        ],
        horizontal=True,
        key=f"teacher_intervention_view_{user['id']}",
        label_visibility="collapsed",
    )

    if intervention_view == "📊 Panorama de intervención":
        st.markdown("### Mapa 2×2 · interacción vs precisión")
        st.caption(
            "Distingue baja participación de dificultad conceptual. "
            "La línea horizontal usa 60% de precisión y la vertical "
            "5 interacciones en 14 días como referencias operativas."
        )

        scatter = base[
            [
                "Estudiante",
                "Interacciones 14d",
                "Precisión 30d (%)",
                "Riesgo académico",
                "Riesgo desconexión",
            ]
        ].copy()

        scatter["Precisión 30d (%)"] = pd.to_numeric(
            scatter["Precisión 30d (%)"],
            errors="coerce",
        ).fillna(0)

        if "px" in globals():
            fig = px.scatter(
                scatter,
                x="Interacciones 14d",
                y="Precisión 30d (%)",
                hover_name="Estudiante",
                size="Riesgo académico",
                color="Riesgo desconexión",
                size_max=32,
                title="Mapa de intervención: actividad × desempeño",
            )
            fig.add_hline(
                y=60,
                line_dash="dash",
                annotation_text="Precisión 60%",
            )
            fig.add_vline(
                x=5,
                line_dash="dash",
                annotation_text="5 interacciones / 14d",
            )
            fig.update_yaxes(range=[0, 100])
            st.plotly_chart(
                fig,
                use_container_width=True,
            )
        else:
            st.scatter_chart(
                scatter.set_index("Estudiante")[
                    [
                        "Interacciones 14d",
                        "Precisión 30d (%)",
                    ]
                ]
            )

        st.markdown("### Distribución de alertas")
        risk_counts = (
            base["Alerta académica"]
            .value_counts()
            .reindex(
                ["Alta", "Media", "Baja"],
                fill_value=0,
            )
            .rename("Estudiantes")
            .to_frame()
        )
        st.bar_chart(risk_counts)

        st.markdown("### Fricción temática del grupo")

        if practice30.empty:
            st.info(
                "Todavía no hay práctica reciente "
                "para calcular fricción por tema."
            )
        else:
            topic_friction = (
                practice30.assign(
                    Tema=(
                        practice30["subtopic"]
                        .fillna(practice30["topic"])
                        .fillna("Sin tema")
                        .astype(str)
                        .str.strip()
                        .replace("", "Sin tema")
                    )
                )
                .groupby("Tema")
                .agg(
                    **{
                        "Prácticas": ("correct_num", "size"),
                        "Precisión (%)": ("correct_num", "mean"),
                        "Estudiantes": ("user_id", "nunique"),
                    }
                )
                .reset_index()
            )
            topic_friction["Precisión (%)"] = (
                topic_friction["Precisión (%)"] * 100
            )
            topic_friction["Índice fricción"] = (
                100 - topic_friction["Precisión (%)"]
            ).round(1)
            topic_friction = topic_friction.sort_values(
                ["Índice fricción", "Prácticas"],
                ascending=[False, False],
            )
            st.dataframe(
                topic_friction.head(15),
                use_container_width=True,
                hide_index=True,
            )

    elif intervention_view == "🚦 Matriz de acompañamiento":
        st.markdown("### Matriz de acompañamiento")
        st.caption(
            "La matriz complementa los registros: prioriza quién necesita "
            "acompañamiento, por qué y qué acción realizar."
        )

        matrix = base[
            [
                "Estudiante",
                "username",
                "course",
                "parallel",
                "Última actividad",
                "Días sin actividad",
                "Interacciones 14d",
                "Prácticas 30d",
                "Precisión 30d (%)",
                "Reanudaciones",
                "Intentos abiertos",
                "Riesgo académico",
                "Riesgo desconexión",
                "Alerta académica",
                "Alerta desconexión",
                "Acción sugerida",
            ]
        ].copy()

        matrix["Precisión 30d (%)"] = pd.to_numeric(
            matrix["Precisión 30d (%)"],
            errors="coerce",
        ).round(1)

        matrix = matrix.sort_values(
            [
                "Riesgo académico",
                "Riesgo desconexión",
                "Días sin actividad",
            ],
            ascending=[False, False, False],
        )

        st.success(
            f"Estudiantes en la matriz: {len(matrix)}"
        )

        matrix_display = matrix[
            [
                "Estudiante",
                "course",
                "parallel",
                "Última actividad",
                "Días sin actividad",
                "Interacciones 14d",
                "Prácticas 30d",
                "Precisión 30d (%)",
                "Riesgo académico",
                "Riesgo desconexión",
                "Acción sugerida",
            ]
        ].copy()

        matrix_display["Última actividad"] = pd.to_datetime(
            matrix_display["Última actividad"],
            errors="coerce",
            utc=True,
        ).dt.strftime("%Y-%m-%d %H:%M").fillna("Sin actividad registrada")

        matrix_display["Días sin actividad"] = matrix_display[
            "Días sin actividad"
        ].apply(
            lambda value: (
                "Sin actividad registrada"
                if pd.isna(value) or float(value) >= 900
                else (
                    "< 1 día"
                    if float(value) < 1
                    else f"{float(value):.0f} días"
                )
            )
        )

        matrix_display["Precisión 30d (%)"] = pd.to_numeric(
            matrix_display["Precisión 30d (%)"],
            errors="coerce",
        ).apply(
            lambda value: (
                "Sin datos"
                if pd.isna(value)
                else f"{float(value):.1f}%"
            )
        )

        matrix_display["Riesgo académico"] = pd.to_numeric(
            matrix_display["Riesgo académico"],
            errors="coerce",
        ).apply(
            lambda value: (
                "Sin datos"
                if pd.isna(value)
                else f"{float(value):.0f}/100"
            )
        )

        matrix_display["Riesgo desconexión"] = pd.to_numeric(
            matrix_display["Riesgo desconexión"],
            errors="coerce",
        ).apply(
            lambda value: (
                "Sin datos"
                if pd.isna(value)
                else f"{float(value):.0f}/100"
            )
        )

        matrix_display = matrix_display.rename(
            columns={
                "course": "Curso",
                "parallel": "Paralelo",
                "Interacciones 14d": "Interacciones",
                "Prácticas 30d": "Prácticas",
                "Precisión 30d (%)": "Precisión",
            }
        )

        st.table(matrix_display)

        st.caption(
            "Referencia operativa: riesgo bajo 0–34, medio 35–59 y alto 60–100. "
            "Las alertas orientan acompañamiento docente y no sustituyen el juicio académico."
        )

        csv_bytes = matrix.to_csv(index=False).encode("utf-8-sig")
        st.download_button(
            "⬇️ Descargar matriz de acompañamiento",
            data=csv_bytes,
            file_name="bunseki_matriz_acompanamiento_docente.csv",
            mime="text/csv",
            use_container_width=True,
        )

        selected_student = st.selectbox(
            "Detalle de estudiante",
            matrix["Estudiante"].tolist(),
            key=f"teacher_intervention_student_{user['id']}",
        )

        selected = matrix[
            matrix["Estudiante"] == selected_student
        ].iloc[0]

        d1, d2, d3 = st.columns(3)
        d1.metric(
            "Riesgo académico",
            f"{selected['Riesgo académico']:.0f}/100",
        )
        d2.metric(
            "Riesgo desconexión",
            f"{selected['Riesgo desconexión']:.0f}/100",
        )
        _selected_days = selected["Días sin actividad"]
        d3.metric(
            "Días sin actividad",
            (
                "Sin registro"
                if pd.isna(_selected_days) or float(_selected_days) >= 900
                else (
                    "< 1 día"
                    if float(_selected_days) < 1
                    else f"{float(_selected_days):.0f}"
                )
            ),
        )

        d4, d5, d6 = st.columns(3)
        d4.metric(
            "Interacciones 14d",
            int(selected["Interacciones 14d"]),
        )
        d5.metric(
            "Prácticas 30d",
            int(selected["Prácticas 30d"]),
        )
        _selected_accuracy = selected["Precisión 30d (%)"]
        d6.metric(
            "Precisión 30d",
            (
                "Sin datos"
                if pd.isna(_selected_accuracy)
                else f"{float(_selected_accuracy):.1f}%"
            ),
        )

        _selected_last_activity = pd.to_datetime(
            selected["Última actividad"],
            errors="coerce",
            utc=True,
        )
        st.caption(
            "Última actividad: "
            + (
                "Sin actividad registrada"
                if pd.isna(_selected_last_activity)
                else _selected_last_activity.strftime("%Y-%m-%d %H:%M UTC")
            )
        )

        st.info(
            "Acción sugerida: "
            + str(selected["Acción sugerida"])
        )

    elif intervention_view == "📋 Registros":
        st.markdown("### 📋 Registros de actividad")
        st.caption(
            "Evidencia histórica de actividad dentro del alcance actual del docente. "
            "Estos registros complementan la Matriz de acompañamiento."
        )

        _student_name_map = {
            int(row["user_id"]): str(row["Estudiante"])
            for _, row in base[["user_id", "Estudiante"]].iterrows()
            if pd.notna(row["user_id"])
        }

        _record_frames = []

        if not interactions.empty:
            _ri = interactions[
                ["user_id", "created_dt", "topic", "subtopic", "latency_ms"]
            ].copy()
            _ri["Tipo"] = "Interacción"
            _ri["Detalle"] = _ri.apply(
                lambda row: (
                    f"{str(row.get('topic') or 'Sin tema')} · "
                    f"{str(row.get('subtopic') or 'Sin subtema')}"
                    + (
                        f" · {int(row['latency_ms'])} ms"
                        if pd.notna(row.get("latency_ms"))
                        else ""
                    )
                ),
                axis=1,
            )
            _ri = _ri.rename(columns={"created_dt": "Fecha"})
            _record_frames.append(
                _ri[["user_id", "Fecha", "Tipo", "Detalle"]]
            )

        if not practice.empty:
            _rp = practice[
                [
                    "user_id",
                    "created_dt",
                    "topic",
                    "subtopic",
                    "difficulty_level",
                    "is_correct",
                ]
            ].copy()
            _rp["Tipo"] = "Práctica"
            _rp["Detalle"] = _rp.apply(
                lambda row: (
                    f"{str(row.get('topic') or 'Sin tema')} · "
                    f"{str(row.get('subtopic') or 'Sin subtema')} · "
                    f"{str(row.get('difficulty_level') or 'Sin nivel')} · "
                    + (
                        "Correcta"
                        if bool(row.get("is_correct"))
                        else "Incorrecta"
                    )
                ),
                axis=1,
            )
            _rp = _rp.rename(columns={"created_dt": "Fecha"})
            _record_frames.append(
                _rp[["user_id", "Fecha", "Tipo", "Detalle"]]
            )

        if not assessment_attempts.empty:
            _ra = assessment_attempts[
                [
                    "user_id",
                    "updated_dt",
                    "teacher_assessment_id",
                    "status",
                    "resume_count",
                ]
            ].copy()
            _ra["Tipo"] = "Evaluación"
            _ra["Detalle"] = _ra.apply(
                lambda row: (
                    f"Evaluación {row.get('teacher_assessment_id')} · "
                    f"Estado: {str(row.get('status') or 'Sin estado')} · "
                    f"Reanudaciones: {int(row.get('resume_count') or 0)}"
                ),
                axis=1,
            )
            _ra = _ra.rename(columns={"updated_dt": "Fecha"})
            _record_frames.append(
                _ra[["user_id", "Fecha", "Tipo", "Detalle"]]
            )

        if not _record_frames:
            st.info(
                "Todavía no existen registros de interacción, práctica o evaluación "
                "para los estudiantes de este alcance."
            )
        else:
            _records = pd.concat(
                _record_frames,
                ignore_index=True,
            )
            _records["Estudiante"] = _records["user_id"].map(
                _student_name_map
            ).fillna("Estudiante")
            _records["Fecha"] = pd.to_datetime(
                _records["Fecha"],
                errors="coerce",
                utc=True,
            )
            _records = _records.sort_values(
                "Fecha",
                ascending=False,
                na_position="last",
            )

            _records_view = _records[
                [
                    "Fecha",
                    "Estudiante",
                    "Tipo",
                    "Detalle",
                ]
            ].copy()

            st.success(
                f"Registros encontrados: {len(_records_view)}"
            )

            _records_display = _records_view.copy()
            _records_display["Fecha"] = pd.to_datetime(
                _records_display["Fecha"],
                errors="coerce",
                utc=True,
            ).dt.strftime("%Y-%m-%d %H:%M").fillna("Sin fecha")

            st.table(
                _records_display.head(100)
            )

            if len(_records_display) > 100:
                st.caption(
                    "Se muestran los 100 registros más recientes. "
                    "La descarga CSV contiene el conjunto completo."
                )

            st.download_button(
                "⬇️ Descargar registros CSV",
                data=_records_view.to_csv(index=False).encode("utf-8-sig"),
                file_name="bunseki_registros_docente.csv",
                mime="text/csv",
                use_container_width=True,
            )

    with st.expander("Metodología y uso responsable"):
        st.markdown(
            """
            **Riesgo académico:** usa precisión, práctica,
            inactividad, interacciones e intentos pendientes.

            **Riesgo de desconexión:** usa recencia,
            volumen de interacción y caída de momentum.

            **No se utiliza ubicación o distancia para puntuar
            el riesgo individual.**

            Estas señales sirven para priorizar acompañamiento;
            no sustituyen la revisión docente ni constituyen
            una decisión académica automática.
            """
        )

def teacher_scoped_page(user):

    """

    Dashboard exclusivo para docentes.

    Nunca llama admin_page() y nunca usa métricas globales.

    """

    # BUNSEKI-TEACHER-SCOPED-NAV-V1

    role = str(user.get("role", "")).lower() if user else ""



    if role not in {"teacher", "docente"}:

        st.error("Acceso docente no autorizado.")

        st.stop()



    scope = _teacher_scope_summary(user.get("id"))



    if not scope:

        st.error(

            "El usuario docente no posee una asignación "

            "académica válida."

        )

        st.stop()



    teacher_pages = (

        "📊 Dashboard docente",

        "📚 Planes analíticos",

    )

    nav_key = "teacher_scoped_nav_page"

    fallback_key = "teacher_scoped_nav_fallback"



    if st.session_state.get(nav_key) not in teacher_pages:

        st.session_state[nav_key] = teacher_pages[0]

    if st.session_state.get(fallback_key) not in teacher_pages:

        st.session_state[fallback_key] = st.session_state[nav_key]



    with st.sidebar:

        st.markdown("## BunsekiChat")

        st.caption("Panel docente")

        st.success("🔒 Acceso limitado a tus asignaciones")



        st.markdown("---")



        for page in teacher_pages:

            if st.button(

                page,

                key=f"teacher_scoped_button_{page}",

                use_container_width=True,

            ):

                st.session_state[nav_key] = page

                st.session_state[fallback_key] = page

                st.rerun()



        st.markdown("---")

        st.write(f"**Usuario:** {user.get('username', 'Docente')}")

        st.write(f"**Cursos asignados:** {scope['courses']}")

        st.write(f"**Paralelos asignados:** {scope['parallels']}")



    def _sync_teacher_scoped_fallback():

        selected = st.session_state.get(fallback_key)

        if selected in teacher_pages:

            st.session_state[nav_key] = selected



    st.markdown("#### 🧭 Navegación docente")

    st.selectbox(

        "Menú docente alternativo",

        teacher_pages,

        key=fallback_key,

        on_change=_sync_teacher_scoped_fallback,

    )

    st.caption(

        "Este selector permanece disponible aunque el panel lateral "

        "esté oculto."

    )



    teacher_page = st.session_state.get(nav_key, teacher_pages[0])



    if teacher_page == "📚 Planes analíticos":

        render_teacher_plan_manager(user)

        return



    st.title("📊 Dashboard docente")

    st.caption(

        "Vista limitada exclusivamente a tus cursos "

        "y paralelos asignados."

    )



    c1, c2, c3 = st.columns(3)



    with c1:

        st.metric("Cursos asignados", scope["courses"])



    with c2:

        st.metric("Paralelos asignados", scope["parallels"])



    with c3:

        st.metric(

            "Estudiantes matriculados",

            scope["enrollments"],

        )



    st.markdown("---")



    if scope["enrollments"] == 0:

        st.info(

            "Actualmente no existen estudiantes matriculados "

            "en tus paralelos."

        )



    st.markdown("---")
    render_teacher_intervention_dashboard(user)
    st.markdown("---")

    st.subheader("🔐 Alcance de seguridad")



    st.success(

        "Tu sesión está restringida a las asignaciones "

        "registradas para este docente."

    )



    st.caption(

        "Las métricas globales y los estudiantes de otros "

        "docentes no están disponibles en esta vista."

    )



if __name__ == "__main__":

    setup_database_once(DATABASE_SCHEMA_VERSION)

    session_guard()

    if 'user' not in st.session_state:

        if st.session_state.get("landing_entered", False):
            login_page()
        else:
            landing_page()

    else:

        u = st.session_state.user

        capture_global_gps = True

        if u.get('role') not in ['admin', 'teacher', 'docente']:

            current_profile = get_profile(u['id'])

            capture_global_gps = not is_official_profile(current_profile) or official_profile_ready(u, current_profile)

        if capture_global_gps:

            capture_browser_gps(uid=u['id'], page='global', event_type='app_open', show_status=False)

        role = str(u.get("role", "")).lower()

        if role == "admin": admin_page(u)

        elif role in {"teacher", "docente"}: teacher_scoped_page(u)

        else: student_page(u)
